# Day 015：进入 Full SFT 真实训练循环

- 日期：2026-09-01 至 2026-09-02（同一个学习日）
- 学习阶段：MiniMind 训练源码主线
- 今日主题：从 epoch 循环进入 DataLoader 与 `train_epoch()`
- 当前状态：已完成
- 配套实验：`notebooks/day-015-sft-training-loop.ipynb`

## 一、与 Day 14 的边界

Day 14 已经按真实执行顺序完成 Full SFT 的训练前准备：

```text
模型与 pretrain 权重
-> SFTDataset 与 labels
-> GradScaler
-> AdamW
-> 可选 checkpoint 恢复
-> 可选 torch.compile 与 DDP
```

Day 14 准确停在：

```python
# trainer/train_full_sft.py 第 158 行
for epoch in range(start_epoch, args.epochs):
```

在这个位置之前还没有取出 batch，没有执行模型 forward，也没有计算
loss、backward 或更新参数。

## 二、Day 15 计划边界

继续遵守真实调用顺序：

```text
epoch 循环
-> 当前 epoch 的样本顺序
-> skip 与 SkipBatchSampler
-> DataLoader
-> 调用 train_epoch()
-> batch 解包与设备迁移
-> forward、loss、backward
-> 梯度累积、裁剪与 optimizer.step()
```

以上是路线，不代表今天已经学完。每个步骤都需要经过讲解、学习者回答
或实践验收后，才能在档案中标为完成。

## 三、逐轮互动档案

### 第 1 轮：恢复到 epoch 循环入口

新学习日从 `trainer/train_full_sft.py` 第 158 行开始。当前继续采用普通
单进程、默认参数路径：

```python
start_epoch = 0
args.epochs = 2
```

下一步只解释：

```python
for epoch in range(start_epoch, args.epochs):
```

尚未进入循环体内的 sampler、随机索引或 DataLoader。

当前恢复点：等待学习 `range(start_epoch, args.epochs)` 实际产生哪些 epoch
值，以及程序索引和人类显示轮次的区别。

### 第 2 轮：`range()` 与 epoch 的真实取值

源码：

```python
for epoch in range(start_epoch, args.epochs):
```

默认真实值：

```python
start_epoch = 0
args.epochs = 2
```

因此 `range(0, 2)` 依次产生 `0、1`。Python 的 `range` 包含起点、不
包含终点。epoch 原则上表示完整遍历一次训练数据，不是一个 batch；程序
内部从 0 编号，所以：

```text
epoch=0 -> 人看到的第 1 轮
epoch=1 -> 人看到的第 2 轮
```

日志中使用 `epoch + 1` 转换成人类习惯的轮次。断点恢复若
`start_epoch=1` 且总轮数为 2，则 `range(1, 2)` 只产生 1。

学习者回答：`0，1；2；遍历一次训练数据`。三项全部正确。[验收]

### 第 3 轮：单进程短路跳过 `DistributedSampler.set_epoch()`

循环体第一行：

```python
train_sampler and train_sampler.set_epoch(epoch)
```

这是利用 Python 短路求值的简写，等价于：

```python
if train_sampler:
    train_sampler.set_epoch(epoch)
```

当前普通单进程路径中 `train_sampler=None`，因此右侧不执行，也不会对
`None` 调用方法而报错；此时仍未从 `SFTDataset` 取出任何样本。

分布式路径中 `train_sampler` 是 `DistributedSampler`，调用
`set_epoch(epoch)` 会让采样器把轮次纳入随机排列，使不同 epoch 得到
不同但可复现的样本顺序。

学习者回答：`None；不会；没有`。三项全部正确。[验收]

当前恢复点：`trainer/train_full_sft.py` 第 160 行。该物理行由分号隔开
两个语句，下一步先读 `setup_seed(42 + epoch)`，尚未执行 `randperm`。

### 第 4 轮：每轮设置不同但可复现的随机种子

第 160 行由分号分隔两个依次执行的语句：

```python
setup_seed(42 + epoch)
indices = torch.randperm(len(train_ds)).tolist()
```

当前只读第一句。默认两轮中：

```text
epoch=0 -> setup_seed(42)
epoch=1 -> setup_seed(43)
```

`setup_seed()` 会设置 Python、NumPy、PyTorch 和 CUDA 的随机数生成器
状态，但本身不会生成样本排列。使用 `42 + epoch` 是为了让不同 epoch
有不同的随机起点，同时在相同配置下可以复现顺序。后续真正的随机排列
由 `torch.randperm()` 产生。

学习者回答：`42；43；不会`。全部正确。[验收]

### 第 5 轮：`randperm()` 生成 item 索引的随机排列

先执行：

```python
len(train_ds)
```

它调用 `SFTDataset.__len__()`，返回 JSONL 数据集中的 item 总数。若总数
为 5：

```python
torch.randperm(5)
```

会生成包含 `0、1、2、3、4` 且每个只出现一次的随机排列，例如：

```python
tensor([2, 4, 3, 0, 1])
```

`.tolist()` 再把 Tensor 转成普通 Python `list[int]`。此时只有访问顺序
被准备好，尚未执行 `train_ds[index]`，所以 `__getitem__()` 尚未运行。

学习者回答：`item索引；一次；没有`。三项正确。[验收]

### 第 6 轮：`skip` 只在恢复训练的起始 epoch 生效

源码：

```python
skip = start_step if (
    epoch == start_epoch and start_step > 0
) else 0
```

两个条件必须同时成立：当前是恢复时的起始 epoch，并且该 epoch 已经
完成过至少一个 step。新训练默认 `start_epoch=0、start_step=0`，所以
`skip=0`。若恢复值为 `start_epoch=1、start_step=320`：

```text
epoch=1 -> skip=320
epoch=2 -> skip=0
```

`start_step` 来自训练循环中的 batch step，因此 `skip=320` 是跳过 320
个 batch，不是 320 个 item。默认 `batch_size=16` 且每批都满时，约对应
`320 * 16 = 5120` 个 item；准确语义仍以 batch 为单位。

学习者先回答：`0；320；0；320个item`。前三项正确，最后一项纠正为
“320 个 batch”。随后重新确认：`320个；320*16`。[纠偏][验收]

### 第 7 轮：创建 `SkipBatchSampler`

源码：

```python
batch_sampler = SkipBatchSampler(
    train_sampler or indices,
    args.batch_size,
    skip
)
```

普通单进程时 `train_sampler=None`，Python 的 `or` 返回右侧真实对象，
所以当前等价于：

```python
batch_sampler = SkipBatchSampler(indices, 16, 0)
```

构造函数只保存：

```python
self.sampler = indices
self.batch_size = 16
self.skip_batches = 0
```

此刻尚未执行 `SkipBatchSampler.__iter__()`，没有切分 batch，也没有
产生任何索引或触发 `SFTDataset.__getitem__()`。

学习者回答：`indices；16；0；没有`。全部正确。[验收]

### 第 8 轮：创建 DataLoader 前区分 batch size 与 worker 数

源码：

```python
loader = DataLoader(
    train_ds,
    batch_sampler=batch_sampler,
    num_workers=args.num_workers,
    pin_memory=True
)
```

当前只创建并配置 DataLoader，不开始迭代。`batch_sampler` 负责每批提供
哪些 item 索引；DataLoader 再用这些索引调用 `train_ds.__getitem__()`，
并把返回的 Tensor 堆叠成 batch。默认：

```text
batch_size=16：每批样本数，由 SkipBatchSampler 保存
num_workers=8：并行准备样本的后台 worker 数
pin_memory=True：为 CPU->GPU 传输准备固定内存；CPU 训练时没有该传输收益
```

学习者第一次回答 `train_ds.__item__；batch_sampler;16;否`。纠正为
`train_ds.__getitem__`，并澄清 `16` 是 batch_size，不是 num_workers；
默认 `num_workers=8`。学习者随后确认：`__getitem__;16;8`。[纠偏][验收]

### 第 9 轮：新训练与恢复训练的调用分支

源码：

```python
if skip > 0:
    Logger(...)
    train_epoch(epoch, loader, len(loader) + skip, start_step, wandb)
else:
    train_epoch(epoch, loader, len(loader), 0, wandb)
```

当前新训练 `skip=0`，所以进入 `else`，但在真正调用函数前需要先计算
第三个实参 `len(loader)`。此时尚未进入 `train_epoch()` 函数体，传入的
`start_step` 为 0，`wandb` 默认是 None。

学习者回答：`false；else；0；还没有`。全部正确。[验收]

### 第 10 轮：`len(loader)` 来自 batch sampler 的长度

由于 DataLoader 使用 `batch_sampler=...`，求 `len(loader)` 会使用
`SkipBatchSampler.__len__()`：

```python
total_batches = (
    len(self.sampler) + self.batch_size - 1
) // self.batch_size
return max(0, total_batches - self.skip_batches)
```

假设：

```python
len(self.sampler) = 50
batch_size = 16
skip_batches = 0
```

则：

```python
total_batches = (50 + 16 - 1) // 16
              = 65 // 16
              = 4
```

对应 16、16、16、2 四批。通过先加 `batch_size-1` 实现向上取整；
`len(loader)` 只计算批次数，不迭代 sampler，不调用 `__getitem__()`。

学习者回答：`4；2；不会`。全部正确。[验收]

当前恢复点：已完成 `len(loader)` 的计算，下一步进入
`train_epoch()` 函数体的第一行 batch 循环。

### 第 11 轮：`train_epoch()` 循环头与 DataLoader 取 batch

进入：

```python
for step, (input_ids, labels) in enumerate(
    loader,
    start=start_step + 1
):
```

新训练时 `start_step=0`，因此第一个 batch 的编号是 `step=1`。循环头
可以概念性理解为：`enumerate` 向 DataLoader 要一批数据，得到
`(step, (batch_input_ids, batch_labels))`，再解包成三个变量：

```text
step
input_ids
labels
```

满批默认 shape 是 `[16, 768]`；最后不足批若只有 2 条，则是 `[2, 768]`。
只有开始迭代 loader 取 batch 时，DataLoader 才会驱动
`SkipBatchSampler.__iter__()`，并让 worker 调用 `SFTDataset.__getitem__()`。
创建 `DataLoader(...)` 本身不会提前得到第一批。

学习者回答：`1；[16,768][16,768]；[2,768][2,768]；迭代时`。全部正确。
[验收]

### 第 12 轮：`SkipBatchSampler.__iter__()` 交付完整批次

`SkipBatchSampler` 当前保存：

```python
self.sampler = indices
self.batch_size = 16
self.skip_batches = 0
```

`__iter__()` 用 `batch=[]` 逐个接收索引；第 16 个索引加入后，因
`skipped < skip_batches` 即 `0 < 0` 为 False，直接执行 `yield batch`。
`yield` 把这 16 个索引交给 DataLoader 但保留生成器执行位置，下一次
请求时可继续清空并组装下一批，而不是永久结束函数。

学习者回答：`不会；false；交给DataLoader；继续产生下一批`。四项正确。
[验收]

### 第 13 轮：不足一个满批的最后索引也会交付

循环结束后：

```python
if len(batch) > 0 and skipped >= self.skip_batches:
    yield batch
```

假设有 50 个 item、`batch_size=16`、`skip_batches=0`：前三次 yield 各
16 个索引，最后剩 2 个；`2 > 0` 且 `0 >= 0`，因此第四次 yield 这 2 个。
源码没有 `drop_last=True`，所以最后不足批不会丢弃。

学习者回答：`4次；16；2；否`。全部正确。[验收]

### 第 14 轮：完整展开 `for step, (input_ids, labels) in enumerate(...)`

这条 `for` 语句是整个数据生产链的驱动点，但各组件分工不同。概念上
可改写为：

```python
iterator = iter(enumerate(loader, start=start_step + 1))
while True:
    try:
        step, batch = next(iterator)
    except StopIteration:
        break
    input_ids, labels = batch
    # 执行当前 batch 的训练代码
```

实际时序为：

```text
for 向 enumerate 要下一项
-> DataLoader 向 SkipBatchSampler 要索引批次
-> worker 调用多个 __getitem__()
-> DataLoader 堆叠成 batch Tensor
-> enumerate 加上 step 编号
-> 循环头解包 input_ids、labels
-> 执行一次训练循环体
```

`enumerate` 的 `start` 只改变编号，不负责跳过 batch；真正的跳过由
`SkipBatchSampler(skip_batches=...)` 完成。多 worker 还可能预取后续样本，
但数据生产从迭代 loader 后才开始。

学习者回答：`1；1 (input_ids, labels); step， input_ids, labels；不会`。
第 2 项补充为 `(1, (batch_input_ids, batch_labels))`，其余理解正确。
[纠偏][验收]

### 第 15 轮：把 batch Tensor 移到模型设备

源码：

```python
input_ids = input_ids.to(args.device)
labels = labels.to(args.device)
```

本地当前检测为 `torch.cuda.is_available()=False`，默认设备是 `cpu`，
所以当前是 CPU -> CPU 的统一写法；GPU 环境中才会执行 CPU -> CUDA 的
传输。`.to(device)` 不改变 shape、token 数值或 dtype：

```text
input_ids：torch.long，[batch_size, 768]
labels：torch.long，[batch_size, 768]
```

两者都要与模型 logits 在同一设备上，以便 Embedding 查表和后续 loss
计算不发生跨设备错误；`labels` 不会因此变成 bfloat16。

学习者回答：`cpu；不会；不会；因为后面要与模型在同一设备上产生的 logits
计算 loss`。全部正确。[验收]

### 第 16 轮：记录 `last_step`，准备计算全局学习率进度

第 30～31 行：

```python
last_step = step
lr = get_lr(
    epoch * iters + step,
    args.epochs * iters,
    args.learning_rate
)
```

`last_step` 只是记录本轮实际处理到的最后 batch 编号，不影响模型计算。
为了调用 `get_lr()`，三个实参先计算为：

示例 `epoch=0、iters=4、step=1、epochs=2、learning_rate=0.00001`：

```text
current_step = epoch * iters + step = 1
total_steps = epochs * iters = 8
lr = 0.00001
```

第二轮 `epoch=1` 的第一个 batch 的全局 `current_step` 是
`1*4+1=5`，不是 1。这样学习率会跨 epoch 连续变化。

学习者回答：`1；8；0.00001；5`。四项全部正确。[验收]

当前恢复点：已进入 `get_lr()`，下一步解释余弦公式如何把基础学习率
变成当前 batch 的实际学习率；尚未修改 optimizer 的参数组学习率。

### 第 17 轮：`get_lr()` 的余弦衰减比例

源码：

```python
def get_lr(current_step, total_steps, lr):
    return lr * (
        0.1 + 0.45 * (
            1 + math.cos(math.pi * current_step / total_steps)
        )
    )
```

先将 `current_step / total_steps` 看成 0 到 1 的训练进度，再乘以
`math.pi` 映射到 0 到 π。余弦值从 1 平滑变到 -1，外层比例因此从
1.0 平滑降到 0.1：

```text
进度 0：cos(0)=1，比例 1.00
进度 1/2：cos(π/2)=0，比例 0.55
进度 1：cos(π)=-1，比例 0.10
```

基础 `lr=0.00001` 时，三处实际值分别是 `0.000010`、`0.0000055`、
`0.000001`。示例第一 batch `current_step=1、total_steps=8` 的比例约
为 `0.9657`，实际学习率约 `0.000009657`。`0.1` 是最低学习率比例，
所以训练结束时并不会降到 0。

学习者回答了三个代入式：
`0.1 + 0.45 * (1 + cos(3.14 * 0))`、对应 `0.5` 和 `1` 的式子。
式子全部正确，计算结果依次为 `1.00；0.55；0.10`。[验收]

当前恢复点：已算出动态学习率，下一步读取 `param_groups`，看它怎样
写回 AdamW。

### 第 18 轮：把动态学习率写回 AdamW 参数组

源码：

```python
for param_group in optimizer.param_groups:
    param_group['lr'] = lr
```

`optimizer.param_groups` 是参数组配置的列表。当前 MiniMind 将
`model.parameters()` 一次性传给 AdamW，通常只有一个参数组，概念上包含：

```python
{
    'params': [...模型真实 Parameter...],
    'lr': 1e-5,
    'weight_decay': 0.01,
    ...
}
```

循环把本 batch 的动态学习率写入每个参数组。它修改的是优化器下一次
`optimizer.step()` 使用的学习率配置，不是模型参数，也不会执行
`optimizer.step()`、forward 或 backward。参数组设计成列表，是为了支持
不同模型参数组使用不同学习率；当前代码通常循环一次。

学习者回答：`1；学习率；否`。第 1、3 项正确，第 2 项补全为“修改
optimizer.param_groups 的学习率配置”。[纠偏][验收]

当前恢复点：第 35 行 `with autocast_ctx:`，下一步进入 forward 前的
精度上下文。

### 第 19 轮：`model` 的来源、对象和作用域

第 36 行使用：

```python
res = model(input_ids, labels=labels)
```

`MiniMindForCausalLM` 是类；`MiniMindForCausalLM(lm_config)` 产生该类
的实例对象。`init_model()` 创建并加载这个对象后，主程序通过：

```python
model, tokenizer = init_model(...)
```

把返回对象绑定到当前模块的全局名称 `model`。Python 的 `if`、`for`、
`while`、`with` 不创建新的变量作用域，因此 `if __name__ == '__main__'`
内部赋值的 `model` 仍属于模块级作用域。

`train_epoch()` 的参数中没有 `model`。函数运行到第 36 行时，局部作用域
找不到 `model`，于是从当前模块全局作用域找到第 135 行绑定的
`MiniMindForCausalLM` 实例并调用它。

学习者回答：`类；对象；否；全局变量中`。四项正确。[验收]

此前关于精度的补正也再次明确：`autocast_ctx` 是运算精度上下文，但
`input_ids` 在进入模型前仍必须是 `torch.long`，不是 bfloat16。

当前恢复点：已确认第 36 行使用的是模块全局模型对象；下一步解释
`model(...)` 如何进入该对象的 `forward()`。

### 第 20 轮：`model(...)` 经 `__call__()` 进入 `forward()`

由于 `MiniMindForCausalLM` 继承 PyTorch `nn.Module` 体系，模型对象可像
函数一样调用：

```text
model(input_ids, labels=labels)
-> nn.Module.__call__()
-> MiniMindForCausalLM.forward(...)
```

`__call__()` 负责 Module 的统一调用机制并最终进入具体类的 `forward()`。
`input_ids` 是位置参数，`labels=labels` 是按参数名传入的关键字参数；未
传入的 `attention_mask`、`past_key_values`、`use_cache` 和
`logits_to_keep` 使用默认值。forward 最终返回
`MoeCausalLMOutputWithPast` 对象，里面有 loss、logits、aux_loss 等多个
字段，而不是只返回一个 loss 数字。

学习者回答：`__call__(); forward();参数名；对象`。四项正确。[验收]

### 第 21 轮：区分全局 `model` 与内部 `self.model`

对象层级：

```text
模块全局变量 model
└── MiniMindForCausalLM 实例
    ├── self.model：MiniMindModel 实例
    └── self.lm_head：Linear(768 -> 6400)
```

外层 `MiniMindForCausalLM.forward()` 第一行：

```python
hidden_states, past_key_values, aux_loss = self.model(
    input_ids,
    attention_mask,
    past_key_values,
    use_cache,
    **kwargs
)
```

当前训练真实值为：`attention_mask=None`、`past_key_values=None`、
`use_cache=False`。完整 batch `[16,768]` 经过已经学习过的 Embedding、
8 层 Block 和 RMSNorm，得到 `hidden_states=[16,768,768]`。默认非 MoE
模型的 `aux_loss` 不是 None，而是值为 0 的标量 Tensor，方便外层统一
相加。

学习者回答：`MiniMindForCausalLM；MiniMindModel；[16, 768, 768]；0`。
全部正确；第 4 项精确表述为“值为 0 的标量 Tensor”。学习者同时确认：
推理主干已经走过，后续重点只关注训练新增流程，不重复深挖相同主干。
[验收][进度调整]

当前恢复点：`MiniMindForCausalLM.forward()` 第 247～248 行，生成训练所需
的完整序列 logits。

### 第 22 轮：完整序列 logits 与 labels 的 next-token 对齐

`logits_to_keep` 默认是 0。由于 `-0 == 0`，
`slice(-logits_to_keep, None)` 实际为 `slice(0, None)`，保留全部序列
位置：

```python
hidden_states.shape = [16, 768, 768]
logits.shape = [16, 768, 6400]
```

`lm_head` 对每个 token 的 768 维隐藏向量独立产生 6400 个词表分数。
这些是原始 logits，不是概率。训练保留所有位置，因为后续 labels 会
决定哪些位置真正计入 SFT loss。

学习者回答：`全部位置；[16, 768, 768]；[16, 768, 6400]；原始分数`。
全部正确。[验收]

### 第 23 轮：为什么 logits 和 labels 要错开一位

源码：

```python
x = logits[..., :-1, :].contiguous()
y = labels[..., 1:].contiguous()
```

因果语言模型在原始位置 `t` 的输出预测原始位置 `t+1` 的 token。因此
原始最后一个 logits 没有本序列内的下一个目标，去掉 logits 最后一位；
labels 去掉第一位后，二者对齐：

```text
x 的位置 0 -> 原始 logits 位置 0 -> y 的位置 0，即原始 labels 位置 1
x 的位置 1 -> 原始 logits 位置 1 -> y 的位置 1，即原始 labels 位置 2
...
```

当前 batch：

```python
x.shape = [16, 767, 6400]
y.shape = [16, 767]
```

学习者回答：`B；因为后面没有token了；前767个；后767个；
[16; 767; 6400] [16; 767]`。理解正确；“后 767 个”精确表述为
原始 labels 的位置 1～767。[纠偏][验收]

### 第 24 轮：`contiguous()` 与展平为分类输入

切片后调用 `.contiguous()`，必要时把选定区域整理成连续内存布局，
不改变数值和 shape，以便后续 `view()`。

```python
x.view(-1, x.size(-1)).shape
= [16 * 767, 6400]
= [12272, 6400]

y.view(-1).shape
= [12272]
```

交叉熵看到的是：

```text
x_flat：12272 行，每行是一个 token 位置对 6400 类的原始分数
y_flat：12272 个目标类别 ID
```

这里的 12272 个“样本”是 token 预测位置，不是 12272 条原始
conversation。学习者回答：`[12272, 6400]；[12272]；12272道6400
分类题；token预测位置`。全部正确。[验收]

当前恢复点：第 252 行 `F.cross_entropy(...)`，下一步解释有效 labels、
`-100` 忽略位置以及标量 `res.loss` 的形成。

### 第 25 轮：交叉熵输入、目标 ID 与 `-100` 忽略

`F.cross_entropy(x_flat, y_flat, ignore_index=-100)` 把每一行
`x_flat[n]` 当成一个 token 预测位置对整个词表的原始 logits；
`y_flat[n]` 是这个位置的正确目标类别 ID。

例如：

```python
y_flat[n] == 463
```

表示该位置应该预测词表 ID 为 463 的下一个 token。它不是在说当前
输入 token 本身必然是 463，而是 next-token 对齐后的目标。

若：

```python
y_flat[n] == -100
```

交叉熵通过 `ignore_index=-100` 跳过这一行，不计算它的 loss，也不把
它计入默认平均分母。于是 system、user、tool、padding 对应位置不参与
SFT loss，assistant 正文和 EOS 对应位置才参与。

学习者回答：`表示第n个token的分类结果；表示第n个token的id是463；否；
汇总后标量tensor`。后两项和总体方向正确；精确纠正为：`x_flat[n]`
是原始分类分数，不是已完成的分类结果；`y_flat[n]=463` 是该预测位置
对应的下一个目标 token ID。[纠偏][验收]

当前恢复点：将用很小的 logits 例子实际观察一行交叉熵和 `-100` 的
忽略效果。

### 第 26 轮：从正确答案概率推导交叉熵

第一次实验输出：

```text
忽略第二行：tensor(0.4076)
只算第一行：tensor(0.4076)
两行都算：  tensor(0.4076)
```

三者相同不是 `ignore_index` 失效，而是两行数据对称：第一行目标类别 0
的 Softmax 概率为 0.6652，第二行目标类别 2 的概率也为 0.6652，所以两
行 loss 都为 `-log(0.6652)=0.4076`，两个相同值的平均仍是 0.4076。

交叉熵的逻辑推导：

```text
logits
-> Softmax 得到类别概率
-> 取训练数据中正确类别的概率 p_correct
-> 多个正确答案的共同可能性原本是概率连乘
-> log 将连乘变成 log 概率相加，避免极小乘积且便于优化
-> 最大化 log(p_correct)
-> 加负号，改写成优化器习惯的最小化 -log(p_correct)
-> 对所有有效 token 位置取平均
```

one-hot 目标下，完整交叉熵 `-sum(y[c] * log(p[c]))` 只有正确类别的
`y[c]=1`，因此退化为 `-log(p_correct)`。正确答案概率越高，loss 越小；
越自信地排斥正确答案，loss 越大。

学习者回答：`A；A；因为loss是按照越小越好`。正确。精确补充：
原目标是最大化不大于 0 的 `log(p_correct)`，加负号后等价为最小化非负
的 loss，适配训练优化方向。[验收]

当前恢复点：把第二行目标改为类别 1，制造不同的单行 loss，再验证
`ignore_index=-100` 的实际排除效果。

### 第 27 轮：用非对称目标验证 `ignore_index`

将第二行目标从类别 2 改成类别 1 后，学习者实际运行得到：

```text
忽略第二行：tensor(0.4076)
只算第一行：tensor(0.4076)
两行都算：tensor(0.9076)
```

第二行自己的 loss 为约 1.4076，所以两行都计算时平均为：

```python
(0.4076 + 1.4076) / 2 = 0.9076
```

而目标为 `-100` 时结果与只算第一行完全相同，证明该位置被排除，没有
进入 loss，也没有进入平均分母。[实践][验收]

当前恢复点：交叉熵与 labels 的训练目标已经闭环，返回
`train_epoch()` 第 37 行读取 `res.loss` 和 `res.aux_loss`。

### 第 28 轮：主任务 loss 与 MoE 辅助 loss 相加

源码：

```python
loss = res.loss + res.aux_loss
```

`res.loss` 是本 batch 中所有未被 `-100` 忽略的 assistant 目标位置的
平均 next-token 交叉熵。默认 `use_moe=False` 时，`res.aux_loss` 是值为
0 的标量 Tensor；MoE 模型中它用于约束专家路由。统一相加后仍保留计算
图，可用于 backward。

`MiniMindForCausalLM.forward()` 内部的局部 `loss` 通过返回对象成为
`res.loss`；`train_epoch()` 中再次赋值的 `loss` 是另一个函数作用域里的
局部变量，两者不是同一个变量。

学习者回答：`本次batch训练得到的loss；0；是；否`。整体正确；精确
补充 `res.aux_loss` 是“值为 0 的标量 Tensor”，`res.loss` 是有效目标
token 位置的平均交叉熵。[纠偏][验收]

当前恢复点：第 38 行 `loss = loss / args.accumulation_steps`。

### 第 29 轮：为梯度累积缩放每个微批次的 loss

源码：

```python
loss = loss / args.accumulation_steps
```

默认 `accumulation_steps=1`，当前真实 loss 不变。若 `batch_size=16`、
`accumulation_steps=4`，程序连续处理四个各含 16 条样本的微批次，四次
backward 累积梯度后再更新一次参数，效果上一次更新综合约 64 条样本。

由于 PyTorch 梯度默认累加，若每个微批次不先除以 4，四批梯度会直接
求和，更新尺度约放大 4 倍。每个 loss 先除以 4，可使累计结果成为四批
梯度的平均尺度。

学习者回答：`否；64；取所有的loss平均；否`。全部正确。精确补充：
直接目标是得到微批次梯度的平均；由于求导线性，可以通过先平均 loss
实现。第 38 行尚未 backward，也未更新参数。[验收]

当前恢复点：第 40 行 `scaler.scale(loss).backward()`。

### 第 30 轮：`backward()` 在默认 bfloat16 路径中的实际作用

源码：

```python
scaler.scale(loss).backward()
```

当前默认 `dtype='bfloat16'`，前面创建的 GradScaler 是
`enabled=False`，所以 `scaler.scale(loss)` 不会放大 loss，实际效果等价
于：

```python
loss.backward()
```

反向传播沿 forward 建立的计算图，从交叉熵一路计算 loss 对 lm_head、
Transformer Block、Attention、FeedForward、RMSNorm 和 Embedding 等可
训练浮点参数的梯度，并把结果写入或累加到：

```python
parameter.grad
```

它不会立刻修改参数本身的数值；参数更新要等后面的
`scaler.step(optimizer)`。`input_ids` 和 `labels` 是 long 整数 Tensor，
不会得到“token ID 的浮点梯度”。`labels=-100` 的位置不直接产生交叉熵
监督，但 user/system 等上下文仍会通过前向表示影响后续 assistant 目标
位置的 loss。

学习者回答：`不会；填充累加grad；否；否；是`。五项全部正确。第 2 项
精确表述为“向 `parameter.grad` 写入或累加梯度”。[验收]

当前恢复点：`train_epoch()` 第 42 行 `if step % args.accumulation_steps
== 0:`。

### 第 31 轮：由 `accumulation_steps` 决定何时更新参数

源码：

```python
if step % args.accumulation_steps == 0:
```

这是用取余判断当前是否完成了一组微批次。默认 `accumulation_steps=1`
时，`step=1` 满足 `1 % 1 == 0`，第一个 batch 的 backward 后立即进入
更新分支。若设为 4：

```text
step=1、2、3：条件 False，只保留并累加梯度
step=4：条件 True，处理累计梯度并更新一次参数
```

条件为 False 时，梯度不会丢掉，会保留在各参数的 `.grad` 中，继续与下
一个微批次的梯度相加。学习者回答：`学会；不会；第4个step完成；保留到
后续batch`。第一项按“会进入更新分支”确认，其余三项正确。[纠偏][验收]

当前恢复点：更新分支第一行 `scaler.unscale_(optimizer)`。

### 第 32 轮：`unscale_()` 恢复 float16 的真实梯度尺度

`scaler.scale(loss).backward()` 在启用 float16 GradScaler 时会把放大后的
梯度写进 `parameter.grad`。更新分支先执行：

```python
scaler.unscale_(optimizer)
```

它按照当前缩放因子把梯度还原到真实尺度，供后面的梯度裁剪和优化器更新
使用。必须先 unscale 再 clip，否则裁剪阈值会被缩放因子扭曲，可能发生
过度裁剪。`unscale_()` 不清零梯度、不改变模型参数，也不执行
`optimizer.step()`。

当前 CPU + 默认 bfloat16 路径中 GradScaler 禁用，因此这行实际是 no-op；
代码保留它是为了同一循环兼容启用 float16 scaler 的情况。

学习者回答：`放大后的梯度；恢复被放大的梯度值；需要防止被过渡裁剪；否；
不会`。五项正确；“过渡”纠正为“过度”。[纠偏][验收]

当前恢复点：更新分支第二行 `torch.nn.utils.clip_grad_norm_(...)`。

### 第 33 轮：梯度范数裁剪保持方向、限制整体大小

源码：

```python
torch.nn.utils.clip_grad_norm_(
    model.parameters(),
    args.grad_clip
)
```

默认 `args.grad_clip=1.0`。函数把所有参数的梯度看成一个整体长向量，
计算其 L2 范数；若不超过阈值则不变，若超过阈值则让所有参数梯度乘以
同一个比例：

```python
scale = max_norm / total_norm
```

它不是逐元素把梯度截断到 `[-max_norm, max_norm]`，而是统一缩放，因此
整体方向保持不变。函数名末尾的 `_` 表示原地修改各参数的 `.grad`，并
返回裁剪前的总范数；当前源码没有接收返回值。

学习者实际运行：

```text
原梯度：p1=[3,4]，p2=[0,12]
裁剪前总范数：13
max_norm：5
裁剪后 p1.grad：[1.1538, 1.5385]
裁剪后 p2.grad：[0, 4.6154]
裁剪后总范数：5
```

即所有分量统一乘以 `5/13`。实践证明裁剪后范数约等于阈值，梯度方向
没有被改变。[实践][验收]

当前恢复点：更新分支第三行 `scaler.step(optimizer)`。

### 第 34 轮：`scaler.step(optimizer)` 真正更新参数

源码：

```python
scaler.step(optimizer)
```

当前 CPU + disabled GradScaler 路径中，这行实际等价于：

```python
optimizer.step()
```

AdamW 读取各参数的 `parameter.grad`，结合 m/v 历史、动态学习率和权重
衰减，真正修改模型参数。前面的 backward 只产生或累积梯度，unscale 只
恢复尺度，clip 只限制梯度大小；本行才发生参数更新。

启用 float16 GradScaler 时，`scaler.step(optimizer)` 还会检查梯度是否
包含 `inf` 或 `nan`：梯度有限才调用 optimizer.step()，异常时跳过本次
更新，避免污染模型参数。

更新之后，参数的 `.grad` 仍然保留，不会自动清零；下一行需要显式清理。

学习者回答：`optimizer.step(); 是；grad；跳过这次更新；不会`。五项全部
正确。[验收]

当前恢复点：更新分支第四、五行 `scaler.update()` 与
`optimizer.zero_grad(set_to_none=True)`。

### 第 35 轮：更新后调整 scaler 并清空梯度

源码：

```python
scaler.update()
optimizer.zero_grad(set_to_none=True)
```

启用 float16 GradScaler 时，`scaler.update()` 根据本轮是否出现 `inf`/
`nan` 调整下一轮 loss 缩放因子；梯度正常时可逐步增大，出现异常时减小。
当前默认 bfloat16、scaler disabled，因此它是 no-op。

`zero_grad(set_to_none=True)` 把每个参数的 `.grad` 直接设为 `None`，而
不是把已有梯度 Tensor 的元素逐一写成 0。这样可减少内存写入，下一次
backward 再按需创建梯度。若不清零，下一次 backward 会在已有 grad 上
继续累加；若在梯度累积的每个 batch 都清零，前面微批次的梯度就会丢失。

完整累积时序：

```text
第一批：grad=None -> 创建梯度
中间批：不清零 -> 累加梯度
达到 accumulation_steps：step/update -> zero_grad -> grad=None
```

学习者回答：`不会；None；只在参数更新后才重置；再次创建梯度`。四项
全部正确。[验收]

当前恢复点：参数更新分支之后的日志条件：
`if step % args.log_interval == 0 or step == iters:`。

### 第 36 轮：日志打印条件

源码：

```python
if step % args.log_interval == 0 or step == iters:
```

默认 `log_interval=100`。第一个条件使程序在 step 100、200……打印；
第二个条件保证 epoch 最后一个 step 即使不是 100 的倍数也会打印。示例
`iters=4` 时只在 step 4 打印；`iters=250` 时在 step 100、200、250
打印。该条件只决定是否进入日志计算，不会重新 forward、backward 或更新
模型参数。

学习者回答：`否；是；100；200；250；不会`。对应四题全部正确：
step 1 不打印，step 4 因为等于 iters 而打印，250 步时打印三次，条件
不会改变模型参数。[验收]

当前恢复点：日志块第 52～55 行，计算耗时和拆分显示 loss。

### 第 37 轮：日志恢复 loss 原始尺度并拆分主/辅助损失

源码：

```python
spend_time = time.time() - start_time
current_loss = loss.item() * args.accumulation_steps
current_aux_loss = res.aux_loss.item() if res.aux_loss is not None else 0.0
current_logits_loss = current_loss - current_aux_loss
```

`spend_time` 是本次 `train_epoch()` 开始到当前日志 step 的累计秒数，不是
单个 batch 耗时。`.item()` 把标量 Tensor 的数值取成 Python 数字用于
日志，不修改原 Tensor，也不会再次 backward。

用于 backward 的 loss 前面除过 `accumulation_steps`；日志再乘回，使
显示值保持在原始总损失尺度。`current_loss` 近似等于主交叉熵加辅助
损失，`current_aux_loss` 单独取 MoE 辅助项，二者相减得到主任务的
`current_logits_loss`。

示例 `res.loss=2.3、res.aux_loss=0.1、accumulation_steps=4`：

```text
backward loss = 0.6
current_loss = 2.4
current_aux_loss = 0.1
current_logits_loss = 2.3
```

学习者回答：`0.6；2.4；0.1；2.3；累计到当前的耗时`。五项全部正确。
[验收]

### 第 38 轮：读取动态学习率并估算本 epoch 剩余时间

源码：

```python
current_lr = optimizer.param_groups[-1]['lr']
eta_min = spend_time / max(step - start_step, 1) * (iters - step) // 60
```

`param_groups[-1]` 是最后一个参数组；当前只有一组，因此就是第 0 组。
这里读取的是前面已经写入 optimizer 的本 batch 动态学习率，不是固定的
基础 `args.learning_rate`。

ETA 拆解：

```text
step - start_step：本次 train_epoch 调用实际处理的 batch 数
spend_time / 已处理批数：平均每批秒数
iters - step：本 epoch 剩余批数
二者相乘：预计剩余秒数
// 60：转换成向下取整的分钟数
```

`max(..., 1)` 防止分母为 0。示例 `spend_time=120、start_step=0、
step=100、iters=250`：已处理 100 批，平均 1.2 秒/批，还剩 150 批，
ETA 为 `1.2*150//60=3` 分钟。

学习者回答：`100；1.2；150；3min；已经写入的动态学习率`。五项全部
正确。[验收]

当前恢复点：第 58～59 行，打印本地日志并按配置可选上传 wandb。

### 第 39 轮：终端日志与可选 wandb 上传

`Logger(...)` 显示：人类轮次 `epoch+1`、当前/总 step、总 loss、主任务
logits loss、MoE aux loss、动态学习率和预计剩余分钟。格式符 `.4f`、
`.8f`、`.1f` 分别控制小数位数。日志字段 `epoch_time` 实际传入的是
`eta_min`，即本 epoch 的预计剩余时间。

`Logger()` 只在主进程 `print`，当前单进程会打印。默认 `wandb=None`，
所以随后的 `if wandb: wandb.log(...)` 不上传指标；本地打印与平台上传
互不依赖。

学习者回答：`后者；aux_loss；0；不会；会`。即 `epoch=0` 显示
`Epoch:[1/2]`，总 loss 与 logits loss 的差额是 aux loss，非 MoE 为 0，
不上传 wandb，但终端会打印。全部正确。[验收]

### 第 40 轮：checkpoint 保存条件

源码条件：

```python
if (
    step % args.save_interval == 0
    or step == iters
) and is_main_process():
```

默认每 1000 step 保存，且 epoch 最后一步无论是否整除 1000 都保存；
分布式中仅主进程落盘，避免多个进程同时写同一文件。例子
`save_interval=1000、iters=2500` 时，保存 step 1000、2000、2500；若
一个 epoch 只有 250 步，也在 step 250 保存。

学习者回答：`1000；2000；2500；因为是最后一个；不会；会`。全部正确。
[验收]

补正教学示例的显示笔误：若 `current_aux_loss=0.1`，`:.4f` 应打印
`aux_loss: 0.1000`。

### 第 41 轮：保存前后只切换模型运行模式

保存块首尾：

```python
model.eval()
# 收集并保存状态
model.train()
```

`eval()` 递归设置模型和子模块的 `training=False`，主要影响 Dropout、
BatchNorm 等训练态和推理态行为不同的层；它不会修改权重、关闭全局梯度、
清除 `.grad` 或把 `requires_grad` 改成 False。MiniMind 使用 RMSNorm，
默认 `dropout=0.0`，且保存期间不做 forward，因此本路径中参数数值不会因
切换模式而变化。

保存后调用 `model.train()` 只是恢复 `training=True`，并不自动执行一轮
训练。学习者回答：`不会；不会；dropdout；切换训练态；不会`。五项理解
正确；术语拼写纠正为 `Dropout`。[纠偏][验收]

当前恢复点：保存块第 63～64 行，构造 MoE 后缀和权重文件路径。

### 第 42 轮：构造模型权重保存路径

当前非 MoE 配置：

```python
moe_suffix = ''
ckp = '../out/full_sft_768.pth'
```

若 `use_moe=True`，文件名变为 `../out/full_sft_768_moe.pth`。相对路径
取决于程序启动时的工作目录；从
`/home/zcf/githubs/minimind/minimind/trainer` 启动时，`..` 指向
`/home/zcf/githubs/minimind/minimind`，所以实际文件位于
`/home/zcf/githubs/minimind/minimind/out/full_sft_768.pth`。

学习者回答中路径误写成 `trainer/out`，已纠正。其余关于空后缀、ckp
字符串、MoE 文件名以及通过 `--save_weight` 改名均正确。特别提醒：
默认训练会覆盖已经下载到同一路径的 `full_sft_768.pth`；短训练实验应
使用例如 `--save_weight full_sft_learning`。[纠偏]

学习者提出后续可一次多读几行以提高效率，教学粒度调整为“小代码块
讲解后验收”，仍保持调用顺序和不跳过训练新增逻辑。[进度调整]

当前恢复点：保存块第 65～68 行，解除可选包装、取得 state_dict 并保存
可直接加载的模型权重。

### 第 43 轮：解除模型包装并保存纯模型权重

保存代码先处理 DDP 和 `torch.compile` 包装：

```python
raw_model = model.module if isinstance(model, DistributedDataParallel) else model
raw_model = getattr(raw_model, '_orig_mod', raw_model)
```

DDP 模型的 `.module` 指向真正子模型；compile 包装通常用 `_orig_mod`
指向原模型。当前单进程且 `use_compile=0`，两个分支都不解包，最终
`raw_model` 就是全局 `model` 指向的 `MiniMindForCausalLM` 实例。

随后：

```python
state_dict = raw_model.state_dict()
torch.save(
    {k: v.half().cpu() for k, v in state_dict.items()},
    ckp
)
```

`state_dict` 是模型参数及持久 buffer 的名称到 Tensor 映射，不含 AdamW
的 m/v、epoch、step 或数据集。字典推导式只把要保存的 Tensor 副本转成
float16 并移到 CPU，减小体积并解除 GPU 设备依赖；不会把正在训练的模型
永久改成 half 或迁到 CPU。该文件是可由 `load_state_dict()` 加载的纯模型
权重，不是完整断点状态；目标文件已存在时 `torch.save` 会覆盖。

学习者回答：`model；不包含；保存的；纯模型权重；覆盖`。五项全部正确。
[验收]

当前恢复点：第 69～70 行 `lm_checkpoint(...)`，保存用于恢复训练的附加
状态。

### 第 44 轮：纯权重文件与 `_resume.pth` 的分工

`lm_checkpoint(...)` 在 `../checkpoints` 中构造：

```text
full_sft_768.pth：checkpoint 目录中的纯模型权重
full_sft_768_resume.pth：完整续训状态
```

加上外层已写入的 `../out/full_sft_768.pth`，一次保存流程会维护两份纯
权重文件和一份 resume 文件。

函数先把纯 state_dict 写到 `.tmp`，成功后用 `os.replace()` 原子替换正式
文件，降低程序在写到一半时留下损坏正式 checkpoint 的风险。resume_data
包含模型参数、AdamW 状态、epoch、step、world_size、wandb_id；调用参数
`scaler=scaler` 被 `**kwargs` 接收，函数检测到它有 `state_dict()` 后写入
`resume_data['scaler']`。最后用相同的临时文件策略保存 `_resume.pth`。

学习者回答：`纯模型参数；优化器相关的参数还有当前训练的一些状态内容；
防止写一半失败；因为scaler缩放会随着训练过程变化；后者`。第 1、2、3、
5 项正确。第 4 项说明了保存 scaler 的原因；机制补充为
`scaler -> **kwargs -> scaler.state_dict() -> resume_data['scaler']`。
[纠偏][验收]

当前恢复点：保存块最后的 `model.train()`、`del state_dict`，以及每个
batch 结束时释放临时对象。

### 第 45 轮：保存后恢复训练态并释放 batch 临时对象

保存完成后：

```python
model.train()
del state_dict
```

`model.train()` 把前面临时切换到的 `eval` 模式恢复为训练模式，不会立即
训练 batch。`del state_dict` 只删除当前函数对临时状态字典的引用，不会
删除模型内部参数。

每个 batch 末尾还会执行：

```python
del input_ids, labels, res, loss
```

尽早释放当前 batch 的输入、标签、模型输出和 loss/计算图引用，降低内存
占用；它不删除模型、optimizer、参数梯度或 AdamW 的 m/v。即使 loss 变量
被删除，只要尚未 `zero_grad()`，梯度仍保存在各参数的 `.grad` 中，继续
供下一微批次累积。

学习者回答：`不会；不会；清理内存；是；继续下一个batch训练`。五项全部
正确。[验收]

当前恢复点：`train_epoch()` 的 `for` 循环耗尽后，函数末尾处理未凑满
`accumulation_steps` 的剩余梯度。

### 第 46 轮：epoch 末尾的不完整梯度累积不会被丢弃

以 `accumulation_steps=4、总 batch=10` 为例：循环体内在 `step=4` 和
`step=8` 各完成一次参数更新；`step=9、10` 仍然执行了完整的 forward
和 backward，只是因为不满足 `step % 4 == 0`，没有在循环体内调用
`optimizer.step()`，它们的梯度继续保存在各参数的 `.grad` 中。

`for` 循环结束后执行：

```python
if last_step > start_step and last_step % args.accumulation_steps != 0:
    scaler.unscale_(optimizer)
    torch.nn.utils.clip_grad_norm_(model.parameters(), args.grad_clip)
    scaler.step(optimizer)
    scaler.update()
    optimizer.zero_grad(set_to_none=True)
```

这里没有重新跑 forward 或 backward；尾部的 `scaler.step(optimizer)` 读取
的正是 `parameter.grad` 中已经累积的 `(g9 + g10) / 4`，并据此完成最后
一次参数更新。之后才清零梯度。

因此，不凑满 `accumulation_steps` 的剩余 batch 不会被丢弃；它们仍被
DataLoader 取出、仍计算梯度，并在 epoch 结束时用尾部更新分支作用到参数。
即使最后一个 DataLoader batch 本身不足 `batch_size`，也会被
`SkipBatchSampler` 的末尾 `yield batch` 交付，除非恢复逻辑明确仍在跳过
尚未跳完的 batch。若本轮根本没有处理任何新 batch，`last_step > start_step`
为 False，才不会进行虚假的尾部更新。

学习者回答：`有；参数的梯度里；79行`，并追问最后不凑整是否丢弃。第
2、3 项方向正确；第 1 项纠正为“没有重新计算梯度”，第 79 行的
`scaler.step(optimizer)` 使用残余梯度完成更新。[纠偏][验收]

当前恢复点：`train_epoch()` 尾部收尾分支已理解；下一步回看函数末尾
的 epoch 结束边界和主程序如何进入下一个 epoch。

### 第 47 轮：残余梯度更新与 checkpoint 保存顺序

学习者追问：最后不完整累积组的梯度到底在哪里生效、是否会保存到参数
文件。精确顺序是：

```text
step=9、10 的 forward/backward
-> 梯度留在各参数的 parameter.grad
-> for 循环结束
-> 尾部第 79 行 scaler.step(optimizer)
-> AdamW 修改内存中的 parameter.data
-> 后续 state_dict()/torch.save() 才能把新 parameter.data 写入磁盘
```

`torch.save()` 本身不执行优化，只是读取当时的参数值并保存；纯模型权重
文件也不保存 `parameter.grad`。学习者先回答“save；不会”，其中第一项
纠正为“第 79 行 `scaler.step(optimizer)`”，第二项正确。[纠偏]

源码还有一个重要顺序细节：epoch 最后一个 step 在循环体内先满足
`step == iters`，因此先执行保存块；`for` 结束后才执行尾部残余梯度的
`scaler.step(optimizer)`。所以当：

```text
accumulation_steps > 1
且 iters % accumulation_steps != 0
```

时，内存中的模型会包含尾部更新，但刚刚写入的 `out/*.pth` 和
`checkpoints/*_resume.pth` 可能还不包含它。若后面还有 epoch 并在之后
再次保存，后续文件会包含这次更新；若这是最后一个 epoch 且没有后续保存，
磁盘最终文件对未来重启而言会漏掉这一次优化。默认
`accumulation_steps=1` 时不存在这个特殊顺序问题。

因此学习者最后的理解“这种情况下就丢失了这一次的梯度优化”需要分层说：

```text
当前训练进程的内存模型：没有丢，更新已经发生
磁盘 checkpoint：若无后续保存，确实缺少这次更新
```

[纠偏][验收]

### 当前阶段性进度

今天完成了从第 158 行 epoch 循环到 `train_epoch()` 末尾残余梯度处理的
主要训练闭环：

```text
epoch / sampler / 随机索引
-> skip 与 SkipBatchSampler
-> DataLoader 迭代和 __getitem__
-> batch 设备迁移
-> 动态学习率
-> model(...) -> forward
-> logits 与 labels 错位
-> cross_entropy 和 ignore_index=-100
-> loss 缩放
-> backward
-> 梯度累积
-> unscale、梯度裁剪
-> AdamW 参数更新
-> scaler 更新和 zero_grad
-> 日志与 checkpoint
-> epoch 末尾残余梯度收尾
```

下一个边界：主循环如何从当前 epoch 进入下一个 epoch，以及训练全部结束
后的分布式 barrier/destroy_process_group；之后可继续做一次真实小规模
训练验证，并决定是否修正“epoch 末尾保存早于残余更新”的顺序。

### 第 48 轮：明确尾部更新何时会在磁盘上丢失

学习者进一步确认：若尾部更新发生后没有再次保存，这次优化是否会丢失。
答案需区分运行内存和磁盘文件：

```text
第 79 行 scaler.step(optimizer) 后：
内存中的模型参数已更新，没有丢

后面还有保存机会：
后续 state_dict()/torch.save() 会包含它

这是最后一个 epoch 且没有后续保存：
磁盘纯权重和 resume checkpoint 都停留在尾部更新之前，
以后重新加载时确实缺少这次优化
```

resume 文件还会缺少这次 AdamW 更新后的 m/v 和 step 状态。一个直接的
修复方向是在循环体中把更新条件改为“达到累积步数或 `step==iters`”，
让残余梯度先更新，再进入末尾保存块，并移除循环后的重复更新。但当前
阶段只记录源码风险，不擅自修改仓库。[源码观察][验收]

当前恢复点：`train_epoch()` 返回调用者后，外层 for 如何进入下一 epoch，
以及全部训练结束后的分布式清理。

### 第 49 轮：`train_epoch()` 返回后，模型状态为什么没有消失

`train_epoch()` 没有显式 `return`，因此默认返回 `None`。但它接收并使用的
`model`、`optimizer` 和 `scaler` 是在主程序外层创建的对象；函数内部的
`optimizer.step()`、`zero_grad()` 等操作会原地修改这些对象的状态，函数
返回后修改仍然存在。

因此第 2 个 epoch 不会重新加载 `pretrain_768.pth`，而是继续使用第 1 个
epoch 更新后的模型。AdamW 的 m/v 也不会在 epoch 边界自动清零；只有
参数的 `.grad` 会按训练循环中的 `zero_grad()` 逻辑清理。

主程序最后的：

```python
if dist.is_initialized():
    dist.barrier()
    dist.destroy_process_group()
```

只在分布式进程组已经初始化时执行。当前 CPU 单进程路径中条件为 False，
两句都跳过。

学习者回答：`None；因为在内存中保留了；不会；不会；不会`。五项均正确；
第 2 项补充为“外部对象被原地修改，所以状态仍在内存中的同一个对象里”。
[验收]

### 第 50 轮：设计不会覆盖正式权重的最小真实训练

完整 `sft_t2t_mini.jsonl` 大约 1.7G，共 905718 条样本。`SFTDataset`
通过 `load_dataset()` 加载数据，因此为了只验证几个 batch，先从原文件
抽取 4 条完整 JSONL 样本：

```bash
sed -n '1,4p' \
  dataset/sft_t2t_mini.jsonl \
  > dataset/sft_learning_4.jsonl

wc -l dataset/sft_learning_4.jsonl
du -h dataset/sft_learning_4.jsonl
```

学习者实际输出：

```text
4 dataset/sft_learning_4.jsonl
12K dataset/sft_learning_4.jsonl
```

实验将 `save_weight` 改为 `learning_probe`，保证不会覆盖现有的
`pretrain_768.pth` 和 `full_sft_768.pth`。配置为 1 个 epoch、batch size
为 1、4 条样本，因而预期执行 4 个 batch。[实践]

### 第 51 轮：训练入口首先暴露缺少 `datasets` 依赖

首次运行训练命令时，程序还没有创建模型或读取 batch，便在模块导入阶段
报错：

```text
ModuleNotFoundError: No module named 'datasets'
```

检查确认：

```text
当前解释器：minimind/.venv/bin/python
torch：已安装
transformers：已安装
datasets：未安装
requirements.txt：声明 datasets==3.6.0
```

这个虚拟环境由 `uv` 创建，没有内置 `pip` 命令，因此使用：

```bash
uv pip install --python .venv/bin/python datasets==3.6.0
```

安装后验证：

```text
datasets 3.6.0
```

此处还区分了 shell 提示中的 `(.venv)` 和工具启动的新 shell：不能只看
提示符猜测解释器，必要时直接使用 `.venv/bin/python` 消除路径歧义。
[环境][纠偏]

### 第 52 轮：学习者亲自跑通 4 个真实 Full SFT batch

学习者从 `minimind/trainer` 执行：

```bash
python train_full_sft.py \
  --save_dir ../out \
  --save_weight learning_probe \
  --epochs 1 \
  --batch_size 1 \
  --learning_rate 1e-5 \
  --device cpu \
  --dtype bfloat16 \
  --num_workers 0 \
  --accumulation_steps 1 \
  --grad_clip 1.0 \
  --log_interval 1 \
  --save_interval 1000 \
  --hidden_size 768 \
  --num_hidden_layers 8 \
  --max_seq_len 64 \
  --use_moe 0 \
  --data_path ../dataset/sft_learning_4.jsonl \
  --from_weight pretrain \
  --from_resume 0 \
  --use_compile 0
```

真实输出：

```text
Model Params: 63.91M
Trainable Params: 63.912M
Generating train split: 4 examples [00:00, 435.33 examples/s]
Epoch:[1/1](1/4), loss: 6.4684, logits_loss: 6.4684, aux_loss: 0.0000, lr: 0.00000868, epoch_time: 0.0min
Epoch:[1/1](2/4), loss: 4.8482, logits_loss: 4.8482, aux_loss: 0.0000, lr: 0.00000550, epoch_time: 0.0min
Epoch:[1/1](3/4), loss: 3.5540, logits_loss: 3.5540, aux_loss: 0.0000, lr: 0.00000232, epoch_time: 0.0min
Epoch:[1/1](4/4), loss: 4.1988, logits_loss: 4.1988, aux_loss: 0.0000, lr: 0.00000100, epoch_time: 0.0min
```

文件检查确认保存成功：

```text
out/learning_probe_768.pth                         132M
checkpoints/learning_probe_768.pth                 132M
checkpoints/learning_probe_768_resume.pth          619M
```

`save_interval=1000` 仍在第 4 步保存，是因为保存条件还有
`step == iters`。`use_moe=0` 使 `aux_loss=0`，所以总 loss 与
`logits_loss` 相同。四个 batch 来自不同样本，最后一个 loss 上升不能单独
证明训练失败。学习者回答：`lr_get改变了lr；因为没有使用moe；不能，因为
数据样本太少；step==iter`。四项方向正确，函数名纠正为 `get_lr()`，变量名
纠正为 `iters`。[实践][验收]

### 第 53 轮：直接比较训练前后参数文件

对比 `pretrain_768.pth` 和 `learning_probe_768.pth` 中的：

```text
model.embed_tokens.weight
```

学习者实际得到：

```text
训练前 shape：torch.Size([6400, 768])
训练后 shape：torch.Size([6400, 768])
发生变化的元素数量：2582494
参数总元素数量：4915200
最大绝对变化：3.0517578125e-05
整体变化范数：0.028845123946666718
```

这证明 `optimizer.step()` 改变了参数数值，而不会改变参数 shape。MiniMind
默认 `tie_word_embeddings=True`，源码执行：

```python
self.model.embed_tokens.weight = self.lm_head.weight
```

因此输入 Embedding 和输出 `lm_head` 使用同一个参数对象。输出端每个有效
位置都是 6400 分类，Softmax 后错误类别通常也有非零概率，所以共享词表
矩阵中远不止“输入里出现过的 token 行”能够获得梯度。

保存纯权重时又执行 `.half()`，微小更新可能在 float16 舍入后看起来没有
变化。因此仅凭“部分元素比较相等”，不能断言它们没有参与反向传播。

学习者回答：`同一份参数；6400；因为得分不为零；不能说明，因为做了精度
丢失`。全部正确；“得分不为零”精确为“Softmax 后错误类别通常有非零
概率”。[实践][验收]

### 第 54 轮：固定样本比较 loss 时发现 `dataset[0]` 仍会变化

为了公平比较训练前后的模型，尝试让 `pretrain` 与 `learning_probe` 对
同一个 `dataset[0]` 计算 loss。学习者连续运行两次却观察到：

```text
第一次：有效训练位置 25
pretrain loss: 6.330538272857666
learning_probe loss: 3.645669937133789

第二次：有效训练位置 21
pretrain loss: 5.674259185791016
learning_probe loss: 2.9653682708740234
```

原因不是模型的 `eval()` 随机，也不是数据文件顺序变化，而是
`SFTDataset.__getitem__()` 内部主动进行随机数据增强：

```python
conversations = pre_processing_chat(sample['conversations'])
prompt = post_processing_chat(prompt)
```

其中包括：

```text
20% 概率在没有 system 的对话前添加随机 system prompt
80% 概率删除空的 <think>...</think> 片段
```

由于 `max_seq_len=64`，前面增加或删除 token 会改变截断点，进而改变前
64 个 token 中落入多少个 assistant label。训练主程序的 `setup_seed()`
会同时固定 Python 与 PyTorch 随机源；独立比较脚本没有设置随机种子，
所以不同进程可能得到不同增强结果。学习者理解原因后选择不再重复验证。
[实践][验收]

### 第 55 轮：把实验权重接回推理链，终端只出现空白行

学习者使用原生模型路径加载：

```bash
.venv/bin/python eval_llm.py \
  --load_from model \
  --save_dir out \
  --weight learning_probe \
  --device cpu \
  --max_new_tokens 32 \
  --temperature 0.1 \
  --top_p 0.9 \
  --show_speed 1
```

输入两次“你是谁”后，模型均只显示很多空白行，但速度统计正常。这先证明：

```text
训练保存的纯权重
-> eval_llm.py 重建 MiniMindForCausalLM
-> load_state_dict 成功
-> generate() 确实运行
```

直接打印新 token 后确认，它不是“没有生成”，而是连续生成 32 个 ID 234：

```text
new ids: [234, 234, ..., 234]
token 234 -> '\n'
```

换行不是 EOS（ID 2），所以生成不会提前停止，只能到
`max_new_tokens=32` 后结束。[实践][诊断]

### 第 56 轮：比较三个权重的首 token 分布

对同一个“你是谁”模板，读取首个新 token 的 logits 和 Softmax 概率：

```text
pretrain：
  第 1 名是换行，概率约 0.099

learning_probe：
  第 1 名是换行，概率约 0.901

full_sft：
  第 1 名是“我”，概率约 0.885
```

因此问题不在加载或 streamer，而是 4 步小样本 Full SFT 已把输出分布强烈
推向换行。复现训练顺序 `[2, 3, 0, 1]` 后，4 个实际 batch 的 135 个
有效训练目标中，换行 ID 234 出现 9 次，是出现次数最高的单一 token；
同时样本极少、全量参数均更新、序列只截取前 64 token，形成了明显的小
样本过拟合和生成退化。

学习者正确回答：只有 EOS 才会使 generate 提前结束，换行不是 EOS；并
指出 `generate()` 已在前面的学习日精读过。本日不重复阅读生成循环，只把
它作为训练后验证工具。[验收][进度调整]

### 第 57 轮：区分训练时的真实前文和生成时的自身输出

训练时，一条真实序列会整体作为 `input_ids` 输入。因果遮罩保证每个位置
只能看前文，而这些前文 token 都来自数据集。生成时则把模型刚预测出的
`next_token` 拼回 `input_ids`，下一轮会把自己的输出作为前文。因此一次
错误的换行可能演变为连续换行。

学习者先选择“训练时使用模型刚生成的 token”，该说法实际描述的是生成
阶段，已纠正为：

```text
训练：前文来自数据集真实 token
生成：新前文逐步来自模型自身输出
```

随后重新确认 next-token 错位：

```python
x = logits[..., :-1, :]
y = labels[..., 1:]
```

若：

```text
input_ids = [10, 20, 30, 40]
```

则：

```text
x[0] 的 6400 个 logits -> 正确目标 20
x[1] 的 6400 个 logits -> 正确目标 30
x[2] 的 6400 个 logits -> 正确目标 40
y = [20, 30, 40]
```

学习者回答 `[20,30,40]`，目标对应正确。再次强调：`x` 是预测分数，不是
输入 token；`y` 是正确 token ID，不是模型预测结果。[纠偏][验收]

## 四、可复现教程：从 4 条数据到重新推理

### 1. 前置文件

```text
minimind/out/pretrain_768.pth
minimind/dataset/sft_t2t_mini.jsonl
minimind/model/tokenizer.json
minimind/model/tokenizer_config.json
```

### 2. 创建实验数据

从仓库根目录执行：

```bash
sed -n '1,4p' dataset/sft_t2t_mini.jsonl > dataset/sft_learning_4.jsonl
wc -l dataset/sft_learning_4.jsonl
```

预期为 4 行。JSONL 的一行就是一个完整训练 item，不能用任意字节截断。

### 3. 安装缺失依赖

仅在导入 `datasets` 失败时执行：

```bash
uv pip install --python .venv/bin/python datasets==3.6.0
.venv/bin/python -c "import datasets; print(datasets.__version__)"
```

### 4. 最小训练

从 `minimind/trainer` 执行第 52 轮的完整命令。必须使用新的：

```text
--save_weight learning_probe
```

避免覆盖正式权重。这个实验验证代码链路，不用于训练可用模型。

### 5. 验证产物

```bash
ls -lh \
  ../out/learning_probe_768.pth \
  ../checkpoints/learning_probe_768.pth \
  ../checkpoints/learning_probe_768_resume.pth
```

前两者是纯模型权重；`_resume.pth` 还包含 optimizer、scaler、epoch、step
等续训状态，所以明显更大。

### 6. 重新推理

回到仓库根目录后执行第 55 轮命令。4 条数据出现无意义输出并不说明链路
失败；应直接检查生成 token，区分“未生成”“特殊 token 被隐藏”和“只生成
空白字符”三种不同情况。

## 五、Day 15 完成边界

本日已经从真实源码完成并实践：

```text
epoch 与样本打乱
-> DataLoader 产生 batch
-> Full SFT forward
-> next-token logits/labels 对齐
-> cross_entropy 与 -100
-> backward 与梯度累积
-> 梯度裁剪与 AdamW 更新
-> 日志、纯权重、resume checkpoint
-> 最小真实训练
-> 参数文件比较
-> 重新加载并生成
-> 诊断连续换行退化
```

保留的源码风险：当 `accumulation_steps > 1` 且 epoch 的 batch 数不能整除
它时，循环体先保存、循环后才应用尾部残余梯度；最后一个 epoch 的磁盘
文件可能漏掉最后一次内存参数更新。本阶段只记录，不修改上游源码。

下一学习日进入 `trainer/train_pretrain.py`，仍按实际调用顺序阅读，但跳过
已经掌握的公共训练骨架，重点比较：

```text
PretrainDataset 与 SFTDataset 的输入和 labels
预训练为何学习“续写”
SFT 为何学习“按角色和指令回答”
pretrain 权重为何不擅长聊天
full_sft 权重为何能按 chat template 回答
```

准确恢复点：Day 16 从预训练入口及其默认参数开始，先建立它与 Full SFT
入口的差异地图，再进入 `PretrainDataset.__getitem__()`。
