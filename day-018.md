# Day 018：从普通 FFN 进入 MiniMind 的 MoE 分支

- 日期：2026-09-03
- 学习阶段：`minimind` 模型结构
- 今日主题：`MOEFeedForward` 的真实调用、router、top-k、专家分发、输出合并与 `aux_loss`
- 当前状态：已完成
- 恢复点：Day 19 从总参数量、激活参数量和计算成本开始

## 一、学习曲线纠偏

本日开始时直接进入了 `MOEFeedForward` 的实现，学习者指出缺少普通 FFN 和 MoE 的概念铺垫。随后退回已经掌握的 `FeedForward`，先建立以下对照：

```text
普通 Transformer Block：所有 token 共用一个 FeedForward
MoE Block：多个 FeedForward（experts），每个 token 只路由到少量专家
```

后续每次只推进一个代码小块，并明确区分：

```text
MOEFeedForward.gate       -> MoE router，选择专家
FeedForward.gate_proj     -> 单个 FFN 内部的一条投影路径
```

## 二、普通 FFN 回顾

MiniMind 的普通 FFN：

```python
def forward(self, x):
    return self.down_proj(self.act_fn(self.gate_proj(x)) * self.up_proj(x))
```

一个 token 的形状经历 `hidden_size -> intermediate_size -> hidden_size`。
`gate_proj` 分支经过激活后与 `up_proj` 分支逐元素相乘，最后由 `down_proj` 压回隐藏维度。FFN 不负责 token 之间的信息交流；Attention 负责跨 token 读取信息。最后回到 `hidden_size` 是为了与 Block 的残差分支相加。

还用一个简化数值确认了专家权重的作用：

```text
[2, 4, 6] * 0.25 = [0.5, 1, 1.5]
```

## 三、MoE 在 Block 中的构造分支

真实代码：

```python
self.mlp = FeedForward(config) if not config.use_moe else MOEFeedForward(config)
```

`use_moe=False` 时每层使用一个普通 `FeedForward`；`True` 时每层使用一个 `MOEFeedForward`，其中创建 `num_experts` 套独立的完整 `FeedForward`。Attention 主干不因这个分支改变。

学习者运行小配置实验得到：

```text
use_moe = False
mlp type = FeedForward

use_moe = True
mlp type = MOEFeedForward
expert count = 4
gate shape = (4, 16)
expert[0] gate_proj shape = (32, 16)
```

`gate` 输入 16 维 token 向量并输出 4 个专家的原始分数；每个 expert 仍是一套 `16 -> 32 -> 16` 的普通 FFN。

## 四、展平 token，准备逐 token 路由

真实代码：

```python
batch_size, seq_len, hidden_dim = x.shape
x_flat = x.view(-1, hidden_dim)
```

`[batch_size, seq_len, hidden_dim] -> [batch_size * seq_len, hidden_dim]`。例如 `[3, 5, 8] -> [15, 8]`。这只是把 15 个 token 排成 15 行，没有拆散单个 token 的 8 个特征，也没有混合不同 token 的内容。这样 router 可以对每一行独立计算专家分数。

## 五、router 分数与 softmax

真实代码：

```python
scores = F.softmax(self.gate(x_flat), dim=-1)
```

当 `x_flat.shape=[15, 8]` 且有 4 个专家时，`gate(x_flat) -> [15, 4]`。`gate` 的输出是尚未归一化的 logits；沿最后一维做 softmax 后，每个 token 得到独立的一行专家概率，行和为 1。学习者实际运行了 2 个 token、3 个专家的实验：

```text
logits shape: (2, 3)
scores shape: (2, 3)
scores:
tensor([[0.4742, 0.2161, 0.3096],
        [0.2583, 0.4638, 0.2779]], grad_fn=<SoftmaxBackward0>)
row sums: tensor([1.0000, 1.0000], grad_fn=<SumBackward1>)
```

## 六、top-k 专家选择

真实代码：

```python
topk_weight, topk_idx = torch.topk(
    scores, k=self.config.num_experts_per_tok, dim=-1, sorted=False
)
```

`topk_idx` 保存专家编号，`topk_weight` 保存对应的原始概率，形状都是 `[num_tokens, k]`。对于上面的分数，`k=1` 时 token 0 -> 专家 0（0.4742），token 1 -> 专家 1（0.4638）；`k=2` 时 token 0 -> 专家 0 和专家 2。

`top-k` 只筛选已有分数，不重新计算分数；`sorted=False` 不保证选中的 k 项按大小排列，但选中的集合仍是正确的 top-k。

当 `norm_topk_prob=True` 时，保留下来的 top-k 权重还会重新归一化：

```text
[0.5, 0.3] -> [0.625, 0.375]
[0.2, 0.3] -> [0.4, 0.6]
```

它不改变专家集合，只重新分配选中专家之间的权重。`k=1` 时唯一权重通常会变成 1。

## 七、专家分发与输出合并

核心代码：

```python
for i, expert in enumerate(self.experts):
    mask = (topk_idx == i)
    if mask.any():
        token_idx = mask.any(dim=-1).nonzero().flatten()
        weight = topk_weight[mask].view(-1, 1)
        y.index_add_(0, token_idx,
                     (expert(x_flat[token_idx]) * weight).to(y.dtype))
```

`mask` 是专家分发清单。若 `topk_idx = [[0], [2], [0], [1]]`，则专家 0 -> `token_idx [0, 2]`，专家 1 -> `[3]`，专家 2 -> `[1]`。每个 expert 只处理被分到自己的 token，输出乘对应路由权重后，通过 `index_add_(0, token_idx, ...)` 写回原 token 行。

学习者实际运行 `index_add_` 得到：

```text
tensor([[1., 2., 3.],
        [0., 0., 0.],
        [4., 5., 6.],
        [0., 0., 0.]])
```

当 `k>1` 时，同一个 token 会被多个专家处理，并多次使用同一个 token 行号累加。例如 `0.7 * [2, 4] + 0.3 * [10, 20] = [4.4, 8.8]`。最后 `y.view(batch_size, seq_len, hidden_dim)` 把二维 token 表恢复成 Block 原本需要的三维形状。

## 八、aux_loss：路由负载均衡

如果 router 长期把所有 token 送给一个专家，其他专家几乎得不到训练，MoE 的多路径容量被浪费。因此训练时计算：

```python
load = F.one_hot(topk_idx, self.config.num_experts).float().mean(0)
self.aux_loss = (load * scores.mean(0)).sum() \\
                * self.config.num_experts \\
                * self.config.router_aux_loss_coef
```

```text
load：实际选择结果，哪些专家真正收到多少 token
scores.mean(0)：router 的平均概率偏好，想把概率分给哪些专家
```

`load=[1,0,0,0]` 表示所有 token 都去了 0 号专家；其他专家接不到足够训练信号，容易闲置，MoE 的多专家优势无法发挥。理想目标不是每个 batch 精确相等，而是长期避免极端集中。

手算对比：

```text
集中：load=[1,0,0,0], mean_scores=[0.7,0.1,0.1,0.1]
      点积=0.7，aux_loss=0.0014
部分集中：load=[0.5,0.5,0,0], mean_scores=[0.6,0.3,0.05,0.05]
      点积=0.45，aux_loss=0.0009
均衡：load=[0.25,0.25,0.25,0.25], mean_scores=[0.25,0.25,0.25,0.25]
      点积=0.25，aux_loss=0.0005
```

系数 `router_aux_loss_coef=5e-4` 很小，因此它是语言建模 loss 之外的附加约束，不替代主任务目标。

## 九、训练模式、评估模式与多层汇总

源码只在训练模式且系数大于 0 时计算辅助损失：

```python
if self.training and self.config.router_aux_loss_coef > 0:
    ...
else:
    self.aux_loss = scores.new_zeros(1).squeeze()
```

学习者实际输出：

```text
train aux_loss: 0.0005188098875805736
eval aux_loss: 0.0
```

评估/推理时该项严格为 0，因为它是训练 router 的正则项，不是生成答案所需的预测损失。

`MiniMindModel` 汇总所有 MoE 层：

```python
aux_loss = sum(
    [l.mlp.aux_loss for l in self.layers
     if isinstance(l.mlp, MOEFeedForward)],
    hidden_states.new_zeros(1).squeeze()
)
```

学习者运行两层 MoE 实验得到：

```text
layer aux_losses: [0.0005022254190407693, 0.0005246296641416848]
model aux_loss: 0.0010268550831824541
manual sum: 0.0010268550831824541
```

证明模型级 `aux_loss` 就是各 MoE 层辅助损失之和；普通 FFN 层不会被加入。

最后，`MiniMindForCausalLM` 将它与语言模型 loss 一起返回；Full SFT、Pretrain 等训练循环执行：

```python
res = model(input_ids, labels=labels)
loss = res.loss + res.aux_loss
loss = loss / args.accumulation_steps
scaler.scale(loss).backward()
```

因此 `aux_loss` 不只是日志指标，而是会通过反向传播影响 router 的训练。

## 十、当前已确认与待继续

```text
[验收] 普通 FFN 与多个专家 FFN 的结构差异
[验收] gate router 的输出维度与专家数对应
[实践] 三维 token 张量展平为二维
[实践] softmax 后每个 token 的专家概率行和为 1
[验收] top-k 的索引、权重和 k=2 的多专家合并
[实践] mask、token_idx 与 index_add_ 的写回行为
[验收] norm_topk_prob 的重新归一化
[实践] train/eval 下 aux_loss 的差异
[实践] 多层 aux_loss 汇总与手工求和一致
```

下一次从这里继续：

```text
MoE 的总参数量 vs 每个 token 的激活参数量
-> 为什么“总容量增加”不等于“每个 token 计算量同比增加”
-> 用小配置统计普通 FFN 与 MoE 的参数量
```

## 十一、后续计划（原 Day 019 计划，已完成）

Day 19 已沿 `minimind/model/model_minimind.py` 完成参数规模学习，不重复今天
已经验收的路由流程：

```text
1. 先回顾一个普通 FeedForward 的参数量组成
   gate_proj、up_proj、down_proj 各自的形状和参数数量
2. 用同一份 hidden_size/intermediate_size 对照普通 FFN 与 MoE
   总参数量为什么约等于 num_experts 倍
3. 区分“总参数量”和“每个 token 激活的专家参数量”
   num_experts_per_tok=1/2 时分别实际调用几套专家 FFN
4. 用小配置统计参数量，并用 forward hook 或计数实验确认实际调用的专家
5. 连接到训练和推理成本：容量增加、单 token 计算量、显存占用分别如何变化
```

Day 19 的详细归档见 [Day 019](day-019.md)。
