# Day 002：从 Embedding 进入 Transformer Block

- 日期：2026-08-12
- 学习阶段：阶段 2 - Dense MiniMind 模型结构
- 今日主题：配置调用链、最小 PyTorch 模型、Linear 与点积
- 当前状态：已完成（按实际学习范围收尾，原计划中的 Block/RMSNorm 顺延）

## 零、如何使用本课

本课展示了一次重要的路线调整：原计划直接进入 `MiniMindBlock`，但学习过程中发现 `nn.Module`、`forward`、参数、Linear 和矩阵计算尚无稳固基础，于是主动退回最小 PyTorch 模型。

新人不要跳过这个回退。后续 Attention、MLP 和训练代码都大量使用 Linear；只有能亲手复算一个 Linear 输出，才适合继续读 Transformer。

可复现实验保存在 [codes/day3.py](codes/day3.py)。这个历史文件名是在 Day 编号规则固定前创建的，实际对应 Day 002。

## 逐轮互动档案

### 第 1 轮：从“今天学什么”开始

学习者要求开始新一天，并询问今天学习内容。助手最初计划从 Day 001 的 Embedding 输出 `[1, 11, 768]` 继续，进入模型配置和 `MiniMindBlock`。

学习者没有直接接受参数解释，而是先问：

> 这个是配置加载的类吗？我应该先了解它的调用栈过程以及配置文件的位置吧。

这个问题把顺序调整为“配置从哪里来”先于“配置数字是什么意思”。

### 第 2 轮：分清两条配置加载路径

针对本地实际使用过的方式，整理出两条路径：

```text
原生 MiniMind：
命令行参数 -> 代码创建 MiniMindConfig -> 创建 MiniMindForCausalLM -> 加载 .pth

Hugging Face 发布模型：
AutoModelForCausalLM.from_pretrained("./minimind-3")
  -> 读取 minimind-3/config.json
  -> 根据 model_type 创建 Qwen3Config
  -> 创建 Qwen3ForCausalLM
  -> 加载 model.safetensors
```

由此区分：

```text
配置类：规定有哪些配置字段以及默认值
配置对象：某次模型实例实际使用的一组字段值
config.json：保存配置值的文件
模型权重：真正学到的大量参数数值
```

状态：`[讲解]` 配置加载路径已建立最低理解；没有要求记住框架内部所有调用层。

### 第 3 轮：配置项太多，不应该背

看到配置文件和配置类中的大量字段后，学习者提出：

> 我需要关注到这么多参数值么，光理解这么多参数名我都记不住啊。

归档后的明确原则是：不背配置表。字段只分三类处理：

```text
当前数据流直接需要：现在理解
即将阅读的模块需要：模块出现时理解
高级特性或兼容字段：暂时忽略并登记
```

当时只需要识别 `vocab_size=6400`、`hidden_size=768` 和 `num_hidden_layers=8` 与已经观察过的 shape/层数的对应关系。Attention heads、KV heads、RoPE、MoE 等字段不应在前置知识缺失时同时展开。

状态：`[纠偏]` 配置项从“待记忆名词”改为“遇到模块时再解释的控制开关”。

### 第 4 轮：专业术语一次出现过多

助手随后仍然过快展示了 Block、RMSNorm、Attention、MLP、残差、hidden states 等术语，并要求学习者理解整体流程。学习者明确指出：

> 你是不是过分了点，这里面全是专业术语，我一脸懵逼啊。

这次反馈说明，即使先画“总图”，如果图中每个节点都陌生，它也不能帮助理解。原练习作废，不计入掌握进度。

教学路线退回：

```text
最小模型 -> 一个层 -> 一组参数 -> forward -> 输入张量 -> 输出张量
```

状态：`[纠偏]` 当天不再以读懂 MiniMindBlock 为目标。

### 第 5 轮：创建只有一个 Linear 的模型

学习者亲自运行：

```python
class SmallModel(nn.Module):
    def __init__(self):
        super().__init__()
        self.layer = nn.Linear(3, 2)

    def forward(self, x):
        return self.layer(x)
```

观察到：

```text
layer.weight shape = [2, 3]
layer.bias shape   = [2]
输入 shape         = [1, 3]
输出 shape         = [1, 2]
```

学习者没有停在 shape，而是追问：

> 输入到输出经过了怎么样的过程？

这成为真正进入 Linear 计算的触发点。

### 第 6 轮：把 Linear 输出逐项算出来

解释从一条权重开始，而不是先给矩阵定义：`Linear(3, 2)` 有两组各含 3 个数的权重，每组权重生成一个输出数；每个输出再加自己的 bias。

学习者打印实际随机权重和 bias，并计算：

```text
输出 1 = 1*w00 + 2*w01 + 3*w02 + b0
输出 2 = 1*w10 + 2*w11 + 3*w12 + b1
```

手工结果、模型结果和矩阵公式结果都是：

```text
[[0.5076, -0.4974]]
```

两次 `torch.allclose(...)` 都是 `True`。

由此验收：

```text
Linear 的实际计算 = x @ weight.T + bias
[1, 3] @ [3, 2] + [2] -> [1, 2]
```

状态：`[实践]`、`[验收]` 已能将 PyTorch 输出还原为具体乘加过程。

### 第 7 轮：一次放入两个 token 会发生什么

学习者把输入扩展为：

```text
shape = [1, 2, 3]
```

其中 `1` 是 batch 数，`2` 是 token 数，每个 token 有 `3` 个输入特征。经过同一个 `Linear(3, 2)` 后，输出 shape 是 `[1, 2, 2]`。

学习者又把两个 token 分开送入模型，与一起计算的结果比较，得到两个 `True`。

这亲自验证了：

```text
Linear 只把最后一维从 3 变成 2
同一套 weight/bias 独立处理每个 token
普通 Linear 本身不交换两个 token 的信息
```

学习者确认没有疑问。

状态：`[实践]`、`[验收]` 这条结论后来成为 Day 006 引出 Attention 的已知起点。

### 第 8 轮：为什么 Linear 和点积都写 `@`

在点积的最小例子中，学习者发现：

> 前面的矩阵计算和这里的点积都使用了 @。

据此澄清：`@` 表示矩阵乘法运算符，它根据两边张量维度表现为向量点积、矩阵乘向量或矩阵乘矩阵。矩阵乘法中的每一个输出数，本质上都是一行与一列做一次点积。

当前只需要记住：

```text
两个同维一维向量 @ -> 一个数
一批向量 @ 一批权重 -> 批量得到很多点积结果
```

状态：`[验收]` 点积与矩阵乘法的关系达到后续需要的最低深度。

### 第 9 轮：主动结束并登记两个真实问题

助手在讲解中提前使用了 `Attention Head`。学习者指出这个术语仍然没有含义，并在结束当天时留下两个明确问题：

```text
1. grad_fn 和 loss.backward() 到底如何让 weight/bias 知道怎么调整？
2. Attention 是什么？Attention Head 又是什么？
```

双方决定按依赖顺序先学习 Backward，再回到 Attention。Day 002 没有把 RMSNorm、残差或 Head 写成已学内容。

## 一、承接 Day 001

昨天已经亲自验证：

```text
文本
  -> Tokenizer
  -> token ID：[1, 11]
  -> Embedding 查表
  -> 初始向量：[1, 11, 768]
```

还验证了相同的两个 `M`：

```text
Embedding 层：向量完全相同，距离 0
经过第 1 层：向量不同
经过后续层：继续形成不同的上下文表示
```

今天从这个变化发生的地方继续：`MiniMindBlock`。

## 二、今日目标

今天计划掌握四件事：

1. 先弄清配置来自哪里、由谁创建、如何进入模型。
2. 读懂 MiniMind 默认配置中的关键数字。
3. 建立一个 Transformer Block 的整体数据流。
4. 理解 RMSNorm 与残差连接的基本作用。

如果前四项完成且仍有精力，再进入 Self-Attention 的入口，只认识 Q/K/V 投影在 Block 中的位置，不要求今天掌握 Attention 公式。

## 三、今日代码范围

```text
minimind/model/model_minimind.py::MiniMindConfig
minimind/model/model_minimind.py::RMSNorm
minimind/model/model_minimind.py::MiniMindBlock
minimind/model/model_minimind.py::MiniMindModel.forward
```

暂不深入：

```text
Attention.forward 的完整推导
RoPE 公式
GQA 的详细 shape
KV Cache
MLP/SwiGLU 的内部公式
MoE
训练与反向传播
```

## 四、今日路线

### 第 1 步：配置调用链

学习者指出，在解释具体配置值之前，应先了解 `MiniMindConfig` 是否负责加载配置、配置文件位于哪里，以及配置对象如何进入模型。

仓库实际存在两条不同路径：

```text
原生 MiniMind 路径：命令行参数 -> MiniMindConfig(...) -> MiniMindForCausalLM -> 加载 .pth 权重

Transformers 发布路径：模型目录/config.json -> AutoModelForCausalLM.from_pretrained(...) -> Qwen3Config -> Qwen3ForCausalLM -> 加载 model.safetensors
```

因此先学习“配置类、配置对象、配置文件、模型权重”的区别，再学习各数字含义。

### 第 2 步：读配置值

把发布模型与原生 MiniMind 配置对应起来：

```text
vocab_size = 6400
hidden_size = 768
num_hidden_layers = 8
num_attention_heads = 8
num_key_value_heads = 4
head_dim = 96
intermediate_size = 2432
```

重点不是背数字，而是理解每个数字控制模型的哪一部分。

### 第 3 步：看 Block 总图

先把 Block 当作两个子模块：

```text
输入 x
  -> RMSNorm -> Self-Attention -> 加回 x
  -> RMSNorm -> MLP            -> 再加回
  -> 输出
```

先理解数据流和 shape，再拆内部算法。

### 第 4 步：学习 RMSNorm

结合极小向量实验理解：

- 为什么不同输入数值尺度会影响后续计算。
- RMS 是什么。
- 归一化后哪些东西改变、哪些不改变。
- 可训练参数 `weight` 的作用。
- 为什么 MiniMind 使用 Pre-Norm。

### 第 5 步：学习残差连接

理解：

```text
output = input + module(norm(input))
```

它如何让子模块学习“在原表示上增加什么”，并为深层网络保留直接的信息与梯度路径。

### 可选第 6 步：进入 Attention 入口

只认识：

```text
RMSNorm 后的 hidden states
  -> q_proj
  -> k_proj
  -> v_proj
```

详细 Q/K/V、因果 Mask、RoPE、GQA 和 KV Cache留到后续学习。

## 五、原计划验收标准（路线调整后顺延）

下面是开课时为 `MiniMindBlock` 制定的验收项。学习中发现缺少 PyTorch 和 Linear 前置知识后，这些项目主动顺延，并非当天已经讲授但没有通过。Day 002 实际完成项见“今日总结”。

只有学习者亲自实践和理解过才标记完成：

- [ ] 能说出 `[1, 11, 768]` 进入 Block 后为什么仍是 `[1, 11, 768]`。
- [ ] 能把一个 MiniMindBlock 画成 Attention 分支和 MLP 分支。
- [ ] 能解释 RMSNorm 解决的基本问题。
- [ ] 能通过小实验观察 RMSNorm 前后的 shape 与数值变化。
- [ ] 能解释两条残差连接分别加回了什么。
- [ ] 能区分“归一化改变数值尺度”和“Transformer 改变上下文表示”。
- [x] 把问题和未完成知识如实登记，不以阅读代码行数代替掌握。

## 六、互动方式

今天继续执行：

```text
先讲清一个概念
  -> 学习者查看对应代码
  -> 学习者亲自运行最小实验
  -> 解释实际结果并提问
  -> 通过后再进入下一项
```

不要求学习者猜测尚未讲过的概念。助手代为执行的检查或演示不计入学习者完成进度。

## 七、学习记录

### 教学纠偏：补齐神经网络代码的最低前置知识

在展示 `MiniMindBlock.forward` 时，助手一次引入了 Block、RMSNorm、Attention、MLP、残差连接、hidden states 和 forward 等多个陌生术语，并立即要求学习者复述执行顺序。学习者指出缺少前置讲解，无法建立理解。

该练习作废，不计入学习进度。Day 002 的实际顺序调整为：

```text
什么是一个神经网络模型
  -> 什么是层和参数
  -> PyTorch 的 nn.Module 是什么
  -> forward 表示什么
  -> hidden_states 是什么
  -> 一个模块如何接收和返回张量
  -> 再回到 MiniMindBlock 总图
```

今天不以进入 Attention 为目标。优先让仓库代码中的基本名词获得清晰、可运行的含义。

### 学习者实践 1：最小 PyTorch 模型

学习者创建了只包含一个 `nn.Linear(3, 2)` 的 `SmallModel`，实际观察到：

```text
SmallModel(
  (layer): Linear(in_features=3, out_features=2, bias=True)
)

layer.weight shape：[2, 3]
layer.bias shape：[2]
输入 shape：[1, 3]
输出 shape：[1, 2]
```

一次随机初始化下的实际输出为：

```text
[[-0.5797, 1.8127]]
```

学习者进一步提出核心问题：输入到输出具体经过了怎样的过程。下一步进入 Linear 层的最小线性代数：

```text
y = x @ weight.T + bias
```

并将通过打印参数与手工分项计算复现模型输出。

学习者打印并使用实际参数复算：

```text
weight =
[[ 0.0655,  0.1122,  0.0899],
 [ 0.0252, -0.0035, -0.0856]]

bias =
[-0.0520, -0.2588]
```

对输入 `[1, 2, 3]` 得到：

```text
手工逐项计算：[[0.5076, -0.4974]]
模型计算：    [[0.5076, -0.4974]]
是否一致：True

矩阵公式：    [[0.5076, -0.4974]]
是否一致：True
```

已由学习者亲自验证：

```text
nn.Linear 的计算 = x @ weight.T + bias
[1, 3] @ [3, 2] + [2] -> [1, 2]
```

本知识点完成机械计算验证；下一步讲清权重行、输出通道、随机初始化和参数学习的含义，再决定是否进入模块组合。

### 学习者实践 2：Linear 处理多个 token

学习者构造 shape 为 `[1, 2, 3]` 的输入：

```text
[[[1, 2, 3],
  [4, 5, 6]]]
```

使用同一个 `Linear(3, 2)` 后得到：

```text
输入 shape：[1, 2, 3]
输出 shape：[1, 2, 2]
```

两个 token 的实际输出为：

```text
token 1 -> [-0.3951, -2.4038]
token 2 -> [-0.9466, -5.5498]
```

将两个 token 一起计算，再与逐个单独计算比较：

```text
token 1：一致 -> True
token 2：一致 -> True
```

由学习者亲自验证：

1. `nn.Linear` 变换张量的最后一个维度：`3 -> 2`。
2. batch 维和 token/sequence 维保持不变。
3. 同一个 Linear 使用同一套 weight 和 bias 分别处理各 token。
4. 普通 Linear 本身不会让不同 token 交换信息；一起计算与单独计算结果相同。

下一步从第 4 点引出 Self-Attention 存在的基本原因，但暂不进入 Q/K/V 和公式。

结果讲解后，学习者确认对以下结论没有疑问：普通 Linear 使用同一套参数独立处理各 token，本身不会让不同 token 交换信息；语言模型因此还需要 Self-Attention 建立 token 之间的信息联系。

下一步补充 Attention 所需的最小线性代数：标量、向量、矩阵和点积。目标仅为理解两个同维向量如何产生一个匹配分数，不提前引入完整 Q/K/V 公式。

### 正式知识点：矩阵乘法与点积的关系

学习者注意到，之前 Linear 的矩阵计算和点积示例都使用 `@`，由此澄清：

```text
torch.dot(a, b)
```

用于两个一维向量的点积；而：

```text
a @ b
```

对应更通用的矩阵乘法。矩阵乘法中的每个输出元素，本质上是一个行向量与一个列向量的点积。

已经结合之前的 Linear 实验建立：

```text
x shape：[1, 3]
weight.T shape：[3, 2]
x @ weight.T -> [1, 2]
```

输出的两个数分别来自输入向量与两组权重向量的点积。由此形成当前阶段的最低理解：

```text
点积：比较一对同维向量，得到一个标量
矩阵乘法：批量完成多组点积
```

讲解末尾过早使用了 `Attention Head` 术语，学习者明确指出尚不知道 Attention 与 Head 是什么。该术语不视为已学习，必须在后续从动机和直观例子重新引入。

## 八、问题与知识欠账

- KV Cache：等 Q/K/V、因果 Mask和 GQA 学完后展开。
- Chat Template、attention mask、logits 与采样：留在完整推理链路阶段。
- `grad_fn=<AddmmBackward0>`、计算图、loss、梯度与 `loss.backward()`：学习者提出“PyTorch 记录反向计算关系，并据此计算 weight 和 bias 如何调整”具体是什么意思。需要从一个参数、一个目标值和一个误差开始，通过可打印的最小实验讲解，不直接使用抽象定义。
- Attention 与 Attention Head：学习者指出讲解中直接使用专业术语，尚未理解它们是什么。后续必须先从“普通 Linear 不能让 token 交换信息”的问题出发，解释 Attention 的任务，再解释为什么把表示分成多个 Head；在此之前不引入 Q/K/V、GQA 或 KV Cache。
- RMSNorm、残差连接与完整 MiniMindBlock：因当天需要补齐 PyTorch 和线性代数前置知识，顺延到相应基础具备之后。

## 九、今日总结

Day 002 原计划直接进入 MiniMindBlock、RMSNorm 和残差连接，但实际学习暴露出神经网络代码和最小线性代数的前置缺口。因此及时缩小范围，以真正理解为准完成了以下内容。

### 学习者亲自完成

```text
[完成] 创建继承 nn.Module 的 SmallModel
[完成] 理解 forward 是模块从输入到输出的计算定义
[完成] 观察 Linear(3, 2) 的结构、weight 和 bias
[完成] 观察 weight shape [2, 3] 与 bias shape [2]
[完成] 将输入 [1, 3] 变换为输出 [1, 2]
[完成] 手工逐项复算两个 Linear 输出
[完成] 使用 x @ weight.T + bias 复算输出
[完成] 验证手工计算、矩阵公式和 PyTorch 输出一致
[完成] 构造 [1, 2, 3] 的多 token 输入
[完成] 验证 Linear 只改变最后一维 3 -> 2
[完成] 验证 token 一起计算与分别计算结果一致
```

### 已建立的概念

1. 模型结构规定怎样计算，权重和偏置是计算中使用的具体可训练数值。
2. `nn.Module` 是 PyTorch 表示模型或模型部件的基础类。
3. `forward` 定义输入进入模块后如何产生输出。
4. `nn.Linear(3, 2)` 执行 `x @ weight.T + bias`。
5. Linear 的每个输出使用一行独立权重和一个偏置。
6. PyTorch Linear 变换最后一个维度，保留前面的 batch 和 sequence 维度。
7. 普通 Linear 使用同一套参数独立加工每个 token，本身不让 token 之间交换信息。
8. 点积把两个同维向量变成一个标量；矩阵乘法可以批量执行点积。

### 教学过程纠偏

1. 不再把代码中出现过的术语视为学习者已经具备的前置知识。
2. 原 `MiniMindBlock` 代码复述练习作废，因为一次引入过多陌生术语。
3. 配置项不要求背诵，改为相关模块出现时再学习。
4. Attention Head 属于尚未讲解的术语，不能从 shape 示例直接带过。

### 今日未学习

```text
MiniMindBlock 内部数据流
RMSNorm
残差连接
Attention 与 Attention Head
Q/K/V
Softmax
反向传播与参数更新
```

这些内容顺延，不把“提到过”记录成“学会了”。

## 十、下一学习日入口

Day 003 从学习者今天留下的两个原始问题开始。

### 问题 1：Backward 到底是什么意思

原问题：

> “Backward 表示 PyTorch 为这次运算记录了反向计算关系，将来训练调用 loss.backward() 时，可以计算 weight 和 bias 应该如何调整。”这句话怎么理解？

下一次不先给抽象定义，而使用只有一个输入、一个 weight、一个目标值的最小模型，依次观察：

```text
当前预测
  -> 与目标的误差 loss
  -> grad_fn 记录的计算关系
  -> loss.backward()
  -> weight.grad
  -> 参数应该增大还是减小
  -> 手工更新后 loss 是否下降
```

需要区分：

```text
backward 负责计算梯度
optimizer.step 负责真正修改参数
```

### 问题 2：Attention 和 Head 是什么

原问题：

> Attention Head 上来又直接说专业术语，我搞不懂这是啥。

处理顺序：

```text
先复用今天的结论：Linear 独立处理 token
  -> 语言理解为什么需要 token 之间交换信息
  -> Attention 要解决的日常语言问题
  -> 一个 Attention 是如何“选择并读取”信息的直觉
  -> Head 为什么可以理解为一套独立的关注方式
  -> 最后才引入 Q/K/V 和代码
```

Day 003 不要求同时吃透反向传播和完整 Attention。先完成 Backward 的最小实验，再根据理解和精力进入 Attention 的直觉层。
