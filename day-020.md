# Day 020：进入 LoRA，先理解运行时注入

- 日期：2026-09-03
- 学习阶段：`minimind` LoRA
- 今日主题：`model_lora.py` 的 LoRA 注入、保存/加载/合并，以及 `train_lora.py` 的冻结与更新
- 当前状态：已完成
- 恢复点：Day 21 从 LoRA 权重与基础模型的真实推理验收开始

## 一、阶段切换

路线文档中 MoE 已完成结构、路由、参数量和激活成本学习。学习者确认已经
理解 MoE，因此从 `model_lora.py` 开始下一专题，不再先做 MoE 训练专项实训。

LoRA 专题的主线是：

```text
冻结原模型 -> 低秩增量 -> 少量可训练参数
-> LoRA 权重保存/加载 -> 合并回普通模型
-> train_lora.py 训练流程
```

## 二、LoRA 的低秩增量

普通 Linear：

```text
y = W x
```

微调可以写成：

```text
y = (W + Delta W) x
```

LoRA 不直接训练完整的 `Delta W`，而是用两个小矩阵近似：

```text
Delta W = B @ A
```

代码：

```python
self.A = nn.Linear(in_features, rank, bias=False)
self.B = nn.Linear(rank, out_features, bias=False)

def forward(self, x):
    return self.B(self.A(x))
```

当 `in_features=16`、`out_features=16`、`rank=4` 时：

```text
A.weight.shape = [4, 16]，参数 64
B.weight.shape = [16, 4]，参数 64
LoRA 总参数 = 128
```

`A` 使用小幅随机初始化，`B` 全零初始化，因此刚应用 LoRA 时：

```text
B(A(x)) = 0
原始输出 + LoRA 输出 = 原始输出
```

## 三、`apply_lora()` 的目标层与 forward 注入

代码遍历所有子模块，只给满足以下条件的 Linear 挂 LoRA：

```python
isinstance(module, nn.Linear)
and module.in_features == module.out_features
```

小配置实验实际命中了：

```text
model.layers.0.self_attn.q_proj
model.layers.0.self_attn.o_proj
```

GQA 配置下的 `k_proj`、`v_proj` 输出维度可能与 hidden size 不同；FFN 的
三张投影和 `lm_head` 也通常不是方形矩阵，因此不会被这个筛选条件选中。

注入后的 forward：

```python
def forward_with_lora(x, layer1=original_forward, layer2=lora):
    return layer1(x) + layer2(x)
```

等价于：

```text
module(x) = W x + B(A(x))
```

学习者实际验证：

```text
LoRA target modules: ['model.layers.0.self_attn.q_proj',
                      'model.layers.0.self_attn.o_proj']
target count: 2
outputs initially equal: True
```

`layer1=original_forward`、`layer2=lora` 用默认参数固定当前循环中的对象，
避免闭包在循环结束后错误引用最后一轮变量。

## 四、`load_lora()`、`save_lora()` 与 `merge_lora()`

`load_lora()` 读取只包含 `.lora.` 参数的文件，去掉 DDP 可能带来的 `module.` 前缀，按完整模块名筛选，再转换为 `A.weight` / `B.weight` 加载到已经执行过 `apply_lora()` 的模块。

`save_lora()` 只遍历带 `.lora` 子模块的目标层，把 A/B 参数移动到 CPU、转为 float16，并保存完整模块路径。真实实验：

```text
full key count: 18
lora key count: 4
full file bytes: 10989
lora file bytes: 2635
all lora keys: True
lora dtypes: ['torch.float16']
```

`merge_lora()` 先加载适配器，再计算：

```text
W_merged = W_original + B @ A
```

并过滤 `.lora.` 参数，导出普通模型结构的权重文件。用命名子模块 `TinyModel.proj` 验证时，合并文件只有 `proj.weight`，没有 LoRA key；运行时和合并后输出的微小差异来自源码保存为 float16 的舍入，而不是合并公式错误。

## 五、冻结参数、反向传播与梯度累积

`train_lora.py` 执行：

```python
param.requires_grad = 'lora' in name
optimizer = optim.AdamW(lora_params, lr=learning_rate)
```

小模型实验确认：

```text
total params: 832
trainable params: 64
lora params: 64
optimizer params: 64
```

原始 q_proj/o_proj 的 `grad is None=True` 且更新后不变；LoRA B 第一次就有非零梯度，LoRA A 第一次梯度为 0，这是 B 全零初始化导致的启动现象。第一次 optimizer step 更新 B 后，后续梯度才能通过非零 B 传回 A。

设置 `accumulation_steps=2` 的两步实验确认：

```text
step 1：scaled loss=1.4931984，optimizer step=no，B 仍为零
step 2：scaled loss=1.4931984，optimizer step=yes，B 变为非零
base q_proj unchanged=True
```

训练循环的完整顺序是：

```text
res.loss + res.aux_loss
-> 除以 accumulation_steps
-> backward
-> 达到累积步数后裁剪 LoRA 梯度
-> optimizer.step()
-> zero_grad()
```

## 六、今日确认边界

```text
[实践][验收] LoRA A/B 形状、参数量和 B=0 初始化
[实践][验收] apply_lora 的目标层筛选和初始输出不变
[实践][验收] load_lora 按模块名恢复 A/B
[实践][验收] save_lora 只保存 CPU float16 的 LoRA 参数
[实践][验收] merge_lora 的 W+B@A 导出与半精度误差
[实践][验收] 原模型冻结、LoRA 参数进入 optimizer
[实践][验收] backward、B 首先获得梯度和梯度累积更新时机
```

## 七、下一天计划（Day 021）

```text
1. 用相同基础模型分别加载 LoRA 前后权重
2. 验证 load_lora 后输出与训练时带 LoRA 模型一致
3. 验证 merge_lora 后普通模型输出在容差内一致
4. 对比基础模型、LoRA 适配器和 merged 权重的文件内容
5. 追踪 eval_llm.py / serve_openai_api.py 如何加载 LoRA
```

Day 21 完成后，再决定是否做极小真实 LoRA 数据训练，或进入下一专题蒸馏。
