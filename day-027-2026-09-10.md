# Day 027：转换、服务、评估（第一阶段收尾）

- 日期：2026-09-10
- 学习阶段：`minimind` 第 9 节（转换、服务、评估）
- 当前状态：快速过完，第一阶段主线收尾
- 入口：
  - `minimind/scripts/convert_model.py`
  - `minimind/scripts/serve_openai_api.py`
  - `minimind/scripts/eval_toolcall.py`

## 一、本日定位

第 8 节 Tool Use 与 Agent RL 已完成。第 9 节不再引入新的训练概念，只把已经
训练好的权重接到生态里：格式转换、OpenAI 兼容服务、工具调用评测。因此本日
按“快速过完”处理，只抓三条主线的关键点，验收后即进入第二阶段。

## 二、权重转换：三个方向

来自 `convert_model.py`，围绕 `lm_config = MiniMindConfig(...)` 展开。

```text
原生 torch (.pth)  --convert_torch2transformers_minimind-->  Transformers-MiniMind
原生 torch (.pth)  --convert_torch2transformers----------->  Qwen3 / Qwen3Moe 结构
Transformers 目录  --convert_transformers2torch----------->  原生 torch (.pth)
```

要点：

```text
convert_torch2transformers_minimind：注册 AutoClass，保存自定义结构，社区需 trust_remote_code
convert_torch2transformers：把权重映射到 Qwen3ForCausalLM，直接兼容 transformers 生态
    - use_moe 时把每个专家的 gate/up/down 权重 stack 成 Qwen3Moe 的合并张量
    - transformers>=5.0 需改写 tokenizer_config 和 config（rope_theta / rope_scaling）
convert_transformers2torch：反向导回 .pth，权重 .cpu().half()
convert_merge_base_lora：apply_lora -> merge_lora，把 LoRA 合并回基模结构（呼应 Day 020/021）
```

chat template 两个方向：

```text
convert_jinja_to_json：把 .jinja 转义成 config 里的 "chat_template" 字符串
convert_json_to_jinja：从 tokenizer_config 抽出模板写回 .jinja
```

## 三、OpenAI 兼容服务

来自 `serve_openai_api.py`，核心是 `/v1/chat/completions`。

加载路径（复用 Day 021 的两条加载路径结论）：

```text
load_from 含 'model' -> 原生 MiniMindForCausalLM 载入 .pth，可再叠加 apply_lora + load_lora
否则                 -> AutoModelForCausalLM.from_pretrained（transformers 格式）
最终 model.half().eval().to(device)
```

流式输出机制：

```text
apply_chat_template(..., tools=tools, open_thinking=...) 拼 prompt
CustomStreamer 继承 TextStreamer，on_finalized_text 把增量文本塞进 Queue
model.generate 跑在子线程，主协程从 Queue 逐段取出
按 </think> 边界切分 reasoning_content 与 content，分别以 delta 事件 yield
结束时 parse_response 抽 tool_calls，最后补 finish_reason = tool_calls / stop
```

`parse_response` 三件事：

```text
<think>...</think> -> reasoning_content
<tool_call>...</tool_call> -> tool_calls（json 解析成 OpenAI function 格式）
剩余文本 -> content
```

## 四、工具调用评测

来自 `eval_toolcall.py`，是第 8 节 Agent 能力的固定回归测试。

```text
TOOLS：8 个函数定义（calculate_math / get_current_time / ... / translate_text）
MOCK_RESULTS：每个工具的假执行结果，评测时不接真实外部服务
TEST_CASES：固定 prompt + 允许使用的工具子集
```

多轮 rollout 循环 `run_case`（与训练侧 rollout 同构）：

```text
messages = [user]
loop:
  local: generate() -> parse_tool_calls()   (正则抓 <tool_call>)
  api  : chat_api()  -> delta.tool_calls 流式拼接
  没有 tool_calls -> break
  有   -> assistant 消息回填 -> execute_tool 逐个执行 -> tool 消息回填 -> 继续下一轮
```

两个后端：

```text
backend=local：直接跑本地权重，靠正则解析 <tool_call>
backend=api  ：走 OpenAI 兼容接口（可指向 serve_openai_api 或 ollama），
               优先用结构化 delta.tool_calls，缺失时回退正则解析
```

## 五、验收

```text
[验收] 三个转换方向的输入产物与适用场景（原生 / Transformers-MiniMind / Qwen3 生态）
[验收] MoE 权重为什么要 stack 成合并张量
[验收] convert_merge_base_lora 与 Day 020/021 LoRA 合并的对应关系
[验收] 流式服务里 Queue + 子线程 generate + </think> 切分的作用
[验收] parse_response 如何分离 reasoning / content / tool_calls
[验收] eval_toolcall 多轮 rollout 与 Agent RL rollout 的同构关系
[验收] local 与 api 两个后端在 tool_call 解析上的差异
```

## 六、阶段切换

第一阶段 `minimind` 主线到此收尾：

```text
已完成：Tokenizer/Attention/推理链/SFT/GPU/DDP、Pretrain、Tokenizer 训练、MoE、
        LoRA、蒸馏、DPO、GRPO、Agent RL、转换/服务/评估
保留可选专题：PPO 的 critic/value、return、TD error、GAE（Day 026 暂停项）
```

下一阶段进入 `minimind-v`，只学视觉新增部分，不重复语言模型主干。第一小步定为
运行调用链：

```text
minimind-v/eval_vlm.py
-> 图片预处理 -> vision encoder -> image feature
-> vision projector -> image token 注入 MiniMind -> 文本回答
```
