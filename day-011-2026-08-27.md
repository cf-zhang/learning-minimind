# Day 011：`generate()` 外层准备与 MiniMind 原生入口定位

- 日期：2026-08-27
- 学习阶段：MiniMind 仓库源码主线
- 今日主题：沿 `model.generate()` 调用栈读完通用准备阶段，定位原生生成入口
- 当前状态：已完成
- 配套实验：[Day 011 `generate()` 调用链 Notebook](notebooks/day-011-generate-call-chain.ipynb)

## 一、承接 Day 010

Day 010 已经沿真实执行顺序读完 `eval_llm.py` 的外层调用：

```text
用户文本
-> chat template
-> tokenizer
-> input_ids / attention_mask
-> model.generate(...)
-> generated_ids
-> 切出新生成部分并 decode
```

今天从尚未展开的调用点继续：

```python
generated_ids = model.generate(...)
```

## 二、真实执行边界

本次运行使用：

```text
--load_from ./minimind-3
```

所以模型实际类型是 `Qwen3ForCausalLM`，当前环境中的实际生成路径是：

```text
Qwen3ForCausalLM
-> Transformers 4.57.6
-> GenerationMixin.generate()
```

源码位置：

```text
minimind/.venv/lib/python3.12/site-packages/
transformers/generation/utils.py
```

仓库原生的 `MiniMindForCausalLM` 在 `model/model_minimind.py` 中也实现
了一个更短的 `generate()`，但它不是 `./minimind-3` 这次运行调用的
函数。今天先读真实 Qwen3 路径；只有在需要降低阅读难度时，才明确
切换到原生短实现作对照，不把两条路径混写。

## 三、今日路线

按实际调用顺序逐步推进：

1. 区分 `generate()` 和单次模型计算的职责。
2. 观察生成参数如何整理成 generation config。
3. 确定本次为什么选择采样循环。
4. 进入循环，观察第一次如何处理完整输入。
5. 只取最后位置的候选分数并选择下一个 token。
6. 将新 token 拼回 `input_ids`，判断是否停止。
7. 再观察下一轮输入以及 KV Cache 的作用。

以上只是路线，不代表已经完成。每次只学习一个小步骤，经过回答、
实践或确认后才进入下一项。

## 四、逐轮互动档案

### 第 1 轮：`generate()` 是生成流程的组织者

已确认实际执行的不是仓库原生短函数，而是 Transformers 4.57.6 的
`GenerationMixin.generate()`。本轮只建立一个边界：`generate()` 不会
通过一次模型计算直接得到整段答案，而是负责准备并组织一个循环。

简化后的流程是：

```text
已有 input_ids
-> 模型计算下一个 token
-> 把新 token 拼到 input_ids 后面
-> 未结束则再次计算下一个 token
-> 直到 EOS 或达到长度限制
-> 返回完整 generated_ids
```

状态：`[讲解]`，等待学习者判断生成 3 个新 token 至少需要完成几次
“预测下一个 token”的步骤，再进入 `generate()` 与模型单次调用的
职责区别。

学习者回答：`3 次`。回答正确。每生成一个新 token，都要完成一次
“根据当前已有序列预测下一个 token”的步骤；新 token 拼回序列后，
下一次预测所依据的序列也随之变长。

状态：`[验收]` 已理解生成多个 token 需要多次预测，而不是一次计算
直接得到整段答案；下一步区分 `generate()` 和 `forward()` 的职责。

### 第 2 轮：`generate()` 组织循环，`forward()` 完成一轮模型计算

在 PyTorch 模型中，调用 `model(...)` 最终会进入模型的 `forward()`。
在生成过程中，两者可以先这样分工：

```text
generate()：管理整个生成过程
forward()：根据当前送入模型的 token，计算本轮的候选分数
```

`forward()` 本身不负责把答案循环生成到结束；`generate()` 会使用本轮
计算结果选出一个新 token，把它追加到序列，再决定是否进行下一轮。

状态：`[讲解]`，等待学习者判断只调用一次 `forward()` 是否会自动完成
3 个新 token 的整个生成过程。

学习者回答：不会。回答正确。一次 `forward()` 只提供本轮模型计算
结果；循环调用、选择 token、拼接序列和停止判断仍由 `generate()`
组织。

状态：`[验收]` 已区分整个生成流程和其中一次模型计算；下一步阅读
循环开始前如何整理本次生成参数。

### 第 3 轮：生成参数先整理到 `GenerationConfig`

`eval_llm.py` 将多个生成控制参数传入 `model.generate(...)`。Transformers
进入 `generate()` 后，会把这些值和默认值整理成一个本次生成使用的
`GenerationConfig` 对象。

这里必须与模型结构配置区分：

```text
Qwen3Config：       模型本体结构，例如层数、hidden_size、Head 数
GenerationConfig：  本次怎样生成，例如最多生成多少、是否采样、温度
```

状态：`[讲解]`，等待学习者将 `hidden_size=768` 和
`max_new_tokens=64` 分别归入两个配置对象。

学习者回答：`num_hidden_layers=8` 属于 `Qwen3Config`，
`temperature=0.1` 属于 `GenerationConfig`。回答正确。

更完整地说：层数、隐藏维度、词表大小和 Head 数决定“模型长什么样”；
最大生成长度、采样开关、温度、top-p 和停止 ID 决定“这次怎样使用
模型生成”。两类配置的职责不同，不能因为都叫 config 就混为一谈。

状态：`[验收]` 已区分模型结构配置和本次生成配置；下一步观察调用者
传入的 `generate()` 参数如何覆盖生成默认值。

### 第 4 轮：调用参数覆盖生成默认值

Transformers 的生成入口会把调用者传入的关键字参数整理进
`GenerationConfig`。对同一个参数，如果调用时显式传入值，就使用本次
调用的值；没有传入时，才使用模型保存的生成配置或框架默认值。

本次调用显式传入：

```python
model.generate(
    max_new_tokens=64,
    temperature=0.1,
    top_p=0.9,
    do_sample=True,
)
```

因此本次生成使用这些值，而不是直接采用配置文件中可能存在的默认值。

状态：`[讲解]`，等待学习者判断：如果模型默认
`temperature=0.85`，但本次调用传入 `temperature=0.1`，实际生成时
使用哪个值。

学习者回答：`0.1`。回答正确。调用处显式传入的值覆盖默认值，只对
本次 `generate()` 调用生效，不会把模型结构或权重永久改成另一份。

状态：`[验收]` 已理解生成配置的本次覆盖规则；下一步观察合并后的
配置如何决定“采样”还是“直接选最高分”等生成策略。

### 第 5 轮：生成配置决定生成策略

参数准备完成后，Transformers 会根据 `GenerationConfig` 判断本次使用
哪种生成策略。对当前调用，关键值是：

```text
do_sample=True
num_beams=1（默认）
```

因此选择的是“采样”策略：每轮把候选分数变成概率，再按概率抽取一个
token。它不是每轮都直接取分数最高的 token。

源码中这一步先得到一个模式，再映射到内部解码函数；当前模式对应
`_sample()`。`_sample()` 是生成循环的实现位置，下一步才进入它的
第一段初始化代码。

状态：`[讲解]`，等待学习者根据 `do_sample=True` 判断本次会进入采样
策略还是“总选最高分”策略。

学习者回答：采样策略。回答正确。

学习者追问：`num_beams` 是第一次出现，是否需要了解？

当前只补理解本次分支所需的最小含义：`num_beams` 控制生成过程中
同时保留多少条候选“序列路径”。默认 `num_beams=1` 表示不保留多条
路径，因此配合 `do_sample=True` 使用普通采样。它不是说词表中只有
一个候选 token；每轮仍然会面对整个词表的概率分布。

暂不展开 Beam Search 的路径评分和剪枝算法；等以后真的使用
`num_beams>1` 时再单独学习。

状态：`[讲解]`，等待学习者确认 `num_beams=1` 的含义，随后进入
`_sample()` 的初始化变量。

学习者确认理解。状态：`[验收]` 已区分 beam 路径数量和每轮词表
候选数量；下一步进入 `_sample()` 循环入口。

### 第 6 轮：原计划跳入 `_sample()`，随后撤回

本轮原计划直接进入 `_sample()`，并准备讲：

```python
batch_size, cur_len = input_ids.shape[:2]
```

但这一步跳过了 `generate()` 中间尚未阅读的准备代码，因此不应算作
真实学习进度。这里保留为教学纠偏记录，不把它当作已经沿调用栈读通
的内容。

### 第 6 轮纠偏：回到 `generate()` 的真实顺序

学习者指出：阅读应该按照调用栈和源码执行顺序，不能从
`generate()` 选出的 `_sample()` 直接跳进去。指出正确。

从源码顺序看，生成模式选择之后，`generate()` 仍会依次完成：

```text
校验额外参数
-> 准备 logits processor 和 stopping criteria 容器
-> 整理模型输入，确认 batch/device
-> 准备特殊 token 和 attention_mask
-> 得到用于自回归生成的 input_ids
-> 根据生成模式扩展输入
-> 通知 streamer 显示原始 prompt
-> 计算本次生成的长度边界
-> 准备 cache
-> 准备 logits processor 和 stopping criteria
-> 设置 use_cache
-> 调用 `_sample()`
```

因此，当前不再直接讲 `_sample()` 内的 `batch_size, cur_len`；下一步从
这些准备步骤中源码实际出现的第一个小调用开始，只解释
`_validate_model_kwargs(model_kwargs.copy())` 为什么要先检查传入参数。

状态：`[纠偏]` 已恢复到真实调用栈；`_sample()` 内部循环尚未开始。

学习者随后追问：刚刚提到的 `_validate_model_kwargs` 代码在哪里？

准确位置不是 MiniMind 仓库文件，而是当前虚拟环境安装的 Transformers
依赖：

```text
调用：
minimind/.venv/lib/python3.12/site-packages/
transformers/generation/utils.py:2390

定义：
minimind/.venv/lib/python3.12/site-packages/
transformers/generation/utils.py:1554
```

对应调用栈为：

```text
eval_llm.py:82
-> Qwen3ForCausalLM.generate()
-> GenerationMixin.generate()
-> GenerationMixin._validate_model_kwargs()
```

定义开头是：

```python
def _validate_model_kwargs(self, model_kwargs: dict[str, Any]):
    """Validates model kwargs for generation. Generate argument typos will also be caught here."""
```

可用下面的命令同时定位定义和调用：

```bash
cd /home/zcf/githubs/minimind/minimind
source .venv/bin/activate
rg -n "def _validate_model_kwargs|_validate_model_kwargs\\(" \\
  .venv/lib/python3.12/site-packages/transformers/generation/utils.py
```

当前只建立文件边界和调用关系，不展开该函数全部实现；它的最小职责
是检查传入模型的参数是否能被生成流程/模型接收，拼写错误也会在这里
暴露。

状态：`[讲解]`，等待学习者判断 `_validate_model_kwargs(...)` 是参数
检查还是模型计算。

学习者回答：先检查参数并报错。回答正确。未知参数会在真正执行模型
计算前被拦截；本次的 `attention_mask` 是合法参数，因此检查通过。

状态：`[验收]` 已理解 `_validate_model_kwargs()` 的检查职责；下一步
继续 `generate()` 中紧邻的 `_validate_generation_mode(...)`。

### 第 7 轮：检查生成模式之间的组合是否合法

源码下一行：

```python
self._validate_generation_mode(
    generation_mode,
    generation_config,
    generation_mode_kwargs,
)
```

它检查的不是参数名是否拼写正确，而是“当前生成模式”和相关设置
能否同时使用。例如 Transformers 当前会拒绝：

```text
Beam Search + streamer
```

因为这类组合在当前实现中不支持实时 streamer。源码因此要求使用
Beam Search 时把 `num_beams` 设为 1，或者不要传 streamer。

本次调用是：

```text
do_sample=True
num_beams=1
streamer=TextStreamer
```

属于普通采样，不是 Beam Search，所以这个限制不会触发，检查通过。
该函数仍然只做规则检查，不执行 `forward()`。

状态：`[讲解]`，等待学习者判断：当前配置下，
`_validate_generation_mode()` 会报错还是通过。

学习者回答：通过。回答正确。普通采样允许使用 `streamer`，当前
`num_beams=1` 也没有进入 Beam Search；检查返回后继续执行
`generate()`。

状态：`[验收]` 已理解生成模式兼容性检查；下一步读取两个生成规则
容器的初始化。

### 第 8 轮：初始化 logits 处理规则和停止规则容器

源码下一段：

```python
logits_processor = (
    logits_processor
    if logits_processor is not None
    else LogitsProcessorList()
)
stopping_criteria = (
    stopping_criteria
    if stopping_criteria is not None
    else StoppingCriteriaList()
)
```

调用 `generate()` 时我们没有手动传入这两个列表，因此本次先创建两个
空容器。可以先用它们的职责理解：

```text
LogitsProcessorList：     生成 token 前，按规则调整候选分数
StoppingCriteriaList：   每轮生成后，按规则判断是否结束
```

例如 temperature、top-p、重复惩罚属于“影响候选分数或概率”的生成
规则；达到最大长度或遇到 EOS 属于“判断是否停止”的规则。具体规则
会在后面的 `_get_logits_processor()` 和 `_get_stopping_criteria()`
中根据 `GenerationConfig` 加入这两个容器。

这一段本身只负责准备容器，不运行模型，也不产生 token。

状态：`[讲解]`，等待学习者判断：temperature/top-p 更接近修改候选
分数的容器，还是判断生成是否结束的容器。

学习者回答：修改的容器。回答正确。`temperature`、`top-p` 和重复
惩罚会在选 token 前影响候选分数或概率；最大长度、EOS 等规则则负责
判断是否结束。

状态：`[验收]` 已区分分数处理规则与停止规则；下一步继续读取
`generate()` 对 `forward()` 参数能力的检查。

### 第 9 轮：确认模型 `forward()` 是否支持 `attention_mask`

源码紧接着是：

```python
accepts_attention_mask = "attention_mask" in set(
    inspect.signature(self.forward).parameters.keys()
)
requires_attention_mask = "encoder_outputs" not in model_kwargs
kwargs_has_attention_mask = model_kwargs.get("attention_mask", None) is not None
```

今天只看第一行。它通过 `inspect.signature(self.forward)` 查看当前模型
`forward()` 的参数列表，再判断其中是否有名字为 `attention_mask` 的
参数。对本次 `Qwen3ForCausalLM`，结果为 `True`。

这里仍然只是“查看函数签名并得到布尔值”，没有调用 `forward()`，也没有
进行张量计算。后两行暂时只记住名字，等读到它们真正参与的分支时再
展开。

状态：`[讲解]`，等待学习者判断：`accepts_attention_mask=True` 表示
模型已经用 `attention_mask` 做了一次 Attention 计算，还是表示
`forward()` 的参数列表里可以接收这个名字。

学习者回答：表示 `forward()` 的参数列表可以接收这个名字。回答正确。

状态：`[验收]` 已区分检查函数签名与实际模型计算；下一步读取相邻的
`requires_attention_mask`。

### 第 10 轮：判断是否需要准备 attention mask

源码下一行：

```python
requires_attention_mask = "encoder_outputs" not in model_kwargs
```

这里的 `model_kwargs` 是准备传给模型的额外参数字典。当前模型是
decoder-only 的 Qwen3 文本模型，输入是 `input_ids`，并没有
`encoder_outputs` 这一项，因此：

```text
"encoder_outputs" not in model_kwargs -> True
requires_attention_mask              -> True
```

这表示生成流程可以继续考虑为当前输入准备 `attention_mask`；它仍然
不是在执行 Attention，而是在为后续 `forward()` 调用检查输入条件。

状态：`[讲解]`，等待学习者根据当前 decoder-only 模型没有
`encoder_outputs`，判断 `requires_attention_mask` 的值。

学习者回答：`True`。回答正确。当前没有 `encoder_outputs`，所以生成
流程需要继续处理普通文本输入的 `attention_mask`。

状态：`[验收]` 已理解 `requires_attention_mask` 的当前值；下一步读取
`kwargs_has_attention_mask`，确认调用者是否已经提供该张量。

### 第 11 轮：确认调用者是否已经传入 `attention_mask`

源码下一行：

```python
kwargs_has_attention_mask = model_kwargs.get(
    "attention_mask", None
) is not None
```

`model_kwargs.get("attention_mask", None)` 的意思是：从额外参数字典
中取出 `attention_mask`；如果没有这个键，就暂时得到 `None`。最后的
`is not None` 把结果转换成布尔值。

本次 `eval_llm.py` 明确传入了：

```python
attention_mask=inputs["attention_mask"]
```

因此 `model_kwargs` 中已经有这个张量，当前结果是：

```python
kwargs_has_attention_mask = True
```

这一行只检查“有没有传入”，不检查 mask 里的每个数值，也不执行
模型计算。

状态：`[讲解]`，等待学习者判断：本次调用已经显式传入
`attention_mask`，所以 `kwargs_has_attention_mask` 是 `True` 还是
`False`。

学习者回答：`True`。回答正确。因为 `eval_llm.py` 已把
`inputs["attention_mask"]` 放入 `generate()` 的关键字参数中。

状态：`[验收]` 已完成三个 attention mask 条件变量的当前路径判断；
下一步进入 `generate()` 的 `_prepare_model_inputs(...)`。

### 第 12 轮：确认本次真正的模型输入类型

源码调用：

```python
inputs_tensor, model_input_name, model_kwargs = self._prepare_model_inputs(
    inputs,
    generation_config.bos_token_id,
    model_kwargs,
)
```

这个辅助函数的职责先压缩为一句话：确认“传给生成流程的主要输入
张量是什么”，并返回它的名称和剩余额外参数。

当前调用者传入的是：

```python
inputs = inputs["input_ids"]
```

所以对本次文本模型，返回结果可以理解为：

```text
inputs_tensor：    昨天的 input_ids 张量，shape [1, 21]
model_input_name： "input_ids"
model_kwargs：     继续携带 attention_mask 等额外参数
```

它还处理没有显式输入时是否用 BOS 创建起始输入、以及
`inputs_embeds` 与 `input_ids` 冲突等通用情况；这些分支本次没有走，
暂不展开。

状态：`[讲解]`，等待学习者判断：本次 `model_input_name` 是
`"input_ids"` 还是 `"inputs_embeds"`。

学习者回答：`input_ids`。回答正确。本次主要输入就是昨天 tokenizer
生成的 token ID 张量，`inputs_embeds` 没有参与这条路径。

状态：`[验收]` 已确认 `_prepare_model_inputs()` 的本次返回边界；下一步
按源码处理 decoding method 是否需要额外的 `inputs_tensor` 参数。

### 第 13 轮：当前 `_sample()` 不需要额外的 `inputs_tensor`

源码紧接着检查：

```python
if "inputs_tensor" in inspect.signature(decoding_method).parameters.keys():
    generation_mode_kwargs["inputs_tensor"] = inputs_tensor
```

当前 `decoding_method` 是前面根据生成模式选出的 `_sample()`。它的
参数列表中没有名为 `inputs_tensor` 的参数，因此本次不会进入 `if` 的
代码体；`inputs_tensor` 仍然作为前面准备好的局部变量继续使用。

这是一处通用框架兼容逻辑：某些特殊生成策略可能需要原始输入张量，
普通采样 `_sample()` 不需要单独接收它。

状态：`[讲解]`，等待学习者判断：当前 `_sample()` 不接收
`inputs_tensor` 参数，所以这个 `if` 的代码体会执行还是跳过。

学习者回答：跳过。回答正确。本次不会把 `inputs_tensor` 额外塞进
`generation_mode_kwargs`。

状态：`[验收]` 已完成该兼容性分支的本次判断；下一步读取
`batch_size = inputs_tensor.shape[0]`。

### 第 14 轮：记录本次同时生成几条序列

源码下一句：

```python
batch_size = inputs_tensor.shape[0]
```

`inputs_tensor` 的 shape 是：

```text
[batch, sequence]
```

昨天的实际输入 shape 为 `[1, 21]`，因此：

```text
batch_size = 1
```

它表示本次调用同时处理 1 条输入序列；这里还没有表示生成了多少
新 token。

状态：`[讲解]`，等待学习者根据 `inputs_tensor.shape=[1, 21]`
写出 `batch_size`。

学习者回答：`1`。回答正确。`batch_size=1` 表示本次同时处理一条
输入序列；第二个维度 `21` 仍表示这条序列当前有 21 个 token。

状态：`[验收]` 已理解 batch 维与 sequence 维在这里的区别；下一步
读取当前输入张量所在的设备。

### 第 15 轮：记录输入张量所在设备

源码下一句：

```python
device = inputs_tensor.device
```

它读取 `inputs_tensor` 当前所在的计算设备，保存到局部变量
`device`。前面 `eval_llm.py` 使用了：

```text
--device cpu
```

并且 `BatchEncoding` 已经通过 `.to(args.device)` 放到了 CPU，因此本次：

```text
inputs_tensor.device -> cpu
device               -> cpu
```

这仍然只是读取设备信息，没有移动张量，也没有执行模型计算。后续
准备特殊 token、cache 和生成循环时，需要知道这些张量应在哪个设备上
创建，避免 CPU/GPU 混用。

状态：`[讲解]`，等待学习者判断：本次 `device` 的值是 `cpu` 还是
`cuda`。

学习者回答：`cpu`。回答正确。输入张量当前在 CPU，后续生成准备所需
的张量也要以此为设备依据。

状态：`[验收]` 已确认本次输入设备；下一步进入
`_prepare_special_tokens(...)`。

### 第 16 轮：把特殊 token 准备到生成配置中

源码下一句：

```python
self._prepare_special_tokens(
    generation_config,
    kwargs_has_attention_mask,
    device=device,
)
```

这个辅助函数处理生成会用到的特殊 token，例如：

```text
BOS：开始标记
EOS：结束标记
PAD：补齐标记
```

它会把配置中记录的 ID 按当前设备准备好，并写入生成配置内部供后续
停止判断和序列维护使用。当前设备是 CPU，因此这些内部 token 张量
会准备在 CPU 上。

这里要区分两个层次：

```text
tokenizer.pad_token_id = 0：      tokenizer 侧的整数 ID
generation_config 内部 token：    generate() 运行时使用的设备张量
```

本次 `eval_llm.py` 已显式传入 `pad_token_id=0` 和
`eos_token_id=2`，因此生成流程知道结束和补齐分别对应哪个 token。
该函数仍然是准备工作，不执行 `forward()`。

状态：`[讲解]`，等待学习者判断 `_prepare_special_tokens()` 是在执行
模型计算，还是在准备生成所需的特殊 token 信息。

学习者回答：准备所需的信息。回答正确。该函数把 BOS/EOS/PAD 等生成
所需 ID 按当前设备整理好，供后续长度和停止逻辑使用；它不调用
`forward()`。

状态：`[验收]` 已理解特殊 token 准备阶段；下一步只观察 decoder-only
模型的批量 padding 检查。

### 第 17 轮：decoder-only 模型的 padding 检查本次跳过

源码随后进入：

```python
if not self.config.is_encoder_decoder:
    if (
        generation_config._pad_token_tensor is not None
        and batch_size > 1
        and len(inputs_tensor.shape) == 2
        and torch.sum(inputs_tensor[:, -1] == generation_config._pad_token_tensor) > 0
    ):
        logger.warning(...)
```

当前 `Qwen3Config.is_encoder_decoder=False`，所以外层条件成立；但本次
`batch_size=1`，`batch_size > 1` 为假，内部 warning 条件整体不会成立。

这段代码是在批量生成时检查 decoder-only 模型是否错误使用了右侧
padding。今天只记住它的目的：发现可能影响批量生成结果的 padding
方向时给出警告；本次单条输入不会触发。

状态：`[讲解]`，等待学习者判断：本次 `batch_size=1` 时，padding
warning 分支会触发还是跳过。

学习者回答：跳过。回答正确。虽然当前模型是 decoder-only，但只有一条
输入序列，不满足 `batch_size > 1`，因此不会发出右侧 padding 警告。

状态：`[验收]` 已完成本次 padding 检查分支；下一步读取
`inputs_embeds` 相关的通用兼容分支。

### 第 18 轮：本次不走 `inputs_embeds` 兼容分支

源码下一段先检查：

```python
if not self.config.is_encoder_decoder and model_input_name == "inputs_embeds":
    generation_config.use_cache = True
```

这段针对的是 decoder-only 模型使用连续向量 `inputs_embeds` 作为输入
的情况。因为生成后续 token 时需要知道哪些是首次输入、哪些是新 token，
这种输入方式必须启用 cache。

当前实际值是：

```text
is_encoder_decoder = False
model_input_name   = "input_ids"
```

所以第二个条件为假，整个 `if` 跳过，`use_cache` 不会因为这一分支
被强制修改。本次仍然使用 tokenizer 产生的离散 `input_ids`。

状态：`[讲解]`，等待学习者判断：当前 `model_input_name="input_ids"`
时，这个 `inputs_embeds` 分支会执行还是跳过。

学习者回答：跳过。回答正确。本次输入名是 `input_ids`，因此不会进入
只针对 `inputs_embeds` 的强制 cache 分支。

状态：`[验收]` 已完成 `inputs_embeds` 兼容分支；下一步进入
`attention_mask` 的实际条件分支。

### 第 19 轮：本次使用调用者已经提供的二维 attention mask

源码接下来是：

```python
if not kwargs_has_attention_mask and requires_attention_mask and accepts_attention_mask:
    model_kwargs["attention_mask"] = self._prepare_attention_mask_for_generation(...)
elif kwargs_has_attention_mask:
    if model_input_name == "input_ids" and len(model_kwargs["attention_mask"].shape) > 2:
        raise ValueError("`attention_mask` passed to `generate` must be 2D.")
```

当前三个条件是：

```text
kwargs_has_attention_mask = True
requires_attention_mask   = True
accepts_attention_mask    = True
```

因此第一个 `if` 的 `not kwargs_has_attention_mask` 为假，不会重新生成
mask；代码进入 `elif kwargs_has_attention_mask`，检查调用者给的 mask。

昨天实际观察到：

```text
attention_mask.shape = [1, 21]
```

这是二维张量，所以：

```python
len(model_kwargs["attention_mask"].shape) > 2
```

为假，不报错，原来的 mask 保留给后续 `forward()` 使用。

状态：`[讲解]`，等待学习者判断：本次已经提供 `[1,21]` 的二维
`attention_mask` 时，代码会重新创建 mask，还是保留已有 mask。

学习者回答：保留已有 mask。回答正确。因为调用者已经传入二维
`attention_mask`，本次不重新创建，只做形状检查。

学习者同时要求提高效率：通用库的准备代码按相关代码块快速读完，
把时间留给后续 MiniMind 核心源码的精学。教学策略据此调整为：

```text
Transformers 通用准备逻辑：按代码块说明本次分支和结果
MiniMind 核心实现：恢复逐行阅读和互动实践
```

状态：`[纠偏]` 已调整阅读粒度；下面合并记录 `generate()` 剩余准备
阶段，不再逐个布尔条件验收。

### 第 20 轮：`generate()` 剩余通用准备阶段的三个代码块

#### A. 准备用于自回归生成的 `input_ids`

源码在 decoder-only 分支中执行：

```python
input_ids = inputs_tensor
```

因为当前 `model_input_name="input_ids"`，所以生成循环使用的
`input_ids` 仍是昨天的 `[1, 21]`。随后：

```python
expand_size = max(num_beams, num_return_sequences)  # 1
```

本次 `expand_size=1`，不会复制输入，也不会改变 shape。`token_healing`
默认关闭，因此对应分支跳过。初始化 streamer 时虽然会收到一次完整
prompt：

```python
streamer.put(input_ids.cpu())
```

但 `skip_prompt=True` 会让它不把这段原始输入重复显示出来。

#### B. 计算长度边界并设置只保留最后位置分数

源码先记录：

```python
input_ids_length = input_ids.shape[1]  # 21
```

再把 `max_new_tokens=64` 换算为本次的最大总长度：

```text
max_length = 21 + 64 = 85
```

如果先生成 EOS，实际长度会小于 85。Qwen3 的 `forward()` 支持
`logits_to_keep` 参数，因此框架还会设置：

```python
model_kwargs["logits_to_keep"] = 1
```

这里的 `logits` 可以先理解为“对词表每个 token 的候选分数”；只保留
最后一个位置的分数，就足够选择下一个 token，并减少第一次前向的无用
输出。今天不展开 logits 的数学细节。

#### C. 准备 cache、规则列表，然后调用 `_sample()`

接下来依次完成：

```text
检查长度设置是否合法
-> 根据 use_cache 准备生成 cache 容器
-> 检查模型和 input_ids 是否在同一设备（本次都是 CPU）
-> 根据 GenerationConfig 生成 logits_processor 列表
-> 根据 GenerationConfig 生成 stopping_criteria 列表
-> 设置 model_kwargs["use_cache"] = True
-> 调用 decoding_method，也就是 _sample()
```

`logits_processor` 会承载 temperature、top-p、重复惩罚等候选分数处理
规则；`stopping_criteria` 会承载最大长度和 EOS 等停止规则。cache 只
在这里先准备好，具体怎样被 `forward()` 写入和下一轮复用，留到下一
阶段深入。

最终调用形态可以压缩为：

```python
result = _sample(
    self,
    input_ids,
    logits_processor=prepared_logits_processor,
    stopping_criteria=prepared_stopping_criteria,
    generation_config=generation_config,
    **model_kwargs,
)
```

至此，`GenerationMixin.generate()` 的通用准备阶段完成，真正的逐 token
生成从 `_sample()` 开始。后续将切换到 MiniMind 自己的短版
`MiniMindForCausalLM.generate()`，逐行理解同一过程。

状态：`[讲解]`，等待学习者用一句话复述本次准备阶段最重要的三个结果：
`input_ids` 的 shape、最大总长度、以及下一步调用的函数。

学习者复述：`input_ids` 的 shape 是 `1,21`，最大总长度是 `85`，下一步
调用 `_sample`。回答正确。

状态：`[验收]` 已确认 Transformers 通用准备阶段的三个关键结果；接下来
按约定切换到 MiniMind 原生实现做白盒精读。

### 第 21 轮：切换到 MiniMind 原生 `generate()` 做原理精读

需要再次标明两条路径：

```text
真实运行 ./minimind-3：
eval_llm.py -> Qwen3ForCausalLM -> Transformers GenerationMixin.generate()

原理精读：
MiniMindForCausalLM.generate() -> MiniMindForCausalLM.forward()
```

原生实现位于：

```text
minimind/model/model_minimind.py:257
```

函数签名是：

```python
@torch.inference_mode()
def generate(
    self,
    inputs=None,
    attention_mask=None,
    max_new_tokens=8192,
    temperature=0.85,
    top_p=0.85,
    top_k=50,
    eos_token_id=2,
    streamer=None,
    use_cache=True,
    num_return_sequences=1,
    do_sample=True,
    repetition_penalty=1.0,
    **kwargs,
):
```

本次 `eval_llm.py` 中的调用参数可以对应到这里的 `inputs`、
`attention_mask`、`max_new_tokens`、`temperature`、`top_p`、
`eos_token_id`、`streamer` 和 `do_sample`。原生版本还直接提供
`top_k`、`use_cache` 等参数。

`@torch.inference_mode()` 表示函数内部按推理模式运行，不记录训练所需
的 autograd 关系；它和 `model.eval()` 不是一回事，前者控制梯度记录，
后者控制模块的训练/评估行为。

状态：`[讲解]`，下一步只看函数第一句如何得到真正参与生成的
`input_ids`，暂不进入 `for` 循环。

### 第 22 轮：从 `inputs` 得到可不断追加的 `input_ids`

源码第一句：

```python
input_ids = kwargs.pop("input_ids", inputs).repeat(
    num_return_sequences,
    1,
)
```

先只看两件事：

```text
kwargs.pop("input_ids", inputs)：优先取名为 input_ids 的关键字参数；
                             没有时使用 inputs 参数

repeat(num_return_sequences, 1)：沿 batch 维复制序列；当前值为 1，
                                 所以不复制，shape 仍是 [1, 21]
```

在 `eval_llm.py` 中调用的是：

```python
inputs=inputs["input_ids"]
```

因此本次没有额外的 `kwargs["input_ids"]`，会使用 `inputs`；
`num_return_sequences=1` 使 `repeat(1, 1)` 保持原来的单条输入。

状态：`[讲解]`，等待学习者判断：本次 `num_return_sequences=1` 时，
执行 `repeat(1, 1)` 后 `input_ids.shape` 会变成 `[1,21]`，还是复制成
多条序列。

学习者随后要求结束今天的学习，并明确下一天要从运行到解析建立完整链路，
再开始原生实现的精读。该要求成为本日停止点；未回答的 `repeat(1, 1)`
问题不计入已掌握内容。

## 五、今日总结

### 1. 今天实际完成的调用链

```text
eval_llm.py:82
-> Qwen3ForCausalLM.generate()
-> GenerationMixin.generate()
-> 整理 GenerationConfig
-> 检查 model_kwargs
-> 检查生成模式组合
-> 准备 logits_processor / stopping_criteria 容器
-> 检查 forward() 是否支持 attention_mask
-> 确认是否需要 attention_mask、调用者是否已提供
-> _prepare_model_inputs()
-> 确认 model_input_name = "input_ids"
-> 确认 batch_size = 1、device = cpu
-> 准备特殊 token
-> 跳过 batch padding warning
-> 跳过 inputs_embeds 兼容分支
-> 保留已有二维 attention_mask
-> 后续准备 input_ids、长度、cache 和规则列表
-> 准备调用 _sample()
```

本日没有执行模型 `forward()`，也没有进入 `_sample()` 的真实循环。

### 2. 通过互动确认的内容

- 生成 3 个 token 至少需要 3 次“预测下一个 token”。
- `generate()` 组织完整生成流程，`forward()` 只完成一轮模型计算。
- `Qwen3Config` 描述模型结构，`GenerationConfig` 描述本次生成设置。
- 调用处显式传入的 `temperature=0.1` 会覆盖默认值 `0.85`。
- `do_sample=True` 使本次使用采样策略。
- `num_beams=1` 表示只维护一条生成路径，不表示词表只有一个候选。
- `_validate_model_kwargs()` 先检查参数，未知参数会在模型计算前报错。
- `_validate_generation_mode()` 检查生成策略组合是否合法，本次通过。
- `LogitsProcessorList` 更接近处理候选分数，`StoppingCriteriaList`
  更接近判断是否停止。
- `accepts_attention_mask=True` 表示 `forward()` 参数列表可以接收该名字，
  不是已经执行过 Attention。
- 当前 `requires_attention_mask=True`、`kwargs_has_attention_mask=True`。
- `_prepare_model_inputs()` 本次返回主要输入 `input_ids`，不是
  `inputs_embeds`。
- `[1,21]` 中 batch 为 1；设备为 CPU。
- 特殊 token 准备只是配置/设备准备，不是模型计算。
- 单条输入时 padding warning 分支跳过；`inputs_embeds` 分支跳过；已有
  二维 `attention_mask` 被保留。
- 通用 Transformers 代码应按代码块理解接口契约；MiniMind 核心实现才
  逐行精读。

### 3. 今天的教学纠偏

曾准备从 `_sample()` 内的：

```python
batch_size, cur_len = input_ids.shape[:2]
```

直接开始，但学习者指出这跳过了 `generate()` 中间准备代码。该段被
明确标为 `[作废]`，没有算作真实学习进度；随后按调用栈回到
`GenerationMixin.generate()`，先补完准备阶段。

由此确定后续阅读规则：

```text
Transformers：看真实调用路径、关键分支和输入/输出契约
MiniMind：沿源码逐行阅读，配合亲自实验理解核心实现
```

### 4. 今日尚未掌握的内容

- 原生 `MiniMindForCausalLM.generate()` 第一行 `kwargs.pop(...).repeat(...)`
  尚未验收；今天只定位到它，未把这行算作完成。
- `_sample()` 的初始化、循环、`forward()` 调用和最后位置 logits。
- `input_ids` 如何拼接新 token，以及 EOS/长度停止判断。
- `past_key_values` / KV Cache 的首次建立和后续复用。

## 六、下一学习日入口

Day 12 不从孤立的中间代码开始，而是重新确认一条完整链路：

```text
运行命令
-> eval_llm.py 入口
-> init_model() 的模型分支
-> MiniMindForCausalLM 实例化（原生路径）
-> 原生 generate()
-> 原生 forward()
-> MiniMindModel / MiniMindBlock
```

由于当前磁盘中确认存在 `minimind/out` 权重目录并不确定，Day 12 开始
时先检查原生权重是否可用；若可用，用 `--load_from model` 实际运行；
若不可用，则用同一配置实例化未加载权重的原生模型做调用链追踪，并明确
区分“代码路径可执行”和“模型回答能力”。

第一阶段只精读原生 `generate()` 的参数接收、`input_ids` 初始化和
`for` 循环边界；确认后再进入单轮 `forward()` 与 KV Cache。

状态：`[完成]` Day 011 结束；配套 Notebook 已补充。下一学习日从
原生 MiniMind 完整运行链路重新建立上下文。
