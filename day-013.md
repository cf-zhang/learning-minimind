# Day 013：闭合推理外层并验证完整数据流

- 日期：2026-08-31
- 学习阶段：MiniMind 仓库源码主线
- 今日主题：从 generated_ids 到回答文本，闭合 eval_llm.py 外层数据流
- 当前状态：已完成
- 配套实验：[Day 013 推理外层数据流 Notebook](notebooks/day-013-eval-output-flow.ipynb)

## 一、承接阶段一

Day 001 至 Day 012 已整理为：

- [阶段一小结：从零基础到读通 MiniMind 原生推理链](stage-01-days-001-012.md)

Day 012 已经读通原生 MiniMind 的模型构造、forward()、Attention、
FeedForward、LM Head、采样循环和 KV Cache，并读到 generate() 返回
完整 input_ids。

原计划 Day 13 有四步：

1. 复核最后一个 decode 切片 shape。
2. 用轻量 trace 验证两轮真实 shape。
3. 回到 eval_llm.py，闭合“用户输入到回答文本”的完整外层数据流。
4. 推理链完成后进入训练入口。

学习者本日明确要求从步骤 3 开始。因此步骤 1、2 暂不作为今天的进入
条件；今天直接从 eval_llm.py 第 88 至 91 行继续。

## 二、今日真实源码位置

当前调用栈已经从：

    model.generate(...)
    -> MiniMindForCausalLM.generate()
    -> return input_ids

返回到：

    eval_llm.py:82
    generated_ids = model.generate(...)

下一段源码：

    response = tokenizer.decode(
        generated_ids[0][len(inputs["input_ids"][0]):],
        skip_special_tokens=True
    )
    conversation.append({"role": "assistant", "content": response})
    gen_tokens = len(generated_ids[0]) - len(inputs["input_ids"][0])
    print(
        f"\n[Speed]: {gen_tokens / (time.time() - st):.2f} tokens/s\n\n"
    ) if args.show_speed else print("\n\n")

## 三、Day 13 路线

按照源码执行顺序推进：

1. 区分 TextStreamer 已经显示的文字和 response 变量。
2. 理解为什么仍需从完整 generated_ids 中切出新 token。
3. 理解 tokenizer.decode() 和 skip_special_tokens=True。
4. 理解 conversation.append() 为什么影响下一轮历史。
5. 理解 gen_tokens 和 tokens/s 的计算边界。
6. 画出完整外层闭环，标记每一步的数据类型和 shape。
7. 根据当天进度决定是否进入训练入口，不提前跳转。

## 四、逐轮互动档案

### 第 1 轮：创建阶段小结并调整 Day 13 起点

学习者要求先把此前内容整理成阶段性小节，然后从原计划步骤 3 开始
Day 13。

已创建独立阶段文档，不覆盖每天的原始互动档案。Day 13 跳过原计划
步骤 1、2，当前真实恢复点是 model.generate() 已返回，准备理解
eval_llm.py 如何得到 response、保存历史并计算速度。

状态：阶段文档和 Day 13 文档已建立；下一步从 TextStreamer 与
response 的职责区别开始。

### 第 2 轮：区分流式显示、回答字符串和跨轮 Cache

`TextStreamer` 在一次 `generate()` 内接收每个新 token，负责把生成内容
实时显示给人；`tokenizer.decode(...)` 则在生成结束后得到完整的
`response` 字符串，供程序保存、写入接口结果或加入 `conversation`。

学习者回答：`TextStreamer` 给人看，`response` 给下一轮推理使用；程序
仍需 `decode()`，是为了拿到完整回答并保存或返回。这个理解正确，但补充
了一个条件：只有 `args.historys` 非零时，保存到 `conversation` 的回答
才会进入下一轮 prompt；默认 `historys=0` 会在下一轮开头清空历史。

学习者进一步追问：下一轮会把拼接后的 history 重新执行 Tokenizer，
还是直接利用历史 KV Cache？

当前源码的真实行为是：

```text
同一次 assistant 回答内部：
generate() 使用 KV Cache，后续每步只处理一个新 token

两次用户提问之间：
保留的 conversation 重新应用 chat template
-> 重新 Tokenizer 整段历史和新问题
-> 新一次 generate() 的 past_key_values 从 None 开始
```

例如 `historys=2`，第二轮会重新编码：

```python
conversation = [
    {"role": "user", "content": "你是谁"},
    {"role": "assistant", "content": "我是MiniMind..."},
    {"role": "user", "content": "你能做什么？"},
]
```

学习者准确复述了这三条消息，并回答第二次进入 `generate()` 时
`past_key_values=None`。[验收]

当前恢复点：`response` 已经解码并加入 `conversation`；下一步按源码顺序
进入 `gen_tokens` 和 tokens/s 统计。

### 第 3 轮：`gen_tokens` 统计新生成的 token 数

源码：

```python
gen_tokens = len(generated_ids[0]) - len(inputs["input_ids"][0])
```

`generated_ids[0]` 包含原始 prompt 和新生成 token；
`inputs["input_ids"][0]` 只包含原始 prompt。因此二者长度相减，得到本次
实际新生成的 token 数，而不是字符数、汉字数或单词数。

触发停止的 EOS 已经先追加到 `generated_ids`，所以会计入 `gen_tokens`；
但最终 `decode(..., skip_special_tokens=True)` 会从 `response` 文本中隐藏
EOS。

验收例子：原始输入长度 21，完整输出长度 46。学习者回答
`gen_tokens=25`、EOS 计入 25、EOS 不显示在最终 response 中。三项均
正确。[验收]

当前恢复点：进入第 91 行的 tokens/s 计算，区分计时开始和结束边界。

### 第 4 轮：tokens/s 的计时边界

计时从 `model.generate(...)` 之前的 `st=time.time()` 开始，到最终打印
速度时再次调用 `time.time()` 结束。它包含 prompt 的第一次完整 forward、
后续逐 token 解码、采样、流式显示以及最后少量 decode/外层处理；不包含
模型加载、用户等待、chat template 和输入 Tokenizer。

因此源码显示的 `[Speed]` 更准确地说是“本次回答的近似生成吞吐量”，
并不是把 prompt prefill 排除后的纯 decode tokens/s。短 prompt 时两者
数值可能接近，长 prompt 时首次完整计算会明显影响结果。

验收例子：`gen_tokens=32`、耗时 `0.5` 秒。学习者计算为 `64 tokens/s`，
并准确回答它是包含首次计算的近似吞吐量。[验收]

当前恢复点：`eval_llm.py` 第 88 至 91 行已经逐行读完；下一步把当前
for 循环中的对象类型、shape 和内容连成一条完整外层数据流。

### 第 5 轮：闭合用户文本到回答文本的外层数据流

本轮把一次对话中的对象按执行顺序连接起来：

```text
prompt：str
-> conversation：list[dict]
-> apply_chat_template：str
-> tokenizer：BatchEncoding
   -> input_ids [1, prompt_len]
   -> attention_mask [1, prompt_len]
-> model.generate：generated_ids [1, prompt_len + generated_len]
-> 切掉前 prompt_len 个位置：[generated_len]
-> tokenizer.decode：response str
-> conversation.append(assistant response)
-> gen_tokens / elapsed_time
```

`TextStreamer` 是旁路显示：每生成一个 token 就显示给人；主线仍通过
`generated_ids` 和 `decode()` 得到程序可保存的完整回答。

学习者正确回答：`generated_ids` 包含 prompt 与新生成 token；`response`
只包含回答文本并隐藏特殊 token；默认 `historys=0` 时保存的 assistant
回答不会真正进入下一轮模型输入。[验收]

第一项起初回答的是 `input_ids` 和 `attention_mask` 的 `[1,21]` shape，
而问题询问的是内容。补充后确认：`input_ids` 选择 B，即它包含 chat
template 排版后的完整 prompt 对应的 token ID，包括角色、问题和特殊
标记，而不是只有“你是谁”。同时明确 `[1,21]` 仅是该例子的 shape，
会随问题、历史和模板变化。[纠偏][验收]

至此 Day 13 原计划步骤 3“闭合完整外层数据流”已完成。下一步沿新的
源码主线进入 `trainer/train_full_sft.py`，从程序入口开始阅读，不从
训练循环中间跳入。

## 五、Day 13 完整结论

本日闭合的数据流：

```text
prompt：str
-> conversation：list[dict]
-> chat template：str
-> tokenizer：BatchEncoding
-> input_ids / attention_mask
-> model.generate()
-> generated_ids（prompt + 新生成 token）
-> 按 prompt 长度切片
-> tokenizer.decode()
-> response：str
-> conversation.append(assistant)
-> gen_tokens / elapsed_time
```

同时确认两个工程边界：

```text
TextStreamer：给人实时显示
response：给程序保存和继续使用

一次回答内部：复用 KV Cache
两轮用户对话之间：重新模板化、重新 Tokenizer，Cache 从 None 开始
```

默认 `historys=0`，所以 assistant 回答虽然被暂时加入 `conversation`，
下一轮开始时仍会清空，不会进入下一轮模型输入。

速度统计：

```text
gen_tokens = 完整 generated_ids 长度 - prompt token 长度
tokens/s = gen_tokens / (time.time() - st)
```

该时间包含 prompt 的首次 forward 和后续生成，因此是近似生成吞吐量，
不是排除了 prefill 的纯 decode 速度。

## 六、Day 13 停止边界

学习者明确要求把训练内容划入新的 Day 14。因此 Day 13 到此结束，不把
后续 SFT 概念和 `train_full_sft.py` 阅读记录在本日。

Day 14 从以下位置开始：

```text
SFT 与推理的区别
-> train_full_sft.py 顶层执行顺序
-> def train_epoch 只定义、不立即执行
-> __main__ 参数解析
```
