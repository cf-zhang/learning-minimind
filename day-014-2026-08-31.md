# Day 014：从 Full SFT 入口进入训练链路

- 日期：2026-08-31
- 学习阶段：MiniMind 训练源码主线
- 今日主题：区分推理与 SFT，按程序执行顺序进入 train_full_sft.py
- 当前状态：已完成
- 配套实验：[day-014-sft-entry.ipynb](notebooks/day-014-sft-entry.ipynb)

## 一、为什么单独开始 Day 14

Day 13 已经闭合从用户输入到回答文本、对话历史和速度统计的完整推理
外层。学习者明确提出：从 SFT 和 train_full_sft.py 开始已经属于新的
训练过程，应重新开 Day 14 记录。

因此边界是：

    Day 13：推理输出 -> response -> conversation -> speed
    Day 14：SFT 数据 -> loss -> backward -> 参数更新 -> checkpoint

## 二、Day 14 的源码入口

主文件：

    minimind/trainer/train_full_sft.py

仓库建议的运行方式：

    cd trainer
    python train_full_sft.py

本日继续遵守源码调用关系：

    执行文件顶部代码
    -> 定义 train_epoch（此时不执行函数体）
    -> __name__ == "__main__"
    -> 参数解析
    -> 环境、模型、数据和优化器初始化
    -> 构造 DataLoader
    -> 真正调用 train_epoch
    -> 此时才进入训练循环
    -> 遇到 SFTDataset 再进入 dataset/lm_dataset.py

## 三、逐轮互动档案

### 第 1 轮：先建立 SFT 与推理的区别

SFT 是 Supervised Fine-Tuning，即监督微调。最小前提是训练数据中已经有：

    用户问题
    + 已知的 assistant 正确回答

推理时，模型只看到 prompt，并通过 generate() 逐 token 产生未知回答。
SFT 时，程序把完整训练对话和用于监督的 labels 送入模型，计算预测与
已知回答之间的 loss，再执行 backward() 和参数更新。

当前只建立边界：

    推理：model.generate(...)
    SFT：model(input_ids, labels=labels) -> loss -> backward()

labels 如何构造、哪些位置参与 loss，必须等调用栈进入 SFTDataset 后再
展开，现在不提前讲结论。

### 第 2 轮：函数定义不等于函数执行

Python 直接运行 train_full_sft.py 时，先执行顶部导入和路径准备。读到：

    def train_epoch(...):

只创建函数对象，不会立即遍历训练数据、计算 loss 或 backward。程序继续
向下，到达：

    if __name__ == "__main__":

条件成立后才进入初始化。训练准备完成并在后面真正调用：

    train_epoch(...)

时，函数体才开始执行。

学习者回答：“不会；只定义函数，等 main 里依次调用时才执行。”回答
正确。[验收]

## 四、当前恢复点

下一步继续 train_full_sft.py 的真实执行顺序：

    if __name__ == "__main__":
    -> argparse.ArgumentParser
    -> parser.add_argument(...)
    -> args = parser.parse_args()

本轮只先理解训练参数如何分组，不进入尚未执行的 train_epoch() 函数体。

### 第 12 轮：完整确认 `init_model()` 的权重加载，并补齐 pretrain 文件

本轮重新回到 `trainer_utils.init_model()` 的核心代码：

```python
model = MiniMindForCausalLM(lm_config)
weight_path = f'{save_dir}/{from_weight}_{lm_config.hidden_size}{moe_suffix}.pth'
weights = torch.load(weight_path, map_location=device)
model.load_state_dict(weights, strict=False)
```

学习者准确回答：

```text
MiniMindForCausalLM(lm_config) 先创建初始化的模型结构
第 126 行 torch.load() 从文件读取参数字典
第 127 行 load_state_dict() 把参数填入模型
from_weight='none' 时不会加载 pretrain_768.pth
```

随后学习者将 `pretrain_768.pth` 下载到默认路径：

```text
/home/zcf/githubs/minimind/minimind/out/pretrain_768.pth
```

只读检查结果：

```text
文件大小：约 132 MiB
参数字典：91 个键
参数 dtype：torch.float16
当前 MiniMindConfig(768, 8, dense) 加载
missing keys：0
unexpected keys：0
```

这证明默认训练所需的起始权重已经准备好，并与当前模型结构完全匹配。
注意，之前存在的 `full_sft_768.pth` 是另一份训练阶段权重；默认
`from_weight='pretrain'` 时，脚本仍然读取 `pretrain_768.pth`。

当前恢复点：`init_model()` 已经完整读完并通过真实文件匹配验证；下一步
回到主文件构造 `SFTDataset`，再进入它在取样本时执行的 `__getitem__()`。

### 第 3 轮：只提取决定默认训练主线的参数

参数数量较多，继续采用“用到再展开”的规则。本轮只关注：

```text
data_path=../dataset/sft_t2t_mini.jsonl
max_seq_len=768
from_weight=pretrain
epochs=2
save_weight=full_sft
hidden_size=768
```

默认训练链由此解析为：

```text
../out/pretrain_768.pth
+ ../dataset/sft_t2t_mini.jsonl
-> 进行 2 个 epoch 的 Full SFT
-> ../out/full_sft_768.pth
```

学习者回答：从 pretrain 权重开始；默认起始文件是
`../out/pretrain_768.pth`；默认输出文件是
`../out/full_sft_768.pth`。三项均正确。[验收]

当前恢复点：`args=parser.parse_args()` 已完成；下一句进入
`init_distributed_mode()`，需要按调用关系先读该函数。

### 第 4 轮：普通进程与分布式进程的入口判断

`train_full_sft.py` 在解析参数后执行：

```python
local_rank = init_distributed_mode()
if dist.is_initialized():
    args.device = f"cuda:{local_rank}"
```

`init_distributed_mode()` 的判断依据是环境变量 `RANK`：

```python
if int(os.environ.get("RANK", -1)) == -1:
    return 0
```

普通命令 `python train_full_sft.py` 通常没有 `RANK`，因此返回
`local_rank=0`，但没有初始化进程组；`dist.is_initialized()` 仍为
`False`，不会执行设备覆盖。通过 `torchrun` 启动时，才会建立 NCCL
进程组并根据 `LOCAL_RANK` 选择当前进程负责的 GPU。

学习者回答：`0；false；不会`。三项正确。[验收]

当前恢复点：回到主文件，继续读取 `setup_seed(...)` 和输出目录初始化。

### 第 5 轮：固定随机起点并准备输出目录

普通单进程运行时，主文件执行：

```python
setup_seed(42 + 0)
os.makedirs(args.save_dir, exist_ok=True)
```

`setup_seed()` 会设置 Python、NumPy、PyTorch 和 CUDA 随机数生成器的起点，
使相同代码、数据和初始权重的实验更容易复现。它不会加载模型、读取数据、
更新参数或开始训练。分布式运行时使用 `42 + dist.get_rank()`，让不同
进程拥有不同随机序列。

`os.makedirs(args.save_dir, exist_ok=True)` 确保默认的 `../out` 输出目录
存在；目录已经存在时不会报错。

学习者回答：`42；不会；不会`。三项均正确。[验收]

当前恢复点：输出目录准备完成，下一句创建 `MiniMindConfig`。

### 第 6 轮：配置对象与恢复检查点不是一回事

主文件继续执行：

```python
lm_config = MiniMindConfig(
    hidden_size=args.hidden_size,
    num_hidden_layers=args.num_hidden_layers,
    use_moe=bool(args.use_moe)
)

ckp_data = (
    lm_checkpoint(
        lm_config,
        weight=args.save_weight,
        save_dir='../checkpoints'
    )
    if args.from_resume == 1
    else None
)
```

本轮区分了三个对象：

```text
lm_config：描述模型结构，例如 hidden_size、层数、是否 MoE
from_weight：后面从哪份基础权重开始，例如 pretrain_768.pth
from_resume：是否恢复上次训练的完整状态
```

默认 `from_resume=0`，因此不会调用 `lm_checkpoint()` 的加载分支，
`ckp_data=None`。这不影响后面通过 `from_weight='pretrain'` 加载基础
模型参数。

学习者回答：`MiniMindConfig，描述模型结构；None；不是一件事情`。三项
正确。[验收]

当前恢复点：`lm_config` 和 `ckp_data` 已准备好；下一句进入设备类型和
混合精度上下文的设置。

### 第 7 轮：设备类型与混合精度上下文

主文件根据 `args.device` 和 `args.dtype` 准备：

```python
device_type = "cuda" if "cuda" in args.device else "cpu"
dtype = torch.bfloat16 if args.dtype == "bfloat16" else torch.float16
autocast_ctx = (
    nullcontext()
    if device_type == "cpu"
    else torch.cuda.amp.autocast(dtype=dtype)
)
```

CPU、默认 `args.dtype="bfloat16"` 时：

```text
device_type = "cpu"
dtype = torch.bfloat16
autocast_ctx = nullcontext()
```

`dtype` 只是记录希望使用的数据类型；CPU 分支使用 `nullcontext()`，
不会启用 CUDA 自动混合精度。只有 CUDA 分支才会在后面的
`with autocast_ctx:` 中使用自动混合精度上下文。

学习者回答：`cpu；bfloat16；nullcontext()`。三项正确。[验收]

当前恢复点：设备和精度策略已经准备好，下一句进入可选的实验日志配置。

### 第 8 轮：可选实验日志分支

主文件先执行：

```python
wandb = None
if args.use_wandb and is_main_process():
    import swanlab as wandb
    ...
```

默认 `args.use_wandb=False`。由于 Python 的 `and` 在左侧为 `False` 时
会短路，右侧的 `is_main_process()` 不需要继续判断，内部的 `swanlab`
也不会导入。因此当前核心训练不依赖远程实验日志，`wandb` 保持 `None`；
后面的 `if wandb: wandb.log(...)` 会跳过。

`swanlab` 只负责记录 loss、学习率和实验名称等曲线，不负责创建模型、
读取数据、计算 loss 或更新参数。

学习者回答：`false；不会；None`。三项正确。[验收]

当前恢复点：可选日志配置完成，下一句进入 `init_model(...)` 创建并加载
训练模型。

### 第 9 轮：训练入口调用 `trainer_utils.init_model()`

主文件第 5 段首先调用：

```python
model, tokenizer = init_model(
    lm_config,
    args.from_weight,
    device=args.device
)
```

按照调用关系进入 `trainer/trainer_utils.py`：

```python
def init_model(
    lm_config,
    from_weight='pretrain',
    tokenizer_path='../model',
    save_dir='../out',
    device='cuda'
):
    tokenizer = AutoTokenizer.from_pretrained(tokenizer_path)
    model = MiniMindForCausalLM(lm_config)
```

第一句从 `../model` 加载 Tokenizer；第二句根据已经准备好的
`MiniMindConfig` 创建模型结构。此时仍然只是“结构 + tokenizer”，下一
个 `if` 才读取已有权重。

训练脚本从 `trainer/` 目录运行时，默认相对路径对应：

```text
../model -> minimind/model
../out   -> minimind/out
```

当前默认 `from_weight='pretrain'`，所以进入：

```python
if from_weight != 'none':
    moe_suffix = '_moe' if lm_config.use_moe else ''
    weight_path = f'{save_dir}/{from_weight}_{lm_config.hidden_size}{moe_suffix}.pth'
    weights = torch.load(weight_path, map_location=device)
    model.load_state_dict(weights, strict=False)
```

默认 dense 配置下，期望读取：

```text
../out/pretrain_768.pth
```

这里使用 `strict=False`，比推理入口的 `strict=True` 宽松；训练脚本允许
checkpoint 与当前结构存在少量不匹配，并继续加载能对应上的参数。是否
应该宽松加载、哪些键可能不匹配，等真实训练权重和 LoRA/MoE 场景出现时
再单独展开。

当前恢复点：已进入 `init_model()`，读完 tokenizer、模型结构和基础权重
加载逻辑；下一步继续看它返回前的参数统计与设备放置。

### 第 10 轮：`init_model()` 返回模型和 Tokenizer

`init_model()` 返回前执行：

```python
get_model_params(model, lm_config)
Logger(f'Trainable Params: ...')
return model.to(device), tokenizer
```

`get_model_params()` 只统计参数；Full SFT 下可训练参数约 64M。
`model.to(device)` 移动模型参数和 buffer，不移动 Tokenizer。函数返回：

```text
model：已创建、加载 pretrain 权重并放到设备的 MiniMindForCausalLM
tokenizer：从 ../model 加载的 Tokenizer
```

学习者回答：不会更新参数；64M；不会移动 Tokenizer。三项正确。[验收]

当前恢复点：返回主文件的 `model, tokenizer = init_model(...)`，下一句
进入 `SFTDataset(args.data_path, tokenizer, max_length=args.max_seq_len)`。

### 第 11 轮：进入 `SFTDataset` 构造函数

主文件：

```python
train_ds = SFTDataset(
    args.data_path,
    tokenizer,
    max_length=args.max_seq_len
)
```

默认实参对应：

```text
jsonl_path = ../dataset/sft_t2t_mini.jsonl
tokenizer  = ../model 的 Tokenizer
max_length = 768
```

`SFTDataset` 继承 PyTorch 的 `Dataset`。它是一个“可以按下标取训练样本”
的对象，后面 `DataLoader` 会反复调用它的 `__getitem__(index)`。构造函数
先准备共享信息，不会在这一行计算模型 logits、loss 或 backward。

进入 `dataset/lm_dataset.py` 后，前几句是：

```python
super().__init__()
self.tokenizer = tokenizer
self.max_length = max_length
```

含义是：初始化父类、保存 Tokenizer、保存每条样本允许的最大 token 长度。

然后定义数据字段结构：

```python
features = Features({
    'conversations': [{
        'role': Value('string'),
        'content': Value('string'),
        'reasoning_content': Value('string'),
        'tools': Value('string'),
        'tool_calls': Value('string')
    }]
})
```

这不是模型参数，而是在告诉 Hugging Face `datasets`：每条 JSONL 样本应有
一个 `conversations` 列，该列是消息列表，每条消息的这些字段按字符串读取。

字段有些样本可能为空，但数据结构先统一。

真正读取文件的是：

```python
self.samples = load_dataset(
    'json',
    data_files=jsonl_path,
    split='train',
    features=features
)
```

其中：

```text
'json'：使用 JSON/JSONL 数据读取器
data_files：要读取的文件路径
split='train'：把这份文件作为 train 数据集
features：按前面声明的字段结构解析
```

当前仓库本地还没有默认的 `dataset/sft_t2t_mini.jsonl`，所以实际执行这
个构造函数会在 `load_dataset(...)` 处因为找不到文件而停止；这不是代码
逻辑已经执行完，而是训练资源尚未准备好。后续需要下载数据后再做真实
`len(train_ds)` 和样本实验。

构造函数最后预先编码两组边界 token：

```python
self.bos_id = tokenizer(
    f'{tokenizer.bos_token}assistant\\n',
    add_special_tokens=False
).input_ids
self.eos_id = tokenizer(
    f'{tokenizer.eos_token}\\n',
    add_special_tokens=False
).input_ids
```

它们用于后面的 `generate_labels()` 识别：哪一段 token 属于 assistant
回答、回答在哪里结束。当前先不展开标签扫描算法。

当前恢复点：已读到 `SFTDataset.__init__()` 的文件读取和边界 token 准备；
下一步只确认构造函数保存的两个核心字段和实际读取发生的位置。

### 第 13 轮：确认数据集字段和数据文件已准备

学习者回答：`features` 是模型结构；`load_dataset(...)` 的结果在
`self.samples`；构造数据集不会计算 logits；具体样本由
`__getitem__(index)` 处理。

其中后三项正确。第一项纠正为：`features` 描述的是 JSONL 的数据字段
结构，例如 `conversations` 是消息列表、消息中的 `role` 和 `content`
按字符串读取；模型结构仍由 `MiniMindConfig` 描述。

随后检查数据文件：

```text
路径：dataset/sft_t2t_mini.jsonl
大小：约 1.7G
类型：New Line Delimited JSON
```

首条样本确认是一个包含 `conversations` 列的 JSON 对象，消息包含
`user`、`assistant`，部分 assistant 消息还带有 `reasoning_content`。
默认 SFT 数据资源已准备好，可以进入真实样本取出流程。[实践]

当前恢复点：返回 `SFTDataset.__getitem__(index)`，从
`sample = self.samples[index]` 开始；下一步只读样本行和
`pre_processing_chat(...)` 的入口。

### 第 14 轮：一个 item 是一条完整对话样本

普通对话例子：

```python
conversations = [
    {'role': 'user', 'content': '你好'},
    {'role': 'assistant', 'content': '你好，有什么可以帮你？'}
]
```

没有 `tools` 字段时：

```text
any(conv.get('tools') for conv in conversations) -> False
```

不会在工具保护分支直接返回；若 `random.random() < 0.2`，就在这组
消息最前面增加一条从 `SYSTEM_PROMPTS` 中随机选择的 system 消息。

学习者回答：`false；不会；增加 system prompt`。三项正确。[验收]

学习者进一步确认：每个 item 实际上是一条聊天记录。补充后的准确层级是：

```text
JSONL 文件：一行一个 item
item：一个字典，主要字段是 conversations
conversations：一组按顺序排列的消息 list
message：一个字典，包含 role、content 等字段
```

因此 `self.samples[index]` 取出的是一条完整对话样本，
`sample['conversations']` 取出的是该样本中的多轮消息；它不是只取一条
消息，也还没有变成 `input_ids` 或 Tensor。[验收]

当前恢复点：已理解 JSONL -> item -> conversations -> message 的层级；
下一步继续读 `pre_processing_chat()` 返回后，`create_chat_prompt()` 如何
把这组消息变成训练用的模板字符串。

### 第 15 轮：`create_chat_prompt()` 先复制消息，再整理工具字段

进入函数后：

```python
messages = []
tools = None
for message in conversations:
    message = dict(message)
```

`messages` 准备用来保存整理后的消息；循环按原顺序遍历
`conversations`；`dict(message)` 创建当前消息的浅复制，后续修改不会
直接改写数据集中的原始字典。

学习者回答：`messages` 保存结构化后的 message；循环不会改变顺序；复制
后修改不会修改原始 conversations 中的同一个字典。三项正确。[验收]

当前恢复点：已读完循环复制部分；下一步读取工具字段转换和
`messages.append(message)`。

### 第 16 轮：工具定义与工具调用的原始数据形态

学习者回答：`json.load()`；复制出来的 `message`；`None`，并要求给出
包含 `tools` 和 `tool_calls` 的样例。这里将第一项纠正为
`json.loads()`：`loads` 从 JSON 字符串解析，`load` 则从已经打开的文件
对象解析。后两项正确。[纠偏][验收]

符合当前 `SFTDataset` 字段声明的一条原始样本可以写成：

```python
{
    'conversations': [
        {
            'role': 'system',
            'content': '你可以调用天气工具。',
            'tools': '[{"type":"function","function":{"name":"get_weather","description":"查询天气","parameters":{"type":"object","properties":{"city":{"type":"string"}},"required":["city"]}}}]'
        },
        {
            'role': 'user',
            'content': '北京今天的天气怎么样？'
        },
        {
            'role': 'assistant',
            'content': '',
            'tool_calls': '[{"type":"function","function":{"name":"get_weather","arguments":"{\\"city\\":\\"北京\\"}"}}]'
        },
        {
            'role': 'tool',
            'content': '北京：晴，25 摄氏度。'
        },
        {
            'role': 'assistant',
            'content': '北京今天晴，气温约 25 摄氏度。'
        }
    ]
}
```

这里的关键是：JSONL 原始文件中的 `tools` 和 `tool_calls` 都可以是 JSON
字符串，因为 `Features` 把这两个字段声明为 `Value('string')`。进入
`create_chat_prompt()` 后：

```text
system.tools 字符串
-> json.loads()
-> tools：Python list/dict，最终传给 chat template

assistant.tool_calls 字符串
-> json.loads()
-> 当前复制 message 的 tool_calls：Python list/dict
```

`tools` 描述“有哪些工具以及参数格式”；`tool_calls` 描述 assistant 本
轮实际请求调用哪个工具、传什么参数；`tool` 角色消息则保存工具执行后
返回给模型的结果。普通问答没有这些字段时，函数外层 `tools` 保持
`None`。

当前恢复点：工具字段已经理解；下一步继续到 `messages.append(message)`
和 `apply_chat_template()`，看整理后的消息怎样变成完整 prompt 字符串。

### 第 17 轮：`tools`、`tool_calls` 解析后怎样进入 `messages`

学习者回答了上一轮的类型问题：`str；json；可用工具定义；描述调用请求`。
方向正确，但需要把“JSON 格式”和“Python 运行时类型”分开：

```python
type(messages)                   # list
type(messages[0])                # dict
type(tools)                      # list（本例）
type(messages[2]['tool_calls'])  # list（本例）
```

JSON 是字符串采用的数据格式，不是 Python 的运行时类型。处理 system
消息时：

```python
tools = json.loads(message['tools'])
```

只把解析结果赋给函数的局部变量 `tools`，没有回写
`message['tools']`。因此：

```text
tools：已经从 JSON 字符串变成 Python list
messages[0]['tools']：仍然是原来的 str
```

处理 assistant 消息时则直接回写复制出来的字典：

```python
message['tool_calls'] = json.loads(message['tool_calls'])
```

因此 append 进去的 assistant message 中，`tool_calls` 已经是 Python
list。`messages.append(message)` 只负责把当前整理好的字典按原顺序加入
列表，不会合并消息、分词或调用模型。

学习者回答类型验收：`list；json；json list；json str；字符串`。纠正后
确认：`messages` 是 list，每条 message 是 dict，外部 `tools` 和解析后
的 `tool_calls` 通常是 list，而 `messages[0]['tools']` 仍是 str。
[纠偏][验收]

### 第 18 轮：`apply_chat_template()` 完成 `create_chat_prompt()`

继续源码：

```python
return self.tokenizer.apply_chat_template(
    messages,
    tokenize=False,
    add_generation_prompt=False,
    tools=tools
)
```

各参数的职责：

```text
messages：按顺序整理好的 list[dict]
tools：整段对话可用的工具定义，Python list/dict 或 None
tokenize=False：这里只返回 str，不直接返回 token ID
add_generation_prompt=False：训练样本已经包含 assistant 回答，不在末尾
                             再追加一个等待生成的 assistant 起始标记
```

学习者回答：因为训练时“不需要等待推理给出下一步内容”。理解正确。更
准确地说，SFT 数据中 assistant 目标答案已经存在，模板要完整保留它；
推理时才常用 `add_generation_prompt=True`，在 user 消息后追加 assistant
起始标记，让模型接着生成。[验收]

至此 `create_chat_prompt()` 已完整读完。

### 第 19 轮：回到 `SFTDataset.__getitem__()` 的前三步

按照调用关系返回：

```python
sample = self.samples[index]
conversations = pre_processing_chat(sample['conversations'])
prompt = self.create_chat_prompt(conversations)
prompt = post_processing_chat(prompt)
```

变量变化：

```text
sample：一条完整 JSONL 样本，通常是 dict
conversations：预处理后的消息列表 list[dict]
prompt：聊天模板拼接后的 str
```

若普通对话没有 tools、随机数小于 0.2，则
`pre_processing_chat()` 在最前面增加随机 system 消息。若模板中存在空
`<think>\n\n</think>\n\n` 且 `post_processing_chat()` 的随机数大于
0.2，则删除该空思考段。

学习者回答：`增加system内容；删掉空的think`。两项正确。[验收]

### 第 20 轮：prompt 字符串变成 token ID，并执行截断

第 111 行：

```python
input_ids = self.tokenizer(prompt).input_ids[:self.max_length]
```

分成三步理解：

```text
self.tokenizer(prompt)：返回 BatchEncoding
.input_ids：取出一维 Python list[int]
[:self.max_length]：最多保留前 max_length 个 token ID
```

这里没有指定 `return_tensors='pt'`，所以此时不是 Tensor，也没有 batch
维度。默认 `self.max_length=768` 时，500 个 token 保留 500 个；900 个
token 只保留前 768 个，索引 768 及之后的内容被丢弃；`prompt` 字符串
本身不会被修改。

学习者回答：`500；768；768以后的被丢弃；python list`。全部正确。
[验收]

### 第 21 轮：padding 把每条样本统一到固定长度

第 112 行：

```python
input_ids += [self.tokenizer.pad_token_id] * (
    self.max_length - len(input_ids)
)
```

本地 tokenizer 的真实配置是：

```python
tokenizer.pad_token      == '<|endoftext|>'
tokenizer.pad_token_id   == 0
tokenizer.bos_token_id   == 1
tokenizer.eos_token_id   == 2
```

若真实 token 有 500 个，则追加 `768 - 500 = 268` 个 0；若上一行已经
截断到 768，则追加 0 个。统一长度后，DataLoader 才能把多条样本堆叠
成规则的 `[batch_size, 768]` Tensor。

学习者回答：12 个 token 会追加 `768-12` 个 pad，最终共 768 个。正确。
[验收]

### 第 22 轮：一条带 tools 的多轮 conversation 怎样形成一组 `input_ids`

应学习者要求，将带工具样例实际传入本地 tokenizer。结构化消息包括：

```text
system：定义助手身份，并在 tools 字段提供 get_weather 定义
user：询问北京天气
assistant：发起 get_weather(city='北京') 的 tool_call
tool：返回 temperature=26、weather='晴'
```

`apply_chat_template()` 将它们拼成一个完整字符串，主要结构为：

```text
<|im_start|>system
...system content...

# Tools
<tools>
...get_weather 的 JSON 定义...
</tools>
...工具调用格式说明...<|im_end|>
<|im_start|>user
北京今天的天气怎么样？<|im_end|>
<|im_start|>assistant
<think>

</think>

<tool_call>
{"name": "get_weather", "arguments": {"city": "北京"}}
</tool_call><|im_end|>
<|im_start|>user
<tool_response>
{"temperature": 26, "weather": "晴"}
</tool_response><|im_end|>
```

这个 prompt 是一个字符串；整组 messages 不会分别变成多条训练样本。
本地实测该示例分词为 294 个 token ID，随后追加
`768 - 294 = 474` 个 padding ID。

关键层级：

```text
一个 JSONL item
-> 一组完整 conversations（可以包含多轮和工具调用）
-> 一个 prompt 字符串
-> 一组一维 input_ids
-> 一条固定长度训练样本
```

DataLoader 才会把多条 item 组成 batch。学习者回答：`294；768-294；
一个样本`。全部正确。[验收]

### 第 23 轮：为什么还要构造 `labels`

第 113 行：

```python
labels = self.generate_labels(input_ids)
```

`input_ids` 包含 system、user、assistant、tool 和 padding，但 Full SFT
主要要求模型学习 assistant 应该输出的内容。`labels` 是与 `input_ids`
等长的并行目标列表：

```text
input_ids：模型实际读取的完整上下文
labels：哪些位置计入 loss，以及这些位置的正确 token ID
```

函数先执行：

```python
labels = [-100] * len(input_ids)
```

PyTorch 交叉熵中，`-100` 是忽略标记，不是词表 token ID。初始化刚完成
时，768 个位置全是 `-100`，所以此刻参与 loss 的位置是 0 个。后续循环
才把 assistant 目标区域改成真实 token ID。

学习者回答：`768；llm输出的部分会参与；不计入`。第一、三项正确；
第二项纠正为“初始化此刻为 0 个，循环完成后训练数据中已有的 assistant
目标内容才参与”。这里不是 `generate()` 临时产生输出。[纠偏][验收]

### 第 24 轮：滑动查找 assistant 起始标记

初始化时保存的真实定位序列：

```python
self.bos_id
# [1, 1388, 570, 811, 234]
# '<|im_start|>assistant\n'

self.eos_id
# [2, 234]
# '<|im_end|>\n'
```

源码：

```python
i = 0
while i < len(input_ids):
    if input_ids[i:i + len(self.bos_id)] == self.bos_id:
```

这里用长度等于 `len(self.bos_id)` 的窗口，从左向右查找完整 assistant
起始标记。简化例子：

```python
self.bos_id = [1, 8, 9]
input_ids = [4, 5, 1, 8, 9, 6, 7]
```

第一次成功时 `i=2`，切片是 `[1, 8, 9]`。学习者回答：`3；2；
[1, 8, 9]`。全部正确。[验收]

### 第 25 轮：`start` 和 `end` 寻找 assistant 正文及结束标记

找到起始标记后：

```python
start = i + len(self.bos_id)
end = start
```

`i` 指向 assistant 起始标记的第一个 token；`start` 指向起始标记之后
的第一个正文 token；`end` 从正文开头向右寻找 `self.eos_id`。

例子：

```python
input_ids   = [4, 5, 1, 8, 9, 6, 7, 2, 0, 4]
self.bos_id = [1, 8, 9]
self.eos_id = [2, 0]
```

得到：

```python
start == 5
end == 7
input_ids[start:end] == [6, 7]
```

`end` 指向结束标记的第一个 token，不是正文最后一个 token。学习者四项
回答全部正确。[验收]

### 第 26 轮：把 assistant 正文和 EOS 写入 `labels`

源码：

```python
for j in range(
    start,
    min(end + len(self.eos_id), self.max_length)
):
    labels[j] = input_ids[j]
```

沿用上例，`range(5, 9)` 覆盖索引 `5、6、7、8`，即正文 `[6, 7]`
加结束标记 `[2, 0]`。最终：

```python
labels = [-100, -100, -100, -100, -100, 6, 7, 2, 0, -100]
```

把 EOS 也纳入目标，是为了让模型学习何时结束 assistant 输出。
`min(..., self.max_length)` 防止有效范围越过固定序列长度。

学习者准确写出了索引和完整 labels。[验收]

### 第 27 轮：处理完一段 assistant 后继续查找下一段

源码：

```python
i = end + len(self.eos_id) if end < len(input_ids) else len(input_ids)
```

找到 EOS 时，`end` 指向 EOS 起点，`end + len(eos_id)` 才是 EOS 后的
第一个位置。如果 `end=14`、EOS 长度为 2，则下一个 `i=16`。若一直
搜索到 `end == len(input_ids) == 20`，说明没有找到 EOS，令 `i=20`，
外层循环结束。

学习者回答 `17；20`；第二项正确，第一项纠正为 16。[纠偏][验收]

未找到 assistant 起始标记时：

```python
else:
    i += 1
```

学习者确认 `i=4` 后变为 5。[验收]

若一条 conversation 有多轮 assistant，函数会在处理完第一段 EOS 后
继续扫描，标记后面的每一段 assistant，而不是只标记最后一段。

学习者总结：`与input_ids中assistant输出相关的内容都映射到labels中，
其他位置保留-100`。理解正确。精确补充：assistant 内容及其结束标记
保留真实 token ID；system、user、工具说明、tool 返回和 padding 保留
`-100`。[验收]

### 第 28 轮：`__getitem__()` 返回两个 Long Tensor

最后执行：

```python
return (
    torch.tensor(input_ids, dtype=torch.long),
    torch.tensor(labels, dtype=torch.long)
)
```

返回值是一个二元 tuple，内部是两个 `torch.Tensor`。`input_ids` 需要
long 类型作为 Embedding 查表索引；`labels` 需要 long 类型作为交叉熵
的目标类别。单条样本还没有 batch 维度：`max_length=8` 时两个 shape
都是 `[8]`；训练默认 `max_length=768` 时才都是 `[768]`。此时尚未计算
模型 loss。

学习者回答：`两个tensor；long；[768]；没有`。纠正为：结构是“包含
两个 Tensor 的 tuple”；题设 `max_length=8` 时 shape 是 `[8]`，默认
真实配置下才是 `[768]`。[纠偏][验收]

至此 `SFTDataset.__getitem__()` 已完整读完。

### 第 29 轮：返回训练入口，创建单机数据采样器

构造 `SFTDataset` 时只执行 `__init__()`；前面精读的 `__getitem__()` 是
DataLoader 以后索取样本时才执行。回到真实执行顺序：

```python
train_sampler = (
    DistributedSampler(train_ds)
    if dist.is_initialized()
    else None
)
```

当前普通单进程启动下 `dist.is_initialized()` 为 False，因此不创建
`DistributedSampler`，真实值为 `train_sampler=None`。分布式采样器的
价值是将样本编号分配给多个训练进程，避免各 GPU 重复处理整个数据集。

学习者回答：`不会；None`。正确。[验收]

### 第 30 轮：`GradScaler` 保护 float16 的小梯度

源码：

```python
scaler = torch.cuda.amp.GradScaler(
    enabled=(args.dtype == 'float16')
)
```

默认 `args.dtype='bfloat16'`，所以对象会创建，但 `enabled=False`。显式
使用 float16 时才启用。它先放大 loss，使很小的梯度在 float16 中不易
变成 0；更新前再把梯度缩回原尺度。它不改变模型结构。

```text
autocast_ctx：控制 forward 中很多计算采用的精度
GradScaler：保护 float16 下过小的梯度
```

学习者回答：`不启用；True；保护过小的梯度`。全部正确。[验收]

### 第 31 轮：创建 AdamW 优化器

源码：

```python
optimizer = optim.AdamW(
    model.parameters(),
    lr=args.learning_rate
)
```

`model.parameters()` 让优化器管理模型中真实注册参数的引用，不是复制
另一套参数。默认学习率 `1e-5` 即 `0.00001`。这里只创建和配置优化器，
尚无 batch、forward、loss、backward 或 `optimizer.step()`，所以参数不会
立刻变化。

学习者回答：`管理；0.00001；不会`。全部正确，并提出需要展开 SGD 和
AdamW。[验收]

### 第 32 轮：从已实践过的 SGD 更新规则开始补基础

先纠正名称：是 SGD（Stochastic Gradient Descent），不是 SDG。核心
更新规则：

```python
parameter = parameter - learning_rate * gradient
```

称为“随机”梯度下降，是因为实际训练通常使用一个 batch 对整体梯度做
估计，而不是每次使用完整数据集。普通 SGD 对本轮某个参数的更新只直接
使用当前梯度，不主动记录此前梯度长期方向或大小。

题目：

```python
parameter = 10.0
gradient = 2.0
learning_rate = 0.1
```

学习者回答：`8；变小；因为正数代表变量越大值就越大导致error越大loss
越大`。方向解释正确，但算术纠正为：

```python
10.0 - 0.1 * 2.0 = 9.8
```

更严谨的梯度解释：正梯度表示在当前位置附近，增大参数会使 loss 增大，
因此为了降低 loss 要减小参数；模型计算图中不一定必须存在名为 error
的中间变量。[纠偏]

当前恢复点：正在补充 SGD 与 AdamW。下一步引入 Adam 的第一个改进：
记录梯度的长期方向；尚未回到 `train_full_sft.py` 的恢复训练分支。

### 第 33 轮：Adam 的第一份记忆 `m` 保存历史方向

普通 SGD 直接采用当前 batch 的梯度，某一批数据的短期波动可能立即改变
本轮更新方向。Adam 维护梯度的指数移动平均：

```python
m_new = beta1 * m_old + (1 - beta1) * current_gradient
```

教学例子使用 `beta1=0.5`，表示 50% 旧记忆加 50% 当前梯度。若：

```python
m_old = 3.0
current_gradient = -1.0
```

则 `m_new=1.0`，仍然为正。学习者回答：`1；正的；没有立刻推翻`，
说明已经理解：一次方向相反的梯度不会立即推翻此前积累的总体方向。
[验收]

PyTorch AdamW 默认 `beta1=0.9`，即方向记忆中 90% 来自旧记忆、10%
来自当前梯度。`m` 正式名称为梯度的一阶矩估计，这里可直观理解为
“近期带符号的平均梯度方向”。

### 第 34 轮：Adam 的第二份记忆 `v` 保存梯度尺度

不同模型参数的梯度大小可能相差很多。Adam 还维护：

```python
v_new = beta2 * v_old + (1 - beta2) * current_gradient ** 2
```

梯度平方后不再保存正负方向，只保存大小。若：

```python
v_old = 8.0
current_gradient = -4.0
beta2 = 0.5
```

则梯度平方为 16，`v_new=12`。学习者回答：`16；12；不能，他一直是
正的，只能表达变化的快慢`。计算和核心理解正确；更精确地说，`v`
表达近期梯度的典型尺度，方向仍由 `m` 表达。[验收]

Adam 大致使用：

```python
m_hat / (sqrt(v_hat) + epsilon)
```

`v` 较大时分母较大，压小该参数的步幅；`v` 较小时分母较小，相对
保留其步幅。因此即使所有参数共享一个全局学习率，也能得到不同的实际
更新幅度。

### 第 35 轮：把 `m`、`v` 合到一次 Adam 更新

核心更新形式：

```python
parameter_new = parameter_old - learning_rate * (
    m_hat / (sqrt(v_hat) + epsilon)
)
```

`epsilon` 是极小正数，用于防止分母为 0。题目：

```python
parameter_old = 5.0
learning_rate = 0.2
m_hat = -2.0
v_hat = 4.0
```

学习者回答：`2；-1；5.2；因为梯度方向是负的`。四项全部正确：

```python
sqrt(4) = 2
-2 / 2 = -1
5.0 - 0.2 * (-1) = 5.2
```

这里 `m_hat` 决定方向，`v_hat` 调节步幅。[验收]

### 第 36 轮：为什么还需要 `m_hat` 和 `v_hat`

Adam 的两份记忆从 0 初始化。训练刚开始时，移动平均中大量比例来自
初始的 0，会把 `m`、`v` 拉得过小。因此 Adam 使用偏差修正：

```python
m_hat = m_t / (1 - beta1 ** t)
v_hat = v_t / (1 - beta2 ** t)
```

其中 `t` 是参数更新次数。修正主要影响训练初期；随着 `t` 增大，分母
趋近 1，修正作用逐渐减弱。

学习者表示不继续做这部分重复算术。教学直接给出简化题答案：当
`gradient=4`、`beta1=beta2=0.5`、`t=1` 时，`m_1=2`、`v_1=8`、
`m_hat=4`、`v_hat=16`。重点保留为“帽子表示纠正从 0 初始化造成的
早期偏小”，不要求继续手算。[进度调整]

### 第 37 轮：AdamW 的 `W` 是独立权重衰减

`W` 表示 Weight Decay。除 Adam 的梯度更新外，它额外使参数绝对值
轻微向 0 收缩：

```python
parameter -= learning_rate * weight_decay * parameter
```

正参数略微减小，负参数略微增大，所以两者的绝对值都会减小。其目的
是限制权重持续变大、降低对训练数据细节的过度依赖。AdamW 把这一
收缩从 Adam 的梯度自适应过程里分离出来，因此称为解耦权重衰减。

MiniMind 没有显式传入 `weight_decay`，使用 PyTorch AdamW 默认值
`0.01`。默认学习率只有 `1e-5`，所以每步衰减很轻微。

学习者总结：`m记住方向；v记住尺度；不让变得太大`。理解正确。完整
对照：

```text
SGD：主要按当前 batch 梯度直接更新
Adam：用 m 记录历史方向，用 v 自适应调节不同参数的步幅
AdamW：Adam 再加独立的权重衰减
```

当前恢复点：SGD、Adam、AdamW 的入门基础已补齐；下一步严格回到
`train_full_sft.py` 第 141 行，从 checkpoint 恢复训练状态的分支继续。

### 第 38 轮：恢复训练分支的前两行

回到 `train_full_sft.py`：

```python
if ckp_data:
    model.load_state_dict(ckp_data['model'])
    optimizer.load_state_dict(ckp_data['optimizer'])
```

普通启动的真实值是 `ckp_data=None`，所以这两行被跳过；为了理解
`--from_resume 1`，继续阅读其含义。

```text
model.load_state_dict(ckp_data['model'])：
恢复上次中断时模型已经学到的参数

optimizer.load_state_dict(ckp_data['optimizer'])：
恢复 AdamW 的 m、v、更新步数、参数组等优化器历史状态
```

`from_weight` 和 `from_resume` 的顺序关系：`init_model()` 先按
`from_weight`（默认 `pretrain`）加载基础权重；若存在 `ckp_data`，再用
断点中的 model 状态覆盖它。只恢复模型权重而不恢复 AdamW 的状态，虽然
模型已经学到的参数仍在，但 `m`、`v` 等历史会丢失，不算完整断点续训。

学习者回答：`在；不在；不算`。全部正确。[验收]

### 第 39 轮：恢复 GradScaler、epoch 和 step

分支剩余代码：

```python
scaler.load_state_dict(ckp_data['scaler'])
start_epoch = ckp_data['epoch']
start_step = ckp_data.get('step', 0)
```

`scaler` 恢复 float16 动态 loss 缩放状态；默认 bfloat16 且 scaler 禁用时
影响很小，但代码保持了统一的恢复接口。`start_epoch` 记录从哪个 epoch
继续，`start_step` 记录该 epoch 中已完成到哪个 batch。`.get('step', 0)`
表示旧 checkpoint 没有该键时安全地从 0 开始。

题目：

```python
ckp_data = {'epoch': 1, 'step': 320}
```

学习者回答：`1；329；0`。第 1 项正确，第 2 项纠正为 `320`；第 3 项
的 `0` 适用于没有 `step` 键的另一个情况。[纠偏]

### 第 40 轮：`torch.compile` 分支

源码：

```python
if args.use_compile == 1:
    model = torch.compile(model)
    Logger('torch.compile enabled')
```

默认 `args.use_compile=0`，所以普通运行不会调用它，也不会打印日志。
若显式传 `--use_compile 1`，PyTorch 会分析模型计算并尝试生成更高效的
执行版本。它主要优化执行速度，不改变模型的层数、参数形状或训练目标；
首次运行可能有额外编译准备时间。

随后：

```python
if dist.is_initialized():
    model = DistributedDataParallel(model, device_ids=[local_rank])
```

当前普通单进程中 `dist.is_initialized()` 为 False，因此也不会进行 DDP
包装。多 GPU 时 DDP 负责多个进程间的梯度同步，这条分支今天只完成定位，
不展开多 GPU 细节。

学习者回答：`不会；优化执行速度；不会`。三项全部正确。[验收]

### Day 14 结束点

今天从 Full SFT 入口继续到训练真正开始前，已经按真实调用顺序完成：

```text
train_full_sft.py
-> init_model() 加载 pretrain 权重
-> SFTDataset.__init__() 读取 JSONL、准备 tokenizer 和标记序列
-> SFTDataset.__getitem__()
   -> 预处理 conversation
   -> tools/tool_calls 解析
   -> apply_chat_template() 得到 prompt 字符串
   -> tokenizer 得到 input_ids
   -> 截断和 padding 到 max_length
   -> generate_labels() 只标记 assistant 内容和 EOS
   -> 返回 (input_ids, labels) 两个 long Tensor
-> 创建 DistributedSampler（单机时为 None）
-> 创建禁用的 GradScaler（默认 bfloat16）
-> 创建 AdamW
-> 可选恢复 model / optimizer / scaler / epoch / step
-> 可选 torch.compile 和 DDP 包装
```

今天尚未进入：

```python
for epoch in range(start_epoch, args.epochs):
```

因此还没有学习 DataLoader 如何批量取样本、`SkipBatchSampler` 如何跳过已
完成的 step，也没有进入 `train_epoch()` 的 forward、loss、backward、梯度
累积、梯度裁剪、`optimizer.step()` 和保存权重。

下一天准确恢复位置：

```text
trainer/train_full_sft.py 第 158 行
```

第一步将解释 `for epoch in range(...)` 的实际范围；随后按顺序阅读随机
索引、`skip`、`SkipBatchSampler`、`DataLoader`，最后进入
`train_epoch()` 的第一行 batch 解包。今天学习到此结束。
