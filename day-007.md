# Day 007：多 Head 的维度拆分与组合

- 日期：2026-08-21 至 2026-08-22（跨自然日继续，仍计一个学习日）
- 学习阶段：Transformer Attention 实现
- 今日主题：从单 Head 扩展到 Multi-Head Attention
- 当前状态：已完成
- 配套实验：[Day 007 Multi-Head Attention Notebook](notebooks/day-007-multi-head-attention.ipynb)

## 一、承接 Day 006

Day 006 已经完成简化单 Head Attention：

```text
h
  -> q_proj / k_proj / v_proj
  -> Q / K / V
  -> Q @ K.T / sqrt(head_dim)
  -> Causal Mask
  -> Softmax
  -> weights @ V
  -> 一个 Head 的读取结果
```

还建立了多 Head 的直觉：同一个 token 同时使用多套独立评分与读取规则，获得多个结果后再组合。

Day 006 配套实验见 [Attention Notebook](notebooks/day-006-attention.ipynb)。

## 二、今日目标

按小关卡推进：

1. 理解 `hidden_size=768`、`num_attention_heads=8`、`head_dim=96` 的关系。
2. 区分“Q 投影输出的维度拆分”和“直接切 Embedding”。
3. 看清 batch、sequence、head、head_dim 四个维度。
4. 理解每个 Head 如何独立计算分数和读取结果。
5. 理解多个 Head 的结果如何拼接。
6. 理解 `o_proj` 为什么把组合结果映射回 `hidden_size`。
7. 用小张量亲自完成一次多 Head shape 实验。

只有当前关卡经过讲解、实践和确认后，才进入下一关。

## 三、今天暂不引入

```text
GQA 中 Q Head 与 KV Head 数量不同的问题
repeat_kv
RoPE
KV Cache
Flash Attention / SDPA
MiniMind Attention 源码逐行阅读
```

这些内容依赖普通 Multi-Head shape，今天不会提前混入。

## 四、验收标准

- [x] 能解释 `768 = 8 * 96` 在 Q 投影结果中的含义。
- [x] 不把多 Head 误解为直接将 Embedding 原始向量切块。
- [x] 能写出 Q 从 `[batch, sequence, hidden_size]` 到多 Head shape 的变化。
- [x] 能说明每个 Head 各自在哪两个维度上计算 Attention。
- [x] 能说明多个 Head 的结果如何恢复为 `hidden_size`。
- [x] 能亲自运行并解释一个最小多 Head 实验。

## 五、逐轮互动档案

### 第 1 轮：从 768、8 和 96 开始

Day 007 的第一个问题：MiniMind-3 的 `hidden_size=768`、`num_attention_heads=8` 和 `head_dim=96` 如何对应？

学习者回答：

> 因为 768 需要分成 8 个 head，所以每个 head 就是 96 个数据。被拆分的是 QKV 计算以后的投影结果。

回答正确。在普通 Multi-Head Attention 的当前学习范围内：

```text
q_proj 输出：768 维 -> 8 组，每组 96 维
k_proj 输出：768 维 -> 8 组，每组 96 维
v_proj 输出：768 维 -> 8 组，每组 96 维
```

被整理的是三套投影的输出，不是 Embedding 原始向量；每套投影都先接收完整的输入向量。MiniMind-3 实际的 K/V 维度会因 GQA 与 Q Head 数量不同，留到普通 Multi-Head shape 掌握后解释。

状态：`[实践]`、`[验收]` 已理解 `768 = 8 * 96` 以及 Q/K/V 投影输出才是拆分对象。

下一步只观察一个更小的 shape 例子：把 `hidden_size=8`、`num_heads=2` 的投影输出整理成两个 Head，每个 Head 4 维。

### 第 2 轮：把 `[1, 3, 8]` 整理为 `[1, 2, 3, 4]`

学习者使用连续数字构造：

```python
q_projected = torch.arange(24).reshape(1, 3, 8)
q_split = q_projected.reshape(1, 3, 2, 4)
q_heads = q_split.transpose(1, 2)
```

实际 shape：

```text
拆分前 Q：                 [1, 3, 8]
直接整理后：               [1, 3, 2, 4]
交换 sequence/head 后：    [1, 2, 3, 4]
```

实际内容验证了没有重新计算数值：

```text
Head 0：
token 1 -> [0, 1, 2, 3]
token 2 -> [8, 9, 10, 11]
token 3 -> [16, 17, 18, 19]

Head 1：
token 1 -> [4, 5, 6, 7]
token 2 -> [12, 13, 14, 15]
token 3 -> [20, 21, 22, 23]
```

状态：`[实践]`、`[验收]` 已验证投影输出从 `[batch, sequence, hidden_size]` 整理为 `[batch, num_heads, sequence, head_dim]`，且只是维度重排，不改变数值。

下一步只观察每个 Head 各自接收哪块张量，暂不合并 Head 结果。

### 第 3 轮：补回被省略的 `h -> q_proj` 数据流

在准备取出两个 Head 时，学习者指出 `q_projected` 的身份不清楚，并询问它是否就是 token 经过 Embedding 后的内容。

这个疑问源于上一实验为了专门观察 reshape，直接用连续数字构造了 `q_projected`，省略了前面的 `h -> q_proj`，造成语义断层。需要明确：

```text
h：第一层时来自 Embedding，后续层时来自上一层输出
q_projected = q_proj(h)：完整 h 经过 Q 的 Linear 后得到的投影结果
q_heads：再将 q_projected 的最后一维整理为多个 Head
```

因此 `q_projected` 不是 Embedding 输出本身。连续数字只是帮助观察 reshape 的假数据，不代表真实 Q 的数值来源。

状态：`[纠偏]`、`[讲解]`、`[待确认]`。暂停 Head 独立计算，先恢复从输入 h 到 q_projected 再到 q_heads 的完整小实验。

学习者确认已经清楚。当前普通 Multi-Head 数据流是：

```text
h -> q_proj -> q_projected -> q_heads
h -> k_proj -> k_projected -> k_heads
h -> v_proj -> v_projected -> v_heads
```

投影阶段由三套不同权重完成数值计算；拆 Head 阶段只是 reshape/transpose。MiniMind-3 的 8 个 Q Head、4 个 K/V Head 作为 GQA 欠账保留。

状态更新：`[验收]` 已理解 Q/K/V 都先投影、后拆 Head，以及普通 MHA 中同编号 Q/K/V Head 配套计算。

### 第 4 轮：用三套真实 Linear 补回投影过程

学习者使用 `h`、`q_proj`、`k_proj` 和 `v_proj` 完整运行：

```text
h shape：             [1, 3, 8]
q_projected shape：   [1, 3, 8]
k_projected shape：   [1, 3, 8]
v_projected shape：   [1, 3, 8]
q_heads shape：       [1, 2, 3, 4]
k_heads shape：       [1, 2, 3, 4]
v_heads shape：       [1, 2, 3, 4]
```

第一个 token 的原始 `h` 为：

```text
[0., 1., 2., 3., 4., 5., 6., 7.]
```

经过 `q_proj` 后，完整 Q 的数值变为：

```text
[-1.2764, -1.7123, 2.3622, 2.6204,
 -1.9740, 1.1737, -0.3782, -0.5188]
```

拆成两个 Head 后：

```text
Head 0：[-1.2764, -1.7123, 2.3622, 2.6204]
Head 1：[-1.9740, 1.1737, -0.3782, -0.5188]
```

三个判断结果：

```text
投影前 h 和投影后 Q 是否相同：False
拆 Head 后再拼回是否等于 q_projected：True
Q 和 K 是否相同：False
```

结果解释：

```text
Linear(8, 8) 保持 shape 不变，但改变数值
reshape/transpose 只改变组织方式，不改变数值
q_proj 与 k_proj 使用不同权重，所以同一个 h 得到不同 Q/K
```

状态：`[实践]`、`[验收]` 已验证投影改变数值、拆 Head 只重排数值，以及 Q/K/V 的三条并行来源。

下一步只观察每个 Head 的 Attention 计算范围，暂不合并 Head 结果。

### 第 5 轮：两个 Head 各自计算分数矩阵

学习者取出两个 Head 的 Q/K，并分别计算：

```python
head_0_scores = head_0_q @ head_0_k.transpose(-2, -1)
head_1_scores = head_1_q @ head_1_k.transpose(-2, -1)
```

实际 shape：

```text
Head 0 的 Q/K：   [1, 3, 4]
Head 1 的 Q/K：   [1, 3, 4]
Head 0 的分数：    [1, 3, 3]
Head 1 的分数：    [1, 3, 3]
```

实际分数：

```text
Head 0：
[[ -9.5292, -22.8997, -36.2701],
 [ -28.6824, -62.8090, -96.9356],
 [ -47.8355, -102.7183, -157.6011]]

Head 1：
[[9.2117, 17.8656, 26.5195],
 [14.1792, 25.2438, 36.3084],
 [19.1466, 32.6220, 46.0974]]
```

这验证了：

```text
Head 0：Q0 @ K0.T -> 自己的 [3, 3] 分数矩阵
Head 1：Q1 @ K1.T -> 自己的 [3, 3] 分数矩阵
```

两个 Head 的原始分数数值明显不同，不能据此说 Head 1“更重要”：当前权重是随机初始化的，且这些分数还没有除以 `sqrt(head_dim)`、没有 Causal Mask、没有 Softmax。输出中的 `grad_fn=<UnsafeViewBackward0>` 只表示 PyTorch 正在追踪矩阵乘法产生的结果，和分数语义无关。

状态：`[实践]`、`[验收]` 已验证多个 Head 在自己的 Q/K 张量上独立计算；下一步对两个 Head 各自执行相同的缩放、Causal Mask 与 Softmax。

### 第 6 轮：两个 Head 各自得到读取比例

学习者分别对两个 Head 执行缩放、Causal Mask 和 Softmax。共同 Mask 为：

```text
[[False, True,  True],
 [False, False, True],
 [False, False, False]]
```

实际读取比例：

```text
Head 0：
[[1.0000, 0,          0],
 [1.0000, 3.8860e-08, 0],
 [1.0000, 1.2088e-12, 1.4612e-24]]

Head 1：
[[1.0000,     0,        0],
 [0.0039412,  0.99606,  0],
 [0.0000014,  0.001184, 0.99881]]
```

两个 Head 每行比例之和均为 1，未来位置比例均为 0。第三个 token 在 Head 0 中几乎全部读取第一个位置，在 Head 1 中几乎全部读取自己，直接验证了同一 token 可以在不同 Head 中形成不同的读取方式。

当前投影权重为随机初始化，因此这些极端比例只用于验证计算结构，不能解释为有意义的语言关系或固定 Head 分工。

状态：`[实践]`、`[验收]` 已验证两个 Head 独立完成缩放、Mask 和 Softmax，并产生不同的读取比例。下一步分别使用 `V0`、`V1` 计算两个 Head 的读取结果。

### 第 7 轮：两个 Head 分别读取自己的 V

学习者计算：

```python
head_0_result = head_0_weights @ head_0_v
head_1_result = head_1_weights @ head_1_v
```

实际 shape：

```text
Head 0/1 的 V：      [1, 3, 4]
Head 0/1 的结果：    [1, 3, 4]
```

Head 0 结果：

```text
[[ 1.6437, 0.3913, -2.0026, 0.2140],
 [ 1.6437, 0.3913, -2.0026, 0.2140],
 [ 1.6437, 0.3913, -2.0026, 0.2140]]
```

三行相同不是多头机制规定，而是这组随机参数下 Head 0 的三个读取者都几乎 100% 读取第一个位置的 V。

Head 1 结果：

```text
[[ -4.0351, -0.3112, 2.0388, 5.2533],
 [-12.0404,  2.6285, 3.8493, 11.0094],
 [-20.0995,  5.5879, 5.6719, 16.8041]]
```

三行不同是因为 Head 1 的三个读取者几乎分别读取了其因果范围内的不同位置。

状态：`[实践]`、`[验收]` 已验证同编号 `weights_head @ V_head` 得到每个 Head 的独立结果。下一步按 token 对齐组合两个 Head 的结果。

### 第 8 轮：按 token 拼接两个 Head 的结果

学习者先把两个结果堆叠为：

```text
all_head_results shape = [1, 2, 3, 4]
```

再执行：

```python
combined = all_head_results.transpose(1, 2).reshape(1, 3, 8)
```

得到：

```text
combined shape = [1, 3, 8]
```

实际每个 token 的 8 个数按以下顺序组成：

```text
前 4 个数：该 token 的 Head 0 结果
后 4 个数：该 token 的 Head 1 结果
```

例如第一个 token：

```text
[ 1.6437, 0.3913, -2.0026, 0.2140,
 -4.0351, -0.3112, 2.0388, 5.2533]
```

这验证了反向 shape 过程：

```text
[B, H, S, D] -> transpose -> [B, S, H, D]
             -> reshape   -> [B, S, H*D]
```

状态：`[实践]`、`[验收]` 已理解多个 Head 如何按同一个 token 对齐并恢复 `hidden_size`。下一步加入 `o_proj`，观察它如何学习混合各 Head 的信息。

### 第 9 轮：使用 `o_proj` 混合多个 Head

学习者创建 `Linear(8, 8, bias=False)`，将拼接结果送入输出投影。实际 shape：

```text
combined：         [1, 3, 8]
attention_output： [1, 3, 8]
```

第一个 token 在投影前：

```text
[1.6437, 0.3913, -2.0026, 0.2140,
 -4.0351, -0.3112, 2.0388, 5.2533]
```

经过 `o_proj` 后：

```text
[-1.7250, 1.8689, 0.9961, -0.2946,
  1.0039, -0.9764, -1.6310, 0.4880]
```

`combined` 与 `attention_output` 数值是否相同为 `False`。这验证了：拼接时已经恢复 shape，`o_proj` 的主要职责是让每个输出维度学习组合所有 Head 的信息，而不是只做 shape 修补。

状态：`[实践]`、`[验收]` 已理解 `o_proj` 保持 `hidden_size` 并重新混合多个 Head 的结果。普通 Multi-Head Attention 主数据流已经闭合，下一步进行简短验收。

### 第 10 轮：验收 Q 的完整 shape 链

学习者补全：

```text
h              [1, 3, 8]
  -> q_proj
q_projected    [1, 3, 8]
  -> reshape
q_split        [1, 3, 2, 4]
  -> transpose
q_heads        [1, 2, 3, 4]
```

并准确判断：

```text
q_proj：会改变数值
reshape + transpose：不会改变或丢失数值，只改变组织方式
```

状态：`[验收]` Q/K/V 投影、拆 Head、独立 Attention、按 token 拼接和 `o_proj` 的普通 Multi-Head 主链全部完成。

## 六、问题与知识欠账

- GQA：普通多 Head 的 Q/K/V shape 掌握后进入。
- RoPE：Q/K 的多 Head shape 掌握后进入。
- KV Cache：Q/K/V、Causal Mask、GQA 与逐 token 生成连通后进入。
- RMSNorm、残差连接、MLP：Attention 子层完成后返回 MiniMindBlock。

## 七、今日总结

Day 007 完成了普通 Multi-Head Attention 从投影到输出的完整 shape 与数值链：

```text
h [B,S,hidden]
  -> q/k/v_proj：改变数值，保持普通 MHA 的 hidden 宽度
Q/K/V projected [B,S,hidden]
  -> reshape + transpose：只重排
Q/K/V heads [B,H,S,D]
  -> 每个 Head 独立执行缩放点积、Mask、Softmax 和读取 V
head results [B,H,S,D]
  -> transpose + reshape：按 token 拼接
combined [B,S,H*D] = [B,S,hidden]
  -> o_proj：学习混合所有 Head
attention output [B,S,hidden]
```

学习者亲自验证：

```text
8 = 2 * 4，以及 768 = 8 * 96 的对应关系
拆分对象是 Q/K/V 投影结果，不是原始 Embedding
q_proj 改变数值；reshape/transpose 不改变或丢失数值
Q/K/V 都先投影、后拆 Head
两个 Head 分别产生自己的 [3,3] 分数和读取比例
未来位置比例为 0，每行比例总和为 1
每个 Head 分别读取同编号 V
[1,2,3,4] 按 token 拼接为 [1,3,8]
o_proj 保持 shape 但重新混合各 Head 数值
```

配套 Notebook 已在 MiniMind 项目 `.venv` 中从空状态顺序执行全部代码 Cell 通过。

## 八、下一学习日入口

下一次实际学习记为 Day 008，进入 MiniMind-3 使用的 GQA：

```text
Q：8 个 Head
K：4 个 Head
V：4 个 Head
```

第一个问题是：普通 Multi-Head 中同编号 `Q_head/K_head/V_head` 配套计算，为什么 GQA 中 K/V Head 数量更少仍然能够与 8 个 Q Head 计算？先从“两个 Q Head 共享一组 K/V”建立直觉，再看 `repeat_kv` 和真实 shape。
