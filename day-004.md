# Day 004：从 grad_fn 到 loss.backward()

- 日期：2026-08-14
- 学习阶段：神经网络训练的最小前置知识
- 今日主题：确认计算图，观察 `loss.backward()` 的实际结果
- 当前状态：已完成当天范围（梯度累加和清零继续到 Day 005）

## 零、如何使用本课

本课承接 Day 003 的同一组数字，不引入新模型：

```text
x=2，target=10，weight=3
prediction=6，error=-4，loss=16
```

学习重点不是记住 `MulBackward0` 等类名，而是用实际状态分清：

```text
grad_fn 存在  != 梯度已经算好
backward 完成 != 参数已经更新
参数更新完成  != 旧梯度已经清除
```

可复现实验前半段保存在 [codes/day4.py](codes/day4.py)。

## 逐轮互动档案

### 第 1 轮：提交上一天的 grad_fn 实验

学习者运行并提交：

```text
weight：tensor(3., requires_grad=True)
prediction：tensor(6., grad_fn=<MulBackward0>)
error：tensor(-4., grad_fn=<SubBackward0>)
loss：tensor(16., grad_fn=<PowBackward0>)

weight.grad_fn：None
prediction.grad_fn：MulBackward0
error.grad_fn：SubBackward0
loss.grad_fn：PowBackward0
```

结果按产生顺序解释为：

```text
weight
  --乘法--> prediction
  --减法--> error
  --平方--> loss
```

`weight` 是直接创建、要求求梯度的叶子张量，所以没有“由上一步什么运算产生”的 `grad_fn`。中间结果则分别记住了产生自己的运算。

此时 `weight.grad` 仍然没有值：记录可反向追踪的路线，不等于已经沿路线计算梯度。

状态：`[实践]`、`[验收]` 已观察并解释最小计算图。

### 第 2 轮：第一次调用 `loss.backward()`

学习者打印 backward 前后状态：

```text
backward 前 weight：3
backward 前 weight.grad：None

loss.backward()

backward 后 weight：3
backward 后 weight.grad：-16
```

这一次输出同时排除了两个误解：

```text
backward 确实计算出了梯度 -16
backward 没有把 weight 从 3 改成别的值
```

状态：`[实践]` backward 与参数更新已经用输出区分。

### 第 3 轮：解释反向传播如何得到同一个 `-16`

Day 003 是从公式前向拆解变化率；本轮从 loss 反向走一遍：

```text
起点：loss 对自己变化率为 1
PowBackward：1 * (2 * error) = -8
SubBackward：-8 * 1 = -8
MulBackward：-8 * x = -16
```

每个 `grad_fn` 只需要知道自己那一步输入变化会怎样影响输出，再把上游传来的数继续乘下去。最终到达叶子参数时，把结果放入 `weight.grad`。

学习者确认这一步清楚。

状态：`[验收]` 能把 `grad_fn` 路线与链式计算对应起来。

### 第 4 轮：为什么梯度是 `-16`，却要让 weight 增大

训练要沿 loss 下降方向移动，因此使用：

```text
new_weight = old_weight - learning_rate * gradient
```

这里学习率是 `0.1`：

```text
new_weight = 3 - 0.1 * (-16) = 4.6
```

“减去负数”等于增加，所以负梯度和 weight 向右移动并不冲突。梯度指向 loss 增大的方向，训练更新取它的反方向。

### 第 5 轮：学习者手工更新并重新 forward

学习者在 `torch.no_grad()` 中更新参数，提交实际输出：

```text
更新后的 weight：4.6000
原来的 weight.grad：-16
新的 prediction：9.2000
新的 error：-0.8000
新的 loss：0.6400
```

与更新前比较：

```text
weight：3 -> 4.6
prediction：6 -> 9.2，更接近 target=10
loss：16 -> 0.64，明显下降
```

这不是只凭公式声称更新有效，而是由下一次 forward 的真实结果验证。

状态：`[实践]`、`[验收]` 完成一次手工参数更新闭环。

### 第 6 轮：`torch.no_grad()` 只管更新动作

参数更新写在 `torch.no_grad()` 中，因为这一步是训练流程对参数的管理动作，不需要再成为模型计算图的一部分。

需要明确它没有做的事：

```text
没有永久关闭 weight.requires_grad
没有清空 weight.grad
没有阻止下一轮 forward 产生新的 grad_fn
```

学习者确认没有疑问。

状态：`[验收]` `no_grad` 与梯度清零没有混为一谈。

### 第 7 轮：在梯度清零前暂停

更新后 `weight.grad` 仍然是 `-16`。当天没有直接告诉学习者“每轮都覆盖”，而是把它保留到下一天做第二次 backward，亲自观察新梯度是覆盖还是累加。

Day 004 因此准确停在：

```text
已经完成一次 backward 和一次更新
旧梯度仍保存在 .grad
尚未执行第二次 backward
```

## 一、承接 Day 003

Day 003 已理解：

```text
prediction、target、error、loss
参数变化如何影响 loss
梯度是当前位置的局部坡度
weight=3 时梯度 -16 的来源
```

Day 003 最后布置了 `grad_fn` 观察实验，学习者今天提交结果，因此按学习日编号在 Day 004 继续。

## 二、学习者实践：计算图

实际结果：

```text
weight：tensor(3., requires_grad=True)
prediction：tensor(6., grad_fn=<MulBackward0>)
error：tensor(-4., grad_fn=<SubBackward0>)
loss：tensor(16., grad_fn=<PowBackward0>)

weight.grad_fn：None
prediction.grad_fn：MulBackward0
error.grad_fn：SubBackward0
loss.grad_fn：PowBackward0
```

对应路径：

```text
weight（直接创建的叶子参数，无 grad_fn）
  -> 乘法 MulBackward
prediction
  -> 减法 SubBackward
error
  -> 平方 PowBackward
loss
```

这表示 PyTorch 已记录各结果由何种运算产生，以及反向经过每一步时使用哪种规则；尚未表示梯度已经计算。

## 三、今日目标

每次只推进一个小步骤：

1. 调用一次 `loss.backward()`，观察 `weight` 与 `weight.grad`。
2. 理解 backward 计算梯度，但不修改参数。
3. 手工更新一次 weight，验证 loss 下降。
4. 观察梯度累加与清零。

完成一项并解决疑问后，再进入下一项。

## 四、学习记录

### 实践 1：第一次 loss.backward()

学习者实际观察：

```text
backward 前 weight：3
backward 前 weight.grad：None

执行 loss.backward()

backward 后 weight：3
backward 后 weight.grad：-16
```

已经亲自验证：

1. 前向计算和 `grad_fn` 只建立可追踪的计算关系，尚未产生 `weight.grad`。
2. `loss.backward()` 沿计算关系得到梯度 `-16`。
3. `loss.backward()` 没有修改参数本身，weight 仍为 3。

下一步讲解反向链条 `1 -> -8 -> -8 -> -16`，暂不更新参数。

### 反向链条理解确认

反向传播路径：

```text
loss 对自身的梯度：1
经过 PowBackward：1 * (2 * error) = -8
经过 SubBackward：-8 * 1 = -8
经过 MulBackward：-8 * x = -16
```

学习者确认已经理解：`backward()` 从 loss 反向经过每一步，将局部变化率连续相乘，最终把 `-16` 存入 `weight.grad`；它本身不修改 `weight`。

下一步手工更新一次参数并重新计算 loss，暂不引入优化器和梯度清零。

### 实践 2：手工更新参数

使用：

```text
learning_rate = 0.1
weight = weight - learning_rate * weight.grad
```

并在 `torch.no_grad()` 中更新，学习者实际观察：

```text
更新后的 weight：4.6
原来的 weight.grad：-16
新的 prediction：9.2
新的 error：-0.8
新的 loss：0.64
```

对比：

```text
更新前：weight=3，prediction=6，loss=16
更新后：weight=4.6，prediction=9.2，loss=0.64
```

已经验证：

1. `backward()` 得到的负梯度使参数更新方向为增大。
2. 更新后预测更接近 target=10，loss 明显下降。
3. 参数更新不会自动清空旧的 `weight.grad`。
4. 更新后的新一轮 forward 再次产生 `Mul/Sub/PowBackward`，说明离开 `no_grad()` 后自动求导记录已恢复。

下一步只解释参数更新为什么使用 `torch.no_grad()`，暂不处理梯度清零。

### `torch.no_grad()` 理解确认

学习者确认没有疑问，理解：参数更新是训练控制动作而非模型 forward 的一部分；`no_grad()` 临时停止记录更新操作，但不会关闭参数之后的求导能力，也不会清空旧梯度。新一轮 forward 已再次产生 `grad_fn`。

下一步只观察 PyTorch 的梯度累加行为，暂不立即清零。

## 五、问题与知识欠账

- Attention 与 Attention Head：Backward 主题完成后再进入。
- MiniMindBlock、RMSNorm、残差连接：等待前置知识。

## 六、今日总结

Day 004 完成了从计算图到一次参数更新的最小闭环。

### 学习者亲自完成

```text
[完成] 观察 weight、prediction、error、loss 的 grad_fn
[完成] 区分叶子参数与运算产生的中间张量
[完成] 第一次调用 loss.backward()
[完成] 验证 backward 前 weight.grad=None
[完成] 验证 backward 后 weight.grad=-16
[完成] 验证 backward 不修改 weight 本身
[完成] 理解反向链条 1 -> -8 -> -8 -> -16
[完成] 在 torch.no_grad() 中手工更新 weight
[完成] 验证 weight：3 -> 4.6
[完成] 验证 prediction：6 -> 9.2
[完成] 验证 loss：16 -> 0.64
[完成] 理解 no_grad 只暂停记录参数更新，不永久关闭求导
```

### 当前能够区分

```text
forward：使用当前参数得到 prediction
loss：衡量 prediction 与 target 的差距
grad_fn：记录张量由什么可求导运算产生
backward：沿计算关系计算梯度
weight.grad：存放 loss 对 weight 的梯度
no_grad：参数更新时临时停止建立计算图
参数更新：真正改变 weight，与 backward 是两件事
```

### 尚未完成

```text
[待实践] 第二次 backward 后梯度是否累加
[待理解] PyTorch 为什么默认累加梯度
[待实践] weight.grad 清零
[待理解] optimizer.zero_grad 与 optimizer.step
[待学习] Attention 与 Attention Head
```

未完成内容不计入 Day 004 掌握范围。

## 七、下一学习日入口

下一次从梯度累加实验恢复。由于 Python REPL 可能已经关闭，不依赖旧进程状态，应重新创建：

```python
import torch

x = torch.tensor(2.0)
target = torch.tensor(10.0)
weight = torch.tensor(3.0, requires_grad=True)

prediction = x * weight
loss = (prediction - target) ** 2
loss.backward()

with torch.no_grad():
    weight -= 0.1 * weight.grad

new_prediction = x * weight
new_loss = (new_prediction - target) ** 2
```

此时应恢复到：

```text
weight=4.6
weight.grad=-16
new_loss=0.64
```

然后只执行一次：

```python
new_loss.backward()
```

观察旧梯度 `-16` 是否与新梯度 `-3.2` 累加为约 `-19.2`。
