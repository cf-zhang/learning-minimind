# Day 021：LoRA 权重加载、合并与推理验收

- 日期：2026-09-03
- 学习阶段：`minimind` LoRA
- 当前状态：已完成（概念与源码主线验收）
- 入口：`minimind/eval_llm.py`、`minimind/scripts/serve_openai_api.py`、`minimind/model/model_lora.py`

## 一、两条加载路径

```text
基础模型 + LoRA 适配器
-> apply_lora
-> load_lora
-> 推理输出

基础模型 + LoRA 适配器
-> merge_lora
-> 普通模型权重
-> 推理输出
```

运行时适配器路径：

```text
基础模型 -> apply_lora -> load_lora -> W x + B(A(x))
```

合并权重路径：

```text
基础模型 + LoRA -> merge_lora -> W_merged=W+B@A -> 普通 forward
```

两条路径在数学上等价；实际保存为 float16 时允许出现很小的舍入误差。两条路径要使用相同的基础模型、LoRA 文件、目标层和 tokenizer，并在 `eval()` 模式比较。

## 二、真实加载入口

`eval_llm.py` 和 `serve_openai_api.py` 都遵循：

```text
创建基础模型 -> 加载基础权重 -> apply_lora -> load_lora -> 推理
```

LoRA 文件是适配器，不包含 Embedding、Attention 原始权重、FFN 和 `lm_head`。服务端通常在启动时加载一次，后续请求复用已经组合好的模型。

## 三、LoRA 阶段完成边界

学习者确认 LoRA 后续内容全部掌握。独立的“基础模型 + LoRA 与合并模型逐输出比较”实验未单独运行，因此本记录不伪造具体输出；此前 `merge_lora()` 已通过命名子模块实验验证权重文件不含 `.lora.`，输出差异来自 float16 舍入。

```text
[验收] 运行时 LoRA 与合并模型的数学等价关系
[验收] 两种模型加载形式和适用场景
[验收] eval_llm.py / serve_openai_api.py 的加载顺序
[验收] 适配器文件与完整训练 checkpoint 的区别
[验收] rank、目标层选择和 LoRA 的能力边界
```

## 四、下一阶段

下一阶段进入蒸馏，入口为 `minimind/trainer/train_distillation.py`：

```text
teacher -> 输出分布
student -> 学习 teacher 的分布
ground-truth CE + distillation KL -> 总损失
```
