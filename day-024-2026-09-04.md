# Day 024：DPO 的 chosen/rejected 数据与偏好目标

- 日期：2026-09-04
- 学习阶段：`minimind` DPO（Direct Preference Optimization）
- 当前状态：已完成（概念、数据处理和 `train_epoch()` 主线验收）
- 入口：`minimind/dataset/lm_dataset.py::DPODataset`、`minimind/trainer/train_dpo.py`

## 一、今天回答的总问题

DPO 是什么、为什么需要它，以及一条偏好数据如何在真实源码中变成 policy 的
训练信号。核心结论是：DPO 属于大语言模型后训练中的离线偏好优化方法。它
使用同一个 prompt 下的一对完整回答，不训练显式 reward model，也不进行 PPO
式在线 rollout，而是直接让 policy 相对于固定 reference 更偏向数据中标注的
`chosen` 回答、远离 `rejected` 回答。

```text
preference data: prompt + chosen + rejected
    -> chat template 生成两条完整对话序列
    -> 只保留 assistant 回答 token 的 loss mask
    -> policy/reference 分别计算回答 log probability
    -> 比较 chosen 与 rejected 的相对概率
    -> 用 reference 作锚点计算 DPO loss
    -> 更新 policy（MoE 时再加 aux_loss）
```

## 二、概念定位：DPO 到底是什么

### 1. 名称和范畴 `[验收]`

`DPO` 是 `Direct Preference Optimization`，中文是“直接偏好优化”。它属于：

```text
大语言模型后训练
    -> 人类/模型偏好对齐
        -> 离线 preference optimization
```

用户已确认输入是同一个 prompt 下的 `chosen` 和 `rejected`。两者不是“再开启
一次新的 assistant 生成”的片段，而是已经包含完整回答、准备作为训练序列的
字符串/消息列表；prompt 部分相同，回答部分是偏好数据提供的两种候选。

### 2. 与强化学习的边界 `[验收]`

DPO 不是 PPO 式传统在线强化学习：训练时不需要 rollout、显式 reward model、
value model、advantage 或环境交互。它的理论出发点仍是带 KL 约束的 RLHF 目标，
所以可以说它与 RLHF/偏好对齐相关，但“直接”体现在把 reward 优化改写成一个
只依赖偏好对和 reference 的监督式分类损失。

### 3. 为什么需要成对回答 `[验收]`

单独知道一条回答的 token 概率，无法判断模型是否更偏爱另一条回答。DPO 只关心
同一 prompt 下的相对偏好，因此 chosen 和 rejected 必须成对，并且要排除 prompt
长度、padding 和回答长度差异带来的无关影响：mask 只让 assistant 回答部分参与
log probability 汇总。

## 三、`DPODataset.__getitem__()`：从样本到六个张量

真实代码位于 `minimind/dataset/lm_dataset.py:122-192`。

### 1. 读取一对消息列表 `[讲解][验收]`

```python
sample = self.samples[index]
chosen = sample['chosen']
rejected = sample['rejected']
```

每个字段都是若干 `{role, content}` 消息。两条消息列表共享同一个对话前缀，
最后的 assistant 回答分别是偏好的和不偏好的版本。

### 2. chat template 和后处理 `[讲解][验收]`

```python
chosen_prompt = tokenizer.apply_chat_template(
    chosen, tokenize=False, add_generation_prompt=False
)
chosen_prompt = post_processing_chat(chosen_prompt)
```

`add_generation_prompt=False` 很关键：这里不是准备“让模型继续生成”的新开头，
而是把已有的完整 chosen/rejected 对话格式化成训练字符串。rejected 路径执行
同样的操作，保证两条序列使用相同模板。

### 3. token 化、padding 和 next-token 对齐 `[讲解][验收]`

```python
chosen_encoding = tokenizer(
    chosen_prompt, truncation=True, max_length=self.max_length,
    padding='max_length'
)
chosen_input_ids = chosen_encoding['input_ids']
chosen_loss_mask = self.generate_loss_mask(chosen_input_ids)

x_chosen = torch.tensor(chosen_input_ids[:-1])
y_chosen = torch.tensor(chosen_input_ids[1:])
mask_chosen = torch.tensor(chosen_loss_mask[1:])
```

`x[t]` 预测 `y[t]`，所以输入去掉最后一个 token、标签去掉第一个 token；mask
也必须同步右移。rejected 路径得到 `x_rejected/y_rejected/mask_rejected`，最终
返回六个形状相同的张量。

### 4. loss mask 的目的 `[验收]`

`generate_loss_mask()` 找到每个 `BOS + assistant` 到对应 `EOS` 的区间，将回答
token 标为 1，其余 prompt、角色标记和 padding 标为 0。这样 DPO 比较的是“回答
在模型中的总 log probability”，而不是 prompt 长度或 padding 数量。

## 四、`train_epoch()`：按源码执行顺序

真实代码位于 `minimind/trainer/train_dpo.py:53-128`。

### 1. 拼接 chosen/rejected batch `[实践][验收]`

```python
x = torch.cat([x_chosen, x_rejected], dim=0)
y = torch.cat([y_chosen, y_rejected], dim=0)
mask = torch.cat([mask_chosen, mask_rejected], dim=0)
```

例如各自是 `[3, 10]`，拼接后就是 `[6, 10]`；前半行是 chosen，后半行是
rejected，且第 `i` 个 chosen 与第 `i` 个 rejected 配对。用户实际确认了这一
排列关系。

### 2. reference forward `[实践][验收]`

```python
with torch.no_grad():
    ref_outputs = ref_model(x)
    ref_logits = ref_outputs.logits
ref_log_probs = logits_to_log_probs(ref_logits, y)
```

reference 是冻结的参考模型，只提供“训练前/基准行为”，不建立反向图，也不接收
optimizer 更新。对 `[6, 10]` 输入，logits 是 `[6, 10, vocab]`，取目标 token 后
得到 `[6, 10]`。

### 3. policy forward `[实践][验收]`

```python
outputs = model(x)
logits = outputs.logits
policy_log_probs = logits_to_log_probs(logits, y)
```

policy 是正在训练的模型，所以 logits 带计算图；用户实际检查过 policy 的
`requires_grad=True`、reference 的 `requires_grad=False`，且两边形状一致。

### 4. `logits_to_log_probs()` `[实践][验收]`

```python
log_probs = F.log_softmax(logits, dim=2)
log_probs_per_token = torch.gather(
    log_probs, dim=2, index=labels.unsqueeze(2)
).squeeze(-1)
```

这不是返回完整词表分布，而是在每个时间位置只取 `y` 指定目标 token 的 log
probability：`[batch, seq_len, vocab] -> [batch, seq_len]`。

### 5. mask 后汇总并拆开两类样本 `[实践][验收]`

```python
ref_log_probs = (ref_log_probs * mask).sum(dim=1)
policy_log_probs = (policy_log_probs * mask).sum(dim=1)

batch_size = ref_log_probs.shape[0]
chosen_ref_log_probs = ref_log_probs[:batch_size // 2]
reject_ref_log_probs = ref_log_probs[batch_size // 2:]
chosen_policy_log_probs = policy_log_probs[:batch_size // 2]
reject_policy_log_probs = policy_log_probs[batch_size // 2:]
```

token-level 分数经过 mask 和序列求和后，每条回答变成一个 scalar；例如 8 条序列
会拆成 4 个 chosen 与 4 个 rejected，并按下标配对。

## 五、三个减法为什么表达偏好

### 1. policy 的相对偏好

```python
pi_logratios = chosen_policy_log_probs - reject_policy_log_probs
```

使用 `log a - log b = log(a / b)`，它等价于：

```text
log [ policy(chosen | prompt) / policy(rejected | prompt) ]
```

因此大于 0 表示 policy 更偏向 chosen，等于 0 表示二者相对无偏好，小于 0
表示 policy 更偏向 rejected。注意 log probability 越接近 0，原概率越大；例如
`-4 > -6` 代表 `exp(-4) > exp(-6)`。

### 2. reference 的原始相对偏好

```python
ref_logratios = chosen_ref_log_probs - reject_ref_log_probs
```

这表示 reference 在训练前已经多大程度偏向 chosen，而不是重新判断数据标签。
`chosen/rejected` 的“好/差”身份由偏好数据预先标注，减法只是把这份标签转成
可计算的相对分数。

### 3. 相对 reference 的偏好提升

```python
logits = pi_logratios - ref_logratios
```

展开后是：

```text
logits = log(policy(chosen)/reference(chosen))
       - log(policy(rejected)/reference(rejected))
```

也就是 policy 相比 reference 是否“额外增强了对 chosen 的偏好”。reference
既是锚点，也是抑制 policy 无限制偏离原模型的 KL 约束来源。三个减法并不是凭空
发现客观好坏，而是把“标注的 chosen 应该胜过 rejected”转成相对概率目标。

## 六、DPO loss 与训练更新

```python
loss = -F.logsigmoid(beta * logits)
return loss.mean()
```

DPO 隐含 Bradley-Terry 偏好模型：

```text
P(chosen 胜过 rejected) = sigmoid(reward(chosen) - reward(rejected))
reward(y) ≈ beta * log(policy(y|x) / reference(y|x))
```

当 policy 相对 reference 更支持 chosen 时，`logits` 变大，`logsigmoid` 变大，
负号后的 loss 变小；反之 loss 变大。`beta` 控制相对 reference 的偏离尺度。

`train_epoch()` 后半段沿已掌握的训练循环执行：

```python
loss = dpo_loss_val + outputs.aux_loss
loss = loss / args.accumulation_steps
scaler.scale(loss).backward()
scaler.step(optimizer)
```

DPO 脚本没有蒸馏中的 CE 项；policy 主要优化 preference loss，使用 MoE 时额外
加入 `outputs.aux_loss`。梯度累积、梯度裁剪、混合精度、保存 checkpoint 和
残余 batch 的收尾与前面 SFT 训练循环相同，用户确认无需再次展开。

## 七、学习者验收与真实性边界

本日实际完成并确认：

```text
[验收] DPO 的全称、定位、输入和与 PPO/RLHF 的关系
[验收] chosen/rejected 是完整字符串/消息序列，prompt 相同
[验收] chat template、padding、BOS/EOS 与 assistant loss mask
[验收] x/y 的 next-token 对齐和 chosen/rejected batch 拼接
[验收] reference/policy forward、梯度属性和 log probability 提取
[验收] mask 汇总、配对拆分、三个 log-ratio 减法的含义
[验收] beta、logsigmoid、preference loss 与 optimizer 更新路径
```

没有把未运行的完整 DPO 训练曲线写成实际结果；本日的实践证据是学习者提交
的张量形状、配对关系、log probability 计算和公式验收。

## 八、Day 025 计划与恢复点

DPO 已结束，下一阶段进入在线/组相对偏好优化，先学习 GRPO，再根据源码需要
补充 CISPO 与 PPO。Day 025 从真实 rollout 入口开始，不重复 DPO 已掌握的
log probability 细节：

```text
minimind/trainer/train_grpo.py
-> rollout_engine.py 如何为一个 prompt 生成多条回答
-> reward 如何给每条回答打分
-> 同组回答如何计算 group-relative advantage
-> policy/reference 的 ratio 与 KL 约束
-> GRPO loss 如何更新模型
```

下一次从 `train_grpo.py` 的训练入口和 `rollout_engine.py` 的调用关系开始，先
建立“同一个 prompt 生成多条回答并组内比较”的目标，再逐段进入真实源码。
