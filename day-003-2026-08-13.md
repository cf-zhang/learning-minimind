# Day 003：Backward、梯度与参数更新

- 日期：2026-08-13
- 学习阶段：神经网络训练的最小前置知识
- 今日主题：计算图、loss、梯度、`backward()` 与参数更新
- 当前状态：已完成当天范围（Backward 主题继续到 Day 004-005）

## 零、如何使用本课

这一天没有直接调用 `loss.backward()`。它只解决 backward 之前必须理解的三个问题：什么是 loss、参数怎样影响 loss、梯度 `-16` 从哪里来。

新人应当亲自完成每一步算术，不要把导数公式当作需要背诵的符号。这里使用单参数模型，是为了让训练过程中的每个数字都能人工核对。

## 逐轮互动档案

### 第 1 轮：教学再次从整章输出改回单步交互

开始本课时，助手一次性输出了 prediction、target、loss、计算图、梯度、backward、学习率和更新步骤。学习者指出：

> 你又一次性的输出完了，我们要交互式的学习过程，你负责输出知识和掌控过程进度，我负责理解吸收。

因此整段内容不计入掌握，重新拆成关卡。这个纠偏比知识结论本身重要：新人课程不能用“已经把答案都展示过”代替逐步理解。

状态：`[纠偏]` 从此每次只处理一个问题并等待反馈。

### 第 2 轮：为什么不能直接使用 `error=-4`

最小例子是：

```text
x = 2
weight = 3
prediction = x * weight = 6
target = 10
error = prediction - target = -4
```

学习者问：

> 为什么不直接使用 -4？

这里需要先明确训练目标不是让一个带符号的数字“越小越好”，而是让预测与目标的距离越小越好。

如果直接最小化 `error`：

```text
prediction=6  -> error=-4
prediction=0  -> error=-10
prediction=-100 -> error=-110
```

数值越来越小，预测却离 10 越来越远。多个样本的正负 error 还可能抵消。因此本例使用：

```text
loss = error ** 2
```

预测 6 和 14 都离目标 10 相差 4，平方后 loss 都是 16。

学习者回复“理解了”。

状态：`[验收]` 已区分带方向的 error 与表示错误程度的非负 loss。

### 第 3 轮：参数与 loss 的关系

固定 `x=2`、`target=10`，逐个查看不同 weight：

```text
weight=2 -> prediction=4  -> loss=36
weight=3 -> prediction=6  -> loss=16
weight=4 -> prediction=8  -> loss=4
weight=5 -> prediction=10 -> loss=0
weight=6 -> prediction=12 -> loss=4
weight=7 -> prediction=14 -> loss=16
weight=8 -> prediction=16 -> loss=36
```

这不是为了提前知道答案 `weight=5`，而是先可视化一个事实：weight 改变会使 prediction 改变，prediction 改变又使 loss 改变。

学习者确认没有疑问。

状态：`[验收]` 已理解参数是通过 forward 计算间接影响 loss。

### 第 4 轮：梯度先理解成当前位置的坡度

在 `weight=3` 附近做一个很小的试探：

```text
weight：3 -> 3.01
loss：16 -> 15.8404
```

于是：

```text
loss 变化 / weight 变化
= (15.8404 - 16) / (3.01 - 3)
= -0.1596 / 0.01
= -15.96
```

试探步足够小时，这个数接近真实梯度 `-16`。负号表示 weight 向右增大时 loss 在下降，绝对值表示当前位置变化较快。

学习者表示梯度已有一些理解，但要求把 `-16` 的计算来源解释得更清楚，再进入下一挑战。这个请求没有被跳过。

### 第 5 轮：把 `-16` 拆成两段局部变化率

当前计算链是：

```text
weight -> prediction -> error -> loss
```

由于 `prediction = 2 * weight`，而 `error = prediction - 10`，可简写为：

```text
error = 2 * weight - 10
loss = error ** 2
```

在 `weight=3` 时，`error=-4`。

第一段：error 变化一点，loss 会变化多快：

```text
d(loss)/d(error) = 2 * error = 2 * (-4) = -8
```

第二段：weight 变化一点，error 会变化多快：

```text
d(error)/d(weight) = 2
```

把两段相乘：

```text
d(loss)/d(weight) = -8 * 2 = -16
```

这里相乘的意义是：weight 的变化先放大 2 倍传给 error，error 的变化再按 `-8` 倍影响 loss。

### 第 6 轮：学习者用自己的话回答

助手用三个已经讲过的问题验收，学习者回答：

> 1. 计算试探点的值可以知道下一步应该向左走还是向右走。  
> 2. 分别来自 loss 对 error，error 对参数。  
> 3. 因为 3 在最佳参数的左侧，7 在右侧。

补充校准：真实训练通常不知道最佳参数在哪里；“左侧/右侧”只用于这个可画出来的一维例子。模型实际依靠当前位置梯度的符号和大小决定下降方向。

状态：`[实践]`、`[验收]` 能解释数值试探和分段相乘如何得到 `-16`。

### 第 7 轮：把下一步留给学习者操作

当日没有继续灌输 `grad_fn` 和 backward。下一任务只要求学习者在 PyTorch 中打印：

```text
weight、prediction、error、loss
它们各自的 grad_fn
```

Day 003 准确停在“数学上理解梯度来源，尚未观察 PyTorch 如何记录计算关系”。实际输出在下一个学习日提交并进入 Day 004。

## 一、承接 Day 002

昨天已经亲自验证一个 Linear 层执行：

```text
y = x @ weight.T + bias
```

并观察到输出包含：

```text
grad_fn=<AddmmBackward0>
```

由此留下问题：

> “PyTorch 为这次运算记录了反向计算关系，将来调用 `loss.backward()` 时，可以计算 weight 和 bias 应该如何调整。”这句话具体怎么理解？

今天先完整解决这个问题。

## 二、今日目标

使用只有一个参数的最小模型，亲自观察：

```text
输入 x
  -> 当前参数 weight
  -> 预测 prediction
  -> 与目标 target 比较
  -> loss
  -> loss.backward()
  -> weight.grad
  -> 手工更新 weight
  -> 再次预测
  -> loss 是否下降
```

需要建立的区别：

```text
forward：根据当前参数计算预测
loss：衡量预测与目标的差距
backward：计算 loss 对各参数的梯度
参数更新：根据梯度真正修改参数
```

## 三、今日范围

今天首先学习：

- 什么是目标值与预测值。
- 什么是 loss。
- 什么是计算图。
- 什么是梯度的方向与大小。
- `loss.backward()` 做什么。
- 为什么 `backward()` 不直接修改参数。
- 为什么更新前通常要清空旧梯度。

如果上述内容理解并实践完成，再根据精力进入昨天的第二个问题：Attention 与 Head 是什么。

暂不学习：

- 多元微积分的系统推导。
- 复杂链式法则证明。
- AdamW 的完整算法。
- 完整 Attention 公式。
- Q/K/V、GQA、RoPE 与 KV Cache。

## 四、验收标准与本日边界

以下清单按 Day 003 结束时的真实状态记录。未勾选项不是遗忘，而是明确移交给 Day 004-005。

只有亲自实践并理解后才标记完成：

- [x] 能区分 prediction、target、error 和 loss。
- [ ] 能解释 `grad_fn` 表示 PyTorch 记录了什么。（Day 004 完成）
- [x] 能解释当前梯度 `-16` 的正负号、大小和计算来源。
- [ ] 能说明 `loss.backward()` 为什么没有直接改变 weight。
- [ ] 能手工用梯度更新一次参数。
- [ ] 能验证沿正确方向更新后 loss 下降。
- [ ] 能理解梯度默认累加，以及为什么更新前要清零。

## 五、互动方式

```text
先用具体数字讲解
  -> 学习者亲自执行最小实验
  -> 根据实际输出解释
  -> 学习者提出疑问
  -> 再决定是否进入下一概念
```

不要求猜测尚未讲过的微积分术语。

## 六、学习记录

### 教学纠偏：控制单次知识量

Day 003 开始时，助手再次一次性讲完 prediction、target、loss、计算图、梯度、`backward()`、学习率和参数更新，并给出完整实验。虽然知识依赖顺序基本正确，但不符合约定的交互式学习方式。

学习者要求：助手负责拆分知识和控制进度，学习者逐步理解吸收；不能先输出整章内容再要求学习者执行。

因此刚才的完整讲解只视为预备材料，不视为学习者已学习。Day 003 重新拆分为以下关卡，每次只推进一项：

```text
关卡 1：prediction、target、error、loss
关卡 2：参数变化如何影响 loss
关卡 3：梯度只表示当前位置的方向和坡度
关卡 4：PyTorch 为什么记录计算图与 grad_fn
关卡 5：loss.backward() 如何得到 weight.grad
关卡 6：backward 不修改参数，手工更新参数
关卡 7：梯度累加与清零
```

只有当前关卡经过讲解、学习者提问或确认、必要实践后，才进入下一关卡。

### 关卡 1：prediction、target、error 与 loss

使用最小例子：

```text
x = 2
weight = 3
prediction = x * weight = 6
target = 10
error = prediction - target = -4
loss = error^2 = 16
```

学习者提出：为什么不能直接使用 `error=-4`？经过讲解后理解：

1. 如果直接最小化带符号的 `prediction-target`，模型可能通过让预测越来越小、误差越来越负来获得更小数值，却离目标越来越远。
2. 位于目标两侧但距离相同的预测，例如 6 和 14，相对目标 10 的错误程度应该相同。
3. 多个样本的正负 error 可能相互抵消，掩盖实际错误。
4. 平方误差和绝对误差都能消除正负抵消；平方还会更强地惩罚大误差。

关卡 1 状态：已理解。进入关卡 2，观察参数变化如何影响预测与 loss。

### 关卡 2：参数变化如何影响 loss

固定：

```text
x = 2
target = 10
prediction = x * weight
loss = (prediction - target)^2
```

观察不同参数：

```text
weight=2 -> loss=36
weight=3 -> loss=16
weight=4 -> loss=4
weight=5 -> loss=0
weight=6 -> loss=4
weight=7 -> loss=16
weight=8 -> loss=36
```

学习者确认没有疑问，理解：参数变化会改变 prediction，进而改变 loss；在 `weight=3` 附近向右移动会让 loss 下降，向左移动会让 loss 上升。

关卡 2 状态：已理解。进入关卡 3，给当前位置的变化方向和快慢引入“梯度”。

学习者表示梯度已有初步理解，但需要更清晰地知道 `-16` 是如何计算出来的；在此问题完成前不进入下一关卡。

### 关卡 3：梯度如何得到

使用两种方式解释 `weight=3` 时梯度为 `-16`。

数值试探：

```text
weight：3 -> 3.01，变化 0.01
loss：16 -> 15.8404，变化 -0.1596
近似坡度：-0.1596 / 0.01 = -15.96，接近 -16
```

分步变化率：

```text
weight -> error -> loss
d(loss)/d(error) = 2 * error = -8
d(error)/d(weight) = x = 2
d(loss)/d(weight) = (-8) * 2 = -16
```

学习者用自己的话回答：

1. 计算试探点可以判断下一步应向左还是向右；补充确认差商还能近似坡度大小。
2. `(-8) * 2` 分别来自 loss 对 error、error 对参数的变化率。
3. `weight=3` 在最低点左侧所以梯度为负，`weight=7` 在右侧所以梯度为正。

需要保留的精确表述：真实训练通常不知道最佳参数位于哪里，而是根据当前位置的梯度符号选择下降方向。

关卡 3 状态：已理解。下一步只观察 PyTorch 如何用 `grad_fn` 记录 `weight -> prediction -> error -> loss` 的计算路径，暂不调用 `backward()`。

## 七、问题与知识欠账

- Attention 与 Attention Head：完成 Backward 主题后再决定是否进入。
- MiniMindBlock、RMSNorm、残差连接：继续等待前置知识。
- KV Cache：等待 Q/K/V、因果 Mask 和 GQA。

## 八、今日总结

Day 003 没有以调用 `backward()` 为完成标准，而是先建立了后续自动求导必须依赖的理解：

```text
prediction 是模型按当前参数算出的结果
target 是训练数据提供的目标
error 保留预测位于目标哪一侧
loss 把错误变成可比较、可优化的非负程度
参数变化 -> prediction 变化 -> error 变化 -> loss 变化
梯度描述当前位置 loss 随参数变化的方向和速度
```

本日用数值试探得到约 `-15.96`，又用两段变化率相乘得到精确的 `-16`。学习者能够说明两段分别来自 loss 对 error、error 对 weight 的变化率，并理解真实训练依靠当前位置梯度，而不是预先知道最佳参数。

准确停止点：尚未调用 `loss.backward()`，只布置了 `grad_fn` 观察实验。计算图、backward、手工更新和梯度清零分别在 Day 004、Day 005 完成。

## 九、下一学习日入口

学习者将在下一学习日提交 `weight -> prediction -> error -> loss` 的数值和 `grad_fn` 输出。随后继续：

```text
loss.backward()
  -> weight.grad
  -> backward 不修改 weight
  -> 手工更新参数
  -> 新 loss 是否下降
```
