# Day 031：第二阶段 minimind-v 收尾

- 日期：2026-09-12
- 学习阶段：第二阶段 `minimind-v` 收尾
- 当前状态：第二阶段完成，准备进入第三阶段 `minimind-o`

## 一、第二阶段完成情况

路线图第二阶段三部分全部覆盖：

```text
1. 运行和调用链    Day 028  eval_vlm.py 从图片到文本回答走完
2. 模型与数据      Day 028  model_vlm.py（VLMConfig/MiniMindVLM/vision_proj/count_vision_proj）
                   Day 029  VLMDataset（真实探查 parquet + __getitem__ 输出）
3. 视觉训练        Day 029  train_pretrain_vlm（预训练，freeze_llm=2）
                   Day 030  train_sft_vlm（SFT，freeze_llm=1）+ 两阶段分工
```

freeze_llm=0/1/2 的原理与动机已讲清（未做实测小实验，按学习者决定跳过）。

## 二、第二阶段核心知识地图

```text
结构：图像 → SigLIP2(冻结,像素→语义) → vision_proj(可训,视觉语义→文本语义)
      → 64 个 visual token 覆盖 64 个 <|image_pad|> 坑 → LLM 8层 → 文本回答
      核心算法改动 < 50 行，语言主干完全复用第一阶段

数据：parquet 两列(conversations JSON + image_bytes JPEG)，<image> 运行时展开 64 坑
      labels 只对 assistant 回答算 loss，图像坑与提问全 -100

训练：两阶段
  预训练  from llm，freeze_llm=2 只训随机 proj，大 lr 4e-4，长描述 → 建立对齐
  SFT     from pretrain_vlm，freeze_llm=1 解冻 LLM 首末层，小 lr 5e-6，指令问答 → 学指令跟随
  信号链  loss(助手文字) → attention → visual token → vision_proj，冻结部分不更新

要点：冻结双保险(requires_grad 管梯度 + filter 管更新)；保存时过滤 SigLIP；
      为何必须先预训练再 SFT(避免随机 proj 乱码带偏解冻的 LLM)
```

## 三、跨阶段能力衔接

```text
第一阶段 minimind   文本如何变 token、训练与生成、Attention/SFT/推理链
第二阶段 minimind-v 图像如何变 visual token 进入 LLM、跨模态对齐、两阶段视觉训练
第三阶段 minimind-o 在此之上再加语音输入与语音生成（Thinker/Talker）
```

## 四、第三阶段 minimind-o 起点

已确认入口存在：`minimind-o/eval_omni.py`、`minimind-o/model/model_omni.py`。

第一小步（下一天）：先整体认识 minimind-o 仓库（能力 + 结构 + 目录地图），
方式与 Day 028 认识 minimind-v 一致——先建地图，再从 eval_omni.py 推理调用链切入。

先重点区分：

```text
Thinker：理解文本/图像/语音，产生文本隐藏状态与文本 logits（类似已学的 VLM 主干 + 音频输入）
Talker： 根据 Thinker 的信息和音频 code，生成音频 code（新增的语音生成侧）
```

预期难点：9 路输入、8 层 audio codebook、文字词表与音频词表分离、多路 loss、
speaker embedding、scheduled sampling、mode=all/audio_proj/vision_proj 的冻结策略。
按老规矩逐个小代码块展开，先地图后细节。
