# Day 033：从 `eval_omni.py` 重新开始

- 日期：2026-09-14
- 学习阶段：第三阶段 `minimind-o`
- 当前状态：已完成
- 本日主题：`eval_omni.py` 的程序入口和外层调度
- 关联计划：[第三阶段学习计划](stage-03-minimind-o-plan.md)

## 一、本日定位

本阶段按“未掌握”重新启动。之前接触过 `minimind-o` 的整体结构和
`eval_omni.py`，但不作为本次掌握依据。本日从真实源码的执行入口开始，
暂时不进入 `MiniMindOmni.forward()`、Thinker 或 Talker 内部。

## 二、源码入口

实际文件是：

```text
minimind-o/eval_omni.py
```

文件底部：

```python
if __name__ == "__main__":
    main()
```

直接执行：

```text
python eval_omni.py
```

时，`__name__` 等于 `"__main__"`，条件成立，程序调用 `main()`。

如果其他文件使用：

```python
import eval_omni
```

此时 `__name__` 不是 `"__main__"`，`main()` 不会因为导入而自动执行。
这样可以复用文件中的函数，而不会在导入时立即开始评估。

## 三、`main()` 进入具体模式前的顺序

根据 `eval_omni.py:90-116`：

```text
main()
  -> 创建 ArgumentParser
  -> 注册命令行参数
  -> parser.parse_args() 读取参数
  -> 根据 args.mode 生成 modes
  -> os.makedirs(args.output_dir, exist_ok=True)
  -> init_model(args)
  -> setup_seed(...)
  -> 进入 mode=0、mode=1、mode=2 等分支
```

学习者复述为：

```text
加载参数，创建输出目录，初始化模型，设置随机种子
```

经核对，复述的步骤和顺序正确；补充了“先注册参数、再解析参数，以及
解析 `modes`”这两个源码细节。

## 四、当前掌握边界

```text
[实践] 能定位 if __name__ == "__main__": main() 的直接运行入口
[验收] 能解释直接运行和 import eval_omni 时的区别
[实践] 阅读 main() 前半段
[验收] 能复述进入 mode=0 前的主要执行顺序
[未展开] init_model() 的模型加载分支
```

## 五、环境备注

当前系统 Python 直接运行脚本时缺少 `torch`；已有虚拟环境虽然有
`torch`，但尚未完整安装 `minimind-o/requirements.txt` 中的依赖。
因此本日先通过源码阅读完成入口学习，不把助手代跑失败当作学习者实验结果。
完整推理运行将在需要真实模型资源时单独准备环境。

## 六、下一步恢复点

下一步只读 `eval_omni.py::init_model(args)` 的开头：

```python
tokenizer = AutoTokenizer.from_pretrained(args.load_from)
if 'model' in args.load_from:
    ...
else:
    ...
```

要回答的问题是：`load_from='model'` 和 `load_from` 指向 Transformers
模型目录时，程序为什么要走两条不同的加载路径。

## 七、第二个小概念：`load_from` 决定权重路径

学习者复述：

```text
load_from = model；走 if；从 ./out/sft_omni_768.pth 加载权重；
load_from = minimind-3o 时走 else。
```

复述正确。默认参数中 `save_dir=out`、`weight=sft_omni`、
`hidden_size=768`，因此权重路径拼成 `./out/sft_omni_768.pth`。

```text
[实践] 代入默认参数分析 if 'model' in args.load_from
[验收] 能区分原生 PyTorch 权重路径和 Transformers 模型目录路径
```

## 八、当前恢复点

下一步读 `init_model()` 的加载后部分：

```python
log_model_params(model)
model.audio_encoder.to(args.device)
model.vision_encoder.to(args.device)
model.mimi_model = MimiModel.from_pretrained("./model/mimi").eval()
return model.half().eval().to(args.device), tokenizer
```

只关注一个问题：主模型权重加载完后，为什么还要单独准备
audio encoder、vision encoder 和 Mimi？

## 九、组件职责纠偏与验收

学习者重新复述为：

```text
主模型：Thinker、Talker、AudioProjector、VisionProjector
audio_encoder：编码声音
vision_encoder：编码图像为视觉特征
mimi_model：把输出转换成语音
```

其中需要保留的精确定义是：

```text
audio_encoder 的输出是连续的音频特征，不是离散 audio code。
vision_encoder 的输出是连续的视觉特征。
Mimi 在 audio code 和波形之间转换；本文件的推理路径主要使用 decode。
```

```text
[验收] 能列出主模型中的 Thinker、Talker 和两个 projector
[验收] 能区分 audio_encoder 的连续特征与 Mimi 的离散 audio code
[验收] 能说明 vision_encoder 和 mimi_model 的职责
```

## 十、下一步恢复点：展开 `init_model()`

后续严格按以下顺序阅读，不一次讲完：

```text
1. AutoTokenizer.from_pretrained(args.load_from)
2. load_from 分支与原生权重路径
3. MiniMindOmni / AutoModelForCausalLM 的结构创建
4. load_state_dict 与 strict=False
5. 外部 encoder、Mimi、设备和 eval/half
6. 返回值如何被 eval_sample 使用
```

下一次先学习第 1 项：Tokenizer 为什么也使用 `from_pretrained`，以及它和
后面加载的模型权重是什么关系。

## 十一、Tokenizer 与模型权重的关系

学习者复述：

```text
tokenizer.json 和 tokenizer_config.json 属于 tokenizer 相关的编码信息，
用于把用户输入映射成模型可使用的整数编码；sft_omni_768.pth 是推理使用的模型权重数据。
```

复述正确。补充边界：

```text
Tokenizer 文件：保存词表、特殊 token、编码/解码规则和模板配置
.pth 文件：保存 Thinker、Talker、projector 等神经网络参数
```

两者必须使用兼容的 token 编号，否则模型参数和输入数字的含义会错位。

```text
[实践] 区分 model/tokenizer.json、model/tokenizer_config.json 和 out/*.pth
[验收] 能说明 Tokenizer 资源与模型权重不是同一类文件
```

## 十二、当前恢复点

下一步阅读原生权重分支：

```python
ckp = f'./{args.save_dir}/{args.weight}_{args.hidden_size}{moe_suffix}.pth'
model = MiniMindOmni(OmniConfig(...), ...)
model.load_state_dict(torch.load(ckp, map_location=args.device), strict=False)
```

核心问题：为什么要先创建模型结构，再加载 `.pth` 中保存的参数；
`OmniConfig` 在这一步提供什么信息。

## 十三、`strict=False` 的边界

学习者判断：

```text
缺少 bias 时加载后是 0；形状不一致时不能加载成功。
```

第二句正确。第一句需要修正：checkpoint 缺少某个参数时，
`load_state_dict(..., strict=False)` 不会自动把它设为 0，而是保留创建模型
结构时已有的初始化值。这个值可能是随机初始化，也可能因为模型代码显式设置
而为 0，不能由 `strict=False` 本身决定。

最小实验中：

```text
checkpoint 只有 weight
-> missing_keys=['bias']
-> weight 被 checkpoint 覆盖
-> bias 保留原来的值
```

同名参数形状不一致仍然报错：

```text
模型需要 weight.shape = [2, 3]
checkpoint 提供 weight.shape = [4, 3]
-> size mismatch
```

因此：

```text
[验收] 知道 strict=False 可放宽 missing/unexpected keys
[纠偏] 知道 strict=False 不能解决同名参数的 shape mismatch
[纠偏] 知道缺失参数保留初始化值，不默认等于 0
```

## 十四、当前恢复点

下一步继续拆解同一行：

```python
torch.load(ckp, map_location=args.device)
```

先学习 `torch.load` 读出的对象是什么，以及 `map_location` 为什么要在加载
checkpoint 时指定设备；暂时不进入模型内部参数的具体名称。

## 十五、按重点展开 `MiniMindOmni` 初始化

根据 `model/model_omni.py:105-121`，初始化顺序可以压缩为：

```text
MiniMindOmni(config)
  -> super().__init__(config)：先创建 MiniMind 语言模型骨架
  -> thinker 别名指向原来的 model
  -> 复用主模型的文本 lm_head
  -> 创建 Talker
  -> 创建 AudioProjector 和 VisionProjector
  -> 记录音频特殊 token
  -> 加载 SenseVoice 及其 processor
  -> 加载 SigLIP2 及其 processor
```

`super().__init__(config)` 来自 `MiniMindForCausalLM`，先建立：

```text
model = MiniMindModel(config)
lm_head = Linear(hidden_size, vocab_size)
```

其中 `MiniMindModel` 包含 embedding、Transformer layers、最终 norm 和 RoPE
位置缓存。这部分就是已经学过的 MiniMind 文本主干，Omni 没有重新发明它。

随后：

```python
object.__setattr__(self, 'thinker', self.model)
```

把同一个语言主干用更有意义的名字叫作 `thinker`。不是复制一份新模型，
`self.thinker` 和 `self.model` 指向同一套参数。

```python
object.__setattr__(self.model, 'lm_head', self.lm_head)
```

让 Thinker 使用主模型的文本输出头。Talker 不复用这个头，而是在自己的模块
中使用 `TalkerHead` 输出音频词表。

新增模块的职责：

```text
TalkerModule
  -> 4 层 MiniMindBlock
  -> TalkerEmbedding：8 路 audio code -> Talker hidden
  -> embed_proj：Thinker bridge hidden -> Talker hidden
  -> codec_proj：音频 embedding 再加工
  -> TalkerHead：Talker hidden -> 8 路 audio logits
  -> spk_proj：speaker embedding -> Talker hidden

audio_proj
  -> SenseVoice 特征维度 -> MiniMind hidden_size

vision_proj
  -> SigLIP2 特征维度 -> MiniMind hidden_size
```

所以 `MiniMindOmni` 不是把几个互不相关的模型并排放置，而是：

```text
复用 MiniMind 作为 Thinker
+ 加两个输入对齐层
+ 加一个负责音频生成的 Talker
```

## 十六、当前恢复点

下一步只验收 `MiniMindOmni` 的结构分工，不进入 `load_sensevoice()` 和
`load_vision()` 的加载细节。需要回答：

```text
1. `super().__init__(config)` 先创建了哪一部分？
2. `audio_proj` 和 `vision_proj` 为什么都要把特征投影到 hidden_size？
3. Talker 为什么要有自己的 `TalkerHead`，不能直接使用 Thinker 的文本 lm_head？
```

## 十七、`MiniMindOmni` 结构验收

学习者复述：

```text
super 创建 Thinker；两个 projector 要喂给 Thinker；Talker 输出音频相关内容。
```

其中后两项正确：

```text
audio_proj / vision_proj 都输出 hidden_size，才能进入 Thinker 的统一序列。
Talker 预测音频词表，不能直接使用预测文本词表的 lm_head。
```

第一项需要更精确：

```text
super().__init__(config)
  -> 先由父类创建 MiniMindModel 和文本 lm_head
  -> 随后 self.model 被命名为 self.thinker
```

所以 `super()` 创建的是 MiniMind 文本模型骨架；“Thinker”这个架构名称是
后面通过别名赋予它的功能角色，并不是 `super()` 里另建了一个不同模型。

```text
[验收] 理解 Projector 的统一 hidden_size 作用
[验收] 理解 Talker 需要独立的音频输出头
[纠偏] 区分父类先创建 MiniMindModel 与子类随后命名 Thinker
```

## 十八、当前恢复点

`MiniMindOmni.__init__()` 的主结构已经展开完成。下一步可继续看其中新增的
`TalkerModule(config)`，但只先学习它的模块组成和输入/输出形状，不进入
`TalkerModule` 每一层 Transformer 的实现。

## 十九、输入占位符的最小实验

实验使用：

```text
文字：Please answer me:
音频占位符：3 个 <|audio_pad|>
图像占位符：4 个 <|image_pad|>
```

Tokenizer 输出：

```text
audio token id = 16
image token id = 12
input_ids length = 31
```

关键观察：

```text
input_ids 中连续出现 3 个 16
-> 对应 3 个 <|audio_pad|>

input_ids 中连续出现 4 个 12
-> 对应 4 个 <|image_pad|>
```

在这次输出中，音频占位 token 位于序列的第 9-11 个位置，图像占位 token
位于第 14-17 个位置（按从 0 开始的下标）。其余 token 包括：

```text
BOS 等起始 token
普通文字 token
换行和聊天格式 token
<|im_start|>、<|im_end|>
assistant 角色和思考模板 token
```

因此 `apply_chat_template()` 的作用不只是“把消息拼起来”，还会补充角色
边界、assistant 起始位置和当前模型配置中的思考模板。

这次实验使用 3 个音频占位符和 4 个图像占位符，是为了便于观察；真实混合
推理中，代码使用：

```text
音频占位符数量 = valid_len
图像占位符数量 = model.config.image_token_len（默认 64）
```

```text
[实践] 运行 Tokenizer 输入实验
[验收] 观察并确认 audio_pad/image_pad 分别映射为 16/12
[验收] 理解 input_ids 还包含 chat template 自动添加的 token
```

## 二十、当前恢复点

下一步进入 `model.generate()` 之前的输入边界，只确认三类对象的区别：

```text
x：已经 token 化的文字序列，内部包含音频/图像占位 token
audio_inputs：真正的音频 fbank 张量
pixel_values：真正的图像处理张量
```

重点问题：为什么占位 token 在 `x` 里，而真实音频和图像又要作为额外参数
传给 `model.generate()`。

## 二十一、占位位置和真实特征必须同时存在

学习者判断：

```text
没有 audio_inputs 会缺少音频编码信息；
有 audio_inputs 但没有 audio_pad 时，模型不知道如何把音频编码加入推理。
```

这个判断正确，进一步精确为：

```text
没有 audio_inputs：
  x 中的 audio_pad 仍然存在，但它们只能使用普通 token embedding，
  没有真正的连续音频特征可以替换进去。

没有 audio_pad：
  audio_inputs 虽然可以被音频编码器处理，
  但 inject_audio_features 找不到插入位置，音频特征不会进入文本序列。
```

因此多模态输入需要两个配合部分：

```text
占位 token：告诉模型“放在哪里”
外部张量：告诉模型“放什么内容”
```

```text
[验收] 理解 audio_pad 和 audio_inputs 的互补关系
```

## 二十二、当前恢复点

进入 `MiniMindOmni.forward()` 前发现学习者对 `input_ids.shape` 和三维输入
还不熟，因此先补充张量形状基础，不要求直接回答 2D/3D 分支问题。

## 二十三、补充：`input_ids.shape` 是什么

`input_ids` 是一个 PyTorch Tensor，`.shape` 用来查看它有几维、每一维多长。

例如：

```python
x = torch.tensor([[10, 20, 30, 40]])
print(x.shape)
```

输出：

```text
torch.Size([1, 4])
```

含义是：

```text
1：batch，一次有 1 条样本
4：sequence，这条样本有 4 个 token
```

在 `eval_sample()` 中：

```python
tokenizer(...).input_ids
```

最初是一个一维列表：

```text
[token_0, token_1, ..., token_(L-1)]
```

加上：

```python
[None, ...]
```

后变为：

```text
x.shape = [1, L]
```

本次实验的 `input_ids` 长度为 31，所以初始输入可以理解为：

```text
x.shape = [1, 31]
```

其中：

```text
1  = 一条输入样本
31 = 这条样本包含 31 个 token
```

在 `stream_generate()` 的第一次 `forward()` 前，模型另外准备 8 路音频输入：

```text
audio_buffer.shape = [1, 8, L]
input_ids.unsqueeze(1).shape = [1, 1, L]
```

把二者在第 2 维拼接：

```text
[1, 8, L] + [1, 1, L] = [1, 9, L]
```

这 9 路是：

```text
第 0-7 路：8 个 audio codebook 的输入
第 8 路：文本 token
```

所以 `forward()` 支持两种输入：

```text
[batch, sequence]
    只有文本；函数内部自动创建 8 路 audio_pad

[batch, 9, sequence]
    已经同时带有 8 路 audio 输入和 1 路文本输入
```

当前只需要记住：

```text
shape = [1, L]：一条样本、L 个文本 token
shape = [1, 9, L]：一条样本、9 路并行序列、每路 L 个位置
```

```text
[纠偏] 暂停直接回答 forward 分支，先补齐 input_ids.shape 基础

## 二十四、9 路输入的含义验收

学习者确认：

```text
前 8 路不是用户输入语音的 fbank，而是历史 audio code；另有 text 路。
```

这个区分正确：

```text
input_ids.shape = [batch, 9, sequence]
  前 8 路：Talker 读取的 8 个 codebook 的历史离散 code
  第 9 路：Thinker 读取的文本 token

audio_inputs
  另一个独立参数，保存用户输入语音经过预处理后的连续 fbank 特征
```

因此“用户输入语音”和“模型输出语音的历史 code”不是同一份数据：

```text
用户语音：audio_inputs -> SenseVoice -> Thinker
历史 audio code：input_ids 前 8 路 -> Talker
```

```text
[验收] 能区分 audio_inputs 与 input_ids 前 8 路
```

## 二十五、当前恢复点

下一步读取 `forward()` 的拆分：

```python
text_ids = input_ids[:, 8, :]
audio_ids = input_ids[:, :8, :]
```

只追踪这两句的输出形状，不进入音频特征注入和 Transformer 层。

## 二十六、9 路输入拆分验收

给定：

```text
input_ids.shape = [2, 9, 100]
```

学习者回答：

```text
text_ids.shape  = [2, 100]
audio_ids.shape = [2, 8, 100]
```

结果正确：

```python
text_ids = input_ids[:, 8, :]  # [2, 100]
audio_ids = input_ids[:, :8, :]  # [2, 8, 100]
```

```text
[验收] 能根据 batch、route、sequence 三个维度推导切片后的形状
```

## 二十七、当前恢复点

下一步回到 `forward()` 的 2D 分支：

```python
if len(input_ids.shape) == 2:
    text_ids = input_ids
    audio_ids = torch.full(
        (batch_size, 8, seq_length),
        self.audio_pad_token,
        ...
    )
```

核心问题：为什么只有文本输入时，也要补出 8 路音频位置。

## 二十八、文本 Embedding 形状验收

学习者回答：

```text
Embedding 后是 [1, 100, 768]；audio_pad 对应 token embedding。
```

第二点正确：音频占位位置在刚完成 Embedding 时仍是普通 token embedding，真实
音频特征尚未注入。若题设 `text_ids.shape=[2,100]`，第一点的 batch 维应为 2：

```text
text_ids       = [2, 100]
Embedding 后   = [2, 100, 768]
```

```text
[验收] 理解 Embedding 只增加 hidden_size 维
[纠偏] 根据题设 batch size 推导输出第一维
```

## 二十九、当前恢复点

下一步进入 `forward()` 的音频注入条件和两个函数：

```python
if audio_inputs is not None and start_pos == 0:
    audio_features = self.encode_audio_inputs(audio_inputs, audio_lens)
    hidden_states = self.inject_audio_features(
        text_ids, hidden_states, audio_features, seq_length
    )
```

只回答一个问题：为什么先调用 `encode_audio_inputs()`，再调用
`inject_audio_features()`？

## 六十八、跳过低优先级的生成初始化细节

学习者决定暂时跳过 `stream_generate()` 开头的以下细节：

```python
start_pos, past_kvs, text_finished, first_finished = ...
audio_codes = [[] for _ in range(8)]
audio_stop_pos = [None] * 8
audio_buffer = ...
```

这些变量的具体初始化和收尾边界留到需要深入流式生成或调试时再补充；当前先
保留已经掌握的主线：

```text
forward -> 文本 logits + 8 路 audio logits + KV cache
-> 采样文本 token 和 audio code
-> 形成完整 audio frame
-> eval_sample 收集并交给 Mimi 解码
```

至此，`eval_omni.py` 的外层评估流程已覆盖主要路径。下一步转入
`model/model_omni.py::forward()`，从混合输入进入 Thinker 的主干开始。

```text
[范围调整] 跳过 stream_generate 的低优先级初始化细节
[下一步] 进入 model_omni.py 的 forward 主线
```

## 六十九、`eval_omni.py` 外层推理主链闭合

截至当前，已经沿一个“文字 + 语音 + 图片输入，文字 + 语音输出”的样例，
把外层推理主链串通：

```text
main
-> init_model
-> mode=5 准备文字、音频 fbank、图像张量
-> 构造 audio_pad / image_pad prompt
-> chat template
-> tokenizer -> input_ids
-> model.generate
-> stream_generate 调用 forward
-> Thinker 注入音频/视觉特征
-> Bridge -> Talker
-> text logits + 8 路 audio logits
-> 采样文本 token 和 audio code
-> 形成完整 audio frame
-> eval_sample 分别处理文本和音频帧
-> [1, 8, T] -> Mimi.decode
-> 保存 24kHz 音频
```

已通过实践或复述确认的重点：

```text
入口、参数和 mode 分支
原生权重/Transformers 模型加载路径
MiniMindOmni 的模块组装
audio_inputs、pixel_values 与占位 token 的互补关系
2D/3D input_ids 形状和 9 路拆分
Thinker、Bridge、Talker 的职责
TalkerEmbedding 的 8 路合并
TalkerHead 的 8 份 audio logits
文本 logits 与 audio logits 的切片和采样
audio_codes、audio_buffer、audio_frame 的关系
Mimi 输入形状和最终解码位置
```

按学习者要求，以下内容暂时跳过或只保留主线认识：

```text
stream_generate 开头的状态变量初始化
KV Cache 的每个内部字段
temperature/top-p 的完整采样细节
MTP 对齐公式的更多边界情况
SenseVoice、SigLIP2、Mimi 的内部网络结构
训练数据和 train_sft_omni.py
VAD、实时 WebSocket 和音色克隆的代码细节
```

因此当前结论是：

```text
[阶段完成] eval_omni.py 的端到端推理主流程已讲完
[未完成] model_omni.py 的每个内部函数尚未全部精读
```

下一步不再重复 `eval_omni.py` 外层流程；进入新的学习单元前，先根据需要
选择“整条链路复述验收”或“直接学习 OmniDataset 数据与训练”。

## 三十、音频编码与注入验收

学习者复述：

```text
先翻译编码方式，再把编码注入 token 序列；不翻译一下会导致语言不通。
```

这个类比正确。更精确地说：

```text
encode_audio_inputs
  -> 把 SenseVoice 的音频表示转换到 Thinker 的 hidden 表示空间

inject_audio_features
  -> 根据 audio_pad 的位置，把转换后的特征放入 token 序列
```

这里的“不通”主要是张量表示不兼容，而不是自然语言语法问题：

```text
SenseVoice 特征：[..., 512]（示意）
Thinker hidden：[..., 768]
```

必须先经过 `audio_proj`，才能作为 Thinker 序列中的向量使用；同时还要有
`audio_pad` 指明这些向量应该放在哪里。

```text
[验收] 能区分“特征转换”和“序列位置注入”两个动作
```

## 三十一、当前恢复点

下一步用完全相同的思路看图像分支：

```python
img_emb = self.get_image_embeddings(pixel_values)
vision_tensors = self.vision_proj(img_emb)
hidden_states = self.count_vision_proj(
    tokens=text_ids, h=hidden_states,
    vision_tensors=vision_tensors, seqlen=seq_length
)
```

只比较它和音频分支的对应关系，不深入 SigLIP2 内部。

## 三十二、图像与音频注入方式验收

学习者复述：

```text
它们都是先编码，再翻译，最后注入 token 序列；使用特殊占位符。
```

核心理解正确。术语进一步精确为：

```text
先由外部 encoder 提取特征
再由 projector 转换到 Thinker 的 hidden_size
最后根据特殊占位符的位置，替换 hidden_states 中对应的 embedding
```

注入时通常不修改 `text_ids` 中的整数 token ID；改变的是：

```text
token ID 序列：保留 audio_pad / image_pad 作为位置标记
hidden_states：用真实的连续音频/视觉特征替换占位 embedding
```

```text
[验收] 理解音频和图像都采用“编码 -> 投影 -> 按占位位置注入”的模式
[纠偏] 区分 token ID 序列不变与 hidden_states 内容被替换
```

## 三十三、当前恢复点

下一步进入 `forward()` 的 Thinker 主干循环：

```python
bridge_states = hidden_states
for i, (layer, past_key_value) in enumerate(...):
    hidden_states, present = layer(...)
    if i == self.config.bridge_layer:
        bridge_states = hidden_states
h_thinker = self.thinker.norm(hidden_states)
```

重点只学习：Thinker 为什么一边继续走到最终层生成文本表示，一边在中间层
保存 `bridge_states` 给 Talker。

## 三十四、Bridge 层数与文本/语音输出关系

学习者判断：

```text
8 层 Thinker、bridge_layer=3 时，bridge_states 来自第 4 层；
h_thinker 来自第 8 层。
```

正确。代码使用从 0 开始的层下标，因此 `i == 3` 是第 4 层；循环结束后的
`h_thinker` 来自最后一层并经过最终 norm。

关于文本和声音：

```text
文本输出：Thinker 最终 hidden -> 文本 lm_head -> 文本 token
语音输出：Thinker 中间 hidden + 历史 audio code -> Talker -> 8 路 audio code
```

二者不是同一种输出，且不是下面这条级联路径：

```text
Thinker 先生成完整文字
-> 把文字 decode 成字符串
-> 再把字符串交给独立 TTS
```

MiniMind-O 的路径更接近：

```text
共同的 Thinker 语义表示
  ├─ 文本 lm_head -> 文本 token
  └─ Bridge -> Talker -> audio code
```

Talker 不直接读取 `tokenizer.decode()` 得到的文字字符串，也不直接使用文本
`lm_head` 的词表分数。它使用的是连续的 hidden state 和音频 code 历史，因此
声音是对同一语义条件的另一种表达，而不是把文本结果重新朗读一遍。

不过“不是读一遍文本”不等于文本和声音互相独立：Thinker 的 hidden state 同时
承载语义，生成出的文本 token 还会在后续自回归步骤中成为输入历史，Talker 也
在这些步骤中持续获得相应的 hidden 条件。因此二者语义应当一致，只是输出头、
词表和表示形式不同。

```text
[验收] 能区分 text_logits 与 audio_logits 的来源和目标
[验收] 理解 Talker 不接收最终文本字符串，不走独立的文本到语音级联
```

## 三十五、当前恢复点

下一步继续读 `forward()` 的 Talker 部分：

```python
talker_emb = self.talker.embed_tokens(audio_ids)
hidden_states = (
    self.talker.embed_proj(bridge_states) * self.talker.text_scale
    + self.talker.codec_proj(talker_emb) * self.talker.audio_scale
)
```

只学习 Bridge hidden 和历史 audio code 如何在 Talker 输入处合并，暂时不展开
Talker 的每层 Attention。

## 三十六、文本与语音输出关系验收

学习者确认理解：

```text
文本和声音不是同一种输出；声音不是把最终文本重新读一遍。
```

当前结论：

```text
共同语义来源：Thinker hidden state
文本分支：最终 h_thinker -> text lm_head -> text token
语音分支：bridge_states + 历史 audio code -> Talker -> audio code
```

这不是 `ASR -> LLM -> 完整文本 -> TTS` 的级联，但文本和声音也不是完全独立，
因为二者共享 Thinker 的语义条件，生成出的文本 token 还会参与后续自回归历史。

```text
[验收] 能区分 text_logits、audio_logits 的来源和输出形式
[验收] 能解释 Talker 不接收最终文本字符串
```

## 三十七、当前恢复点

下一步继续 `forward()` 的 Talker 输入合并：

```python
talker_emb = self.talker.embed_tokens(audio_ids)
hidden_states = (
    self.talker.embed_proj(bridge_states) * self.talker.text_scale
    + self.talker.codec_proj(talker_emb) * self.talker.audio_scale
)
```

只关注两条向量路径的来源、形状和相加关系。

## 三十八、Talker 输入合并的形状纠偏

题设：

```text
bridge_states.shape = [1, 31, 768]
audio_ids.shape     = [1, 8, 31]
talker_hidden_size  = 768
```

学习者回答为 `[1,8,768] + [1,8,768] -> [1,8,768]`，这里把 codebook 数量
8 误当成了输出序列长度。

正确形状是：

```text
bridge_states
  -> embed_proj
  -> [1, 31, 768]

audio_ids [1, 8, 31]
  -> TalkerEmbedding：8 路 codebook 在每个位置被组合
  -> [1, 31, 768]
  -> codec_proj
  -> [1, 31, 768]

两条路径相加
  -> hidden_states.shape = [1, 31, 768]
```

关键规则：

```text
8 是音频 codebook 路数，会在 TalkerEmbedding 中被组合掉；
31 是序列位置数，会保留下来；
768 是每个位置的 Talker hidden 维度。
```

```text
[纠偏] 区分 audio_ids 的 codebook 维和输出 hidden 的 sequence 维
```

## 三十九、当前恢复点

下一步只展开 `TalkerEmbedding` 如何把 `[batch, 8, sequence]` 变成
`[batch, sequence, talker_hidden_size]`，先用一个小张量手算，不进入 Talker
Transformer 层。

## 四十、Talker 输入形状验收

学习者复述：

```text
8 表示通道数，31 表示 token 数，因为 8 in 1。
```

核心理解正确。术语精确为：

```text
8  = 8 个 Mimi audio codebook 路（也可暂时理解为 8 个音频通道）
31 = 序列位置数
```

`TalkerEmbedding` 在每个位置把 8 个 codebook 的表示组合成一个向量，因此：

```text
[1, 8, 31]
-> [1, 31, 768]
```

这里不是把 8 个时间位置合成 1 个位置，而是把同一个位置上的 8 路音频信息
合成为一个 768 维 hidden 向量；31 个位置仍然保留。

```text
[验收] 理解 codebook 路数被合并，sequence 位置数被保留
```

## 四十一、当前恢复点

下一步读取 `TalkerEmbedding.forward()`：

```python
base_out = self.base(x)
return sum(
    base_out[:, i, :] + self.adapters[i](x[:, i, :])
    for i in range(len(self.adapters))
) / self.num_layers
```

只用极小的 `[batch=1, codebook=2, sequence=3]` 例子说明 `base`、每路
`adapter` 和求平均的关系。

## 四十二、单个序列位置的最终表示

学习者回答：

```text
一个序列位置最终产生 1 个 hidden 向量，维度为 768。
```

回答正确。8 个 codebook 不是产生 8 个并行 hidden 序列，而是在每个位置先
各自产生表示，再合成为该位置的一个 Talker hidden 向量：

```text
同一位置的 8 个 audio code
-> 8 个 768 维表示
-> 求和并平均
-> 1 个 768 维表示
```

对 31 个位置重复后，整体输出就是：

```text
[batch, 8, 31]
-> [batch, 31, 768]
```

```text
[验收] 能从单个位置推导 TalkerEmbedding 的最终向量数量和维度
```

## 四十三、当前恢复点

下一步区分 `TalkerEmbedding` 中的两类参数：

```python
base_out[:, i, :] + self.adapters[i](x[:, i, :])
```

重点是：`base` 为所有 codebook 提供共享基础表示，`adapters[i]` 为第 `i`
个 codebook 提供专属修正；暂时不进入 adapter 内部的 MLP 细节。

## 四十四、共享 base 与专属 adapter 验收

学习者回答：

```text
相同数字在不同 codebook 中的 base embedding 相同；因为 adapter 不同，最终表示不同。
```

回答正确：

```text
同一个 code 数值
-> 共享 base 查表结果相同
-> 根据所在 codebook 经过不同的 adapter
-> 最终的 codebook 表示可以不同
```

这让模型同时获得：

```text
共享部分：不同 codebook 对相同数值的共同基础表示
专属部分：每个 codebook 自己的层级语义和声学修正
```

```text
[验收] 理解 code 数值和 codebook 身份共同决定最终 embedding
```

## 四十五、当前恢复点

下一步回到 Talker 输入融合：

```python
self.talker.embed_proj(bridge_states) * self.talker.text_scale
self.talker.codec_proj(talker_emb) * self.talker.audio_scale
```

只学习两个 projector 的作用和加权相加，不展开它们内部每一层 Linear。

## 四十六、Talker 两路 projector 验收

学习者回答：

```text
两个投影层还可以学到各自的变换方式。
```

回答正确。完整作用是：

```text
维度对齐：让两条路径都变成 talker_hidden_size
语义适配：分别学习 Thinker hidden 和 audio embedding 如何转换为 Talker 表示
```

因此：

```text
bridge_states -> embed_proj -> 语义路径
talker_emb    -> codec_proj -> 音频历史路径
```

再经过 `text_scale`、`audio_scale` 加权相加，才交给 Talker Transformer。

```text
[验收] 理解 projector 同时承担维度对齐和表示变换
```

## 四十七、当前恢复点

下一步读取 Talker 的层循环：

```python
for layer, past_key_value in zip(self.talker.layers, past_key_values[n_thinker:]):
    hidden_states, present = layer(...)
h_talker = self.talker.norm(hidden_states)
```

只确认融合后的 hidden 经过 Talker 层和 norm 后，才进入 `TalkerHead`；暂时
不重新展开 Attention 的 Q/K/V。
```

下一步进入 `MiniMindOmni.forward()` 的开头：

```python
if len(input_ids.shape) == 2:
    ...
else:
    ...
```

只学习它如何区分：

```text
[batch, sequence] 的普通文本输入
[batch, 9, sequence] 的音频 8 路 + 文本 1 路输入
```

## 四十八、Talker Transformer 形状验收

学习者回答：

```text
不会改变，因为 Transformer 不改变形状。
```

结论正确。更精确地说，Attention 和 FFN 内部可以有临时的投影维度，但每个
模块最后都会通过输出投影和残差连接回到 `talker_hidden_size`：

```text
[batch, sequence, 768]
-> Talker layer
-> [batch, sequence, 768]
```

4 层重复后，再经 `RMSNorm`，仍然是：

```text
h_talker.shape = [batch, sequence, 768]
```

```text
[验收] 理解 Talker 主干层前后 hidden shape 保持一致
```

## 四十九、当前恢复点

下一步进入 `TalkerHead`：

```python
audio_logits = self.talker.lm_head(h_talker)
```

只学习一个 hidden 序列为什么会得到 8 份 audio logits，以及每份 logits 的
最后一维代表什么。

## 五十、`TalkerHead` 输出形状验收

题设：

```text
h_talker.shape = [2, 50, 768]
audio_vocab_size = 2112
```

学习者回答“列表；`[2,8,50,2112]`”。“列表”正确，但需要区分 Python 列表
和把列表堆叠后的 Tensor：

```text
audio_logits 是长度为 8 的列表
audio_logits[0].shape = [2, 50, 2112]
audio_logits[1].shape = [2, 50, 2112]
...
audio_logits[7].shape = [2, 50, 2112]
```

只有显式执行：

```python
torch.stack(audio_logits, dim=1)
```

才会得到整体形状：

```text
[2, 8, 50, 2112]
```

其中：

```text
2    = batch
8    = codebook 路数
50   = sequence 位置
2112 = 每个 codebook 的候选 audio code 数量
```

```text
[纠偏] 区分“长度为 8 的列表”与“stack 后的四维 Tensor”
```

## 五十一、当前恢复点

下一步回到 `forward()` 的返回值：

```python
out = MoeCausalLMOutputWithPast(
    aux_loss=aux_loss,
    logits=text_logits,
    past_key_values=presents
)
out.audio_logits = audio_logits
```

只看一次 `forward()` 如何同时返回文本 logits、音频 logits 和 KV cache，不展开
`aux_loss`。

## 七十、`mode=audio_proj` 冻结策略验收

学习者回答：

```text
不会，TalkerHead 被冻结了。
```

正确。代码先执行：

```python
for p in model.parameters():
    p.requires_grad = False
```

再只打开：

```python
for p in model.audio_proj.parameters():
    p.requires_grad = True
```

因此 `mode=audio_proj` 的训练对象只有音频 Projector，以下模块都不更新：

```text
Thinker
Thinker text lm_head
Talker
TalkerHead
vision_proj
```

这个阶段的目的，是固定已经存在的 Thinker/Talker，先让 `audio_proj` 学会把
SenseVoice 特征接入这套 hidden 表示空间。

```text
[验收] 能根据 mode=audio_proj 判断 TalkerHead 是否更新
```

## 七十一、训练主线转入独立文档

训练主线原本可以从 `train_sft_omni.py::train_epoch()` 的一个 batch 开始：

```python
res = model(...)
text_loss = ...
audio_loss = ...
loss = text_loss + audio_loss + res.aux_loss
```

但在进入 loss 之前，必须先理解 `OmniDataset` 如何构造一条训练样本。因此
训练数据已经转入下一份学习文档，不在本日继续展开：

[Day 034：minimind-o 训练样本的数据结构](day-034.md)

## 五十二、`forward()` 返回值进入生成循环

`stream_generate()` 的核心循环可以先简化为：

```text
准备当前文本/音频历史
  -> forward()
  -> 从 out.logits 采样下一个文本 token
  -> 从 out.audio_logits 的 8 项中各采样一个 audio code
  -> 保存 out.past_key_values
  -> 把新 token/code 追加到下一轮历史
  -> yield 当前文本增量和可用音频帧
```

第一次循环时：

```python
out = self.forward(
    torch.cat((audio_buffer, input_ids.unsqueeze(1)), dim=1),
    past_key_values=None,
    use_cache=use_cache,
    **args
)
```

此时：

```text
input_ids                 [batch, sequence]
audio_buffer              [batch, 8, sequence]
input_ids.unsqueeze(1)    [batch, 1, sequence]
拼接后                    [batch, 9, sequence]
```

`forward()` 返回：

```text
out.logits                -> 文本 logits
out.audio_logits          -> 8 份音频 logits
out.past_key_values       -> Thinker + Talker 的 KV cache
```

这一轮之后：

```python
past_kvs = out.past_key_values
```

并采样一个文本 token、8 个 audio code，再把它们写入下一位置：

```python
input_ids = torch.cat((input_ids, new_text_token), dim=1)
audio_buffer = torch.cat((audio_buffer, new_audio_column), dim=2)
```

下一轮如果启用 cache，不再把完整序列重新送入模型，而只送最后一个位置：

```python
audio_buffer[:, :, -1:]       [batch, 8, 1]
input_ids[:, -1:].unsqueeze(1) [batch, 1, 1]
拼接后                       [batch, 9, 1]
```

因此生成过程的形状变化是：

```text
第一次 forward：处理完整 prompt，约 [batch, 9, L]
后续 forward：  借助 KV cache，每次处理 [batch, 9, 1]
```

```text
[验收] 理解一次 forward 的三类返回值分别服务于文本、音频和下一轮缓存
[验收] 理解 KV cache 让后续循环只处理一个新位置
```

## 五十三、当前恢复点

下一步只展开 `out.logits` 如何采样一个文本 token；先不展开 audio logits 的
temperature 和 MTP 对齐。

## 五十四、文本 logits 切片验收

题设：

```text
out.logits.shape = [1, 31, 6400]
```

学习者回答：

```text
[6400]，表示词汇表的概率。
```

形状正确。含义需要精确为：

```text
out.logits[0, -1, :].shape = [6400]
```

它表示最后一个序列位置上，6400 个候选文本 token 的 logits 分数；此时还不是
概率。后续经过：

```text
logits / temperature
-> softmax
-> 概率分布
-> multinomial 采样
```

才得到候选 token 的概率并采样出一个文本 token ID。

```text
[验收] 能从 [batch, sequence, vocab] 切出最后位置的词表分数
[纠偏] 区分 logits 分数与 softmax 后的概率
```

## 五十五、当前恢复点

下一步进入 `stream_generate()` 的音频分支：

```python
for i, al in enumerate(out.audio_logits):
    ...
```

只学习它如何对 8 个 codebook 分别取出最后位置的 logits，不展开 MTP 时间错位。

## 五十六、单个 codebook 的音频 logits 验收

题设：

```text
al.shape = [1, 31, 2112]
```

学习者回答：

```text
[2112]；第一个 codebook 的 logits。
```

回答正确。在循环的当前索引为 `i=0` 时：

```python
al[0, -1, :].shape = [2112]
```

表示第 0 个 Mimi codebook 在最后一个序列位置上，对 2112 个候选 audio code
的 logits 分数。若 `i=3`，则对应第 3 个 codebook。

```text
[验收] 能把列表索引 i、codebook 身份和 [2112] 候选分数对应起来
```

## 五十七、当前恢复点

下一步阅读音频采样的简化部分：

```python
top_val, top_idx = logits_i.topk(50)
code = top_idx[torch.multinomial(F.softmax(top_val, dim=-1), 1)].item()
```

只理解“先取 top-50，再在这 50 个候选中采样”，暂时不展开历史 code 惩罚和
MTP 时间错位。

## 五十八、Top-k 采样索引验收

实验：

```python
top_idx = [100, 25, 1900]
局部采样下标 = 1
```

学习者回答最终 `code = 25`，正确：

```python
code = top_idx[1]
```

需要区分：

```text
局部下标 1：top-k 候选列表中的位置
真实 code 25：音频词表中的 token ID
```

```text
[验收] 能从 top-k 局部下标映射回真实 audio code ID
```

## 五十九、当前恢复点

下一步读取采样结果的保存：

```python
audio_codes[i].append(code)
```

只学习 `audio_codes` 为什么是 8 个列表，以及第 `i` 个 codebook 的新 code
为什么必须放进自己的列表。

## 六十、8 个 codebook 的历史列表验收

学习者回答：

```text
8；2。
```

回答正确：经过两轮生成后：

```text
audio_codes = [list0, list1, ..., list7]
```

共有 8 个列表，每个列表通常有 2 个时间位置。某些早期位置可能是
`audio_pad_token`，因为不同 codebook 可能在不同的生成步开始产出真实 code。

```text
[验收] 理解第一维是 codebook，第二维是生成时间位置
```

## 六十一、当前恢复点

下一步阅读 `audio_buffer` 的更新：

```python
audio_buffer = torch.cat((
    audio_buffer,
    torch.full((1, 8, 1), self.audio_pad_token, ...)
), dim=2)
for i in range(min(audio_step + 1, 8)):
    audio_buffer[0, i, -1] = audio_codes[i][-1]
```

只学习为什么要给下一时间位置先补一列，再把已经生成的 code 写入对应路。

## 六十二、`audio_buffer` 追加位置验收

学习者回答：

```text
[1, 8, 32]；先占位置。
```

回答正确：

```text
[1, 8, 31]
-> 沿 sequence 维追加一列
-> [1, 8, 32]
```

新列先全部填充 `audio_pad_token`，因为当前时间位置已经需要存在，但 8 个
codebook 中可能只有前面若干路已经产生真实 code；尚未产生的路仍需保持占位，
以便下一次 `forward()` 的 8 路输入形状和时间位置保持对齐。

```text
[验收] 理解 audio_buffer 的 sequence 维增长和新列占位作用
```

## 六十三、当前恢复点

下一步阅读 `stream_generate()` 的返回：

```python
if not text_finished:
    yield input_ids[:, start_pos:], audio_frame
else:
    yield None, audio_frame
```

只看一轮生成如何同时把文本增量和音频帧交给 `eval_sample()`，暂时不展开
完整 audio frame 的 diagonal 对齐公式。

## 六十四、`yield` 返回顺序纠偏

题设：文本尚未结束，音频尚未形成完整帧。

学习者回答“`None`；一帧”，把两个返回值的顺序弄反了。

源码：

```python
yield input_ids[:, start_pos:], audio_frame
```

返回顺序始终是：

```text
第一个值：y，文本结果
第二个值：audio_frame，音频帧或 None
```

因此题设情况下应为：

```text
(文本结果, None)
```

三种常见组合：

```text
(文本结果, None)       文本继续生成，当前还没有完整音频帧
(文本结果, 一帧)       文本继续生成，同时形成了一帧完整音频
(None, 一帧)            文本已结束，但音频还在收尾
```

```text
[纠偏] 固定 `for y, audio_frame in res_y` 的两个返回值顺序
```

## 六十五、当前恢复点

下一步回到 `eval_sample()` 的消费端，确认它如何分别处理 `y` 和
`audio_frame`，以及为什么使用 `history_idx` 避免重复打印文本。

## 六十六、文本结束后的音频收尾验收

学习者回答：

```text
Node; 一帧
```

其中 `Node` 按上下文理解为 `None`。正确返回顺序是：

```text
(None, 一帧)
```

含义：

```text
第一个位置 `y=None`：文本已经结束
第二个位置 `audio_frame`：音频仍有一帧可以交给调用者
```

```text
[验收] 能区分文本结束后的 `(None, audio_frame)` 返回情况
```

## 六十七、完整音频帧细节的范围调整

完整音频帧的 diagonal 对齐细节原本是下一步候选内容：

```python
if return_audio_codes and audio_step >= 7:
    frame = [audio_codes[i][step - 7 + i] for i in range(8)]
```

按学习者要求，本日停留在“8 路 audio code 组成一帧并交给 Mimi”的主线，
不继续展开 `audio_step >= 7` 和 8 路索引错位的底层公式。该细节保留为后续
需要调试流式生成时再补的内容。

```text
[范围调整] 主线已理解，暂不精读完整音频帧的 diagonal 对齐公式
```

## 七十二、本日收尾与文档分工

Day 033 的主线已经闭合：从 `eval_omni.py` 的 `__name__ == "__main__"` 入口
开始，完成了文字、语音和图片输入到文字输出、音频 code 生成以及 Mimi 解码的
端到端推理流程。

本日已经确认的主链是：

```text
main
  -> init_model
  -> 构造 prompt 和多模态输入
  -> tokenizer 得到 input_ids
  -> model.generate / stream_generate
  -> Thinker 注入音频和视觉特征
  -> Bridge 连接 Talker
  -> 文本 logits + 8 路 audio logits
  -> 采样文本 token 和 audio code
  -> 形成音频帧
  -> Mimi.decode
```

以下内容按学习者要求保留为“主线认识”或暂时跳过，没有冒充已经精读：

```text
stream_generate 开头的状态变量初始化
KV Cache 的内部字段
MTP 对齐公式的更多边界
SenseVoice、SigLIP2、Mimi 的内部网络结构
VAD、实时 WebSocket 和音色克隆的工程细节
```

训练数据不继续放在本日文档中，已经单独整理到：

[Day 034：minimind-o 训练样本的数据结构](day-034.md)

下次从 Day 034 的验收问题开始，再进入 `omni_collate_fn` 和训练损失：

```text
audio_inputs 和 audio_labels，哪个是模型输入，哪个是监督目标？
```

```text
[完成] Day 033：eval_omni.py 端到端推理主流程
[转入] Day 034：OmniDataset 训练样本
```
