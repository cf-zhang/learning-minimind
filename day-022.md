# Day 022：蒸馏中的 teacher、student 与损失目标

- 日期：2026-09-03
- 学习阶段：`minimind` 知识蒸馏
- 当前状态：已完成
- 入口：`minimind/trainer/train_distillation.py`

## 一、今日恢复点

LoRA 阶段已经结束。下一阶段先建立蒸馏的角色关系：

```text
teacher：提供较丰富的输出分布，冻结、不更新
student：接受训练，学习真实标签和 teacher 分布
```

今天按源码顺序完成 teacher/student、`distillation_loss()`、temperature、KL、CE/KL 组合和训练循环的概念验收。

## 二、teacher 与 student

`train_distillation.py` 分别构造两套 `MiniMindConfig` 和模型：

```python
lm_config_student = MiniMindConfig(...)
lm_config_teacher = MiniMindConfig(...)
model, tokenizer = init_model(lm_config_student, ...)
teacher_model, _ = init_model(lm_config_teacher, ...)
```

student 是要训练的模型；teacher 提供参考输出。teacher 可以是 MoE、更多层或更大隐藏维度，student 可以更小。真实小配置实验确认：

```text
student layers: 1
teacher layers: 2
student training: True
teacher training: False
student trainable params: 768
teacher trainable params: 0
optimizer params: 768
```

teacher 执行：

```python
teacher_model.eval()
teacher_model.requires_grad_(False)
```

`eval()` 影响 dropout 等模块行为，`requires_grad_(False)` 关闭 teacher 参数梯度；两者是不同开关。teacher 仍然执行 forward，但不 backward、不更新。

## 三、同一输入的 logits 对齐

训练循环中两套模型接收相同 `input_ids`：

```python
student_logits = model(input_ids).logits[..., :-1, :].contiguous()
with torch.no_grad():
    teacher_logits = teacher_model(input_ids).logits[..., :-1, :].contiguous()
```

`[..., :-1, :]` 去掉最后一个时间位置，使 logits 与 next-token labels 对齐。真实实验：

```text
student logits shape: (1, 3, 20)
teacher logits shape: (1, 3, 20)
student logits requires_grad: True
teacher logits requires_grad: False
student logits grad_fn: SliceBackward0
teacher logits grad_fn: None
same shape: True
```

## 四、temperature 与 soft target

logits 是词表候选的原始分数，不是概率。代码对 teacher 和 student 使用同一个温度：

```python
teacher_probs = F.softmax(teacher_logits / T, dim=-1)
student_log_probs = F.log_softmax(student_logits / T, dim=-1)
```

`T>1` 会压小 logits 差异，使分布更平滑，保留 teacher 对多个候选 token 的相对偏好；不会改变候选排序。学习者实际观察到：

```text
T=1.0 最大概率 0.8438
T=1.5 最大概率 0.7148
T=2.0 最大概率 0.6285
T=4.0 最大概率 0.4810
```

所有概率行的总和都为 1。

## 五、KL 蒸馏损失

当前函数：

```python
def distillation_loss(student_logits, teacher_logits, temperature=1.0, reduction='batchmean'):
    with torch.no_grad():
        teacher_probs = F.softmax(teacher_logits / temperature, dim=-1).detach()
    student_log_probs = F.log_softmax(student_logits / temperature, dim=-1)
    kl = F.kl_div(student_log_probs, teacher_probs, reduction=reduction)
    return (temperature ** 2) * kl
```

teacher 提供 soft target 分布 `p`，student 提供预测分布 `q`。`F.kl_div(input, target)` 在这里传入 `log q` 和 `p`，对应：

```text
KL(teacher || student) = sum p_i * log(p_i / q_i)
```

分布相同则 KL 为 0；分布越不一致，KL 通常越大。KL 非负但不对称：`KL(p||q) != KL(q||p)`，所以这里的 teacher/student 方向有意义。

`F.log_softmax` 直接产生稳定的 log 概率；teacher 的 `no_grad()` 和 `detach()` 让 KL 梯度只回到 student。最后乘 `T**2`，补偿高温度使 logits 梯度变小的影响。

## 六、CE 与 KL 的组合

训练循环先计算真实标签 CE：

```python
ce_loss_raw = ...
if lm_config_student.use_moe:
    ce_loss = ce_loss_raw + res.aux_loss
else:
    ce_loss = ce_loss_raw
```

然后计算 KL：

```python
distill_loss = distillation_loss(...)
```

最终目标：

```python
loss = alpha * ce_loss + (1 - alpha) * distill_loss
```

含义：

```text
alpha=1 -> 只使用 CE
alpha=0 -> 只使用 KL
alpha=0.5 -> CE/KL 各占一半
```

学习者用 `ce_loss=2.0`、`distill_loss=0.6` 验证：

```text
alpha=0.0 -> 0.6
alpha=0.5 -> 1.3
alpha=0.8 -> 1.72
alpha=1.0 -> 2.0
```

`alpha` 越大，训练越偏向真实标签 CE；`1-alpha` 越大，越偏向 teacher KL。MoE student 的 `ce_loss` 变量实际包含 `aux_loss`，日志仍单独记录 `ce_loss_raw` 和 `aux_loss`。

## 七、mask、词表与 reduction

训练只在有效 next-token 位置比较：

```python
loss_mask = (labels[..., 1:] != -100).float()
loss_mask_flat = loss_mask.view(-1)
```

logits 展平为 `[batch*(seq-1), vocab]` 后，用 `loss_mask_flat == 1` 筛掉 PAD 和无效位置，传给 CE 与 KL 的位置集合保持一致。

如果 teacher 词表维度大于 student，源码截取：

```python
teacher_logits = teacher_logits[..., :student_vocab_size]
```

这只能解决形状对齐，不能替代 token ID 语义兼容；teacher 和 student 必须使用兼容 tokenizer/词表映射。

`reduction='batchmean'` 可理解为先对每个有效位置的词表分布求 KL，再对有效预测位置平均。

## 八、今日确认边界

```text
[实践][验收] teacher/student 配置、模式和可训练参数差异
[实践][验收] 同一输入的 logits 形状、梯度图和 next-token 对齐
[实践][验收] temperature 对分布尖锐程度的影响
[验收] soft target 与 hard target 的区别
[验收] KL(teacher || student)、log_softmax、no_grad 与 T^2
[实践][验收] alpha 对 CE/KL 总目标的影响
[验收] loss_mask、teacher 词表截取与 batchmean
```

## 九、下一天计划（Day 023）

下一天进入真实蒸馏训练落地：

```text
1. 用极小 student/teacher 完成一次 forward
2. 分别打印 ce_loss_raw、distill_loss、aux_loss 和 total loss
3. backward 后确认 student 有梯度、teacher 没有梯度
4. optimizer.step() 后确认只更新 student
5. 对比 alpha 和 temperature 改变时的 loss/梯度
6. 再决定是否使用仓库内极小 SFT 数据跑一轮蒸馏
```
