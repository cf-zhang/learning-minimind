# Day 005：梯度累加、清零与优化器

- 日期：2026-08-20
- 学习阶段：神经网络训练的最小前置知识
- 今日主题：完成 Backward 主题的最后一段
- 当前状态：已完成

## 零、如何使用本课

本课完成一个最小训练循环。它故意从 Day 004 留下的旧梯度开始，因为只有亲眼看见错误的累加结果，才能理解为什么训练代码每轮都有 `optimizer.zero_grad()`。

可复现实验分散在两个历史脚本中：

- [codes/day4.py](codes/day4.py)：第二次 backward、手工清空和重新 backward。
- [codes/day5.py](codes/day5.py)：使用 SGD 完成五轮训练。

## 逐轮互动档案

### 第 1 轮：第二次 backward 是覆盖还是累加

学习者恢复 Day 004 结束状态：

```text
weight=4.6
旧 weight.grad=-16
new_prediction=9.2
new_loss=0.64
```

执行 `new_loss.backward()` 后提交：

```text
第二次 backward 前：tensor(-16.)
第二次 backward 后：tensor(-19.2000)
```

新梯度单独应该是：

```text
new_error = 9.2 - 10 = -0.8
new_gradient = 2 * new_error * x
             = 2 * (-0.8) * 2
             = -3.2
```

实际 `.grad` 是：

```text
-16 + (-3.2) = -19.2
```

因此结论来自真实对比：PyTorch 默认把新梯度加到现有 `.grad`，不是覆盖。

状态：`[实践]`、`[验收]` 学习者确认了累加事实。

### 第 2 轮：为什么默认累加

累加并非纯粹麻烦。一个参数可能通过多条计算路径影响同一个 loss，也可能需要把多个小批次的梯度积累后再更新。每条路径或每批数据都对“参数应如何改变”提供一份贡献，这些贡献需要相加。

但一次参数更新完成后，旧 `.grad` 属于旧参数位置。如果下一轮训练不清空，它就会和新参数位置的梯度错误混合。

学习者确认理解这一动机。

状态：`[验收]` 已区分“有意在一次更新前累加”和“忘记清除上轮梯度”。

### 第 3 轮：清空梯度不会重置参数

学习者执行：

```python
weight.grad = None
```

观察：

```text
清零后的 weight.grad：None
weight 本身：4.6000
```

这证明 `.grad` 和参数值是两个不同存储位置。清空梯度没有让 weight 回到 3，也没有改动已经学到的参数。

重新 forward 和 backward 后：

```text
weight.grad：-3.2000
```

不再是 `-19.2`，说明本轮从空状态只写入当前梯度。

状态：`[实践]`、`[验收]` 清空效果已亲自验证。

### 第 4 轮：把三种操作映射到优化器 API

在手工过程已经完整之后，才引入优化器：

```text
loss.backward()       -> 根据当前 loss 计算梯度，写入/累加到 .grad
optimizer.step()      -> 使用 .grad 真正修改参数
optimizer.zero_grad() -> 清除旧梯度，为下一轮准备
```

在当前例子中，`torch.optim.SGD` 的 `step()` 执行的核心更新与手工版本一致：

```text
weight = weight - learning_rate * weight.grad
```

学习者确认没有疑问。

状态：`[验收]` 三个 API 的职责已能准确区分。

### 第 5 轮：亲自运行五轮 SGD

学习者从 `weight=3` 重新开始，运行五轮：

```python
for step in range(5):
    prediction = x * weight
    loss = (prediction - target) ** 2

    optimizer.zero_grad()
    loss.backward()
    optimizer.step()
```

提交的实际输出为：

```text
step=0 weight=4.5999999 prediction=6.0       loss=16.0
step=1 weight=4.9200001 prediction=9.1999998 loss=0.6400003
step=2 weight=4.9840002 prediction=9.8400002 loss=0.02559995
step=3 weight=4.9967999 prediction=9.9680004 loss=0.00102397
step=4 weight=4.9993601 prediction=9.9935999 loss=0.00004096
```

逐行阅读时要注意顺序：本脚本先用旧 weight 计算 prediction/loss，再调用 `step()`，最后一起打印。因此同一行的 weight 是更新后的值，prediction 和 loss 是更新前计算的值。

五轮结果证明：

```text
weight -> 逼近 5
prediction -> 逼近 10
loss -> 逼近 0
```

小数没有显示成精确的 `4.6` 或 `4.92` 是浮点表示现象，不改变训练结论。

状态：`[实践]`、`[验收]` 最小训练循环完成。

### 第 6 轮：学习者主动要求收口并校正 Day 编号

学习者要求结束这个知识点，并明确：Day 不按最初日期机械计算，而是“真正学习一天算一个 Day”；这一天是学习的第五天。整理文档后，再重新开始 Day 006 学习 Attention。

因此 Backward 主题最终跨三个真实学习日完成：

```text
Day 003：loss 与梯度来源
Day 004：grad_fn、backward 与手工更新
Day 005：梯度累加、清零与优化器循环
```

## 一、承接上次进度

已经亲自完成：

```text
forward -> loss -> backward -> weight.grad
手工更新 weight -> 新 prediction -> 新 loss 下降
```

具体结果：

```text
初始 weight=3
初始 loss=16
第一次 backward 后 weight.grad=-16
更新后 weight=4.6
新 prediction=9.2
新 loss=0.64
```

尚未实践：

```text
第二次 backward 是否累加梯度
如何清空梯度
optimizer.zero_grad() 与 optimizer.step() 各做什么
```

## 二、今日路线

每次只推进一个小关卡：

1. 恢复单参数实验状态。
2. 调用第二次 `backward()`，观察梯度累加。
3. 解释为什么 PyTorch 默认累加。
4. 亲自清空梯度并验证。
5. 用最小优化器替代手工参数更新。

完成当前关卡并解决疑问后，才进入下一关卡。

## 三、今日验收标准

- [x] 验证旧梯度 `-16` 与新梯度约 `-3.2` 累加。
- [x] 理解“累加”与“覆盖”的区别。
- [x] 理解多个样本或多条计算路径为什么需要梯度相加。
- [x] 亲自清空梯度并观察结果。
- [x] 区分 `backward()`、`zero_grad()` 和 `step()`。
- [x] 完成一个最小训练循环的单步或多步实验。

## 四、学习记录

### 实践 1：第二次 backward 的梯度累加

恢复单参数状态后，学习者实际观察：

```text
第二次 backward 前：weight.grad = -16
第二次 backward 后：weight.grad = -19.2000
```

新一轮前向位于 `weight=4.6`：

```text
new_error = 9.2 - 10 = -0.8
new_gradient = 2 * (-0.8) * 2 = -3.2
```

因此实际结果符合：

```text
旧梯度 + 新梯度
= -16 + (-3.2)
= -19.2
```

已经亲自验证：PyTorch 默认把新的反向结果累加到参数已有的 `.grad` 中，而不是覆盖旧值。

下一步只解释为什么累加有用，暂不执行清零。

### 梯度累加的动机确认

学习者确认理解：同一参数更新周期中，多条数据对参数的梯度建议需要相加；完成一次参数更新后，旧梯度属于旧参数状态，不应无限混入下一轮。

下一步亲自清空梯度，只观察 `.grad` 的状态变化。

### 实践 2：清空梯度

学习者执行 `weight.grad = None` 后观察：

```text
清零后的 weight.grad：None
weight 本身：4.6
```

重新 forward 并调用一次 backward 后：

```text
weight.grad：-3.2
```

已经亲自验证：

1. 清空 `.grad` 不修改参数本身。
2. 清空后下一次 backward 从空状态写入当前梯度。
3. 当前梯度为 `-3.2`，没有继续混入旧梯度 `-16`。

至此手工版 `forward -> loss -> backward -> update -> clear grad` 已形成闭环。下一步只把手工更新和清零映射到优化器 API。

### 优化器职责确认

学习者确认没有疑问，理解：

```text
loss.backward()          -> 计算梯度并写入 .grad
optimizer.step()         -> 根据 .grad 修改参数
optimizer.zero_grad()    -> 清空旧梯度
```

其中 `SGD.step()` 在当前最小例子中替代手工的 `weight -= lr * weight.grad`。下一步用 SGD 重复数轮训练，完成 Backward 主题收口。

### 实践 3：SGD 五轮训练

学习者从 `weight=3` 重新开始，使用 `SGD([weight], lr=0.1)` 执行五轮：

```text
step  weight(update 后)  prediction(update 前)  loss(update 前)
0     4.600000            6.000000               16.000000
1     4.920000            9.200000                0.640000
2     4.984000            9.840000                0.025600
3     4.996800            9.968000                0.001024
4     4.999360            9.993600                0.0000409614
```

已经亲自验证：

1. `optimizer.zero_grad()`、`loss.backward()`、`optimizer.step()` 可以替代手工清零、求梯度和更新。
2. weight 从 3 逐步逼近最佳参数 5。
3. prediction 从 6 逐步逼近 target 10。
4. loss 从 16 持续下降到约 `4.1e-05`。
5. 同一行中 prediction/loss 属于 step 前的参数，weight 属于 step 后的参数。

Backward 最小主题的实践链已经完成。学习者确认对优化器职责没有疑问，并通过五轮 SGD 结果验证训练闭环，正式收口。

## 五、知识欠账

- Attention 与 Attention Head：Backward 主题收口后再进入。
- MiniMindBlock、RMSNorm、残差连接：等待相应前置知识。

## 六、今日总结

Day 005 完成了从梯度累加到优化器训练循环的最后一段，也为跨 Day 003、Day 004、Day 005 的 Backward 知识点正式收口。

### 今日亲自完成

```text
[完成] 第二次 backward
[完成] 验证新梯度默认累加到旧梯度
[完成] 验证 -16 + (-3.2) = -19.2
[完成] 理解同一更新周期为什么需要梯度累加
[完成] 理解参数更新后为什么不能继续保留旧梯度
[完成] 使用 weight.grad = None 清空梯度
[完成] 验证清空梯度不修改 weight
[完成] 验证清空后 backward 只得到当前梯度 -3.2
[完成] 区分 backward、optimizer.step 和 optimizer.zero_grad
[完成] 使用 SGD 自动完成五轮训练
[完成] 观察 weight 逼近 5、prediction 逼近 10、loss 逼近 0
```

### Backward 知识点完整链路

```text
输入 x 与参数 weight
  -> forward 得到 prediction
  -> prediction 与 target 计算 error
  -> error 变成非负 loss
  -> grad_fn 记录可求导的运算关系
  -> loss.backward() 反向计算参数梯度
  -> 参数的 .grad 保存或累加梯度
  -> optimizer.step() 根据梯度修改参数
  -> optimizer.zero_grad() 清除旧梯度
  -> 下一轮 forward
  -> prediction 更接近 target，loss 下降
```

### 当前能够准确区分

```text
prediction：模型当前预测
target：训练数据提供的目标
error：预测相对目标的有符号差值
loss：用于优化的错误程度
梯度：loss 在当前参数位置的局部变化方向和速度
grad_fn：中间张量由什么可求导运算产生
backward：计算梯度，不修改参数
.grad：参数保存梯度的位置，默认累加
no_grad：手工更新参数时临时停止记录计算图
zero_grad：清除旧梯度
step：真正更新参数
学习率：控制每次沿梯度方向移动的步长
```

### 五轮 SGD 的最终证据

```text
初始：weight=3，prediction=6，loss=16
最终：weight=4.99936，prediction=9.9936，loss≈0.00004096
目标：weight=5，prediction=10，loss=0
```

浮点输出没有显示为精确的十进制数，源于有限精度二进制浮点表示，不影响本实验结论。

### 尚未扩展的内容

Backward 的最小机制已经掌握，但以下内容留到真实 MiniMind 训练代码出现时再学习：

```text
一个模型包含大量参数时的梯度张量
batch 中多个样本的 loss reduction
梯度累积与 accumulation_steps
梯度裁剪
SGD 以外的 AdamW
学习率调度
混合精度与梯度缩放
```

这些是扩展内容，不影响本知识点当前阶段完成。

## 七、下一学习日入口

下一次实际学习记为 Day 006，不按自然日期空档增加编号。

Day 006 处理此前第二个遗留问题：

> Attention 是什么？Attention Head 又是什么？

严格按以下顺序展开：

```text
复用已知结论：普通 Linear 独立处理每个 token
  -> 为什么语言理解要求 token 读取其他 token 的信息
  -> Attention 要解决的任务是什么
  -> 不使用专业公式，先做一次人工“关注与加权读取”
  -> 再解释一套 Attention 关注方式是什么
  -> 最后解释为什么需要多个 Head
```

Day 006 开始时不直接引入 Q、K、V、Softmax、GQA、RoPE 或 KV Cache。先让 Attention 和 Head 获得清晰的直观含义，再逐步进入实现。
