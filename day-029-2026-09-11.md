# Day 029：视觉预训练——VLMDataset 数据组织 + train_pretrain_vlm 训练一步

- 日期：2026-09-11
- 学习阶段：第二阶段 `minimind-v`（视觉预训练）
- 当前状态：已完成（数据侧 + freeze 策略 + 训练循环 + 真实样本验证）
- 入口：`minimind-v/trainer/train_pretrain_vlm.py`、`minimind-v/dataset/lm_dataset.py::VLMDataset`、
  `minimind-v/trainer/trainer_utils.py::init_vlm_model`

## 一、pretrain_i2t.parquet 数据组织（真实探查）

```text
格式：Parquet 列式存储，255 个 row groups，懒加载按需读
总行数：1,274,698（约 127 万）
两列：
  conversations : String（对话，存成 JSON 字符串）
  image_bytes   : binary（一张 JPEG 的完整字节，head=FF D8 FF 即 JPEG 魔数）
```

一行 = 一个样本 = 一段对话 + 一张图。conversations 真实内容：

```text
[{"role":"user","content":"...write a detailed caption?<image>"},
 {"role":"assistant","content":"The image showcases a coastal scene ..."}]
```

要点：
- parquet 里存的是单个 `<image>`（紧凑）；运行时 `create_chat_prompt` 才 `.replace` 成 64 个 `<|image_pad|>`。
- image_bytes 是 JPEG 字节内嵌，不是路径。`__getitem__` 用 `Image.open(io.BytesIO(img))` 从内存解码。
- 图像已统一 resize 到 256×256 再编码 JPEG，每张仅几 KB。
- SFT 用的 `sft_i2t.parquet` 结构相同，只是对话是指令问答、可能多轮。

## 二、VLMDataset.__getitem__ 真实产出（探针验证）

对第 0 条样本实际运行，结果：

```text
bos_id = [1,1388,570,811,234] => '<|im_start|>assistant\n'   助手回答起点
eos_id = [2,234]              => '<|im_end|>\n'              回答终点
image_pad id = 12

图像坑：64 个连续 id=12，位置 21~84（紧跟 user 问句的 '?' 之后）
labels：总长 450，-100(不算loss) 178 个，算loss 272 个，区间 92~363
  - 位置 21~84（图像坑）labels 全是 -100  ← 图像坑不算 loss
  - 位置 92 起是 assistant 描述 "The image showcases..."，label=真实token
image_data['pixel_values'] shape = (1,3,256,256)
```

generate_labels 逻辑：默认全 -100 → 扫到 `<s>assistant\n` 起、`</s>\n` 止 →
只把这段区间的 labels 设为真实 token。即只对助手回答算 loss。

## 三、freeze 策略（init_vlm_model，本日核心）

```text
from_weight='llm' -> 加载第一阶段的 llm_768.pth（纯语言主干），strict=False
建模型后先无条件冻结"除 vision_proj 外的全部参数"
再按 freeze_llm 微调：
  0：解冻除 SigLIP 外全部（LLM 全训 + proj）
  1：额外解冻 LLM 首层 layers.0 和末层 layers.{last}（+ proj）
  2：什么都不加放 = 只训 vision_proj（预训练默认）
```

三部分状态（freeze_llm=2）：

```text
LLM 主干     第一阶段训好，冻结
SigLIP2      预训练权重，冻结（from_pretrained 时 requires_grad=False）
vision_proj  随机初始化，唯一可训练（约 1M）
```

冻结的双保险（关键理解）：

```text
requires_grad=False  -> 反向传播不算梯度（.grad 保持 None）
optimizer 用 filter(p.requires_grad) 只拿可训练参数 -> 优化器根本不碰冻结参数
两者都要做：只冻 requires_grad 而优化器仍持有全部参数时，
  AdamW 的 weight decay 会让 grad=0 的冻结参数也缓慢缩小，悄悄破坏 LLM。
一句话：冻结管梯度，filter 管更新，两个都做冻结才彻底。
```

## 四、train_pretrain_vlm 第 5 段（训练三大件准备）

```text
init_vlm_model(...)          三段式模型，只有 vision_proj 可训
VLMDataset(...)              127 万条，懒加载产出 (input_ids[450], labels[450], pixel_values)
DistributedSampler           多卡时切分数据，单卡为 None
GradScaler(enabled=fp16)     bfloat16 默认不缩放（空转），float16 才防梯度下溢
AdamW(filter(requires_grad)) 只把 vision_proj 交给优化器，lr=4e-4
vlm_collate_fn               堆叠成 [16,450] / [16,450] / {'pixel_values':[16,3,256,256]}
```

## 五、train_epoch 训练一步（带 batch=16 输入串）

```text
1. 搬 GPU + get_lr 余弦退火（4e-4 → 4e-5）写回 optimizer
2. forward：embed → (start_pos==0)图像注入 vision_proj+count_vision_proj覆盖坑
            → 8层 → lm_head → cross_entropy(ignore_index=-100)
   aux_loss dense 下为 0（含 *0 的 dummy gradient 供 DDP 占位）
3. backward：梯度从 loss(助手描述位置) 经 attention 流回 visual token → vision_proj
             LLM/SigLIP requires_grad=False，不算梯度
4. clip_grad_norm_(1.0) → scaler.step(AdamW 只更新 vision_proj) → zero_grad
5. 每100步打 loss；每1000步存 pretrain_vlm_768.pth
   保存时过滤 vision_encoder.*（SigLIP 不存），.half().cpu() 省体积
```

## 六、vision_proj 训练信号完整链（本阶段核心结论）

```text
loss 只在助手描述文字(位置92~363)计算
  → 要正确说出 "The image showcases a coastal scene..."
  → 这段描述的对象正是位置 21~84 的 64 个 visual token
  → 描述 token 通过 attention 回看 visual token
  → backward：梯度经 attention 流回 visual token → 流回 vision_proj
  → AdamW 只更新 vision_proj
图像坑本身 label=-100 不算 loss，但作为"被描述对象"让梯度间接流回。
LLM、SigLIP 冻结，绝大部分能力白嫖，只训中间薄翻译层 → "3 块钱训 VLM"。
```

## 七、验收

```text
[验收] parquet 两列结构（conversations JSON + image_bytes JPEG）与懒加载
[验收] <image> 在数据里是 1 个，运行时展开成 64 个 <|image_pad|>（真实位置 21~84）
[验收] generate_labels：只有 assistant 回答算 loss，图像坑与提问全 -100
[验收] freeze_llm=0/1/2 差异；预训练用 2 只训 vision_proj
[验收] 冻结双保险：requires_grad 管梯度 + filter 管更新，缺一不可
[验收] train_epoch 一步：forward→backward(梯度只到proj)→AdamW 只更新 proj
[验收] 保存时为何过滤 vision_encoder；与推理侧 strict=False 的呼应
[验收] vision_proj 训练信号链：loss→attention→visual token→proj
```

## 八、环境备注

- minimind 的 uv venv 缺 PIL，本日用 `uv pip install pillow` 补装（pillow 12.3.0）后
  跑通 VLMDataset 真实探查。探针脚本已删除，未改动仓库源码。

## 九、下一天计划

进入视觉指令微调，对比预训练：

```text
minimind-v/trainer/train_sft_vlm.py
```

重点：

```text
train_sft_vlm 相比 train_pretrain_vlm 改了什么（默认 freeze_llm、数据、from_weight）
freeze_llm=1 折中策略：解冻 LLM 首末层 + proj 的动机
SFT 数据 sft_i2t.parquet 的指令问答形式与多轮对 labels 的影响
预训练(对齐 proj) → SFT(解冻部分 LLM 学指令跟随) 的两阶段分工
```
