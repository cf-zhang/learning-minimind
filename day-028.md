# Day 028：minimind-v 整体认识 + eval_vlm.py 推理链

- 日期：2026-09-10
- 学习阶段：第二阶段 `minimind-v`（视觉语言模型）
- 当前状态：已完成（整体地图 + 推理调用链验收）
- 入口：`minimind-v/eval_vlm.py`、`minimind-v/model/model_vlm.py`

## 一、仓库整体认识

MiniMind-V 是纯语言模型 MiniMind 的视觉扩展：在已训练好的 LLM 基础上，只新增
「视觉编码器 + 特征投影」两个子模块，核心算法改动 < 50 行。

```text
图像 ──> Vision Encoder ──> Projector ──> visual token ──┐
                                                          ├─> MiniMind LLM ──> 文本回答
文本 ──> tokenizer ──> text token ────────────────────────┘
```

三段式关键点：

```text
Vision Encoder：SigLIP2 (siglip2-base-p32-256-ve)，全程冻结，只提特征
                256×256 图 / patch32 = 8×8 = 64 个 patch token，每个 768 维
Projector：LayerNorm + 2层MLP(Linear→GELU→Linear)，把视觉特征投影到 LLM 隐藏空间
           产出 64 个 visual token，这是唯一新增、需要训练的模块（~1M）
image_pad 占位：prompt 里用 64 个 <|image_pad|> 占位符挖坑，模型内部把 visual token 填进坑
```

参数账：LLM ~64M + Projector ~1M = 可训练 65M；SigLIP2 ~95M 冻结不训。

目录地图（★ 为视觉新增，其余复用第一阶段）：

```text
minimind-v/
├── eval_vlm.py                    推理入口（本日已走完）
├── model/
│   ├── model_minimind.py          复用的语言主干
│   ├── model_vlm.py          ★★  VLMConfig + MiniMindVLM + MMVisionProjector
│   └── siglip2-base-p32-256-ve/   视觉编码器权重（冻结）
├── dataset/lm_dataset.py     ★    VLMDataset：pixel_values / 多模态 labels
├── trainer/
│   ├── train_pretrain_vlm.py ★    视觉预训练（对齐 projector）
│   └── train_sft_vlm.py      ★    视觉指令微调（freeze_llm 策略）
└── scripts/                       convert_vlm.py / web_demo_vlm.py
```

## 二、eval_vlm.py 推理链（逐块走完）

### init_model（装载三件套）

```text
AutoTokenizer.from_pretrained('model')            # 复用第一阶段 tokenizer，词表 6400
MiniMindVLM(VLMConfig(...), vision_model_path)：
  super().__init__()          -> 建 LLM 主干（复用，随机初始化待加载）
  get_vision_model(path)      -> 加载冻结 SigLIP2（真实权重）+ processor
  MMVisionProjector(768,768)  -> 新建投影层（随机初始化待加载）
torch.load('./out/sft_vlm_768.pth')
load_state_dict(过滤 'mask' 键, strict=False)      # 只加载 LLM + Projector
eval() + half() -> 返回 model / tokenizer / processor
```

要点：
- checkpoint 只存会变的部分（LLM + Projector），不存冻结的 SigLIP2。
- `strict=False` 容忍「模型有、checkpoint 没有」的键（SigLIP2 参数、重新生成的 mask）。
- mask 是注意力里的固定下三角常量，不是学习参数，构造时重新生成。

### 图像那一路

```text
Image.open().convert('RGB')
image2tensor(image, processor)：SigLIP processor 做 resize(256×256整张缩放,非裁剪)
                                + 归一化 + 转张量
-> {'pixel_values': [1,3,256,256]}   规范化像素（此刻还没编码，无语义）
```

### 文本那一路

```text
prompt "<image>\n..."
.replace('<image>', '<|image_pad|>' * 64)          # 挖 64 个坑
apply_chat_template(...)                            # 套对话模板，<|image_pad|> 当普通文本
tokenizer(...)                                      # 编码
-> input_ids 里出现连续 64 个 id=12（VLMConfig.image_ids=[12]）
```

### generate -> forward（图像注入）

```text
model.generate(..., pixel_values=pixel_values)      # pixel_values 透传进每次 forward
forward:
  hidden_states = embed_tokens(input_ids)           # [1,seq,768]，64 个坑是无意义占位词向量
  if pixel_values is not None and start_pos == 0:   # 只在第一次前向注入
      get_image_embeddings(pixel_values, SigLIP)    # no_grad, [1,3,256,256]->[1,64,768] 视觉特征
      vision_proj(...)                              # [1,64,768] 投影到 LLM 语义空间(可训练)
      count_vision_proj(...)                        # 用 64 个 visual token 覆盖 64 个坑
  之后加 RoPE -> 8 层 transformer -> norm -> lm_head  # 与纯文本完全一致
```

`count_vision_proj` 核心（torch.cat 三段重拼，序列总长不变）：

```text
hb[:start] + vf[b][k][:i-start] + hb[i:]
坑前文本   +   64个 visual token  + 坑后文本
marker=12 定位坑；k 记录用到第几张图（多图时递增）
```

## 三、关键理解

- 图像只在 `start_pos == 0`（第一次前向）注入一次，之后靠 KV Cache 回看；去掉这个条件
  不会报错，但每步重复跑 SigLIP（浪费），且后续单 token 序列里无坑，填坑循环空转。
- 为什么必须过 SigLIP + vision_proj，不能直接拿像素：

```text
像素 [3,256,256]  只是亮度数字，无语义       —— LLM 读不懂
    ↓ SigLIP（冻结，像素→语义）
视觉特征 [64,768]  SigLIP 视觉空间的语义      —— 坐标系和 LLM 对不上
    ↓ vision_proj（可训练，视觉语义→文本语义）
visual token [64,768]  LLM 文本空间的语义     —— LLM 能懂
```

- SigLIP 是预训练好的「视觉翻译官」（冻结白嫖）；vision_proj 是唯一要新学的薄翻译层，
  把视觉语义对齐到 MiniMind 文本空间。这就是「3 块钱训出 VLM」的本质：绝大部分能力
  复用 SigLIP + 已有 LLM，只训中间的对齐层。

## 四、验收

```text
[验收] 三段式结构与各自职责（Vision Encoder / Projector / LLM）
[验收] init_model 为什么 strict=False + 过滤 mask，checkpoint 存什么
[验收] <image> -> 64 个 <|image_pad|> -> input_ids 里 64 个 id=12 的对齐链
[验收] 图像注入位置（embedding 后、transformer 前）与 start_pos==0 的作用
[验收] count_vision_proj 用 torch.cat 覆盖占位词向量、序列长度不变
[验收] 为什么不能直接用像素：SigLIP 做像素->语义，vision_proj 做跨模态对齐
```

## 五、下一天计划

已知推理时 vision_proj 负责跨模态对齐，下一天进入「如何训出这层翻译器」：

```text
minimind-v/trainer/train_pretrain_vlm.py   视觉预训练：基础对齐
minimind-v/dataset/lm_dataset.py::VLMDataset
```

重点：

```text
VLMDataset 如何产出 pixel_values 和 input_ids（训练侧的挖坑与图像加载）
多模态 labels 如何构造（哪些位置算 loss，image_pad 位置是否 -100 忽略）
train_pretrain_vlm 的 freeze 策略：冻结 LLM + SigLIP，只训 vision_proj
预训练任务形式（"请描述这张图片"长描述）为什么能对齐视觉与语言 token
```

之后再进 `train_sft_vlm.py`，对比 `freeze_llm=0/1/2` 三种解冻策略的差异。
