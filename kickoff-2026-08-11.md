# 学习启动记录：建立 MiniMind 仓库驱动学习计划

- 日期：2026-08-11
- 学习阶段：准备阶段
- 当前主仓库：`minimind`
- 后续仓库：`minimind-v`、`minimind-o`
- 今日主题：明确学习背景、目标、方法、路线与验收标准

## 零、归档定位与真实决策过程

本文件是课程启动档案，不计作 Day 001。这里没有把仓库扫描或学习计划误记为学习者已经掌握的模型知识。

启动过程按真实问题推进如下。

### 第 1 轮：先确认学习材料

学习者说明当前目录放有三个开源仓库，要求先阅读仓库内容再继续交流。助手据此检查三个项目的 README、目录和核心模型文件，确认它们分别面向纯文本、视觉语言和全模态方向。

这一轮只建立材料地图：

```text
minimind   -> 语言模型底座
minimind-v -> 在语言模型上接入图像
minimind-o -> 在语言模型上继续接入图像和语音
```

状态：`[环境]` 仓库边界已确认；不代表学习者已经理解其中模型结构。

### 第 2 轮：确定学完能达到什么水平

学习者的问题是：通过这三个仓库学习，最终能达到什么水平？

讨论后的目标不是“会调用 API”，而是分层定义：

```text
先能运行和观察
  -> 能解释数据流和张量 shape
  -> 能读懂并修改核心模型
  -> 能完成训练与后训练实验
  -> 能脱离原仓库实现一个简化模型
  -> 能把理解迁移到同类仓库
```

这成为后文“吃透 MiniMind”的验收标准来源。

### 第 3 轮：估算学习时间

学习者说明自己具备编程基础，但没有数学和模型基础，询问整个过程需要多久。

基于每周约 8 至 10 小时，初步估算 MiniMind 主线需要约 5 至 7 个月。这个数字只是容量规划，不是截止日期。实际进度服从以下原则：

```text
理解和实践验收优先于日历进度
```

### 第 4 轮：学习者确定仓库驱动方式

学习者提出自己的方法：直接跟随仓库，以“吃透 MiniMind”为目标；遇到障碍时再补数学、PyTorch 和模型知识，一点点把整个仓库吃下来。

这一建议成为课程正式方法：

```text
仓库代码提出问题
  -> 只补当前问题需要的基础
  -> 用小实验验证
  -> 立即返回仓库主线
```

不先脱离项目学习数月抽象课程，也不跳过必要前置知识硬读源码。

### 第 5 轮：建立每日学习档案

学习者确认这种过程可行，并要求助手持续带领学习；学习过程按天记录，每个学习日一个 Markdown 文件，背景、目标和计划必须清晰。

由此创建 `learning-journal/`。后续又进一步明确：Day 按真正发生的学习计算，只有安装环境、代跑程序或整理文档不新增 Day 编号。

本次重新归档后，每份 Day 文档还必须保留逐轮互动、学习者实际输出、结果解释、纠偏和准确恢复点。统一规则见 [学习档案总索引](README.md)。

## 一、学习背景

当前目录包含三个相互关联的开源仓库：

```text
minimind/
minimind-v/
minimind-o/
```

学习者具备编程基础，但此前没有系统学习数学、机器学习和模型原理。目标不是只会调用现成大模型 API，也不是只按照 README 复制训练命令，而是以真实仓库为主线，一边阅读、运行和修改代码，一边补齐当前问题所需的数学、PyTorch和模型知识。

三个仓库构成了一条逐步扩展的学习路线：

```text
MiniMind
  纯文本语言模型：文本输入 -> 文本输出
  Pretrain / SFT / LoRA / Distillation / DPO / PPO / GRPO / Agentic RL
       |
       +-- MiniMind-V
       |     视觉语言模型：文本 + 图像输入 -> 文本输出
       |     SigLIP2 -> Vision Projector -> MiniMind
       |
       +-- MiniMind-O
             Omni 模型：文本 / 图像 /语音输入 -> 文本 + 流式语音输出
             Audio/Vision Encoder -> Thinker -> Bridge -> Talker -> Mimi Codes
```

其中，`minimind` 是语言模型底座，也是第一阶段唯一需要关注的主线。`minimind-v` 和 `minimind-o` 暂时只作为后续方向存在，在没有吃透语言模型之前不并行展开。

## 二、已经确认的仓库情况

### 1. MiniMind

`minimind` 是整个系列的语言底座。当前默认结构是一个小型 Decoder-only Transformer，主要特征包括：

- 默认词表大小为 6400。
- 默认隐藏维度为 768。
- 默认包含 8 个 Transformer Block。
- 使用 8 个 Query 注意力头和 4 个 Key/Value 头，即 GQA。
- 使用 RMSNorm、RoPE/YaRN、SwiGLU、KV Cache。
- 支持 Dense 和 MoE 两种前馈网络。
- 训练链路包含 Tokenizer、Pretrain、SFT、LoRA、蒸馏、DPO、PPO、GRPO 和 Agentic RL。
- 推理侧包含命令行推理、OpenAI 兼容 API、WebUI 和 Tool Call。

核心模型文件：

```text
minimind/model/model_minimind.py
```

### 2. MiniMind-V

`minimind-v` 在 MiniMind 语言模型前接入冻结的 SigLIP2 视觉编码器。图片被编码成视觉特征，经 MLP Projector 映射到语言模型隐藏空间，然后替换输入序列里的 `<|image_pad|>` 占位 token embedding，最终仍由语言模型生成文本。

核心模型文件：

```text
minimind-v/model/model_vlm.py
```

### 3. MiniMind-O

`minimind-o` 在语言主干上同时加入视觉和语音输入，并增加独立 Talker 生成音频 token。Thinker 负责理解和文本生成，Talker 接收 Thinker 中间层的语义表示，预测 8 路 Mimi audio codebook，最终解码成流式语音。

核心模型文件：

```text
minimind-o/model/model_omni.py
```

### 4. 三个仓库的关系

三个仓库当前各自保存了一份 `model_minimind.py`，而且三份文件内容完全一致。这说明 MiniMind-V 和 MiniMind-O 是以复制语言底座代码的方式扩展，而不是运行时依赖 `minimind` Python 包。

当前本地目录主要包含源码、Tokenizer 和少量评估资源，没有训练数据、发布模型权重以及 SigLIP2、SenseVoice、Mimi 等外部模型。因此当前可以进行代码阅读和轻量代码实验，正式推理和训练前还需要按具体阶段准备相应资源。

## 三、学习总目标

总目标定义为：

> 以 `minimind` 为主线，从实际代码出发掌握小型语言模型的结构、数据、训练、后训练、推理和部署；最终能够脱离仓库重新实现一个简化版 MiniMind，并能独立阅读、修改、实验和排查同类模型代码。完成语言底座后，再依次学习 `minimind-v` 和 `minimind-o`。

这里的“吃透”不等于读完全部文件，也不等于成功运行 README 命令。只有达到以下标准，才算完成 `minimind` 阶段：

1. 能画出原始文本从 Tokenizer 到模型输出回答的完整数据流。
2. 能解释核心模型各模块的输入、输出、张量 shape、参数和作用。
3. 能解释 Embedding、Attention、GQA、RoPE、RMSNorm、SwiGLU、残差连接、KV Cache 和 MoE。
4. 能解释 next-token prediction、交叉熵、label shift 和 loss mask。
5. 能区分并运行 Pretrain、SFT、LoRA、蒸馏、DPO、GRPO 等训练阶段。
6. 能准备自己的数据并接入训练流程。
7. 能根据 loss、梯度、显存和生成样例排查常见问题。
8. 能修改层数、隐藏维度、注意力头、上下文长度或 MoE 配置，并预测修改影响。
9. 能解释模型训练与自回归推理之间的差异。
10. 能不参考原实现，从空文件写出一个简化但可训练、可生成的 MiniMind Lite。
11. 能设计一个包含基线、假设、实验和结论的模型改造实验。
12. 能把在 MiniMind 中建立的理解迁移到 Llama、Qwen 等相近模型代码。

## 四、学习方法

采用“仓库驱动、问题触发、即时补课”的方式。

### 1. 仓库是主线

不先脱离项目学习数月数学或机器学习课程。直接沿着 MiniMind 的数据流和代码结构前进，在遇到知识障碍时暂停主线，补足理解当前代码所需的最小知识，然后立刻返回仓库。

例如，读到注意力代码中的矩阵乘法时，再集中补充：

- Tensor shape。
- 向量、矩阵和矩阵乘法。
- Q、K、V 的含义。
- softmax 的作用。
- 为什么 attention score 要除以 `sqrt(head_dim)`。

补课范围以“能够解释、运行和修改当前代码”为准，不无边界扩展。

### 2. 每个知识点通过四层验证

```text
能够读懂
    ↓
能够用自己的话解释
    ↓
能够运行小实验验证
    ↓
能够脱离原代码重新实现
```

只产生“好像看懂了”的感觉不算完成。学习过程中会持续使用预测输出、手写 shape、局部改动、最小实验和复述来检验理解。

### 3. 不平均阅读所有文件

不同文件采用不同深度：

- `model/model_minimind.py`：核心代码，要求逐模块深入理解。
- `dataset/lm_dataset.py`：重点理解数据格式、Tokenization 和 label mask。
- Pretrain/SFT：要求理解并独立运行完整训练链路。
- LoRA、蒸馏、DPO、GRPO：要求理解目标、数据和损失实现。
- PPO、Agentic RL、Rollout Engine：后期深入。
- API、WebUI、模型转换：以掌握工程数据流和接口为主。
- README 和辅助脚本：按当前任务选择性阅读。

### 4. 以小实验获得快速反馈

学习阶段优先使用：

- 极小模型配置。
- 极小数据集。
- 能快速过拟合的数据样本。
- 可打印和检查的短序列。
- 单变量对照实验。

首要目标是验证理解，不是一开始训练高质量聊天模型。

### 5. 按天记录

从今天开始，每个学习日创建一个独立 Markdown 文件，命名规则为：

```text
learning-journal/day-NNN-YYYY-MM-DD.md
```

每天的日志至少包含：

- 当日目标。
- 阅读的文件和代码范围。
- 新知识与关键概念。
- 重要张量 shape 或数据流。
- 实际运行的实验。
- 遇到的问题和解决过程。
- 当日练习及验收结果。
- 尚未解决的知识欠账。
- 下一学习日的入口。

日志记录真实学习进度，不把计划中的内容写成已经掌握的内容。

## 五、整体学习计划

### 阶段 0：建立代码地图

目标：了解仓库边界、三个项目的关系以及 MiniMind 的主要入口。

主要内容：

- README 与目录结构。
- 模型、数据集、训练器和推理脚本的职责。
- 三个仓库之间的继承关系。
- 建立总体学习计划和验收标准。

状态：今天已完成。

### 阶段 1：打通 MiniMind 推理链路

目标：理解一段文本如何变成 token，并通过模型逐个生成回答。

代码范围：

```text
minimind/eval_llm.py
minimind/model/model_minimind.py::MiniMindConfig
minimind/model/model_minimind.py::MiniMindForCausalLM
minimind/model/model_minimind.py::generate
```

需要回答：

1. 文本如何变成 `input_ids`？
2. `input_ids` 的每个维度表示什么？
3. 模型输出的 `logits` 是什么？
4. 为什么 logits 最后一维等于词表大小？
5. temperature、top-k、top-p 如何影响采样？
6. 为什么生成回答需要循环预测？
7. KV Cache 在循环中保存了什么？

### 阶段 2：吃透 Dense 模型结构

建议阅读顺序：

1. `MiniMindConfig`
2. `RMSNorm`
3. `FeedForward`
4. `Attention`
5. `MiniMindBlock`
6. `MiniMindModel`
7. `MiniMindForCausalLM`
8. `generate`

按需补充：

- Tensor、shape、broadcast、view 和 transpose。
- 向量、矩阵和矩阵乘法。
- 均值、方差和归一化。
- softmax 与概率分布。
- Q、K、V 和 scaled dot-product attention。
- causal mask、多头注意力和 GQA。
- RoPE、残差连接、RMSNorm 和 SwiGLU。
- 自回归生成和 KV Cache。

阶段验收：能够从 `[batch, sequence]` 的 token ID 开始，逐层写出关键张量 shape，直到得到 `[batch, sequence, vocab_size]` 的 logits。

### 阶段 3：MoE

目标：理解 Dense FFN 如何扩展为稀疏专家模型。

主要内容：

- Router 和 expert。
- Top-K 选择。
- token 到 expert 的分派。
- 激活参数量与总参数量。
- 负载均衡辅助损失。
- Dense 与 MoE 的速度、显存和容量差异。

阶段验收：能解释每个 token 如何选择 expert，并完成 Dense/MoE 的参数量和前向行为对比实验。

### 阶段 4：数据与预训练

代码范围：

```text
minimind/dataset/lm_dataset.py::PretrainDataset
minimind/trainer/train_pretrain.py
minimind/trainer/trainer_utils.py
```

主要内容：

- 原始文本、Tokenizer 与训练序列。
- next-token prediction。
- 输入和标签错位。
- padding、attention mask 和 `ignore_index=-100`。
- DataLoader、batch、epoch 和 step。
- forward、loss、backward 和 optimizer step。
- AdamW、学习率、梯度累积、梯度裁剪和混合精度。
- checkpoint 保存与恢复。

阶段验收：在极小数据上训练极小模型并成功过拟合，同时能解释一次参数更新的完整过程。

### 阶段 5：SFT 与对话数据

代码范围：

```text
minimind/dataset/lm_dataset.py::SFTDataset
minimind/trainer/train_full_sft.py
model/tokenizer_config.json
```

主要内容：

- Chat Template。
- system/user/assistant 消息格式。
- assistant-only loss mask。
- 多轮对话。
- `<think>`、`<tool_call>` 和 `<tool_response>`。
- Pretrain 与 SFT 的目标差异。

阶段验收：构造自己的小型 SFT 数据，逐 token 检查 input、label 和 mask，并完成一次可解释的微调。

### 阶段 6：LoRA 与蒸馏

主要内容：

- 全量微调与参数高效微调。
- 低秩矩阵的直觉和实现。
- LoRA 注入、保存、加载与合并。
- Teacher/Student。
- hard target、soft target、temperature 和蒸馏损失。

阶段验收：完成 LoRA 微调及权重合并，并比较全量微调、LoRA 和蒸馏的参数量、显存和输出变化。

### 阶段 7：偏好学习与强化学习

建议顺序：

```text
DPO -> GRPO -> PPO -> Agentic RL
```

主要内容：

- chosen/rejected 偏好数据。
- policy model 与 reference model。
- reward、KL、advantage 和 policy update。
- rollout 与生成后端。
- 多轮 Tool Use 环境交互。
- RLAIF 与 Agentic RL。

阶段验收：能够对每种方法解释“输入数据、所需模型、损失目标、资源开销和行为变化”，并至少完成 DPO 或 GRPO 的小型实验。

### 阶段 8：推理服务与工程链路

主要内容：

- checkpoint 与 Transformers 格式。
- 模型转换和 LoRA 合并。
- OpenAI 兼容 API。
- 流式输出。
- Tool Call 解析与执行。
- WebUI。
- DDP 与外部 rollout engine 的基本职责。

阶段验收：能从权重加载开始，完整解释请求如何经过服务端、Tokenizer、模型生成和流式响应返回客户端。

### 阶段 9：脱离仓库复现

目标：从空文件实现 MiniMind Lite。

最低范围：

- Config。
- Token Embedding。
- RMSNorm。
- RoPE。
- Causal Self-Attention。
- SwiGLU FFN。
- Transformer Block。
- Causal LM loss。
- 自回归生成。
- 最小训练循环。

阶段验收：实现可训练、可过拟合、可生成的最小语言模型，并能将每个模块映射回原仓库。

### 阶段 10：MiniMind-V 与 MiniMind-O

只有完成语言模型主线后才进入：

1. MiniMind-V：视觉编码、projector、图像占位 token、冻结策略和图文训练。
2. MiniMind-O：语音编码、Thinker、Bridge、Talker、Mimi codes、流式生成和 VAD。

这两个仓库继续沿用相同方法：代码驱动、即时补课、小实验验证、按日记录。

## 六、预计周期

按照每周约 8 至 10 小时估算：

- 建立地图与推理链路：1 至 2 周。
- Dense MiniMind 模型结构：4 至 6 周。
- 数据、Pretrain 和 SFT：3 至 4 周。
- MoE、LoRA、蒸馏、DPO 和 GRPO：4 至 6 周。
- PPO、Agentic RL 与 rollout：3 至 5 周。
- 工程链路与综合整理：2 至 3 周。
- 脱离源码复现与最终实验：3 至 4 周。

预计 5 至 7 个月完成 MiniMind 主线。实际进度以理解和实验验收为准，不以阅读行数或日历时间为准。完成 MiniMind-V 和 MiniMind-O 还需要继续投入时间。

## 七、每日协作方式

每个学习日采用以下循环：

1. 明确当天只解决哪一个小目标。
2. 标记需要阅读的精确文件、类或函数。
3. 学习者先阅读、预测或尝试解释。
4. 根据暴露的问题补充最少必要知识。
5. 运行小实验，打印数据、shape 或中间结果。
6. 学习者用自己的话重新解释。
7. 完成一个小练习或局部代码修改。
8. 根据明确标准验收。
9. 更新当日 Markdown 日志和知识欠账。
10. 写明下一天从哪里继续。

在学习过程中，助手负责：

- 控制学习范围，避免一次引入过多概念。
- 解释代码和必要理论。
- 设计最小实验与练习。
- 检查理解中的错误或模糊点。
- 帮助排查环境、数据、训练和模型问题。
- 维护学习日志的结构和连续性。

学习者负责：

- 主动阅读和运行代码。
- 在不确定时明确提出问题。
- 尝试用自己的语言解释，而不是只确认“看懂了”。
- 完成必要的小练习和实验。
- 如实记录仍然不理解的内容。

## 八、今日成果

今天完成的是准备工作，而不是模型知识课程：

- 确认了三个仓库的定位和关系。
- 确认先吃透 `minimind`，再进入 `minimind-v` 和 `minimind-o`。
- 选择仓库驱动、遇到问题即时补课的学习方式。
- 定义了“吃透 MiniMind”的可验证标准。
- 建立了按阶段推进的学习路线。
- 建立了每日一个 Markdown 文件的记录规则。
- 明确了学习者与助手的协作方式。

## 九、当前进度

```text
[完成] 阶段 0：仓库概览与学习计划
[待开始] 阶段 1：MiniMind 推理链路
[未开始] 阶段 2：Dense 模型结构
[未开始] 阶段 3：MoE
[未开始] 阶段 4：数据与预训练
[未开始] 阶段 5：SFT
[未开始] 阶段 6：LoRA 与蒸馏
[未开始] 阶段 7：偏好学习与强化学习
[未开始] 阶段 8：工程链路
[未开始] 阶段 9：脱离仓库复现
[未开始] 阶段 10：MiniMind-V 与 MiniMind-O
```

## 十、下一学习日入口

Day 001 的唯一目标是：

> 建立 MiniMind 的推理数据流，理解一段文本如何变成 `input_ids`，模型如何产生 logits，以及 `generate` 如何循环选择下一个 token。

第一批阅读范围：

```text
minimind/eval_llm.py
minimind/model/model_minimind.py::MiniMindConfig
minimind/model/model_minimind.py::MiniMindForCausalLM
minimind/model/model_minimind.py::generate
```

Day 001 首先需要回答：

1. Tokenizer 接收什么，返回什么？
2. `input_ids` 是什么类型和 shape？
3. logits 表示什么？
4. 为什么 logits 的最后一维是 6400？
5. `generate` 为什么是一个循环？
6. temperature、top-k 和 top-p 分别改变了什么？

这些问题不要求提前准备。下一次从 `eval_llm.py` 的模型和 Tokenizer 初始化入口开始，遇到 Tensor、概率或模型概念时再即时补充。
