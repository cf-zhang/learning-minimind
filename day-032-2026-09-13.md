# Day 032：minimind-o 整体认识 + eval_omni.py 推理链

- 日期：2026-09-13
- 学习阶段：第三阶段 `minimind-o`（Omni 模型）
- 当前状态：已完成（整体地图 + eval_omni.py mode=0 逐行推理链）
- 入口：`minimind-o/eval_omni.py`、`minimind-o/model/model_omni.py`

## 一、仓库整体认识

系列第三站：单一 ~0.1B 权重，同时支持 文/音/图 三模态输入，输出 文本 + 流式语音。
端到端 Omni：语音与文本在 hidden state 层面直接连通，不走 ASR→LLM→TTS 级联。

```text
minimind    文本进 → 文本出
minimind-v  文本+图像进 → 文本出
minimind-o  文本+图像+语音进 → 文本+语音出（新增：语音输入 + 语音输出）
```

最大新增是"语音输出"，带来 Thinker–Talker 双路径。

## 二、核心结构：Thinker–Talker 双路径

```text
文本 ─────────────┐
语音→SenseVoice→AudioProj─┤  Thinker（理解+想）= MiniMind主干+多模态注入 → 文本回复
图像→SigLIP→VisionProj───┘        │ 中间层 Bridge 引出语义表征
                                   ↓
                          Talker（说）：在Thinker语义条件上用MTP一次预测多层Mimi codes
                                   ↓
                          Mimi 解码器 → 24kHz 流式语音
```

分工：
```text
Thinker：接收文本/语音/图像，产出语义层面的文本回复（文本 hidden + logits）
         本质=前两阶段的 LLM 主干 + 多模态输入注入
Talker： 拿 Thinker 中间层表征当条件，生成 audio code（不是直接生成波形）——全新
```

## 三、关键新术语

```text
Mimi        音频编解码器，语音↔离散code（8层codebook, 12.5Hz, 24kHz）
            类比：Mimi之于语音 ≈ tokenizer之于文本
audio code  语音经Mimi编码的离散token，8层，Talker的预测目标
MTP         Multi-Token Prediction，一次同时预测8层code而非逐层，加速流式
SenseVoice  语音输入编码器（冻结）→ AudioProj 注入 Thinker
SigLIP2     视觉编码器（冻结，复用 minimind-v）
Bridge      Thinker取"中间层"hidden传给Talker（默认 num_layers//2-1）
            不用embedding层(语义不足)也不用最后层(太贴近文本预测目标)
Speaker/CAMPPlus  音色编码器，支持音色克隆
VAD         语音活动检测，支持边听边答/打断/近双工
```

## 四、为什么先生成 audio code 而非波形

```text
波形连续超高密度：24kHz=每秒24000采样点，直接逐点预测序列过长无法建模
Mimi 压缩：1秒语音 → 12.5Hz×8层 = 每秒仅100个code（压缩几百倍）
模型只需像预测文本token一样预测离散code，再交Mimi解码回波形
类比：文本(预测token→解码字) / 语音(预测code→Mimi解码波形)
离散化让"生成连续信号"变成"预测离散token"，模型才建模得动。
```

## 五、输入侧对称性（复用 minimind-v）

```text
minimind-v：图像→SigLIP(冻结)→VisionProj→填 <image_pad> 坑
minimind-o：图像→SigLIP(冻结)→VisionProj→填坑（完全复用）
           语音→SenseVoice(冻结)→AudioProj→填坑（同套模式，换编码器）
三模态输入的占位符挖坑机制统一（image_special_token / audio_special_token × token_len）。
真正全新的是输出侧 Talker + Mimi + MTP。
```

## 六、eval_omni.py mode=0 逐行推理链（text → 文本+语音）

### init_model
```text
AutoTokenizer.from_pretrained('model')                        复用分词器
MiniMindOmni(OmniConfig, audio_encoder_path, vision_model_path)
  → 建 Thinker(LLM主干) + SenseVoice(冻结) + SigLIP(冻结) + AudioProj + VisionProj + Talker
load_state_dict(sft_omni_768.pth, strict=False)               载入 Thinker+两proj+Talker
  strict=False 容忍冻结的 SenseVoice/SigLIP/Mimi 不在 ckpt（同 minimind-v）
model.mimi_model = MimiModel.from_pretrained('./model/mimi')  挂载音频编解码器
half().eval().to(device)
```

### eval_sample（prompt="Tell me an interesting fact about space."）
```text
messages = [{"role":"user","content":prompt}]                 history=None
apply_chat_template(...add_generation_prompt=True)            套模板，纯文本无坑
x = tokenizer(inputs_text).input_ids[None,...]                [1,seq]

res_y = model.generate(x, eos, stream=True, return_audio_codes=True,
                       audio_inputs=None, pixel_values=None, ...)
  ★ 与前两阶段最本质差别：generate 不只吐文本，而是同时驱动
    Thinker(出文本token) + Talker(出audio code)，流式迭代器逐步产出 (y, audio_frame)

for y, audio_frame in res_y:
  y          → tokenizer.decode → 增量打印文本（answer[history_idx:]，'�'半字符先不打印）
  audio_frame→ 每帧8个code，attend 进 audio_frames
```

### 音频解码（code → 波形）
```text
codes = [f for f in audio_frames if len(f)==8]   只保留完整8层帧
mimi_codes = tensor(codes).T.unsqueeze(0)         [T,8]→[8,T]→[1,8,T]
             Mimi解码器要求 [batch, 8层codebook, 时间帧]
filtered = where(codes>=2049, 0, codes)           过滤特殊code(结束符等)防噪声
audio = mimi_model.decode(filtered).audio_values  [1,8,T]离散code → 连续波形
sf.write(..., 24000) → mp3                         存24kHz语音
  len(f)==8 坐实"8层codebook"：每帧语音用8个离散code联合表示（多层量化）
```

### 整条链
```text
prompt文本 → template → input_ids[1,seq]
→ generate(stream, return_audio_codes)：Thinker出文本 + Talker出code 流式并行
→ 文本：decode增量打印
→ 音频：攒T帧×8code → [1,8,T] → mimi.decode → 24kHz波形 → mp3
```

其他模式（mode=2音频输入/4图像输入/5混合）只是 prompt 多挖 audio/image 占位符坑、
多传 audio_inputs/pixel_values，注入机制与 minimind-v 对称，generate 主体一致。

## 七、验收

```text
[验收] Thinker/Talker 分工（理解出文本 / 语义条件上出audio code）
[验收] 为何先生成audio code而非波形（离散化压缩，模型才建模得动）
[验收] Mimi 角色=语音的tokenizer+解码器；8层codebook含义
[验收] 输入侧三模态占位符挖坑机制统一，复用 minimind-v 套路
[验收] generate 与前两阶段的本质差别：流式同时产出文本token与audio frame
[验收] 音频解码：帧×8code → [1,8,T] → mimi.decode → 24kHz波形
```

## 八、下一天计划

核心新增全部指向 `model.generate` 内部的 Thinker–Talker 协作。下一小步进入
`minimind-o/model/model_omni.py`，重点：

```text
OmniConfig / MiniMindOmni 结构（相对 VLMConfig/MiniMindVLM 新增了什么）
Thinker 多模态输入注入：audio/vision projector 如何填坑（对照 count_vision_proj）
Bridge：中间层 hidden 如何取出并传给 Talker（bridge_layer = num_layers//2-1）
Talker + MTP audio head：如何在语义条件上一次预测 8 层 Mimi code
generate 内部：Thinker 与 Talker 在同一循环中如何协作产出 (文本, audio_frame)
```
