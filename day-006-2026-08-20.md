# Day 006：Attention 与 Head 的直觉

- 日期：2026-08-20 至 2026-08-21（跨自然日完成，仍计一个学习日）
- 学习阶段：Transformer 的最小前置知识
- 今日主题：Attention 为什么存在、它做什么，以及 Head 的直观含义
- 当前状态：已完成
- 配套实验：[Day 006 Attention Notebook](notebooks/day-006-attention.ipynb)

## 零、如何使用本课

本课仍在进行，文档只记录到最后一个真实交互点。复习时必须按关卡停顿：先理解一个小概念，再完成确认或计算，不能直接把整篇读完当作掌握。

前置知识只有两项：

```text
Embedding 给每个 token 一个初始向量             Day 001
普通 Linear 使用同一套参数独立加工每个 token    Day 002
```

本课暂时用二维人工向量建立直觉，不要求理解 MiniMind 的 768 维真实向量，也不进入 Q/K/V 和源码。

## 逐轮互动档案

### 第 1 轮：从遗留问题进入 Attention

学习者在 Day 005 收口后要求开始 Day 006。目标来自 Day 002 留下的原始问题：

> Attention 是什么？Attention Head 又是什么？

本次没有先展示公式，而是复用已验证事实：两个 token 一起经过普通 Linear，与分别经过 Linear 的结果完全相同。因此普通 Linear 只能各自加工，不能让当前 token 读取另一个 token 的信息。

用两句话中的“苹果”说明为什么仅有独立加工不够：

```text
我吃了一个苹果
我买了一部苹果手机
```

同一个“苹果”需要结合“吃了”或“手机”形成不同语境表示。Attention 的最低任务由此定义为：

```text
让当前 token 有选择地读取其他可见 token 的信息，
再用读到的信息更新当前 token 的表示。
```

学习者回复“讲清楚了”。

状态：`[验收]` 已理解 Attention 为什么存在。

### 第 2 轮：“有选择地读取”是什么意思

仍以“我 吃了 一个 苹果”为例，当前要更新“苹果”。人工给四个位置分配读取比例：

```text
我      10%
吃了    60%
一个    10%
苹果    20%
```

Attention 不是只能选择一个位置，也不是平均读取所有位置；它可以从每个可见位置读取不同的比例，再合成一份信息。

学习者回复“理解”。

状态：`[验收]` 已理解读取比例的直觉。这里的比例是人工指定，尚未解释模型如何计算。

### 第 3 轮：亲手完成一次加权读取

给每个 token 一个二维信息向量：

```text
我      [1, 0]
吃了    [0, 2]
一个    [1, 1]
苹果    [2, 0]
```

要求学习者手工计算：

```text
0.1 * [1, 0]
+ 0.6 * [0, 2]
+ 0.1 * [1, 1]
+ 0.2 * [2, 0]
```

学习者回答：

```text
[0.6, 1.3]
```

核对：

```text
第一维：0.1 + 0 + 0.1 + 0.4 = 0.6
第二维：0 + 1.2 + 0.1 + 0 = 1.3
```

状态：`[实践]`、`[验收]` 第一次人工加权读取计算正确。

### 第 4 轮：区分“读多少”和“读什么”

从刚才的同一项拆开：

```text
0.6      -> 从“吃了”这里读多少
[0, 2]   -> “吃了”这个位置提供什么信息
```

两者相乘得到 `[0, 1.2]`。相同读取比例乘不同的信息向量会得到不同内容，因此比例本身不是信息。

学习者回复“清楚”。

状态：`[验收]` 已区分读取比例和信息内容。

### 第 5 轮：读取比例之前先有匹配分数

人工比例不能永远由人填写。模型首先需要判断当前 token 与各个可见 token 的相关程度。例如，给“苹果”读取各位置时产生：

```text
我       1 分
吃了     3 分
一个     1 分
苹果     2 分
```

匹配分数只负责比较相对高低，可以是任意数，也不要求总和为 1。读取比例则真正用于加权读取，通常为非负数且总和为 1。

过程暂时写成：

```text
匹配分数 -> 转换成读取比例 -> 按比例读取信息向量
```

学习者回复“清楚”。

状态：`[验收]` 已区分匹配分数、读取比例和信息内容三种量。

### 第 6 轮：Softmax 的职责已经讲解，但尚未验收

助手介绍了负责“把匹配分数转换成读取比例”的工具 `Softmax`：

```text
匹配分数：1，3，1，2
Softmax 后约为：0.08，0.61，0.08，0.23
```

只说明了三个输出性质：

```text
高分仍对应较大比例
每个比例大于 0
所有比例之和为 1
```

也明确了职责边界：Softmax 不产生匹配分数，不产生信息内容，只把已有分数转换成可加权读取的比例。

在学习者回答是否理解之前，学习者暂停课程并指出当前文档过于像总结，要求先把所有历史文档重整为包含每一步互动、可供其他新人复现的完整资料。

状态：`[讲解]`、`[待确认]`。Softmax 的职责不能标记为已掌握；公式也尚未学习。

### 第 7 轮：课程暂停，先修复记录方式

学习者提出新的文档标准：

> 应该把过程记录清楚，甚至包括每一步的交互过程。最终学习完了，这就是一套完整的入门学习资料，让每个新人都能按照这个步骤学一遍，而不是只有我能看懂。

因此 Day 006 暂停在 Softmax 职责确认处。文档归档本身不算新的模型学习步骤，也不创建 Day 007。

### 第 8 轮：归档完成后确认 Softmax 的职责

文档重新归档完成后，学习者恢复 Day 006，并明确确认：

> Softmax 只负责把匹配分数转换成读取比例。

由此准确区分：

```text
Softmax 的输入：已经计算好的匹配分数
Softmax 的输出：可以用于加权读取的比例
Softmax 不负责：产生匹配分数或提供信息内容
```

状态：`[验收]` Softmax 在 Attention 流程中的职责已经掌握。下一步开始学习它具体如何完成转换。

### 第 9 轮：理解 Softmax 的具体计算

使用匹配分数 `1、3、1、2`，将 Softmax 拆成两步：

```text
第一步：分别计算 e^1、e^3、e^1、e^2
       ≈ 2.718、20.086、2.718、7.389

第二步：每个数除以总和 32.911
       ≈ 0.083、0.610、0.083、0.224
```

学习者确认理解：Softmax 先通过 `e^分数` 得到正数，再通过除以总和得到总和为 1 的比例。公式中的 `Σ` 只表示把所有位置对应的数相加。

状态：`[验收]` Softmax 的两步计算过程已经理解；下一步使用 PyTorch 验证数值。

### 第 10 轮：使用 PyTorch 验证 Softmax

学习者亲自运行：

```python
import torch

scores = torch.tensor([1.0, 3.0, 1.0, 2.0])
positive_scores = torch.exp(scores)
manual_weights = positive_scores / positive_scores.sum()
torch_weights = torch.softmax(scores, dim=0)
```

实际输出：

```text
匹配分数： tensor([1., 3., 1., 2.])
e^分数： tensor([ 2.7183, 20.0855,  2.7183,  7.3891])
e^分数的总和： tensor(32.9112)
手工 Softmax： tensor([0.0826, 0.6103, 0.0826, 0.2245])
PyTorch Softmax： tensor([0.0826, 0.6103, 0.0826, 0.2245])
读取比例总和： tensor(1.0000)
两种算法是否一致： True
```

由实际结果验证：

```text
手工执行 exp 后除以总和 = torch.softmax
Softmax 输出全部为正数
原匹配分数的高低顺序得到保留
输出比例之和为 1
```

状态：`[实践]`、`[验收]` Softmax 的手工过程与 PyTorch 实现已经对照验证。下一步把这组读取比例用于信息向量，完成一条完整的 Attention 读取链。

### 第 11 轮：完成一条完整的 Attention 读取链

把 Softmax 得到的读取比例：

```text
0.0826、0.6103、0.0826、0.2245
```

用于四个信息向量：

```text
我      [1, 0]
吃了    [0, 2]
一个    [1, 1]
苹果    [2, 0]
```

学习者手工计算：

```text
0.0826 * [1, 0]
+ 0.6103 * [0, 2]
+ 0.0826 * [1, 1]
+ 0.2245 * [2, 0]
= [0.6142, 1.3032]
```

核对：

```text
第一维：0.0826 + 0 + 0.0826 + 0.2245*2 = 0.6142
第二维：0 + 0.6103*2 + 0.0826 + 0 = 1.3032
```

结果正确。读取比例不再是人工指定，而是由匹配分数经过 Softmax 得到。

状态：`[实践]`、`[验收]` 已经亲手完成“匹配分数 -> Softmax -> 读取比例 -> 加权读取信息”的完整简化链路。

### 第 12 轮：明确匹配分数的双方是谁

在总结完整链路时，学习者没有直接略过，而是追问：

> 这里的分数指的是“苹果”与 `[我、吃了、一个、苹果]` 四个之间的分数关系吗？

答案是：在当前例子中，是。当前正在更新的 token 是句末“苹果”，四个分数分别表示当前“苹果”与四个可见位置的匹配程度：

```text
当前“苹果” 对 “我”      -> 1
当前“苹果” 对 “吃了”    -> 3
当前“苹果” 对 “一个”    -> 1
当前“苹果” 对 “苹果”自己 -> 2
```

这里比较的是模型内部向量所形成的匹配程度，不是直接比较两个汉字字符串。当前只澄清分数属于谁与谁，尚不引入计算这些分数的 Q/K。

状态：`[讲解]`、`[待确认]`。等待学习者确认“当前读取者”和“各个被读取位置”的关系。

学习者随后确认已经清楚。因此本例中：

```text
读取者：当前“苹果”
被读取位置：“我、吃了、一个、苹果”
分数 [1, 3, 1, 2]：当前“苹果”分别与四个位置的匹配分数
```

还明确了：每个 token 在轮到自己读取上下文时，都会有属于自己的一组匹配分数；`[1, 3, 1, 2]` 不是整句话共用的唯一一组分数。

状态更新：`[验收]` 已能区分当前读取者、被读取位置以及这一组匹配分数的归属。

### 第 13 轮：一个 Attention Head 是什么

此前实验中的匹配分数和信息向量都是人工提供的。真实模型需要学习：

```text
怎样计算当前 token 与各个位置的匹配分数
每个位置应该提供什么信息向量
```

再执行：

```text
匹配分数 -> Softmax -> 读取比例 -> 加权读取 -> 综合信息
```

把这一整套独立的评分与读取计算称为一个 `Attention Head`。当前最低定义是：

```text
一个 Head
= 一套计算匹配分数的规则
+ 一套产生信息内容的规则
+ Softmax
+ 加权求和
```

学习者确认理解，并排除了“一个 token 对应一个 Head”的误解。同一个 Head 使用同一套规则处理所有 token，但每个 token 会根据自己和当前可见上下文产生不同的分数与读取结果。

状态：`[验收]` 已建立单个 Attention Head 的直观含义；尚未学习多 Head 的必要性和实现。

### 第 14 轮：单个 Head 的局限

一个 Head 对当前 token 只有一套评分与读取规则，因此只产生：

```text
一组匹配分数 -> 一组读取比例 -> 一个综合信息向量
```

以“小明 吃了 一个 红 苹果”为例，更新“苹果”时可能同时需要动作、颜色、执行者和自身等多方面信息。一个 Head 理论上仍能混合复杂信息，但所有关系要共用一套规则，并被合入同一个读取结果，表达空间会受到限制。

学习者确认这个局限已经清楚。

状态：`[验收]` 已理解为什么一套关注方式可能不够；下一步原本应由此引出多个 Head。

### 第 15 轮：每个位置有多少个可用分数

在进入多 Head 前，学习者主动回忆自回归模型的可见范围并提问：

> token 只会关注它之前的 token，所以第二个 token 有 2 个分数、第三个有 3 个分数、第四个有 4 个分数，是这样吗？

需要补充的准确表述是：Decoder-only 因果 Attention 中，当前 token 可以读取“自己和之前”的位置，不能读取未来位置。因此在四个 token 的简化序列中，有效可见分数数量依次为 `1、2、3、4`。

状态：`[讲解]`、`[待确认]`。下一步澄清概念上的有效分数与代码中先算完整分数矩阵再遮住未来位置的区别。

学习者确认这与原有理解一致。准确可见范围为：

```text
第 1 个 token：自己，共 1 个有效分数
第 2 个 token：过去 + 自己，共 2 个有效分数
第 3 个 token：过去 + 自己，共 3 个有效分数
第 4 个 token：过去 + 自己，共 4 个有效分数
```

代码通常批量得到完整的 `4 x 4` 分数矩阵，再使用 `Causal Mask` 遮住右上方的未来位置，使其经过 Softmax 后读取比例为 0。

状态更新：`[验收]` 已理解因果 Attention 中“自己和过去可见、未来不可见”的下三角范围；尚未解释为什么训练时必须这样限制。

### 第 16 轮：为什么训练时必须遮住未来

MiniMind 的当前位置表示用于预测下一个 token。例如：

```text
当前位置“我”   -> 预测“吃了”
当前位置“吃了” -> 预测“一个”
当前位置“一个” -> 预测“苹果”
```

如果“一个”在 Attention 中可以读取右侧的“苹果”，就相当于在预测前直接看到了正确答案。训练 loss 即使下降，也不能证明模型学会了根据过去预测未来；真实生成时未来 token 尚不存在。

`Causal Mask` 因此让整句并行训练中的每个位置都遵守真实生成的时间顺序：

```text
训练：整句一起计算，但遮住每个位置的未来
生成：未来尚不存在，逐个产生新 token
```

这也解释了为什么当前位置可以读取自己：当前位置的表示用于预测它后面的 token，而不是预测自己。

学习者确认已经清楚。

状态：`[验收]` 已理解 Causal Mask 防止答案泄漏，并使训练可见范围与真实生成一致。现在返回多 Head 主线。

### 第 17 轮：为什么需要多个 Head

一个 Head 对当前 token 只产生一组读取比例和一个综合信息向量。多个 Head 则让同一个 token 同时使用多套独立规则：

```text
Head 1 -> 自己的一组分数和比例 -> 读取结果 A
Head 2 -> 另一组分数和比例     -> 读取结果 B
...                              
最后把多个读取结果组合起来
```

以“小明 吃了 一个 红 苹果”为例，可以用一个 Head 较多读取“吃了”、另一个 Head 较多读取“红”来帮助建立直觉。但这不是人为规定的固定岗位；不同 Head 拥有不同可学习参数，可能在训练中形成不同关注方式，也不保证每个 Head 都能被人类准确命名。

学习者确认已经清楚。

状态：`[验收]` 已理解多 Head 用多套独立读取规则缓解单套规则的局限。

### 第 18 轮：Q/K/V 不是 Embedding 的三块数据

在引入 Q/K/V 后，学习者指出：Embedding 不是通过 token ID 查到一整行权重吗？Q/K/V 是否只是把这行数据分成三部分？

需要明确纠正：

```text
不是：Embedding 向量切成三段，分别叫 Q、K、V
而是：同一个完整输入向量分别经过三套不同的 Linear，得到三个新向量
```

以第一层 Attention 为例：

```text
input_ids
  -> Embedding 查表
  -> h，shape [batch, sequence, hidden_size]
  -> q_proj(h) -> Q
  -> k_proj(h) -> K
  -> v_proj(h) -> V
```

MiniMind-3 的发布模型打印结果中可以直接看到这些是分开的模块：

```text
embed_tokens: Embedding(6400, 768)
q_proj:       Linear(768, 768)
k_proj:       Linear(768, 384)
v_proj:       Linear(768, 384)
```

因此：

```text
Embedding 权重：一个 [6400, 768] 的词向量表，按 token ID 取行
q_proj/k_proj/v_proj 权重：三套独立的可训练矩阵
Q/K/V：每次 forward 根据当前输入向量计算出来的结果，不是词表中预存的三段
```

这里先用简化 Attention 理解三套投影；`k_proj/v_proj` 为什么在 MiniMind-3 中是 384 维，留到 GQA 和多头 shape 阶段解释。

状态：`[讲解]`、`[待确认]`。需要学习者确认“完整 Embedding 向量分别经过三套 Linear，而不是被切成三块”。

学习者确认已经清楚。当前准确理解为：

```text
token ID -> Embedding 查表 -> 完整输入向量 h
                              ├-> q_proj(h) -> Q
                              ├-> k_proj(h) -> K
                              └-> v_proj(h) -> V
```

三套 Linear 使用不同的可训练权重，但都接收完整的 `h`。Q/K/V 是每层 forward 临时计算出来的结果，不是 Embedding 权重表中预先存放的三段数据。

状态更新：`[验收]` 已区分 Embedding 查表与 Q/K/V 三套线性投影。

### 第 19 轮：手算三套线性投影

为了验证“完整输入向量分别经过三套 Linear”，使用简化输入和权重：

```text
h = [1, 2, 3]

Wq = [[1, 0, 0], [0, 1, 0]]
Wk = [[0, 1, 0], [0, 0, 1]]
Wv = [[1, 0, 1], [0, 1, 1]]
```

按 `Q = h @ Wq.T`、`K = h @ Wk.T`、`V = h @ Wv.T` 计算，学习者得到：

```text
Q = [1, 2]
K = [2, 3]
V = [4, 5]
```

结果正确。三套投影都接收完整的 `[1, 2, 3]`，并非分别接收输入的一段。

状态：`[实践]`、`[验收]` 已用具体数字验证同一个 `h` 如何产生 Q、K、V。下一步用 Q 与另一个位置的 K 做点积，观察匹配分数的来源。

### 第 20 轮：用 Q 与 K 点积得到匹配分数

给定当前读取者“苹果”的 Q，以及两个可读取位置的 K：

```text
苹果的 Q = [1, 2]
吃了的 K = [2, 3]
红的 K   = [4, 1]
```

学习者手工计算：

```text
苹果对吃了的分数 = [1, 2] @ [2, 3] = 1*2 + 2*3 = 8
苹果对红的分数   = [1, 2] @ [4, 1] = 1*4 + 2*1 = 6
```

并正确判断：因为 `8 > 6`，在当前 Head 的这套规则下，“吃了”比“红”更匹配当前“苹果”。

此时得到的 `8、6` 仍是匹配分数，尚未经过 Softmax，也尚未使用两个位置的 V 读取信息。

状态：`[实践]`、`[验收]` 已验证 `当前 token 的 Q @ 各位置的 K` 如何产生一组匹配分数。

学习者进一步确认理解当前分工：

```text
当前读取者的 Q 与各位置的 K 点积 -> 产生匹配分数，决定之后读多少
各位置的 V                           -> 在读取比例确定后，提供真正的信息内容
```

V 不参加当前简化例子中匹配分数的计算。完整实现还会在 Q/K 点积后进行数值缩放，但不影响这里对三者职责的理解，缩放原因留到后续解释。

状态更新：`[验收]` 已能准确说明 Q、K 与 V 在评分和读取阶段的不同职责。

### 第 21 轮：运行一条简化 Q/K/V Attention

学习者使用：

```text
苹果的 Q = [1, 2]
吃了的 K = [2, 3]，V = [10, 0]
红的 K   = [4, 1]，V = [0, 10]
```

运行：

```python
scores = q @ keys.T
weights = torch.softmax(scores, dim=0)
result = weights @ values
```

实际输出：

```text
匹配分数： tensor([8., 6.])
读取比例： tensor([0.8808, 0.1192])
比例总和： tensor(1.0000)
读取结果： tensor([8.8080, 1.1920])
```

学习者随后提出关键问题：为什么读取结果只有一个向量，而不是继续保留“吃了”和“红”各自的向量？这个结果是否是“苹果最终的权重”？

需要澄清三者：

```text
吃了和红的 V：两个来源各自提供的信息向量，仍然存在
0.8808 和 0.1192：当前苹果对两个来源的读取比例（Attention weights）
[8.8080, 1.1920]：当前苹果汇总读到的信息（当前 Head 的 Attention 输出）
```

最后一项不是模型参数权重，也不是苹果经过完整 Transformer 后的最终表示。状态：`[讲解]`、`[待确认]`。

学习者确认已经清楚。一个 Q 对多个来源的 V 加权求和，得到一份属于当前读取者的综合信息，因此当前只计算“苹果”时输出一个向量。各来源的 V 仍各自存在；整句话并行计算时，每个 token 都有自己的 Q，也会各自得到一个 Attention 输出向量。

状态更新：`[验收]` 已区分模型参数、Attention 读取比例、来源 V 和当前读取者的 Attention 输出，并理解“一名读取者得到一个汇总向量”。

### 第 22 轮：从一个读取者扩展到整句并行计算

设序列有 4 个 token，每个 Q、K、V 都是二维向量：

```text
Q、K、V shape = [4, 2]
scores = Q @ K.T：[4, 2] @ [2, 4] -> [4, 4]
weights shape = [4, 4]
result = weights @ V：[4, 4] @ [4, 2] -> [4, 2]
```

学习者回答两个结果 shape 为 `4x4、4x4`。第一项正确；第二项应为 `4x2`。需要进一步澄清：`weights` 每行的 4 个数用于合并 4 个来源，但每个来源 V 的内容维度为 2，所以每个读取者合并后仍得到一个二维向量。

状态：`[实践]`、`[待确认]`。等待学习者修正 `weights @ V` 的 shape。

学习者重新回答：

```text
Q @ K.T     -> 4x4
weights @ V -> 4x2
```

结果正确。4 个读取者各自产生对 4 个位置的一行分数，最后各自将 4 个二维 V 合成一个二维结果，因此输出仍有 4 行、每行 2 个数。

状态更新：`[验收]` 已理解单 Head 中 `[sequence, head_dim] -> [sequence, sequence] -> [sequence, head_dim]` 的基本 shape 变化。

### 第 23 轮：观察 Softmax 对分数尺度的敏感性

学习者对两组高低关系相同但数值尺度不同的分数运行 Softmax：

```python
small_scores = torch.tensor([2.0, 1.0])
large_scores = torch.tensor([20.0, 10.0])
```

实际输出：

```text
较小分数的 Softmax： tensor([0.7311, 0.2689])
较大分数的 Softmax： tensor([9.9995e-01, 4.5398e-05])
```

第一组分数差为 `1`，第二组分数差为 `10`。Softmax 不按原始分数的倍数关系直接分配比例；指数运算会显著放大分数差距，使第二组输出接近 `[1, 0]`。

状态：`[实践]`、`[讲解]`、`[待确认]`。下一步先解释指数为何放大差距，再连接 Q/K 维度与缩放。

学习者确认理解：Softmax 不是按原分数的简单比例分配，指数运算会放大分数差距；差距过大时，读取比例会趋近于“一边接近 1、其他接近 0”。

状态更新：`[验收]` 已理解 Softmax 对分数尺度的敏感性。

### 第 24 轮：观察点积维度与缩放

使用每一维都为 `1` 的 Q/K，学习者运行：

```python
for dimension in [2, 8, 32, 128]:
    q = torch.ones(dimension)
    k = torch.ones(dimension)
    raw_score = q @ k
    scaled_score = raw_score / (dimension ** 0.5)
```

实际输出：

```text
维度 = 2   原始点积 = 2.0    除以 sqrt(d) 后 = 1.4142
维度 = 8   原始点积 = 8.0    除以 sqrt(d) 后 = 2.8284
维度 = 32  原始点积 = 32.0   除以 sqrt(d) 后 = 5.6569
维度 = 128 原始点积 = 128.0  除以 sqrt(d) 后 = 11.3137
```

这个特例中：

```text
原始点积 = d
缩放后   = d / sqrt(d) = sqrt(d)
```

因此除以 `sqrt(d)` 没有让全 `1` 向量的结果完全不增长，而是把线性增长降成平方根增长。真实 Q/K 通常包含正数和负数，点积中的项会相互抵消；下一步用随机向量观察更典型的分数尺度。

状态：`[实践]`、`[讲解]` 已看到缩放会减缓点积分数增长；随机向量下的原因和效果待继续验证。

### 第 25 轮：随机 Q/K 下验证 `sqrt(d)` 缩放

学习者生成 10000 组随机 Q/K，比较原始点积分数与除以 `sqrt(d)` 后的分散程度。实际输出：

```text
维度 = 2   原始标准差 = 1.4277   缩放后标准差 = 1.0095
维度 = 8   原始标准差 = 2.7961   缩放后标准差 = 0.9886
维度 = 32  原始标准差 = 5.7009   缩放后标准差 = 1.0078
维度 = 128 原始标准差 = 11.3012  缩放后标准差 = 0.9989
```

先把标准差理解成一批分数通常分散多宽。结果显示：

```text
原始点积分数的分散程度约为 sqrt(d) 的量级
除以 sqrt(d) 后，不同维度的分散程度都稳定在 1 附近
```

直观原因是：点积由 `d` 个正负乘积相加组成，项数增加会让总和的典型波动约按 `sqrt(d)` 增长；除以同样的 `sqrt(d)`，就把分数恢复到相近尺度。这样进入 Softmax 时，不会因为 head_dim 变大而自动变得极端。

状态：`[实践]`、`[验收]` 已验证 scaled dot-product 中除以 `sqrt(d)` 的必要性和效果。

学习者进一步确认：除以 `sqrt(head_dim)` 是为了抵消 Q/K 点积分数随维度增加而扩大的典型尺度，使进入 Softmax 的分数保持稳定，避免读取比例无缘无故变得过于极端。

状态更新：`[验收]` 已理解 Scaled Dot-Product Attention 中缩放项的职责。

### 第 26 轮：验证 Causal Mask 如何产生零读取比例

给第三个 token 对四个位置的缩放后分数：

```text
[2, 1, 3, 9]
```

第四个位置属于未来。学习者分别在无遮罩和将未来分数设为 `-inf` 后执行 Softmax，实际输出：

```text
原始分数： tensor([2., 1., 3., 9.])
不遮罩的读取比例： tensor([9.0850e-04, 3.3422e-04, 2.4696e-03, 9.9629e-01])
遮罩后的分数： tensor([2., 1., 3., -inf])
遮罩后的读取比例： tensor([0.2447, 0.0900, 0.6652, 0.0000])
遮罩后比例总和： tensor(1.)
```

没有遮罩时，未来位置的高分 `9` 获得约 `99.629%` 的读取比例。遮罩在 Softmax 前将该分数改成 `-inf`，于是 `exp(-inf)=0`，未来位置比例变成 0；其余可见位置重新归一化，总和仍为 1。

状态：`[实践]`、`[验收]` 已亲自验证 Causal Mask 的数值实现和效果。

学习者确认已经清楚：Causal Mask 不删除输入 token，而是在 Softmax 前将未来位置的匹配分数设为 `-inf`，使未来读取比例为 0，并让 Softmax 只在可见位置之间重新归一化。

状态更新：`[验收]` 已理解 Mask 必须位于 Softmax 之前的原因。

### 第 27 轮：组装完整的简化单 Head Attention

学习者将 4 个三维输入向量依次完成 Q/K/V 投影、缩放点积、Causal Mask、逐行 Softmax 和 V 加权求和。关键 shape 与输出为：

```text
Q/K/V shape  = [4, 2]
scores shape = [4, 4]
result shape = [4, 2]

每行读取比例之和 = [1, 1, 1, 1]

result =
[[1.0000, 1.0000],
 [1.0000, 1.0000],
 [1.3333, 0.6667],
 [2.6321, 0.9077]]
```

学习者随即提出：输入中每个 token 是三维，为什么最终 `result` 变成了二维？

原因是本实验明确设置 `Wv` 将输入从 3 维投影到 2 维：

```text
h [4, 3] @ Wv.T [3, 2] -> V [4, 2]
weights [4, 4] @ V [4, 2] -> result [4, 2]
```

当前 `result` 只是单个 Head 的读取结果，还不是完整 Attention 子层交回模型主干的 token 表示。完整结构还需要组合各 Head，并通过输出 Linear 映射回 `hidden_size`，才能与原输入进行残差相加。

状态：`[实践]`、`[讲解]`、`[待确认]`。等待学习者确认单 Head 输出维度由 V 的维度决定。

学习者确认已经清楚：单 Head 的 `result` 维度由 V 的输出维度决定；完整 Attention 还要组合多个 Head，并通过输出 Linear 映射回 `hidden_size`，才能与残差路径相加。

状态更新：`[验收]` 已理解单 Head 中间输出与完整 Attention 子层输出的区别。

## 一、承接 Day 005

Backward 最小知识链已经收口。此前还留下第二个原始问题：

> Attention 是什么？Attention Head 又是什么？直接出现这些专业术语时无法理解。

今天从已经亲自验证的事实出发：

```text
普通 Linear 使用同一套参数独立处理每个 token
多个 token 一起计算和分别计算，结果完全相同
```

因此，普通 Linear 本身不会让 token 之间交换信息。

## 二、今日路线

每次只推进一个关卡：

1. 为什么语言理解要求一个 token 读取其他 token。
2. Attention 的任务：选择信息来源并决定读取比例。
3. 人工完成一次加权读取。
4. 区分匹配分数、读取权重和被读取内容。
5. 理解一套 Attention 关注方式。
6. 理解为什么需要多个 Head。

只有当前关卡理解并解决疑问后，才进入下一关。

## 三、今天暂不引入

```text
Q、K、V
Softmax 公式
Scaled Dot-Product Attention
Causal Mask
Multi-Head 的张量拆分
GQA
RoPE
KV Cache
MiniMind Attention 源码
```

这些术语必须在 Attention 和 Head 的直觉建立后逐步引入。

## 四、今日验收标准

- [x] 能解释普通 Linear 为什么不能完成 token 间的信息交换。
- [x] 能用非公式语言说明 Attention 的基本任务。
- [x] 能亲自完成一次加权读取。
- [x] 能区分“关注谁”和“从对方读取什么”。
- [x] 能说明 Softmax 在当前流程中的唯一职责。
- [x] 能说明 Softmax 的两步计算：`e^分数`，再除以总和。
- [x] 能解释一套关注方式的局限。
- [x] 能用直观语言解释为什么需要多个 Head。
- [x] 不把听到术语或照抄公式视为已经掌握。

## 五、学习记录

### 关卡 1：Attention 为什么存在

已经确认理解：

```text
普通 Linear：每个 token 只加工自己的向量，不能读取其他 token 的信息。
Attention：让当前 token 有选择地读取其他可见 token 的信息，
再用读到的信息更新当前 token 的表示。
```

通过“苹果”在“吃苹果”和“苹果手机”中的不同含义，理解了一个 token
不能只看自己；它需要结合周围 token 才能形成符合当前语境的表示。

### 关卡 2：有选择地读取

已经确认理解：Attention 不必只选择一个 token，也不会平均读取所有 token，
而是给各个可见 token 分配不同的读取比例，再把按比例读到的信息合在一起。

例如，更新“苹果”时可以人工设定：

```text
我      10%
吃了    60%
一个    10%
苹果    20%
```

比例之和为 100%。这些比例目前只是帮助理解的人工设定，尚未学习模型如何计算它们。

### 关卡 3：人工完成加权读取

给定信息向量：

```text
我      [1, 0]
吃了    [0, 2]
一个    [1, 1]
苹果    [2, 0]
```

以及读取比例 `0.1、0.6、0.1、0.2`，亲手计算：

```text
0.1 × [1, 0]
+ 0.6 × [0, 2]
+ 0.1 × [1, 1]
+ 0.2 × [2, 0]
= [0.6, 1.3]
```

计算结果正确，完成了第一次人工加权读取。

### 关卡 4（第一部分）：读取比例与信息内容

已经确认能区分：

```text
读取比例 0.6：回答从这个 token 读多少。
信息向量 [0, 2]：回答从这个 token 读什么。
```

读取比例不是信息本身。相同的读取比例乘以不同的信息向量，得到的内容也不同。

### 关卡 4（第二部分）：匹配分数与读取比例

已经确认能区分：

```text
匹配分数：用于比较当前 token 与各个可见 token 的相关程度；
          可以是任意数，不要求非负，也不要求总和为 1。

读取比例：真正用于加权读取；通常非负，而且所有比例之和为 1。
```

当前理解的处理顺序是：

```text
计算匹配分数 -> 转换成读取比例 -> 按比例读取信息向量
```

## 六、问题与知识欠账

- Q/K/V：Attention 与 Head 直觉已经完成；下一步从匹配分数和信息向量的来源进入。
- Softmax：职责已经确认；公式和具体计算正在进入。
- KV Cache：等待 Q/K/V、因果生成和 GQA。
- MiniMindBlock、RMSNorm、残差连接：Attention 基础建立后返回。

## 七、今日总结

Day 006 从“普通 Linear 不能让 token 交换信息”出发，完成了 Attention 从直觉到可运行单 Head 核心计算的完整入门链路。

学习者亲自完成并验收：

```text
人工加权读取
匹配分数、读取比例和信息内容的区别
Softmax 手算与 PyTorch 对照
当前读取者与各个被读取位置的关系
一个 Head 和多个 Head 的直觉
Embedding 与 Q/K/V 三套 Linear 的区别
Q @ K.T 产生匹配分数
weights @ V 产生当前读取者的汇总信息
整句 scores/result 的 shape
sqrt(head_dim) 缩放的数值实验
Causal Mask 在 Softmax 前将未来位置设为 -inf
四 token 简化单 Head Attention 的完整运行
单 Head 中间输出与完整 Attention 子层输出的区别
```

完整实验已整理到配套 Notebook，并在 MiniMind 项目 `.venv` 中按顺序执行全部代码 Cell 通过。

尚未进入：

```text
多 Head 的真实拆分、转置和拼接
o_proj 的实际计算
MiniMind-3 的 GQA（8 个 Q Head、4 个 KV Head）
RoPE
KV Cache
Attention 源码逐行阅读
```

## 八、下一学习日入口

Day 007 从多 Head 的真实 shape 开始。第一个问题是：

> MiniMind-3 的 `hidden_size=768` 和 `num_attention_heads=8` 如何对应到每个 Head 的 `head_dim=96`？

先只理解维度拆分与每个 Head 各自计算，不立即引入 GQA、RoPE 或 KV Cache。
