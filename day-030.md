# Day 030：视觉指令微调 train_sft_vlm 与两阶段分工

- 日期：2026-09-12
- 学习阶段：第二阶段 `minimind-v`（视觉指令微调 SFT）
- 当前状态：已完成（与预训练对比 + 两阶段分工验收）
- 入口：`minimind-v/trainer/train_sft_vlm.py`（对照 `train_pretrain_vlm.py`）

## 一、本日方法：只看差异

`train_sft_vlm.py` 与昨天的 `train_pretrain_vlm.py` 几乎完全相同：`train_epoch` 函数
逐字一致，9 段结构一致。差异全部集中在命令行默认参数和一行 optimizer 上，因此本日
不重复走训练流程，只盯差异。

## 二、默认参数差异对照

```text
参数            预训练(pretrain_vlm)     SFT(sft_vlm)          关键
──────────────────────────────────────────────────────────────────
save_weight     pretrain_vlm            sft_vlm
data_path       pretrain_i2t.parquet    sft_i2t.parquet       ★ 换指令问答数据
from_weight     llm                     pretrain_vlm          ★ 基座换成预训练产物
freeze_llm      2                       1                     ★ 解冻 LLM 首末层
learning_rate   4e-4                    5e-6                  ★ 降约 80 倍
batch_size      16                      4
max_seq_len     450                     768                   ★ 容纳多轮/更长
epochs          2                       2
```

## 三、逐个关键差异

### from_weight：llm → pretrain_vlm（两阶段衔接点）

```text
预训练加载 llm_768.pth        -> vision_proj 随机初始化，从零学对齐
SFT 加载 pretrain_vlm_768.pth -> vision_proj 已预训练好（对齐已建立）
```

呼应昨天：预训练存权重时过滤 SigLIP、保留 LLM + vision_proj，所以
`pretrain_vlm_768.pth` 带着训好的 proj，SFT 直接接力。

### freeze_llm：2 → 1（解冻首末层）

init_vlm_model 的 freeze_llm==1 分支：先全冻结只留 proj，再解冻
`layers.0`（首层）和 `layers.{num_hidden_layers-1}`（末层，8 层时为 layers.7）。

```text
SFT 可训练：vision_proj + LLM layers.0 + LLM layers.7
仍冻结：    LLM layers.1~6 + SigLIP
```

动机：
```text
首层——最靠输入，消化刚注入的 visual token，更好接收图像信息
末层——最靠输出，调整回答组织方式，学指令跟随的表达
中间层——保留预训练语言能力核心，不动以免破坏
```
是介于 freeze_llm=0（全放开易过拟合/遗忘）和 =2（全冻结学不会指令）之间的折中。

### learning_rate：4e-4 → 5e-6（降约 80 倍）

```text
预训练大 lr：只训随机初始化的 proj，从零学，大步子快收敛，且动它不伤 LLM
SFT 小 lr：  解冻的是已训好的 LLM 首末层，大 lr 会把它们推离好位置 = 灾难性遗忘
规律：从零学的模块用大 lr；微调已训好的权重用小 lr。
```

### 数据 sft_i2t.parquet：指令问答

结构与预训练相同（conversations JSON + image_bytes JPEG），差别在内容形式：
从"请描述这张图"的单轮长描述，变为具体指令问答、可能多轮、回答更短更针对。
generate_labels 多轮时会把每一轮 `<s>assistant\n...</s>\n` 都设为算 loss，
user 提问与图像坑仍全 -100。

## 四、唯一的代码级差异：optimizer 少了 filter

```python
# 预训练： optim.AdamW(filter(lambda p: p.requires_grad, model.parameters()), lr=...)
# SFT：    optim.AdamW(model.parameters(), lr=...)   # 直接传全部参数
```

分析：冻结参数 requires_grad=False → backward 不给梯度 → .grad 为 None →
PyTorch AdamW 对 grad is None 的参数直接 skip（连 weight decay 都不做）。
所以功能上安全，被更新的仍是 proj + 首末层。区别只是 SFT 写法"不够干净"：
优化器持有了用不到的冻结参数、多占一点状态内存。预训练的 filter 是更严谨写法。

## 五、两阶段分工（本日核心结论）

```text
阶段1 预训练：LLM 完全冻结，只训随机 proj，大 lr(4e-4)，长描述数据
             目标——建立视觉↔语言基础对齐，让 visual token 进入文本语义空间
阶段2 SFT：  接预训练权重，解冻 LLM 首末层 + proj，小 lr(5e-6)，指令问答数据
             目标——在对齐基础上学指令跟随/对话，同时不破坏语言能力
```

为什么必须先预训练再 SFT（不能跳过预训练直接 SFT）：
```text
若跳过：vision_proj 仍随机 -> 产出"乱码"visual token
       而 SFT 同时解冻 LLM 首末层 -> 首末层去适应乱码，被带偏
       对齐没建好 + LLM 被污染，两头都学不好
核心矛盾：对齐(proj)和指令跟随(LLM)若同时从零开始会互相干扰
         随机 proj 给不出可靠视觉信号；LLM 在动又让 proj 目标不稳。
拆两阶段 = 先稳一头再动另一头：
  先冻 LLM 把 proj 对齐练可靠 -> 再小心微调 LLM，此时它收到的是有意义的 visual token
这也是 LLaVA 等主流 VLM 普遍用两阶段的原因。
```

## 六、验收

```text
[验收] train_sft_vlm 与 train_pretrain_vlm 的全部差异（数据/基座/freeze/lr/seq_len）
[验收] from_weight=pretrain_vlm 如何接力预训练的 vision_proj
[验收] freeze_llm=1 解冻首末层的机制与动机
[验收] SFT 用小 lr 的原因：微调已训好权重防灾难性遗忘
[验收] optimizer 少 filter 为何仍安全（requires_grad=False + AdamW skip None grad）
[验收] 两阶段分工与"必须先预训练再 SFT"的根本原因
```

## 七、下一天计划

视觉训练主线（预训练 + SFT）已通。下一小步在 minimind-v 收尾，二选一后再定：

```text
方向A（推荐）：freeze_llm=0/1/2 三策略的完整对照 + 极小实验实测可训练参数量差异
              （init_vlm_model 打印 Trainable Params，跑 3 次对比）
方向B：scripts/convert_vlm.py 视觉权重转换 + web_demo_vlm.py 演示调用链
方向C：直接进第三阶段 minimind-o（Omni：文本+图像+语音，Thinker/Talker 结构）
```

建议先做方向 A 做一次小实验坐实 freeze 策略，再收尾 minimind-v 进入第三阶段。
