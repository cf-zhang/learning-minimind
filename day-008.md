# Day 008：GQA 与共享 K/V Head

- 日期：2026-08-24
- 学习阶段：Transformer Attention 实现
- 今日主题：MiniMind-3 为什么使用 8 个 Q Head 和 4 个 K/V Head
- 当前状态：已完成
- 配套实验：[Day 008 GQA Notebook](notebooks/day-008-gqa.ipynb)

## 一、承接 Day 007

Day 007 已经完成普通 Multi-Head Attention：

```text
h
  -> q_proj / k_proj / v_proj
  -> Q / K / V 投影结果
  -> 拆成相同数量的 Head
  -> 每个同编号 Q/K/V Head 独立完成 Attention
  -> 按 token 拼接多个 Head 的结果
  -> o_proj 混合各 Head 信息
```

普通两 Head 实验中：

```text
Q：2 个 Head
K：2 个 Head
V：2 个 Head

Q0 使用 K0/V0
Q1 使用 K1/V1
```

Day 007 配套实验见 [Multi-Head Attention Notebook](notebooks/day-007-multi-head-attention.ipynb)。

## 二、今天的问题

MiniMind-3 的真实配置不是 Q/K/V Head 数量完全相同：

```text
hidden_size = 768
head_dim = 96
num_attention_heads = 8
num_key_value_heads = 4

Q：q_proj 768 -> 768 -> 8 个 Q Head
K：k_proj 768 -> 384 -> 4 个 K Head
V：v_proj 768 -> 384 -> 4 个 V Head
```

今天需要回答：

> 为什么 8 个 Q Head 可以和 4 个 K/V Head 一起计算？减少 K/V Head 后，哪些东西共享，哪些东西仍然独立？

## 三、今日路线

每次只推进一个小关卡：

1. 建立“两个 Q Head 共享一组 K/V”的直觉。
2. 区分共享 K/V 与共享 Attention 结果。
3. 用 4 个 Q Head、2 个 K/V Head 画出配对关系。
4. 观察 Q/K/V 投影和拆分后的真实 shape。
5. 理解 `repeat_kv` 只为批量计算复制视图中的配对关系。
6. 亲自运行一个最小 GQA Attention。
7. 对比 MHA、GQA 和 MQA 的 Head 数量关系。
8. 理解 GQA 为什么减少参数、计算量和 KV Cache。

只有当前关卡经过讲解、提问或确认以及必要实践后，才进入下一关。

## 四、今天暂不引入

```text
RoPE 的旋转计算
KV Cache 的张量增长与推理代码
Flash Attention / SDPA
MiniMind Attention 源码逐行阅读
```

今天可以说明 GQA 对 KV Cache 大小的影响，但不展开 Cache 的内部生成流程。

## 五、验收标准

- [x] 能说明 8 个 Q Head 与 4 个 K/V Head 的分组关系。
- [x] 能区分“共享 K/V”与“Q Head 自己的分数、比例和输出”。
- [x] 能写出 MiniMind-3 投影后 Q/K/V 的 shape。
- [x] 能解释 `repeat_kv` 为什么不产生新的学习规则。
- [x] 能亲自运行并解释一个最小 GQA 实验。
- [x] 能区分 MHA、GQA 和 MQA。
- [x] 能解释减少 K/V Head 的主要收益和代价。

## 六、逐轮互动档案

### 第 1 轮：8 个 Q Head 如何使用 4 组 K/V

Day 008 从 MiniMind-3 的实际 Head 数量开始。普通 Multi-Head 的前置知识已经完成，现在只建立 GQA 的分组共享关系。

普通 MHA 中每个 Q Head 有同编号 K/V Head。GQA 将多个 Q Head 分为一组，让组内 Q Head 共享一组 K/V。MiniMind-3 的分组关系为：

```text
Q0、Q1 -> 共享 K0、V0
Q2、Q3 -> 共享 K1、V1
Q4、Q5 -> 共享 K2、V2
Q6、Q7 -> 共享 K3、V3
```

虽然 Head 数量不同，但每个 Q/K/V Head 都是 96 维，因此组内每个 Q 都可以与共享 K 做点积。

学习者确认已经清楚，并准确区分：

```text
共享：K、V
不共享：各自的 Q、匹配分数、Softmax 比例和 Head 输出
```

状态：`[验收]` 已理解 MiniMind-3 每两个 Q Head 共享一组 K/V，但仍分别完成 Attention。

下一步用二维数值验证：两个不同 Q 使用同一个 K，仍会产生不同匹配分数。

### 第 2 轮：最小数值例子缺少变量前提

助手直接给出两个 Q：

```text
Q0 = [1, 2]
Q1 = [3, 1]
```

以及一组 K 中两个 token 位置的向量：

```text
K0_token1 = [2, 0]
K0_token2 = [0, 2]
```

学习者正确计算：

```text
Q0 的分数 = [2, 4]
Q1 的分数 = [6, 2]
```

但学习者指出，这个实验没有先说明为什么有两个 Q、为什么只有一组 K，以及 `token1/token2` 与 Head 的关系。这个反馈正确：数值虽然算对，变量语义尚未建立，不能把计算正确当作概念已经掌握。

状态：`[纠偏]`。暂停后续计算，重新从明确的 `sequence=2`、`num_q_heads=2`、`num_kv_heads=1`、`head_dim=2` 建立张量和变量关系。

### 第 3 轮：先固定每个 token 拥有几个 Q/K/V

在重新解释 Head 维和 token 维后，学习者仍感到混乱，并尝试用更直接的方式概括：

> 是不是说每个 token 可以有 2 个 Q，只有 1 个 K，还有 1 个 V？

在当前最小 GQA 配置中，这个理解正确。更准确地说，是每个 token 在当前 Attention 层会计算出：

```text
2 个 Q 向量：分别属于 Q Head 0、Q Head 1
1 个 K 向量：属于共享的 K Head 0
1 个 V 向量：属于共享的 V Head 0
```

这比先讨论分数矩阵更适合作为 GQA 的起点。状态：`[讲解]`、`[待确认]`。

学习者确认已经清楚。当前固定认识：

```text
最小 GQA 配置：每个 token -> 2 个 Q、1 个 K、1 个 V
MiniMind-3：    每个 token -> 8 个 Q、4 个 K、4 个 V
```

这里的数量是当前 Attention 层根据该 token 的输入向量计算出的向量数量，不是永久存放在 Embedding 表中的数据。

状态更新：`[验收]` 已理解每个 token 在 Q Head 数和 K/V Head 数不同的配置下分别产生多少个 Q/K/V 向量。

### 第 4 轮：一个当前 token 的两个 Q 如何共享整组 K/V

仍以两个 token“吃了、苹果”为例。更新第二个 token“苹果”时，它拿出：

```text
Q0_苹果
Q1_苹果
```

唯一的 K Head 不是整句只有一个 K 向量，而是包含每个 token 位置各自的 K：

```text
K Head 0：K0_吃了、K0_苹果
```

因此两个 Q 分别与同一组 K 计算：

```text
Q0_苹果 @ [K0_吃了, K0_苹果] -> 分数 [2, 4]
Q1_苹果 @ [K0_吃了, K0_苹果] -> 分数 [6, 2]
```

共享的 V Head 同样包含：

```text
V0_吃了、V0_苹果
```

两个 Q Head 根据各自的读取比例分别加权同一组 V，最终仍得到两个独立 Head 输出。

学习者确认已经清楚。状态：`[验收]` 已区分 Head 数量与 token/sequence 数量，并理解“共享一个 K/V Head”仍包含序列各位置自己的 K/V 向量。

下一步将关系写成 `[batch, heads, sequence, head_dim]` shape。

### 第 5 轮：用张量验证 Head 维与 sequence 维

学习者构造最小 GQA 张量并实际观察：

```text
Q shape：[1, 2, 2, 2]
K shape：[1, 1, 2, 2]
V shape：[1, 1, 2, 2]
```

索引得到：

```text
Q0_苹果：[1, 2]
Q1_苹果：[3, 1]
K0_吃了：[2, 0]
K0_苹果：[0, 2]
```

这与语言描述完全对应：

```text
Q：[1 batch, 2 Q Head, 2 token, 2 head_dim]
K/V：[1 batch, 1 KV Head, 2 token, 2 head_dim]
```

状态：`[实践]`、`[验收]` 已用实际张量区分 Q Head 数、K/V Head 数、token 数和每个 Head 的向量维度。

下一步只观察为了批量矩阵乘法将 K/V 的 Head 维从 1 对齐到 2 的过程。

### 第 6 轮：重复 K/V 以对齐 Q Head 数

学习者执行 `repeat_interleave(..., dim=1)`，实际观察：

```text
重复前 K/V：[1, 1, 2, 2]
重复后 K/V：[1, 2, 2, 2]
```

重复后的两个 K Head 均为：

```text
[[2, 0],
 [0, 2]]
```

两个判断：

```text
两个 K Head 内容是否相同：True
两个 V Head 内容是否相同：True
```

这验证了重复只为让批量计算中的 Head 维与两个 Q Head 对齐，不会产生新的 `k_proj/v_proj` 权重或新的 K/V 学习规则。

实现细节边界：当前教学代码的 `repeat_interleave` 会实际复制数据；Qwen/MiniMind 常见的 `repeat_kv` 通常通过 `expand` 建立共享视图再 reshape，以减少不必要的物理复制。两者表达的 GQA 配对语义相同。

状态：`[实践]`、`[验收]` 已理解 `repeat_kv` 的逻辑作用以及它不增加可学习 K/V Head。

下一步使用对齐后的 K/V 完成最小 GQA Attention，验证两个 Q Head 得到不同分数、比例和输出。

### 第 7 轮：完成最小 GQA Attention

学习者使用对齐后的 K/V，依次完成缩放点积、Causal Mask、Softmax 和读取 V。实际 shape：

```text
scores：       [1, 2, 2, 2]
weights：      [1, 2, 2, 2]
head_results： [1, 2, 2, 2]
```

当前读取者“苹果”在两个 Q Head 中的缩放后分数分别为：

```text
Q Head 0：[1.4142, 2.8284]
Q Head 1：[4.2426, 1.4142]
```

对应读取比例：

```text
Q Head 0：[0.1956, 0.8044]
Q Head 1：[0.9442, 0.0558]
```

两个 Head 每行比例之和都为 1。它们读取同一组：

```text
V0_吃了 = [10, 0]
V0_苹果 = [0, 10]
```

最终“苹果”的两个 Head 输出为：

```text
Q Head 0：[1.9557, 8.0443]
Q Head 1：[9.4419, 0.5581]
```

这直接验证：

```text
共享相同 K/V
+ 各自不同 Q
-> 不同匹配分数
-> 不同 Softmax 比例
-> 不同 Head 输出
```

第一个 token 在两个 Head 中都输出 `[10, 0]`，是因为 Causal Mask 使它只能读取第一个位置，读取比例都只能是 `[1,0]`；这不表示两个 Q Head 的规则相同。

状态：`[实践]`、`[验收]` 已完成最小 GQA Attention，并用数值验证共享 K/V 不会使 Q Head 输出相同。

学习者确认已经清楚：GQA 共享 K/V 信息来源，但每个 Q Head 仍保留自己的查询、匹配分数、Softmax 比例和 Head 输出。

状态更新：`[验收]` GQA 的共享边界已经通过概念解释和数值实验共同确认。

### 第 8 轮：映射 MiniMind-3 的真实 shape

使用 Day 001 的 `batch=1、sequence=11`，MiniMind-3 的数据流为：

```text
h：             [1, 11, 768]
q_projected：   [1, 11, 768] -> Q [1, 8, 11, 96]
k_projected：   [1, 11, 384] -> K [1, 4, 11, 96]
v_projected：   [1, 11, 384] -> V [1, 4, 11, 96]

K/V 对齐后：   [1, 8, 11, 96]
scores/weights：[1, 8, 11, 11]
head_results： [1, 8, 11, 96]
combined：     [1, 11, 768]
```

学习者正确补全：

```text
weights：      [1, 8, 11, 11]
head_results： [1, 8, 11, 96]
combined：     [1, 11, 768]
```

这验证了：GQA 减少的是原始 K/V Head 数；8 个 Q Head 仍各自产生一个 96 维结果，最终照常拼回 `8*96=768`。

状态：`[验收]` 已能写出 MiniMind-3 的 Q/K/V 投影、Head 对齐、分数和结果 shape。

### 第 9 轮：区分 MHA、GQA 与 MQA

固定 8 个 Q Head 时：

```text
MHA：8 个 K Head、8 个 V Head；每 1 个 Q 使用专属 K/V
GQA：4 个 K Head、4 个 V Head；每 2 个 Q 共享 K/V
MQA：1 个 K Head、1 个 V Head；全部 8 个 Q 共享 K/V
```

随后使用 12 个 Q Head 验收。学习者回答：

```text
MHA：12，12
GQA（3 个 KV Head）：每组 4 个 Q
MQA：1，12
```

第一项因“多少个 K/V Head”措辞可能被理解为分别询问 K 和 V。若 `12，12` 表示 12 个 K Head和 12 个 V Head，则正确；还需补充 MHA 每组只有 1 个 Q Head。其余两项正确：`12/3=4`，MQA 的 12 个 Q Head 全部共享 1 个 K Head和 1 个 V Head。

学习者确认已经清楚：MHA 中每个 Q Head 都有专属 K/V，
因此每个共享组中只有 1 个 Q Head。

状态更新：`[验收]` 已能根据 Q Head 与 K/V Head 数量区分
MHA、GQA 和 MQA，并计算每组共享的 Q Head 数。

### 第 10 轮：减少 K/V Head 如何减少投影参数

继续使用 Day 007 的小模型：

```text
hidden_size = 8
num_q_heads = 2
head_dim = 4
```

Q Head 不减少，所以 MHA 和 GQA 的 Q 投影输出宽度都是
`2*4=8`。K Head 数量发生变化：

```text
MHA：2 个 K Head，输出宽度 = 2*4 = 8，k_proj = Linear(8, 8)
GQA：1 个 K Head，输出宽度 = 1*4 = 4，k_proj = Linear(8, 4)
```

根据此前已经掌握的 Linear 参数关系：

```text
weight shape = [out_features, in_features]
无 bias 时参数量 = out_features * in_features
```

学习者计算：

```text
MHA k_proj 参数量：64
GQA k_proj 参数量：32
```

结果正确。K Head 从 2 个减到 1 个，使 K 投影输出宽度从 8
减到 4，`k_proj` 参数量随之从 64 减到 32，正好减半。

随后将相同的变化应用到 V 投影。Q Head 数量不变，因此两种结构的
`q_proj` 都是 `Linear(8, 8)`：

```text
MHA：q_proj=64，k_proj=64，v_proj=64
GQA：q_proj=64，k_proj=32，v_proj=32
```

学习者计算：

```text
MHA v_proj 参数量：64
MHA Q/K/V 总参数量：192

GQA v_proj 参数量：32
GQA Q/K/V 总参数量：128
```

四项计算全部正确。这里使用 `bias=False`，统计范围只包括 Q/K/V
三个投影，不包括 Attention 后面的 `o_proj`。

状态：`[实践]` 已算出完整 Q/K/V 投影参数量；下一步解释为什么
K、V 各减半，但 Q/K/V 总量没有减半。

学习者解释：

> 因为 Q 是全量的；如果 Q 也减半，总数就会减半。

解释正确。GQA 只减少 K/V Head，Q Head 数量保持不变，所以
`q_proj` 的 64 个参数完整保留。若 Q 也从 64 减到 32，则总量会是
`32+32+32=96`，确实是 MHA 的一半；但这意味着 Q Head 也减少，
不再是当前讨论的 GQA 配置。

状态更新：`[验收]` 已理解 GQA 的参数节省来自 K/V 投影变窄，
而 Q 投影保持全量。

### 第 11 轮：K/V 投影结果的数据量

为了区分“模型参数”和“运行时产生的数据”，继续沿用
`batch=1、sequence=3` 的小模型：

```text
MHA：K [1,3,8]，V [1,3,8]
GQA：K [1,3,4]，V [1,3,4]
```

学习者计算 K 和 V 合计包含的数值数量：

```text
MHA：48
GQA：24
```

结果正确。计算分别为：

```text
MHA：1*3*8*2 = 48
GQA：1*3*4*2 = 24
```

最后的 `*2` 表示 K 和 V 两份数据。在这个配置下，GQA 不仅减少
K/V 投影参数，也让每次运行产生的 K/V 数据减半。

状态：`[实践]` 已区分投影参数量和投影结果的数据量；下一步只在
存储量层面连接 KV Cache，暂不展开其完整生成流程。

### 第 12 轮：GQA 与 KV Cache 大小

生成文本时，已经出现过的 token 的 K/V 不会因后续新增 token 而
改变。保存这些旧 K/V、供下一步生成直接使用的数据称为 KV Cache。
本轮只建立这一层关系，不展开缓存代码和完整逐 token 生成流程。

在小模型中，每新增一个 token：

```text
MHA：K 有 8 个数，V 有 8 个数
GQA：K 有 4 个数，V 有 4 个数
```

学习者计算每个新 token 给 KV Cache 增加的数据量：

```text
MHA：16
GQA：8
```

计算正确。因此在此配置下，GQA 的 KV Cache 增长速度是 MHA 的
一半。序列越长，累计节省的数据越多。

状态：`[验收]` 已在存储量层面理解减少 K/V Head 会缩小 KV Cache；
KV Cache 的内部生成流程仍按计划留到后续专门学习。

### 第 13 轮：K/V 投影计算量为什么减少

一个 token 经过 `Linear(8, 8)` 时，需要得到 8 个输出，每个输出
使用 8 个输入，约涉及 `8*8=64` 次乘法。经过 `Linear(8, 4)` 时，
只需得到 4 个输出，约涉及 `8*4=32` 次乘法。

因此小模型中：

```text
q_proj：64 -> 64，计算量不变
k_proj：64 -> 32，计算量减半
v_proj：64 -> 32，计算量减半
```

学习者解释：

> 8x4 矩阵只需要 32 个计算。

方向正确。按 PyTorch 的参数形状，`Linear(8, 4)` 的权重是 `[4,8]`；
实际矩阵乘法可以看作输入 `[1,8] @ weight.T [8,4]`，因此仍是
`8*4=32` 次乘法。输出宽度减半，K/V 投影计算也随之减半。

状态：`[验收]` 已理解 GQA 减少 K/V 投影计算量的原因。

### 第 14 轮：GQA 的代价

MHA 为每个 Q Head 提供一套专属 K/V 投影规则；GQA 让多个 Q Head
共享同一套 K/V 投影规则。共享后 Q 仍然不同，所以匹配分数、读取
比例和 Head 输出仍可不同；减少的是能够独立学习的 K/V 规则数量。

学习者判断：

```text
“共享 K/V 后，两个 Q Head 的最终输出一定相同”：错
GQA 可独立学习的 K/V 投影规则：比 MHA 更少
```

两项均正确。GQA 的交换关系至此明确：

```text
收益：更少的参数、更少的 K/V 投影计算、更小的 KV Cache
代价：多个 Q Head 共享 K/V 信息表示，可独立学习的 K/V 规则更少
```

状态：`[验收]` 已能解释 GQA 的主要收益与代价。

### 第 15 轮：检查 MiniMind-3 的真实投影权重

学习者读取 MiniMind-3 第 1 个 Decoder Layer 的 Attention 投影层：

```python
attention = model.model.layers[0].self_attn

for name in ["q_proj", "k_proj", "v_proj", "o_proj"]:
    projection = getattr(attention, name)
    print(
        name,
        "weight shape =", projection.weight.shape,
        "参数量 =", projection.weight.numel(),
        "bias =", projection.bias
    )
```

实际输出：

```text
q_proj weight shape = torch.Size([768, 768]) 参数量 = 589824 bias = None
k_proj weight shape = torch.Size([384, 768]) 参数量 = 294912 bias = None
v_proj weight shape = torch.Size([384, 768]) 参数量 = 294912 bias = None
o_proj weight shape = torch.Size([768, 768]) 参数量 = 589824 bias = None
```

`Linear` 权重 shape 是 `[out_features, in_features]`，因此真实权重验证了：

```text
q_proj：768 -> 768，对应 8 个 96 维 Q Head
k_proj：768 -> 384，对应 4 个 96 维 K Head
v_proj：768 -> 384，对应 4 个 96 维 V Head
o_proj：768 -> 768，接收 8 个 Q Head 各自的 96 维结果
```

四个投影均无 bias。K/V 的输出宽度和参数量都是 Q 的一半，符合
`8` 个 Q Head、`4` 个 K/V Head 的 MiniMind-3 GQA 配置。

状态：`[实践]` 已检查真实模型权重；下一步根据实际输出计算 Q/K/V
投影的总参数量。

学习者根据真实权重计算：

```text
589824 + 294912 + 294912 = 1,179,648
```

结果正确。MiniMind-3 单个 Decoder Layer 中，Q/K/V 三个投影合计
包含 `1,179,648` 个参数。

状态：`[实践]` 已算出真实 GQA 的单层 Q/K/V 参数总量；下一步与
相同 Q Head 数量的普通 MHA 对比。

若改为普通 MHA，8 个 K Head 和 8 个 V Head 都会产生 768 维输出，
三个投影均为 `768*768=589824`。学习者计算：

```text
MHA 单层 Q/K/V 参数：1,769,472
GQA 单层节省参数：589,824
```

两项均正确。MHA 的 Q/K/V 相当于 3 个完整的 `768*768` 矩阵；
当前 GQA 相当于 1 个完整 Q 矩阵，加两个半宽的 K/V 矩阵，总计
相当于 2 个完整矩阵。因此 GQA 在 Q/K/V 范围内减少了三分之一，
省下的数量正好等于一个完整投影矩阵。

状态：`[验收]` 已用 MiniMind-3 真实权重验证 GQA 的投影参数收益；
下一步计入保持不变的 `o_proj`，明确统计范围。

MiniMind-3 的 `o_proj` 在 MHA 和 GQA 中都为 `768*768=589824`，
因为两种结构最终都有 8 个 Q Head 结果，每个结果 96 维，拼接后
仍是 768 维。学习者计算包含 Q/K/V/O 的参数总量：

```text
MHA：1,769,472 + 589,824 = 2,359,296
GQA：1,179,648 + 589,824 = 1,769,472
```

两项均正确。计入 `o_proj` 后，节省的绝对数量仍是 `589,824`；
改变的是计算节省比例时使用的总量。

状态：`[实践]` 已算出单层 Attention 四个投影的真实参数对比；
下一步计算其节省比例。

学习者回答：`25%`，计算正确。

统计范围需要明确区分：

```text
只统计 Q/K/V：MHA 相当于 3 个完整矩阵，GQA 相当于 2 个，节省 1/3
统计 Q/K/V/O：MHA 相当于 4 个完整矩阵，GQA 相当于 3 个，节省 1/4
```

状态更新：`[验收]` 已能基于真实 MiniMind-3 权重计算 GQA 的参数
节省，并能区分不同统计范围下的节省比例。

### 第 16 轮：Day 008 整体复述验收

要求学习者不看局部计算，用自己的话复述 MiniMind-3 的 GQA 分组、
收益和代价。学习者回答：

> 每个 token 会把 8 个 Q Head 分为 4 组，然后分别对应 1 个 K/V。
> 每 2 个 Q Head 共享一个 K/V。参数量变少了，代价是可以学习到的
> 多样性变少了。

核心关系和代价正确。需要补全两处，使表述完整、精确：

1. 每组共享的是 1 个 K Head 和 1 个 V Head；每个 token 位置在这些
   Head 中仍各有自己的 K/V 向量，并非整句话只有一个 K/V 向量。
2. 除参数减少外，已经通过实验验证的收益还包括 K/V 投影计算减少、
   KV Cache 更小。共享 K/V 后，各 Q Head 的 Q、匹配分数、Softmax
   比例和 Head 输出仍然各自独立。

学习者确认补充后的完整表述与自己的理解一致。

状态更新：`[验收]` Day 008 全部验收项完成。学习者已能从每个
token 的 Q/K/V 数量出发，解释 GQA 的共享边界、完整计算、shape、
参数收益和能力代价。

## 七、问题与知识欠账

- RoPE：GQA shape 完成后进入。
- KV Cache：GQA、RoPE 与逐 token 生成连通后进入。
- RMSNorm、残差连接、MLP：Attention 子层完成后返回 MiniMindBlock。

## 八、今日总结

Day 008 从 MiniMind-3 中 `8` 个 Q Head 和 `4` 个 K/V Head 的实际
配置出发，完成了 GQA 的概念、数值、shape 和真实模型权重验证。

学习过程中最重要的纠偏是：不能在尚未定义 token、Head 和 Q/K/V
归属关系时，直接给出 `Q0/Q1` 和一组 K 让学习者计算。纠偏后先固定：

```text
最小 GQA：每个 token -> 2 个 Q、1 个 K、1 个 V
MiniMind-3：每个 token -> 8 个 Q、4 个 K、4 个 V
```

随后完成并验收：

```text
两个 Q Head 共享同一组 K/V
-> repeat_kv 对齐 Head 维
-> 各 Q Head 独立计算分数、Softmax 和 Head 输出
-> 共享 K/V 不等于共享 Attention 结果
```

学习者亲自运行最小 GQA，得到两个不同 Head 输出；又检查了真实
MiniMind-3 第一层 Attention 权重：

```text
q_proj：[768,768]，589824 参数
k_proj：[384,768]，294912 参数
v_proj：[384,768]，294912 参数
o_proj：[768,768]，589824 参数
```

由此算出：只看 Q/K/V 时，GQA 比同规模 MHA 节省三分之一；计入
不变的 `o_proj` 后，四个投影合计节省四分之一。最终能够说明完整
交换关系：参数、K/V 投影计算和 KV Cache 更少；代价是可独立学习
的 K/V 投影规则更少。

配套 Notebook 将最小张量、`repeat_kv`、完整 GQA、shape、参数比较
和真实权重检查整理为可顺序复现的实验。

Notebook 已使用 MiniMind 项目的 `.venv` 从空命名空间顺序执行全部
6 个代码 Cell 通过；本地 `minimind-3` 的真实权重检查也成功完成。

## 九、下一学习日入口

下一次发生实际学习时记为 Day 009，进入 RoPE。仍按交互式方式，
先从“只有 token 内容向量时，Attention 怎样知道先后位置”这个问题
建立位置需求，再解释旋转；不直接从公式或 MiniMind 源码开始。

KV Cache 本日只掌握了“减少 K/V Head 会减少缓存数据量”。它的逐
token 生成过程与代码仍是明确欠账，等待 RoPE 和 Attention 位置处理
连通后再专门学习。
