# Day 016：进入 Pretrain 训练主线

- 日期：2026-09-02
- 学习阶段：`minimind` 训练源码
- 今日主题：从 `train_pretrain.py` 进入预训练数据链，并与 Full SFT 对照
- 当前状态：已完成
- 配套 Notebook：`notebooks/day-016-pretrain.ipynb`

## 一、今天的学习目标

沿真实调用顺序读完预训练入口中与 Full SFT 不同的部分，回答：

```text
普通 text 数据如何进入模型？
BOS、EOS、PAD 分别做什么？
Pretrain 的 labels 从哪里来？
为什么预训练不需要人工逐条标注？
Pretrain 和 Full SFT 的模型、loss、训练循环是否不同？
```

今天不重复 Day 15 已经掌握的 DataLoader、next-token loss、梯度累积、
梯度裁剪、AdamW 和 checkpoint 主循环。

## 二、环境和真实文件边界

此前已经准备好 GPU 环境，并启动过双卡 Full SFT。当天检查时训练进程仍在
运行，不能因为学习源码而终止它。已有权重包括：

```text
out/pretrain_768.pth
out/full_sft_768.pth
out/full_sft_gpu_768.pth（训练过程中生成）
```

Day 16 学习当时本地没有默认的：

```text
dataset/pretrain_t2t_mini.jsonl
```

因此 Day 16 当时可以完整阅读预训练数据处理源码，也可以用已有
`pretrain_768.pth` 做推理对照，但没有伪称已经从随机初始化完整跑过一轮预训练。
学习结束后该文件已经由学习者下载到本地；这不改变 Day 16 的历史实践边界，
完整预训练实跑留待后续安排。

README 中给出的预训练数据格式是：

```jsonl
{"text": "如何才能摆脱拖延症？治愈拖延症并不容易。"}
{"text": "清晨的阳光透过窗帘洒进房间。"}
{"text": "Transformer 通过自注意力机制建模上下文关系。"}
```

## 三、逐轮互动档案

### 第 1 轮：定位预训练入口

源码导入的关键差异：

```python
from dataset.lm_dataset import PretrainDataset
```

Full SFT 使用 `SFTDataset`，预训练使用 `PretrainDataset`。其余模型、优化器、
DataLoader、分布式和 checkpoint 工具基本复用同一套设施。

### 第 2 轮：预训练入口参数

核心参数：

```python
parser.add_argument('--save_weight', default='pretrain', type=str)
parser.add_argument('--epochs', type=int, default=2)
parser.add_argument('--batch_size', type=int, default=32)
parser.add_argument('--learning_rate', type=float, default=5e-4)
parser.add_argument('--accumulation_steps', type=int, default=8)
parser.add_argument('--max_seq_len', default=340, type=int)
parser.add_argument('--data_path', type=str,
                    default='../dataset/pretrain_t2t_mini.jsonl')
parser.add_argument('--from_weight', default='none', type=str)
```

助手解释了：`save_weight` 是保存文件名前缀；`batch_size` 是一次取出的样本
数量；预训练默认学习率较大是因为通常从随机初始化开始，需要较快学习语言
规律。学习者回答：

```text
保存权重的文件名；训练时每个 batch 的大小；因为要从随机初始化的模型开始，
需要较快的学习语言规律。
```

这三点已验收。[实践][验收]

补充说明：`5e-4` 是本仓库默认配置，不是预训练永远必须使用的大学习率。

### 第 3 轮：梯度累积和最大序列长度

助手快速说明：

```text
batch_size=32、accumulation_steps=8
单进程约 32 * 8 = 256 条样本才执行一次 optimizer.step()
第 1 到第 7 个 batch 只累加梯度，第 8 个 batch 才更新参数
```

`max_seq_len=340` 是这次训练实际使用的固定 token 长度，不等于模型理论上
支持的 `max_position_embeddings=32768`。一条正文如果超过 340，正文最多先
保留 338 个 token，因为还要预留 BOS 和 EOS；不足则在右侧补 PAD。

学习者要求不重要的参数快速带过，因此这一轮以讲解为主，没有单独作答验收。
[讲解]

### 第 4 轮：`data_path` 和 `from_weight`

`data_path` 指向数据文件：

```text
Pretrain：../dataset/pretrain_t2t_mini.jsonl，字段是 text
Full SFT：../dataset/sft_t2t_mini.jsonl，字段是 conversations
```

`from_weight` 决定模型从哪里开始：

```text
none：创建模型结构，保留随机初始化参数
pretrain：创建模型结构，再加载 ../out/pretrain_768.pth
```

这解释了两个入口的默认值：

```text
train_pretrain.py：from_weight='none'
train_full_sft.py：from_weight='pretrain'
```

### 第 5 轮：`init_model()` 的预训练分支

调用：

```python
model, tokenizer = init_model(
    lm_config,
    args.from_weight,
    device=args.device
)
```

函数按顺序执行：

```python
tokenizer = AutoTokenizer.from_pretrained(tokenizer_path)
model = MiniMindForCausalLM(lm_config)

if from_weight != 'none':
    weight_path = f'{save_dir}/{from_weight}_{lm_config.hidden_size}{moe_suffix}.pth'
    weights = torch.load(weight_path, map_location=device)
    model.load_state_dict(weights, strict=False)

return model.to(device), tokenizer
```

因此预训练默认 `from_weight='none'` 时，不读取 `../out/pretrain_768.pth`，
而是创建随机初始化模型。Full SFT 才默认读取已有预训练权重。这一轮讲解
完成，但学习者没有单独重复作答。[讲解]

### 第 6 轮：`PretrainDataset` 初始化

源码：

```python
class PretrainDataset(Dataset):
    def __init__(self, data_path, tokenizer, max_length=512):
        super().__init__()
        self.tokenizer = tokenizer
        self.max_length = max_length
        self.samples = load_dataset(
            'json', data_files=data_path, split='train'
        )

    def __len__(self):
        return len(self.samples)
```

`self.samples` 保存从 JSONL 加载的所有样本；`__len__()` 让 DataLoader 知道
数据集大小。这里的输入是一条普通 `text`，不是一组对话消息。[讲解]

### 第 7 轮：正文 token 化和 BOS/EOS

源码：

```python
sample = self.samples[index]
tokens = self.tokenizer(
    str(sample['text']),
    add_special_tokens=False,
    max_length=self.max_length - 2,
    truncation=True
).input_ids
tokens = [self.tokenizer.bos_token_id] + tokens + [self.tokenizer.eos_token_id]
```

`add_special_tokens=False` 是为了由数据集手动控制 BOS/EOS。正文最多使用
`max_length - 2`，前后各预留一个特殊 token。学习者回答：

```text
因为要在前后拼接上开始和结束的 token。
```

回答正确并验收。[验收]

例如正文 token 是 `[10, 20, 30]`，BOS 为 `1`、EOS 为 `2`，添加后是：

```text
[1, 10, 20, 30, 2]
```

### 第 8 轮：固定长度、PAD 和 labels

源码：

```python
input_ids = tokens + [self.tokenizer.pad_token_id] * (
    self.max_length - len(tokens)
)
input_ids = torch.tensor(input_ids, dtype=torch.long)

labels = input_ids.clone()
labels[input_ids == self.tokenizer.pad_token_id] = -100
return input_ids, labels
```

例子：

```text
input_ids：[1, 10, 20, 30, 2, 0, 0, 0]
labels：   [1, 10, 20, 30, 2, -100, -100, -100]
```

`input_ids` 保持固定长度，供 Embedding 和 Transformer 处理；`labels` 复制
输入后，把 PAD 的位置改为 `-100`，让交叉熵忽略它们。学习者确认：

```text
Pretrain 的正文 token 基本都会参与 loss；Full SFT 的用户问题 token 不参与 loss。
```

该区别验收。[验收]

### 第 9 轮：自监督 labels 和 next-token 对齐

模型的 `forward()`：

```python
x = logits[..., :-1, :].contiguous()
y = labels[..., 1:].contiguous()
loss = F.cross_entropy(
    x.view(-1, x.size(-1)),
    y.view(-1),
    ignore_index=-100
)
```

数据集不需要人工写“每个问题的正确答案”。假设：

```text
input_ids：[BOS, 天空, 是, 蓝色, EOS]
labels：   [BOS, 天空, 是, 蓝色, EOS]
```

错位后训练题自动变成：

```text
BOS       -> 天空
天空      -> 是
是        -> 蓝色
蓝色      -> EOS
```

学习者回答：

```text
因为 labels 就是原始数据集错了一位的内容。
```

需要更精确地记录为：labels 创建时和 input_ids 相同，真正错一位是在
`forward()` 中通过 `logits[..., :-1]` 与 `labels[..., 1:]` 实现的。核心理解
正确。[纠偏][验收]

这就是自监督学习：答案直接从原文的下一个 token 自动得到，而不是人工逐条
标注。

### 第 10 轮：BOS、EOS、PAD 的目标位置

样本：

```text
[BOS, 10, 20, EOS, PAD, PAD]
```

错位并忽略 PAD 后，实际需要预测的目标是：

```text
10、20、EOS
```

学习者回答：

```text
10，20，EOS
```

回答正确。[验收]

结论：BOS 提示文本开始，EOS 是模型需要学会预测的结束标志，PAD 只用于
对齐长度。

### 第 11 轮：没有 `attention_mask` 的影响

`PretrainDataset` 只返回：

```python
return input_ids, labels
```

训练时也只调用：

```python
res = model(input_ids, labels=labels)
```

右侧 PAD 因为位于真实文本之后，且使用 Causal Mask，不能反过来影响前面
真实 token 的表示；但 PAD 位置本身仍会经过 Embedding、Attention、
FeedForward 和 LM Head，消耗算力。

学习者第一次回答“不会；不会”，其中第二个“不会”需要纠正：`-100` 只在
最后的交叉熵阶段忽略 PAD，不会让前面的网络自动跳过 PAD。随后学习者确认：

```text
在最后。
```

最终边界已验收：[纠偏][验收]

### 第 12 轮：与 Full SFT 的代码和行为对照

对两个训练入口做源码 diff 后确认，公共训练闭环几乎相同：

```text
DataLoader
-> model(input_ids, labels=labels)
-> 交叉熵 loss
-> backward
-> 梯度裁剪
-> optimizer.step
-> 保存权重
```

不同点只有主要三项：

```text
起始权重：Pretrain 默认 none，SFT 默认 pretrain
数据格式：Pretrain 是 text，SFT 是 conversations
监督位置：Pretrain 几乎所有正文 token，SFT 主要 assistant token
```

学习者回答“不是”，确认不是两套完全不同的模型和 loss。[验收]

### 第 13 轮：已有权重的实际生成对照

使用已有 `pretrain_768.pth` 测试：

```text
💬: 为什么天空是蓝色的
🧠: ？天空之所以呈现蓝色，是因为大气中的气体分子对太阳光的散射作用。...
```

使用已有 `full_sft_768.pth` 测试同样问题：

```text
💬: 为什么天空是蓝色的
🧠: 天空之所以呈现蓝色，主要是因为大气散射的原因。...
```

`eval_llm.py` 对预训练权重使用：

```python
inputs = tokenizer.bos_token + prompt
```

对 SFT 权重使用聊天模板和 generation prompt：

```python
inputs = tokenizer.apply_chat_template(
    conversation,
    tokenize=False,
    add_generation_prompt=True
)
```

所以预训练模型更像在续写一段普通文本，可能先补出问题的问号；SFT 模型
通过 `user -> assistant` 结构，更直接进入回答。两者都能回答，不代表行为
目标相同。[实践][验收]

### 第 14 轮：Day 16 收束

学习者复述：

```text
Pretrain 学到：这种问题后面通常跟着怎样的解释。
Full SFT 进一步学到：当 user 提出这种问题时，assistant 应当直接给出解释。
```

复述准确。[验收]

## 四、今日可复现教程

### 1. 用最小例子模拟 `PretrainDataset.__getitem__()`

```python
import torch

max_length = 8
bos_id, eos_id, pad_id = 1, 2, 0
body_tokens = [10, 20, 30]

tokens = [bos_id] + body_tokens[:max_length - 2] + [eos_id]
input_ids = tokens + [pad_id] * (max_length - len(tokens))
input_ids = torch.tensor(input_ids, dtype=torch.long)

labels = input_ids.clone()
labels[input_ids == pad_id] = -100

print('input_ids:', input_ids)
print('labels:', labels)
```

预期：

```text
input_ids: tensor([1, 10, 20, 30, 2, 0, 0, 0])
labels: tensor([1, 10, 20, 30, 2, -100, -100, -100])
```

### 2. 观察 next-token 目标

```python
x_positions = input_ids[:-1]
y_targets = labels[1:]
print(list(zip(x_positions.tolist(), y_targets.tolist())))
```

目标中包含正文 token 和 EOS，不包含第一个 BOS；PAD 的目标是 `-100`。

## 五、今日掌握边界

已验收：

```text
PretrainDataset 与 SFTDataset 的输入差异
max_length - 2 为什么要预留 BOS/EOS
Pretrain 正文 token 和 SFT assistant token 的监督差异
自监督 labels 与 next-token 对齐
BOS、EOS、PAD 的职责
-100 只在交叉熵最后忽略 PAD
Pretrain 与 Full SFT 共享模型、loss 和训练循环
预训练模型与 SFT 模型的输入模板和生成行为差异
```

只完成讲解、尚未单独作答的内容：

```text
accumulation_steps 和 max_seq_len 的参数解释
init_model() 中 tokenizer/model/权重分支的逐项复述
```

尚未完成的实践：

```text
用 dataset/pretrain_t2t_mini.jsonl 从随机初始化完整执行预训练
```

原因是该数据文件当前不在本地；README 只说明了格式和下载位置，不能把
缺少文件时的源码阅读写成完整训练实践。

## 六、下一天恢复点

Day 17 从 `minimind/trainer/train_tokenizer.py` 开始，目标是理解：

```text
6400 词表如何训练
训练语料如何读取
特殊 token 如何定义
tokenizer.json / tokenizer_config.json 保存什么
训练后的 tokenizer 如何被模型和 Dataset 加载
```

已经掌握 Tokenizer 的使用方式，因此不再从 token 基础概念重新开始；只在
源码遇到新 API 或训练算法时补足必要背景。
