# Day 017：从 `train_tokenizer.py` 开始阅读 Tokenizer 训练

- 日期：2026-09-02
- 学习阶段：`minimind` Tokenizer 源码
- 今日主题：定位 Tokenizer 训练入口，理解训练数据、词表和保存产物
- 当前状态：进行中
- 配套 Notebook：本日结束后创建

## 一、与前面学习的关系

Day 001 已经学习过如何使用已有 tokenizer：

```text
文本 -> token -> token ID -> Embedding
```

Day 017 不重新讲使用阶段，而是反过来追问：

```text
这些 token 和 token ID 是谁训练出来的？
6400 这个词表大小从哪里进入程序？
tokenizer.json 和 tokenizer_config.json 各保存什么？
训练完成后，模型和 Dataset 如何加载它？
```

## 二、阅读边界

严格沿当前目录内 `minimind` 仓库的真实源码调用顺序：

```text
trainer/train_tokenizer.py
-> 参数与入口判断
-> 训练语料读取
-> tokenizer trainer / model 配置
-> special tokens
-> tokenizer 训练
-> tokenizer.json 等文件保存
-> 后续 AutoTokenizer 加载
```

不会跳到 `minimind-v` 或 `minimind-o`，也不会在本日提前进入模型训练主线。

## 三、逐轮互动档案

### 第 1 轮：脚本提醒、全局配置和 tokenizer 配套关系

脚本开头明确写着不建议重复训练 tokenizer，因为模型参数中的 embedding
和 lm_head 使用的词表 ID 必须与 tokenizer 保持同一套映射。学习者回答：

```text
因为 token id 和 embedding 的 id 对应关系错乱了；避免覆盖文件；因为他们有一一对应的关系。
```

三点均正确并验收：[验收]

关键配置：

```python
DATA_PATH = '../dataset/sft_t2t_mini.jsonl'
TOKENIZER_DIR = '../model_learn_tokenizer/'
VOCAB_SIZE = 6400
SPECIAL_TOKENS_NUM = 36
```

`TOKENIZER_DIR` 使用学习目录而不是现有 `model/`，避免覆盖正在使用的词表；
`VOCAB_SIZE=6400` 与 MiniMind 的 `[6400, 768]` embedding/lm_head 维度配套。

### 第 2 轮：`get_texts()` 读取并扁平化对话

源码：

```python
def get_texts(data_path):
    with open(data_path, 'r', encoding='utf-8', errors='ignore') as f:
        for i, line in enumerate(f):
            if i >= 10000:
                break
            try:
                data = json.loads(line)
                contents = [
                    item.get('content')
                    for item in data.get('conversations', [])
                    if item.get('content')
                ]
                if contents:
                    yield '\n'.join(contents)
            except json.JSONDecodeError:
                continue
```

学习者亲自执行：

```bash
minimind/.venv/bin/python - <<'PY'
from trainer.train_tokenizer import get_texts

texts = get_texts('minimind/dataset/sft_t2t_mini.jsonl')
for i, text in zip(range(2), texts):
    print(f'--- sample {i} ---')
    print(repr(text[:300]))
    print('字符数：', len(text))
PY
```

真实输出的关键片段：

```text
--- sample 0 ---
'你背后的模型是哪个版本？它由谁开发？\n我是由jingyaogong开发的高效小参数AI模型。\n你模型的训练数据来源是什么？\n...'
字符数： 396

--- sample 1 ---
'你究竟是哪个模型开发的？\n我是由jingyaogong创建的高效小参数AI模型。\n你最近有没有看过什么有趣的电影？\n...'
字符数： 519
```

实际现象确认：

```text
一条 JSONL 记录 -> 一段普通字符串
多轮消息之间 -> 使用换行连接
role 字段 -> 没有进入训练文本
空 content -> 被过滤
独立 reasoning_content -> 当前函数没有提取
```

`yield` 使 `get_texts()` 成为生成器，tokenizer 迭代时才逐条读取；
`i >= 10000` 让这个学习脚本最多使用前 10000 行。该轮实践并验收。[实践][验收]

### 第 3 轮：BPE 容器与训练器的区别

源码先创建：

```python
tokenizer = Tokenizer(models.BPE())
tokenizer.pre_tokenizer = pre_tokenizers.ByteLevel(add_prefix_space=False)
```

随后才创建训练规则并执行：

```python
trainer = trainers.BpeTrainer(...)
tokenizer.train_from_iterator(texts, trainer=trainer)
```

学习者确认：

```text
Tokenizer(models.BPE()) 只是个容器。
```

精确含义是：此时已经选择了 BPE 作为内部模型类型，但还没有通过语料学出
完整的 vocab 和 merges；它们要等 `train_from_iterator()` 执行后才填入。[验收]

本轮还纠正了输出形式：此前同一段教学内容同时出现在过程消息和结尾消息中，
造成重复。后续过程消息只报告进度，知识讲解只出现一次。[纠偏]

### 第 4 轮：训练器参数与特殊 token 的时机

训练器配置：

```python
trainer = trainers.BpeTrainer(
    vocab_size=vocab_size,
    show_progress=True,
    initial_alphabet=pre_tokenizers.ByteLevel.alphabet(),
    special_tokens=all_special_tokens
)
```

学习者回答 BPE 训练器为什么提前接收 `special_tokens`：

```text
因为要保证语料内容的特殊 token 被有效的处理。
```

方向正确。精确边界是：训练开始前登记特殊 token，可以让训练器把它们作为
完整控制标记处理、提前占用稳定的词表 ID，并排除普通 BPE 的拆分和合并；若
训练完成后才追加，虽然可以加入编码器，但它们没有参与训练阶段的特殊处理，
还可能改变词表 ID/总量与模型预期。[验收]

当前已有 tokenizer 的实际 ID 也验证了这一点：

```text
<|endoftext|> -> 0
<|im_start|>  -> 1
<think>       -> 25
<|buffer1|>   -> 27
```

这些控制标记都各占一个完整 token。

### 第 5 轮：ByteLevel 解码器和特殊 token 注册

训练完成后执行：

```python
tokenizer.decoder = decoders.ByteLevel()
tokenizer.add_special_tokens(special_tokens_list)
```

ByteLevel 预切分把文本转换成可逆的字节级内部表示，因此需要对应的
ByteLevel decoder 把内部片段恢复为中文、空格和标点等原文。`add_special_tokens`
则把控制标记登记到 tokenizer 的特殊 token 管理中，供后续编码、解码和
`skip_special_tokens` 使用。学习者确认两行的职责。[验收]

### 第 6 轮：保存 tokenizer 产物

源码：

```python
os.makedirs(tokenizer_dir, exist_ok=True)
tokenizer.save(os.path.join(tokenizer_dir, 'tokenizer.json'))
tokenizer.model.save(tokenizer_dir)
```

执行顺序是：

```text
创建输出目录（已存在也不报错）
-> 保存完整 tokenizer 配置到 tokenizer.json
-> 保存 BPE 模型的独立文件
```

完整 `tokenizer.json` 包含预切分器、BPE 模型、词表、合并规则、decoder 和
added tokens 等信息；`tokenizer.model.save()` 对 BPE 通常会额外写出：

```text
vocab.json   token -> ID
merges.txt   BPE 合并规则及顺序
```

两种保存形式提供了完整单文件加载和 BPE 模型拆分文件两种使用方式。当前
仓库运行时主要直接加载 `model/tokenizer.json` 和 `tokenizer_config.json`。

### 第 7 轮：为什么训练后又修改 `special` 标志

源码读取并修改刚保存的 JSON：

```python
tokenizer_json_path = os.path.join(tokenizer_dir, 'tokenizer.json')
with open(tokenizer_json_path, 'r', encoding='utf-8') as f:
    tokenizer_data = json.load(f)
for token_info in tokenizer_data.get('added_tokens', []):
    if token_info['content'] not in special_tokens_list:
        token_info['special'] = False
with open(tokenizer_json_path, 'w', encoding='utf-8') as f:
    json.dump(tokenizer_data, f, ensure_ascii=False, indent=2)
```

原因是训练器收到的是 `all_special_tokens` 共 36 个 token，但项目只希望
`special_tokens_list` 中的 21 个基础控制标记被 Transformers 当作“真正的
特殊 token”。`additional_tokens_list` 中的 `<think>`、`<tool_call>` 等以及
9 个 buffer token 仍然是完整的 added token，但不应被 `skip_special_tokens=True`
自动删除。随后脚本还会把相同规则写入 `added_tokens_decoder`。

现有 tokenizer 的实测：

```text
<|im_start|>  skip_special_tokens=True -> ''
<|endoftext|> skip_special_tokens=True -> ''
<think>       skip_special_tokens=True -> '<think>'
<tool_call>   skip_special_tokens=True -> '<tool_call>'
<|buffer1|>   skip_special_tokens=True -> '<|buffer1|>'
```

这保证正常对话边界标记可以隐藏，而思考和工具调用标记在需要解析或调试时
仍然可见。[实践][验收]

### 第 8 轮：`tokenizer_config.json` 与手动 BOS/EOS

配置中：

```python
"add_bos_token": False,
"add_eos_token": False,
```

表示普通的 `tokenizer(text)` 不自动在两端添加 BOS/EOS。预训练数据集随后
显式执行：

```python
tokens = [tokenizer.bos_token_id] + tokens + [tokenizer.eos_token_id]
```

学习者确认：需要这些标记时由具体调用者明确添加。这样可以让
`PretrainDataset` 和 `chat_template` 各自控制边界，避免 tokenizer 自动添加
一份、调用方又添加一份。[验收]

当前已讲清 `tokenizer.json`（BPE 词表、merges、ByteLevel、added tokens）与
`tokenizer_config.json`（BOS/EOS/PAD、chat template、加载和使用配置）的
职责差异。

### 第 9 轮：`eval_tokenizer()` 的可逆性检查和 BPE 拆分文件

学习者确认：

```text
response == new_prompt 检查编码和解码是否一一对应。
```

进一步区分保存产物：

```text
tokenizer.save('tokenizer.json')
    保存完整 Tokenizer 快照，包括 BPE、vocab、merges、ByteLevel、added tokens 等。

tokenizer.model.save(tokenizer_dir)
    只保存内部 BPE 模型，通常生成 vocab.json 和 merges.txt。
```

其中：

```text
vocab.json：token -> token ID 映射
merges.txt：BPE 片段的合并规则和优先顺序
```

学习者确认 `vocab.json` 保存的是映射，并追问加载时是否只需要两个文件。
结论是：对当前 MiniMind 的 `AutoTokenizer.from_pretrained('../model')`，
核心是：

```text
tokenizer.json
tokenizer_config.json
```

因为前者已经包含 BPE 词表和 merges，后者包含 BOS/EOS/PAD、added token
行为和 chat template。`vocab.json`、`merges.txt` 是 BPE 模型的可选拆分产物，
不是当前目录加载所必需的。[验收]

### 第 10 轮：压缩率和流式解码测试

`eval_tokenizer()` 还计算：

```python
compression_ratio = char_count / token_count
```

它粗略衡量一段文本需要多少 token。压缩率较高通常表示 token 更少，但不能
脱离词表大小、语言种类和模型效果单独判断好坏。

流式解码逐个接收 token ID，并暂存到 `token_cache`：

```python
current_decode = tokenizer.decode(token_cache)
if current_decode and '\ufffd' not in current_decode:
    print(...)
    token_cache = []
```

ByteLevel 的一个中文字符可能由多个 UTF-8 字节组成。缓存中的字节序列不完整
时，解码可能出现替换字符 `\ufffd`；代码等到能够无错误解码后再输出，避免
流式显示半个字符。[讲解]

## 四、今日掌握边界

已验收：

```text
Tokenizer 词表必须与 embedding/lm_head 的 ID 映射配套
get_texts() 如何把 JSONL 对话变成训练文本
BPE 容器、BpeTrainer 和 train_from_iterator 的职责
ByteLevel 预切分和 decoder 的作用
特殊 token 的完整编码与 special 标志的区别
buffer token 的预留用途
tokenizer.json、vocab.json、merges.txt、tokenizer_config.json 的关系
add_bos_token/add_eos_token 关闭后由调用者手动控制边界
encode/decode 可逆性检查
```

已讲解但未单独实践：

```text
使用完整 10000 条语料重新训练 tokenizer
压缩率数值在不同 tokenizer 之间的系统比较
流式解码缓存的逐 token 现场输出
```

脚本顶部明确不建议把学习脚本训练出的新 tokenizer 用于现有 MiniMind 权重。
本日没有覆盖或修改 `model/` 中的现有 tokenizer 文件。[完成]

## 五、下一天恢复点

Day 18 进入 `minimind` 的 MoE 分支：

```text
model/model_minimind.py
-> MOEFeedForward
-> num_experts / num_experts_per_tok
-> router、top-k、专家选择和 aux loss
```

已经掌握的 Dense FeedForward 不再重复，只补充 MoE 新增逻辑。
