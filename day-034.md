# Day 034：minimind-o 训练样本的数据结构

日期：2026-09-15

学习阶段：第三阶段 `minimind-o`

当前状态：进行中（本日内容已讲解，尚未完成验收）

## 一、今天学习了什么

今天从 `minimind-o/dataset/omni_dataset.py` 进入训练数据，重点回答：

> `minimind-o` 训练时，一条数据样本里到底有什么？

先区分两层：

```text
Parquet 中保存的原始记录
        -> OmniDataset.__getitem__() 整理
        -> 交给 model.forward() 的单条样本
```

## 二、Parquet 原始字段

一条原始记录主要包括：

```text
conversations：system/user/assistant 对话的 JSON
question_audios：用户语音的音频字节
image_bytes：图片二进制数据
answer_audios：助手语音的 Mimi audio code
ref_audios：可选的音色参考 Mimi code
spk_emb：可选的 speaker embedding，通常为 192 维
```

其中 `answer_audios` 不是 wav，而是 Mimi 编码后的整数序列。每连续 8 个
整数对应一个时间位置上的 8 个 audio codebook，数据读取后会拆成 8 路。

## 三、`__getitem__()` 返回的单条样本

真实返回值为：

```python
(
    input_ids,
    text_labels,
    audio_labels,
    audio_inputs,
    audio_len,
    pixel_values,
    spk_emb,
)
```

大致含义和形状如下：

```text
input_ids：     [9, T-1]
                 前 8 路是 audio code 输入，第 9 路是文本 token 输入

text_labels：   [T-1]
                 assistant 文本回答的监督目标

audio_labels：  [8, T-1]
                 assistant 语音的 8 路 Mimi code 监督目标

audio_inputs：  [1, 音频帧数, 560]
                 用户语音经过 SenseVoice 前处理后的 fbank

audio_len：     整数，用户语音的有效帧数

pixel_values：  图像处理器输出的图像张量

spk_emb：       [192] 的音色条件；没有时使用零向量
```

## 四、最重要的输入与目标对应关系

```text
question_audios
    -> audio_inputs
    -> 模型输入，用于理解用户语音

answer_audios
    -> audio_labels
    -> 监督目标，用于训练 Talker 预测助手语音 code
```

特别注意：`audio_inputs` 是用户问题的连续 fbank 特征，`audio_labels` 是
助手回答的离散 Mimi code。它们不是同一份数据，也不处在同一个处理阶段。

文字部分也遵循同样的“输入/目标”区分：

```text
conversations 中的 prompt token
    -> input_ids 的第 9 路

最后一个 assistant 的文字回答
    -> text_labels
```

训练时，用户问题、system prompt 和 padding 位置的 label 通常是 `-100`，
这些位置不参与对应损失计算。

## 五、与训练循环的连接

`omni_collate_fn` 会把多条单样本堆成 batch，之后交给：

```python
res = model(
    input_ids,
    audio_inputs=audio_inputs,
    audio_lens=audio_lens,
    pixel_values=pixel_values,
    spk_emb=spk_emb,
)
```

训练目标可以概括为：

```text
text_loss：  Thinker 预测 assistant 文本
audio_loss： Talker 预测 assistant 的 8 路 Mimi code
aux_loss：   模型的辅助损失
```

Mimi 把 audio code 解码成声音是在推理输出阶段发生的，不是训练样本读取时
直接保存或预测 wav。

## 六、今天的掌握状态

```text
[讲解] 已区分 Parquet 原始字段与 __getitem__ 返回字段
[讲解] 已说明 question_audios -> audio_inputs 的输入路径
[讲解] 已说明 answer_audios -> audio_labels 的监督路径
[讲解] 已连接到 model.forward() 和 text/audio loss
```

今天没有进行这部分的回答验收，因此不把它标记为 `[验收]` 或“已掌握”。

## 七、下次恢复点

先完成一个最小验收：

```text
audio_inputs 和 audio_labels 哪个是模型输入，哪个是监督目标？
```

验收通过后，再按顺序阅读 `omni_collate_fn`，理解单条样本如何组成 batch，
最后进入 `model.forward()` 中的文字损失和 8 路音频损失。
