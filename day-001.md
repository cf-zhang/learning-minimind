# Day 001：运行基线、Tokenizer 与 Embedding

- 日期：2026-08-11
- 学习阶段：阶段 1 - MiniMind 推理链路
- 今日主题：建立本地运行基线，理解并实践 Tokenizer、token ID、Embedding 与初步上下文化
- 当前状态：已完成

## 零、如何使用本课

这一天包含两种不同性质的工作：

```text
环境准备：助手检查硬件、安装依赖、下载权重、代跑冒烟测试
真实学习：学习者亲自运行模型，提出问题，执行 Tokenizer、Embedding 和 hidden states 实验
```

新人复现时，不需要把所有环境排障当成模型知识背下来。建议按下面的互动档案理解问题来源，再按照“实验记录”中的命令亲自运行。只有亲自观察并能解释结果，才算完成本课。

前置条件：

- 具备基本 Python 和命令行经验。
- 不要求懂机器学习、矩阵、Transformer 或微积分。
- 模型权重和环境准备方法见本文件后文；硬件不同会导致速度不同，不影响核心结论。

## 逐轮互动档案

以下按真实先后保存 Day 001 的教学过程。解释文字是归档时为新人补充的过程整理；学习者提交的命令、问题和输出按实际记录保留。

### 第 1 轮：先在本地跑起来

学习者提出：第二天先把本地仓库运行起来作为基础，并询问是否有更好的建议。

助手先进行了环境、GPU、依赖、随机小模型和发布模型检查。这些操作证明仓库具备学习条件，但当时出现了第一个重要问题：助手把自己完成的运行工作推进得过快。

### 第 2 轮：学习方式第一次纠偏

学习者明确指出：

> 这事儿闹的，是你学完了又不是我学完了。我们应该是交互式的方式，你掌控进度和内容，我来实践和理解才对啊。

这次纠偏固定了角色分工：

```text
助手：选择一个小目标、讲解、布置实验、检查理解、控制进度
学习者：亲自执行、观察、思考、提问和复述
```

状态：`[纠偏]` 助手代跑不再计入学习者完成进度。

### 第 3 轮：学习者亲自运行真实模型

学习者在 `minimind/.venv` 中执行：

```bash
python eval_llm.py \
  --load_from ./minimind-3 \
  --device cpu \
  --max_new_tokens 64 \
  --temperature 0.1 \
  --top_p 0.9 \
  --show_speed 1
```

选择手动输入后向模型提出“做一下自我介绍”和“你是谁”，获得了两段真实回答，速度分别为 `62.11 tokens/s` 和 `55.39 tokens/s`。完整输出保存在后文“学习者实践 1”。

状态：`[实践]` 学习者已经亲自启动仓库入口并完成交互。

### 第 4 轮：不能让新人盲猜

助手根据第一段回答突然停止的现象，要求学习者猜原因。学习者指出：

> 我哪里知道，应该是我提问题你告诉我猜对啊。

问题不在学习者，而在教学顺序：此时还没有讲过 token、生成上限或停止条件，不能用猜谜代替知识输入。

随后才结合真实输出解释：回答恰好生成了 64 个 token，与 `--max_new_tokens 64` 相同，所以是达到上限停止；自然生成结束还可能由 EOS token 触发。

状态：`[纠偏]` 以后只检查已经讲过的知识，不要求猜陌生机制。

### 第 5 轮：由学习者决定先问“为什么”

学习者提出：

> 为什么要 Tokenizer 以及 token，然后才是怎么 Tokenizer。

因此没有直接进入 BPE 算法，而先建立动机：

```text
模型的计算对象是数值张量，不能直接拿字符串做矩阵运算
  -> 需要把文本切成有限词表中的离散单位 token
  -> 每个 token 对应一个整数 ID
  -> Tokenizer 负责文本、token 与 token ID 之间的转换
```

为什么不简单地“一个字一个 ID”或“一个词一个 ID”：纯字符会使序列较长，整词词表又无法覆盖所有新词；子词切分在词表大小、序列长度和生词处理之间折中。MiniMind 使用的 ByteLevel BPE 还可以退回到字节层面处理陌生字符。

状态：`[讲解]` Tokenizer 的必要性已说明，随后通过实验验收。

### 第 6 轮：输出不断追加，输入会不会无限变长

学习者追问：

> 如果一直把输出拼接到输入的后面，那岂不是会把输入变得非常的长？

答案是“会增长，但有边界”：

```text
提示词 token + 已生成 token = 当前逻辑上下文
```

单次生成受 `max_new_tokens`、EOS 和模型上下文窗口限制；多轮聊天是否继续增长，取决于程序是否把历史消息再次放回输入。KV Cache 只减少对旧 token 的重复计算，不会让逻辑上下文消失，也不会让缓存不再增长。

学习者随后决定先进入 Tokenizer，再学习 Embedding。KV Cache 只保留最低直觉，详细机制明确延期。

状态：`[验收]` 理解上下文会增长且有上限；`[欠账]` KV Cache 内部原理。

### 第 7 轮：亲自观察 Tokenizer 和 Embedding

学习者在 Python REPL 中对文本：

```text
我正在学习MiniMind大模型！
```

依次执行了：

```text
文本 encode
  -> 查看 token IDs
  -> 查看内部 token 字符串
  -> 整体 decode
  -> 加载模型 Embedding
  -> 构造 input_ids
  -> 查表得到 vectors
  -> 查看各张量 shape
```

实际得到 11 个 token ID，Embedding 权重 shape 为 `[6400, 768]`，输入 shape 为 `[1, 11]`，输出 shape 为 `[1, 11, 768]`。内部 token 出现乱码状字符，引出了 ByteLevel 的显示方式问题。

状态：`[实践]` 完整命令和输出见后文“学习者实践 2”。

### 第 8 轮：验证 Embedding 真的是查表

学习者没有停留在 shape 描述，而是继续逐 ID 解码，得到：

```text
我 | 正在 | 学习 | M | in | i | M | ind | 大 | 模型 | ！
```

然后验证：

```text
vectors[0, 0] == embedding.weight[463] -> True
两个 M 的 token ID 都是 80
两个 M 的初始向量完全相同 -> True
```

这直接证明 Embedding 当前阶段可以理解为：使用 token ID 选择权重表中的一行。`[6400, 768]` 表示共有 6400 行可查，每行是一个 768 维向量。

状态：`[实践]`、`[验收]` Embedding 查表机制与三个 shape 已掌握。

### 第 9 轮：相同 token 为什么后来会不同

学习者让模型返回全部 hidden states，比较两个 `M` 所在位置：

```text
进入 Transformer 前：相同
经过第 1 层后：不同
经过第 8 层后：不同
```

距离从 `0.0` 依次增大到 `1.903...`、`8.682...` 和 `49.399...`。这说明模型外部 shape 一直保持 `[1, 11, 768]`，但每层都在修改内部数值；相同 token 的表示会因位置和上下文不同而分化。

REPL 中一度出现 `IndentationError: unexpected indent`。原因是在顶层 `for` 前多输入了缩进，删除多余缩进后成功执行，与模型无关。这个排错过程也保留给复现者参考。

状态：`[实践]`、`[验收]` 已观察上下文化现象；尚未学习 Attention 与位置编码如何实现它。

### 第 10 轮：决定是否现在学习 KV Cache

学习者主动询问前面提到的 KV Cache 是否需要现在理解；如果不需要，就整理文档结束当天学习。

根据前置依赖，双方决定不展开。原因不是它不重要，而是要解释“缓存什么、为什么缓存 K/V、shape 如何变化”，必须先理解 Q/K/V、因果生成和 GQA。现在硬讲只会堆积术语。

Day 001 因此停在一个准确边界：

```text
已经知道 KV Cache 用于复用历史计算
尚不知道 K/V 是什么，也不假装已经掌握缓存实现
```

## 一、今日目标

今天不急于逐行解释模型，而是先建立一条可靠、可重复的本地运行基线：

1. 确认硬件、Python、PyTorch、CUDA 和依赖状态。
2. 创建与系统 Python 隔离的 MiniMind 学习环境。
3. 在不依赖发布权重的情况下加载仓库自带 Tokenizer。
4. 随机初始化一个极小 MiniMind，完成一次前向传播。
5. 使用同一个极小模型完成一次自回归生成。
6. 根据结果决定是否下载发布权重，进入真实对话推理。

## 二、为什么先做两层运行验证

仓库当前没有发布模型权重。直接追求真实对话，需要先下载较大的模型文件，还会把环境、权重格式、模型实现和生成逻辑等问题混在一起。

因此采用两层基线：

```text
第一层：零权重结构冒烟测试
Tokenizer -> 随机初始化极小模型 -> Forward -> Logits -> Generate

第二层：发布权重真实推理
Tokenizer -> 预训练/SFT 权重 -> eval_llm.py -> 对话输出
```

第一层不能生成有意义的文本，因为模型参数是随机的；它的价值是确认代码、依赖、张量 shape 和生成循环可以正常工作。第一层通过后，第二层遇到问题时就可以更准确地定位到权重或推理配置。

## 三、初始环境检查

检查结果：

```text
系统：Linux
Python：3.12.12
Python 路径：/home/zcf/Miniforge3/bin/python
GPU 0：NVIDIA GeForce RTX 3090，24576 MiB
GPU 1：NVIDIA GeForce RTX 3090，24576 MiB
当前默认环境 PyTorch：未安装
当前默认环境 Transformers：未安装
当前默认环境 Tokenizers：未安装
```

仓库本地已有：

- `model/tokenizer.json`
- `model/tokenizer_config.json`
- MiniMind 模型和训练源码

仓库本地缺少：

- `out/*.pth` 原生 PyTorch 模型权重
- Transformers 格式的发布模型目录
- 训练数据集

## 四、环境策略

不直接污染当前 Miniforge 默认环境，也不在第一步安装完整 `requirements.txt`。

原因：完整依赖包含数据清洗、WebUI、API、评测、日志平台和强化学习等组件。它们不是今天验证 Tokenizer、模型前向和生成所必需的，而且会显著增加安装时间与依赖冲突面。

今天优先采用：

1. 检查本机是否已有包含 PyTorch 的 Conda 环境。
2. 若没有合适环境，为 `minimind` 创建项目级隔离环境。
3. 只安装最小运行依赖。
4. 冒烟测试通过后，再按实际学习内容逐步增加依赖。

## 五、环境准备验收

以下是助手完成的环境准备结果，只表示实验条件已经具备，不表示学习者已经掌握相关知识：

- [x] 隔离环境可稳定激活或通过固定 Python 路径调用。
- [x] `import torch` 成功；GPU 状态通过独立 CUDA 环境验证。
- [x] `import transformers` 成功。
- [x] 仓库自带 Tokenizer 可以加载并完成 encode/decode。
- [x] 极小 MiniMind 前向传播成功。
- [x] 前向输出 logits shape 与配置一致。
- [x] 极小 MiniMind 的 `generate` 循环成功产生新 token。
- [x] 下载发布模型并完成真实文本生成。
- [x] 直接运行仓库原始 `eval_llm.py`。
- [x] 把实际命令、输出和发现记录到本文件。
- [x] 明确后续环境与学习入口。

## 六、当前知识提示

今天先认识但不要求立即完全掌握三个词：

- Tokenizer：在文本与整数 token ID 序列之间转换。
- Forward：给定一批 token ID，模型一次性计算各位置的输出 logits。
- Generate：反复调用 Forward，每次选择一个新 token，再把它接回输入继续生成。

这些概念会在运行成功后结合实际张量和代码详细学习。

## 七、实验记录

### 实验 1：借用已有 CUDA 环境进行结构冒烟测试

本机已有一个可用的 Python 环境：

```text
/home/zcf/whisper/ebook2audiobook/python_env/bin/python
```

关键版本：

```text
PyTorch：2.7.1+cu126
Transformers：4.55.4
CUDA 可用：True
GPU 数量：2
GPU：NVIDIA GeForce RTX 3090 x 2
```

使用仓库自带 Tokenizer 编码文本：

```text
原始文本：你好，MiniMind！
token IDs：[1968, 294, 80, 301, 108, 80, 916, 1364]
token 数量：8
decode 结果：你好，MiniMind！
词表大小：6400
```

然后随机初始化一个用于快速实验的极小模型：

```text
hidden_size：64
num_hidden_layers：2
num_attention_heads：4
num_key_value_heads：2
head_dim：16
intermediate_size：128
参数量：483,712
```

实际输出：

```text
input_ids shape：(1, 8)
hidden_states shape：(1, 8, 64)
logits shape：(1, 8, 6400)
生成前序列长度：8
生成 5 个 token 后长度：13
```

随机模型生成了重复标点和无意义字符。这是预期结果，因为模型结构虽然能运行，但随机参数没有从数据中学到语言规律。

这个实验第一次建立了以下对应关系：

```text
1            -> batch size，一次输入 1 条文本
8            -> sequence length，这条文本被编码为 8 个 token
64           -> hidden size，每个 token 在模型内部使用 64 维向量表示
6400         -> vocab size，每个位置为词表中的 6400 个 token 产生一个分数
```

### 实验 2：创建项目隔离环境

在 `minimind/` 内创建：

```text
minimind/.venv
```

环境被仓库 `.gitignore` 忽略，不会提交到上游仓库。

最终保留的基础版本：

```text
Python：3.12.3
PyTorch：2.7.1+cpu
Transformers：4.57.6
Tokenizers：0.22.2
```

创建与安装命令：

```bash
cd /home/zcf/githubs/minimind/minimind
uv venv .venv --python 3.12
uv pip install --python .venv/bin/python \
  --index https://download.pytorch.org/whl/cpu \
  --index-strategy unsafe-best-match \
  'torch==2.7.1+cpu' 'transformers==4.57.6'
```

最初尝试安装 GPU 版 PyTorch，但 CUDA 依赖下载量超过 2 GB，当时网络速度过低。由于已有 CUDA 环境可以验证 GPU，而今天主要目标是建立代码学习基线，因此停止大型下载，保留完整可用的 CPU 项目环境。进入正式训练阶段前，再建立 MiniMind 专用 GPU 环境。

### 实验 3：在项目环境重复结构冒烟测试

项目 `.venv` 中得到：

```text
input_ids shape：(1, 8)
hidden_states shape：(1, 8, 64)
logits shape：(1, 8, 6400)
generated shape：(1, 13)
```

结果与 CUDA 环境一致，说明模型的逻辑输出 shape 不依赖 CPU 或 GPU；设备主要影响可运行算子、精度、显存和速度。

### 实验 4：验证 Hugging Face 网络

初次下载时，连接 `huggingface.co:443` 返回 `Network is unreachable`。配置本地网络代理后重新检查：

```text
https://huggingface.co -> HTTP/2 200
CloudFront 节点 -> HKG61-P1
```

说明 Hugging Face 网络已经恢复。

下载命令：

```bash
cd /home/zcf/githubs/minimind/minimind
.venv/bin/hf download jingyaogong/minimind-3 --local-dir ./minimind-3
```

下载结果：

```text
文件数：29
本地目录：minimind/minimind-3
目录大小：约 134 MB
核心权重：model.safetensors，约 122 MB
```

发布配置的主要参数：

```text
模型类型：Qwen3ForCausalLM
参数量：63,912,192
hidden_size：768
num_hidden_layers：8
num_attention_heads：8
num_key_value_heads：4
head_dim：96
intermediate_size：2432
vocab_size：6400
max_position_embeddings：32768
dtype：float16
```

### 实验 5：发布模型 GPU 真实生成

使用 CUDA 环境加载 `minimind-3/model.safetensors`，输入：

```text
请用一句话介绍你自己。
```

结果：

```text
输入 token 数：25
生成 token 数：15
用时：0.715 秒
速度：20.98 token/s
回答：我是一个AI助手，旨在提供高效、准确的信息和帮助。
```

这证明以下路径已经真实可用：

```text
文本
  -> Chat Template
  -> Tokenizer
  -> input_ids / attention_mask
  -> MiniMind-3 权重
  -> GPU Forward
  -> Generate
  -> decode
  -> 中文回答
```

### 实验 6：直接运行仓库原始 eval_llm.py

使用项目 CPU 环境执行：

```bash
cd /home/zcf/githubs/minimind/minimind
printf '1\n请用一句话介绍你自己。\n\n' | \
  .venv/bin/python eval_llm.py \
  --load_from ./minimind-3 \
  --device cpu \
  --max_new_tokens 32 \
  --temperature 0.1 \
  --top_p 0.9 \
  --show_speed 1
```

实际结果：

```text
Model Params: 63.91M
回答：我是一个AI助手，能够提供各种信息和帮助，无论是解答问题、提供建议、进行对话还是执行任务。我能够理解并回应各种
速度：85.47 token/s
```

回答在 32 个新 token 处被截断，这是 `--max_new_tokens 32` 的直接结果，不是程序异常。

至此，仓库原始推理入口、项目环境、发布权重和 Tokenizer 已全部跑通。

### 学习者实践 1：亲自启动模型并完成手动对话

学习者在项目环境中亲自执行：

```bash
python eval_llm.py \
  --load_from ./minimind-3 \
  --device cpu \
  --max_new_tokens 64 \
  --temperature 0.1 \
  --top_p 0.9 \
  --show_speed 1
```

程序报告：

```text
Model Params: 63.91M
```

选择手动输入模式后，进行了两次提问。

第一次：

```text
用户：做一下自我介绍
模型：自我介绍是向他人介绍自己的第一印象，它有助于建立自信，展示你的专业素养和个性。以下是一些自我介绍的基本要点：

1. 基本信息：包括你的全名、联系方式（电话、邮箱）、居住地、职业或兴趣爱好等。
速度：62.11 tokens/s
```

第二次：

```text
用户：你是谁
模型：我是由jingyaogong创建的高效小参数AI模型，专注于提供精准、快速的信息和帮助。
速度：55.39 tokens/s
```

这一步只验收“学习者能够亲自启动程序并完成交互”，尚未验收对参数、生成过程或多轮对话的理解。

从输出中发现、等待学习者分析的现象：

1. “做一下自我介绍”被模型理解成介绍自我介绍的方法，而不是直接介绍模型自己。
2. 第一次回答在内容没有自然结束时停止。
3. 两次生成速度分别为 62.11 和 55.39 tokens/s。
4. 两次输入出现在同一个程序进程中，但是否携带第一轮对话历史尚未确认。

### 教学纠偏：先讲解，再实践和验收

在学习者尚未接触 token 和生成参数时，助手直接要求学习者根据参数名猜测回答为何停止。学习者指出这种方式缺少必要知识输入。

后续教学顺序调整为：

```text
结合真实现象讲解一个新概念
    -> 展示代码或最小实验
    -> 学习者提问并亲自实践
    -> 学习者复述或应用
    -> 助手验收
```

不再要求学习者盲猜尚未讲授的术语或机制。提问用于帮助理解和检查已经学习的内容，而不是代替讲解。

针对第一次回答突然停止，使用仓库 Tokenizer 核对：

```text
可见字符数：100
token 数：64
命令参数：--max_new_tokens 64
```

结论：第一次回答没有自然结束，而是生成了恰好 64 个新 token，达到 `max_new_tokens` 上限后被生成循环强制停止。

### 正式知识点 1：为什么需要 Tokenizer 和 token

学习者提出应先理解“为什么需要 Tokenizer 和 token”，再学习 Tokenizer 的具体工作方式。教学顺序据此调整为：

```text
为什么模型不能直接处理文本
    -> 为什么选择 token 作为离散单位
    -> Tokenizer 承担什么职责
    -> MiniMind Tokenizer 具体如何切分和编码
```

本知识点已通过后续 Tokenizer 编码、逐 token 解码和 Embedding 查表实验完成当前阶段的实践验证。BPE 训练算法的实现细节留到 Tokenizer 训练阶段。

学习者随后提出：不断把输出 token 追加到输入，序列是否会变得非常长。这个问题将引出：

- 上下文窗口与序列长度上限。
- 单次生成内的序列增长。
- 多轮对话历史是否保留。
- KV Cache 如何避免重复计算，但不能消除上下文长度和缓存增长。

在回答这一问题后，学习主线进入 Tokenizer 的具体机制，然后进入 Embedding。下一项实践将由学习者亲自观察文本、token、token ID、Embedding 权重 shape 和输出向量 shape。

### 学习者实践 2：观察 Tokenizer 与 Embedding

学习者亲自在 Python REPL 中编码：

```text
我正在学习MiniMind大模型！
```

从仓库目录启动项目环境：

```bash
cd /home/zcf/githubs/minimind/minimind
.venv/bin/python
```

在 REPL 中逐段执行：

```python
from transformers import AutoTokenizer, AutoModelForCausalLM
import torch

tokenizer = AutoTokenizer.from_pretrained("./minimind-3")
text = "我正在学习MiniMind大模型！"

ids = tokenizer.encode(text, add_special_tokens=False)
tokens = tokenizer.convert_ids_to_tokens(ids)

print("原文：", text)
print("token IDs：", ids)
print("内部 token：", tokens)
print("整体解码：", tokenizer.decode(ids))
print("对应关系：", list(zip(ids, tokens)))

model = AutoModelForCausalLM.from_pretrained("./minimind-3")
embedding = model.get_input_embeddings()

input_ids = torch.tensor([ids])
vectors = embedding(input_ids)

print("Embedding 权重 shape：", embedding.weight.shape)
print("input_ids shape：", input_ids.shape)
print("Embedding 输出 shape：", vectors.shape)
print("第一个 token 向量的前 10 个数：", vectors[0, 0, :10])
```

模型加载需要一些时间。向量数值来自已下载权重，在相同权重和精度下应接近记录值；本课验收重点是 token 切分、查表关系和 shape，而不是背向量中的具体小数。

Tokenizer 结果：

```text
token IDs：
[463, 3593, 768, 80, 301, 108, 80, 916, 565, 1135, 1364]

内部 token：
['æĪĳ', 'æŃ£åľ¨', 'åŃ¦ä¹ł', 'M', 'in', 'i', 'M', 'ind', 'å¤§', 'æ¨¡åŀĭ', 'ï¼ģ']

整体 decode：
我正在学习MiniMind大模型！
```

Embedding 结果：

```text
Embedding 权重 shape：torch.Size([6400, 768])
input_ids shape：torch.Size([1, 11])
Embedding 输出 shape：torch.Size([1, 11, 768])

第一个 token 的 768 维向量中，前 10 个数：
[0.0663, -0.0378, -0.0210, -0.0563, 0.0350,
 0.0164, 0.0390, 0.0367, 0.0529, -0.0529]
```

已经由实践确认：

```text
文本
  -> 11 个 token
  -> 11 个 token ID
  -> shape [1, 11] 的整数张量
  -> Embedding 查表
  -> shape [1, 11, 768] 的浮点向量张量
```

本实践的 shape、查表机制和 ByteLevel 内部表示已在后续实验中进一步验证。

学习者继续完成查表验证。

执行：

```python
pieces = [tokenizer.decode([token_id]) for token_id in ids]
print(list(zip(ids, pieces)))

print(torch.allclose(
    vectors[0, 0],
    embedding.weight[463]
))

print("第一个 M 的 ID：", ids[3])
print("第二个 M 的 ID：", ids[6])

print(torch.allclose(
    vectors[0, 3],
    vectors[0, 6]
))
```

不要用 `convert_ids_to_tokens()` 的乱码状 ByteLevel 字符直接判断中文切分；逐个执行 `decode([token_id])` 更适合观察人类可读片段。

逐 token 解码结果：

```text
463  -> 我
3593 -> 正在
768  -> 学习
80   -> M
301  -> in
108  -> i
80   -> M
916  -> ind
565  -> 大
1135 -> 模型
1364 -> ！
```

验证结果：

```text
vectors[0, 0] == embedding.weight[463] -> True
第一个 M 的 token ID -> 80
第二个 M 的 token ID -> 80
vectors[0, 3] == vectors[0, 6] -> True
```

由学习者亲自验证的结论：

1. ByteLevel 内部 token 虽然显示特殊字符，但逐 ID decode 可以恢复可读文本片段。
2. Embedding 操作确实按照 token ID 从权重矩阵中取对应行。
3. 相同 token ID 在 Embedding 层得到完全相同的初始向量，不因所在位置不同而变化。

下一实践将比较两个 `M` 在 Embedding 输出与第一层 Transformer 输出中的向量，观察位置和上下文何时开始产生影响。

### 学习者实践 3：相同 token 的上下文化

学习者使用发布模型输出全部 hidden states，得到：

```python
model.eval()

with torch.inference_mode():
    outputs = model(
        input_ids=input_ids,
        output_hidden_states=True,
        use_cache=False
    )

hidden_states = outputs.hidden_states
print("hidden_states 数量：", len(hidden_states))

for layer_index, state in enumerate(hidden_states):
    print(layer_index, state.shape)

first_m_position = 3
second_m_position = 6

print(
    "进入 Transformer 前是否相同：",
    torch.allclose(
        hidden_states[0][0, first_m_position],
        hidden_states[0][0, second_m_position]
    )
)

print(
    "经过第 1 层后是否相同：",
    torch.allclose(
        hidden_states[1][0, first_m_position],
        hidden_states[1][0, second_m_position]
    )
)

print(
    "经过第 8 层后是否相同：",
    torch.allclose(
        hidden_states[-1][0, first_m_position],
        hidden_states[-1][0, second_m_position]
    )
)

for layer_index in [0, 1, 4, 8]:
    distance = torch.norm(
        hidden_states[layer_index][0, first_m_position]
        - hidden_states[layer_index][0, second_m_position]
    )
    print(f"第 {layer_index} 个状态的距离：", distance.item())
```

`torch.inference_mode()` 表示这次只观察推理结果，不为训练保存反向传播信息。`use_cache=False` 是为了让本实验只关注各层 hidden states；KV Cache 尚未进入本课。

实际得到：

```text
hidden_states 数量：9
每组 shape：torch.Size([1, 11, 768])
```

两个 `M` 的比较结果：

```text
进入 Transformer 前是否相同：True
经过第 1 层后是否相同：False
最终状态是否相同：False
```

两个位置向量的欧氏距离：

```text
第 0 个状态：0.0
第 1 个状态：1.9034634828567505
第 4 个状态：8.682793617248535
第 8 个状态：49.399234771728516
```

由学习者亲自验证：

1. 相同 token ID 在初始 Embedding 层获得完全相同的向量。
2. 经过第一层 Transformer 后，相同 token 因位置和可见左侧上下文不同而获得不同表示。
3. Transformer 保持 `[batch, sequence, hidden_size]` 的外部 shape，但会持续改变张量内部数值。

实践过程中出现并自行修正了一次 Python REPL 顶层多余缩进导致的 `IndentationError`。这属于交互式解释器的输入格式问题，不是模型错误。

本实践已完成结果讲解。关于 Self-Attention、RoPE 和上下文化的内部计算将在后续独立学习。

## 八、问题与知识欠账

### 1. `token_type_ids` 参数问题

自定义测试中曾直接执行：

```python
model.generate(**tokenizer_output)
```

Tokenizer 输出除了 `input_ids` 和 `attention_mask`，还可能包含 `token_type_ids`。Qwen3 模型的 `forward` 不接收这个字段，因此 Transformers 报错：

```text
The following model_kwargs are not used by the model: ['token_type_ids']
```

这不是 GPU、权重或 Transformers 版本故障。仓库原始代码只显式传入：

```python
model.generate(
    inputs=inputs["input_ids"],
    attention_mask=inputs["attention_mask"],
    ...
)
```

因此原始入口不会触发这个错误。

暂时欠账：后续学习 Tokenizer 输出时，再详细理解 `input_ids`、`attention_mask` 和 `token_type_ids` 各自的职责。

### 2. 外部 GPU 环境中的 `trainer` 模块名冲突

借用的 CUDA 环境安装了另一个名为 `trainer` 的 Python 包，而 MiniMind 仓库的 `trainer/` 当前没有 `__init__.py`。直接用该外部环境运行 `eval_llm.py` 时，Python 导入了外部包：

```text
ImportError: cannot import name 'setup_seed' from 'trainer.trainer_utils'
```

项目 `.venv` 没有同名包，因此仓库原始脚本运行正常。当前不修改上游源码掩盖这个外部环境冲突。

暂时欠账：进入 Python 包结构或训练脚本时，再系统理解模块搜索路径、普通包、namespace package 和 `__init__.py`。

### 3. 项目专用 GPU 环境尚未完成

GPU 硬件和真实模型推理都已验证成功，但 `minimind/.venv` 当前是 CPU 版。正式训练前需要将它升级或另建 GPU 环境，并安装训练阶段所需依赖。

这不阻塞模型结构学习。当前约 64M 模型在 CPU 上也能快速推理。

### 4. KV Cache 暂缓展开

当前已经建立的最低直觉是：自回归生成会让逻辑上下文持续增长，KV Cache 保存历史计算结果，从而避免每生成一个 token 都重新计算全部旧 token；它不能消除上下文长度和缓存占用的增长。

现在不继续学习以下细节：

- 缓存的 K 和 V 分别是什么。
- 为什么缓存 K/V 而通常不缓存 Q。
- Cache 张量各维度的含义。
- GQA 下 KV head 数量如何影响缓存大小。
- `past_key_values` 如何使下一步只处理最新 token。

这些问题依赖 Self-Attention 中的 Q、K、V、因果 Mask 和多头/GQA。学习触发点确定为：完成 Self-Attention 前向过程后、进入自回归生成优化时。

## 九、今日总结

### 1. 已完成的环境准备

助手已经建立了可以持续学习的本地基线：

- 两张 RTX 3090 工作正常。
- CUDA PyTorch 能识别两张 GPU。
- Hugging Face 代理和下载路径正常。
- MiniMind-3 发布模型完整下载到本地。
- 项目拥有独立、版本匹配的 CPU `.venv`。
- 随机极小模型完成 Tokenizer、Forward、Logits 和 Generate 冒烟测试。
- 63.91M 发布模型完成真实 GPU 生成。
- 仓库原始 `eval_llm.py` 在项目环境直接运行成功。
- 识别并解释了 `token_type_ids` 多传参数问题。
- 识别了外部环境的 `trainer` 模块名冲突，没有误改仓库源码。

以上工作属于环境准备和演示，不计作学习者的掌握进度。

### 2. 协作方式纠偏

最初错误地把“助手运行完仓库”记录成“Day 2 学习完成”。学习者指出：真正的学习过程应当由助手控制范围和节奏，由学习者亲自实践、观察、思考和解释，再由助手检查理解并决定是否进入下一步。

从本次纠偏开始，采用以下规则：

1. 助手每次只给一个范围明确的小任务。
2. 学习者亲自执行命令、阅读输出或检查代码。
3. 学习者先用自己的语言描述观察和理解。
4. 助手根据回答定位知识缺口，只补当前需要的知识。
5. 必须经过实践与解释两项验收，才标记该知识点完成。
6. 助手代为执行的环境操作、诊断和演示，只记为“环境已准备”或“演示结果”，不记为学习者已掌握。
7. 在 Day 2 的学习验收完成前，不创建 Day 3 日志。

Day 2 当前真正的学习状态：

```text
[完成] 本地环境和模型权重准备
[完成] 助手演示随机模型与发布模型可以运行
[完成] 学习者亲自启动仓库推理入口并完成两次提问
[完成] 理解为什么模型需要 Tokenizer 和 token
[完成] 观察 ByteLevel BPE 对中英文混合文本的实际切分
[完成] 区分可读文本、内部 token 与 token ID
[完成] 验证 encode/decode 可以往返还原文本
[完成] 理解并验证 Embedding 是按 token ID 查表
[完成] 解释 [6400, 768]、[1, 11] 与 [1, 11, 768]
[完成] 验证相同 token ID 的初始 Embedding 完全相同
[完成] 验证相同 token 经 Transformer 后会被上下文化
[完成] 理解 max_new_tokens 与 EOS 是两类生成停止条件
[完成] 建立上下文增长与 KV Cache 的最低必要直觉
[后续] Chat Template、attention_mask、logits 与采样
[后续] Transformer Block、RMSNorm、Attention、RoPE、MLP
[后续] 学完 Q/K/V 后深入 KV Cache
```

本日需要由学习者亲自建立的核心认识仍是：

> “代码能够执行”“模型结构正确”“模型已经学会语言”“运行环境适合长期使用”是四个不同层次的问题，需要分别验证。

## 十、下一学习日入口

Day 002 从 Embedding 的输出继续向模型内部前进：

> 先建立一个 MiniMind Transformer Block 的整体数据流，再按依赖顺序学习 RMSNorm、Self-Attention、RoPE、残差连接和 MLP。

阅读范围：

```text
minimind/model/model_minimind.py::MiniMindConfig
minimind/model/model_minimind.py::RMSNorm
minimind/model/model_minimind.py::MiniMindBlock
minimind/model/model_minimind.py::MiniMindModel.forward
```

Day 002 的顺序：

1. 阅读模型配置，确认 768、8、8/4、96、2432、6400 分别控制什么。
2. 只看 Block 整体，不立即推导 Attention 公式。
3. 理解 Pre-Norm 与两条残差路径。
4. 用 hook 或中间输出验证一个 Block 输入输出 shape 不变、数值改变。
5. 再进入 RMSNorm 的代码与最小数学。

KV Cache 不在 Day 002 开头学习。它将在 Self-Attention 的 Q/K/V、因果 Mask 和 GQA 已经具备代码与张量基础后展开。
