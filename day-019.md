# Day 019：MoE 总参数量、激活参数量与计算成本

- 日期：2026-09-03
- 学习阶段：`minimind` 模型结构
- 今日主题：普通 FFN 与 MoE 的参数量、expert 激活、route 数量和近似计算成本
- 当前状态：已完成
- 恢复点：Day 20 从 MoE 训练入口和真实小规模训练验证开始

## 一、与 Day 018 的关系

Day 018 已经沿 `MOEFeedForward.forward()` 走通了：

```text
router -> softmax -> top-k -> 专家分发 -> 加权合并 -> aux_loss
```

Day 019 不重复路由实现，改为回答三个规模问题：

```text
模型里总共存了多少参数？
一个 token 本次实际激活了多少专家参数？
这些参数对应多少近似计算量和显存？
```

## 二、普通 FeedForward 的参数量

MiniMind 的 FFN 有三张无 bias 的矩阵：

```python
gate_proj: [intermediate_size, hidden_size]
up_proj:   [intermediate_size, hidden_size]
down_proj: [hidden_size, intermediate_size]
```

令 `H=hidden_size`、`I=intermediate_size`，三张矩阵各有 `H*I` 个元素：

```text
普通 FFN 参数量 = 3 * H * I
```

学习者运行 `H=16, I=32` 的实验：

```text
gate_proj.weight (32, 16) 512
down_proj.weight (16, 32) 512
up_proj.weight (32, 16) 512
total: 1536
```

激活函数 `SiLU` 没有可训练权重，`bias=False` 也意味着这里不需要加 bias 参数。

## 三、MoE MLP 的总参数量

源码为每个 expert 创建一整套独立的 `FeedForward`：

```python
self.experts = nn.ModuleList([
    FeedForward(config, intermediate_size=config.moe_intermediate_size)
    for _ in range(config.num_experts)
])
```

令 `E=num_experts`，router 是一个 `H -> E` 的无 bias Linear，因此：

```text
一个 expert       = 3 * H * I
router            = E * H
MoE MLP 总参数量 = E * (3 * H * I) + E * H
```

当 `H=16, I=32, E=4`：

```text
router params: 64
expert[0] params: 1536
expert[1] params: 1536
expert[2] params: 1536
expert[3] params: 1536
total: 6208
```

相对一个普通 FFN 增加：

```text
(E - 1) * 3 * H * I + E * H
```

## 四、总参数量与激活参数量

这两个词统计的对象不同：

```text
总参数量：模型中实际保存的全部参数
激活参数量：某个 token 本次前向真正经过的参数
```

MoE 的所有 expert 权重都要保存，即使某个 token 没有使用它们。若
`num_experts_per_tok=k`，单 token 的 MoE MLP 激活参数近似为：

```text
router + k 个 expert
= E * H + k * (3 * H * I)
```

小配置 `H=16, I=32, E=4`：

```text
普通 FFN：1536
MoE k=1：64 + 1*1536 = 1600
MoE k=2：64 + 2*1536 = 3136
```

在实际学习实验中，为了便于对照还使用了 `H=8, I=16`：

```text
普通 FFN：384
MoE MLP 总参数：4*384 + 4*8 = 1568
MoE k=1 激活：32 + 384 = 416
MoE k=2 激活：32 + 2*384 = 800
```

因此 MoE 的优势不是保证任何 `k` 都比普通 FFN 更省，而是用较小的 `k` 访问更大的总专家容量。

## 五、完整模型参数量对照

使用相同的两层小模型配置，只切换 `use_moe`：

```text
use_moe = False
total params = 1368

use_moe = True
total params = 3736
```

差值：

```text
3736 - 1368 = 2368
```

该配置每层 MLP 增加：

```text
MoE MLP 1568 - 普通 FFN 384 = 1184
```

两层增加：

```text
2 * 1184 = 2368
```

Embedding、Attention、归一化和 tied 的 `lm_head` 没有因 `use_moe` 改变。

## 六、expert-token routes

如果输入展平后有 `T` 个 token，每个 token 选 `k` 个 expert，那么 route 数为：

```text
T * k
```

route 是一个 `(token, expert)` 配对，不是不同 token 的数量。6 个 token 的 hook 实验：

```text
k=1
output shape: (2, 3, 8)
experts called: 4
tokens per called expert: [1, 3, 1, 1]
total expert-token routes: 6
expected routes: 6
aux_loss in eval: 0.0

k=2
output shape: (2, 3, 8)
experts called: 4
tokens per called expert: [4, 5, 2, 1]
total expert-token routes: 12
expected routes: 12
aux_loss in eval: 0.0
```

`experts called=4` 只是这次随机输入的结果，不是结构保证；router 集中时可能有专家没有收到 token。`tokens per called expert` 的总和在 `k=2` 时为 12，因为同一个 token 可以同时出现在两个 expert 的输入中。

## 七、参数量、计算量与显存

参数量不等于计算量。忽略 bias、激活函数和 dispatch 开销，用一次矩阵乘加约一个 MAC 近似：

```text
普通 FFN：3 * H * I MACs / token
router：H * E MACs / token
MoE experts：k * 3 * H * I MACs / token
MoE MLP 总计：H * E + k * 3 * H * I MACs / token
```

若把一次乘加近似算作 2 FLOPs，则 FLOPs 约为 MACs 的两倍。对 `H=8,I=16,E=4`：

```text
普通 FFN：384 MACs
MoE k=1：32 + 384 = 416 MACs
MoE k=2：32 + 768 = 800 MACs
```

所以单 token 的专家计算主要随 `k` 增长；大型配置中 router 的 `H*E` 往往相对较小。

显存方面：

```text
所有 expert 权重都要存储
训练还要保存梯度和优化器状态
只降低 k 不会自动降低模型权重显存
```

推理时可以减少实际矩阵计算，但如果所有 expert 都在同一张 GPU 上，权重显存仍接近完整 MoE 模型。专家分片、offload 等部署方式才会进一步改变单卡占用。

## 八、今日确认边界

学习者确认今天的全部知识点：

```text
[实践][验收] 普通 FFN 参数量公式与真实参数形状
[实践][验收] MoE expert 与 router 参数量
[实践][验收] 完整普通模型与 MoE 模型参数量差值
[实践][验收] k=1/k=2 的 expert-token route 数量
[验收] 总参数量、激活参数量和计算量的区别
[验收] 稀疏激活不等于自动减少权重显存
```

## 九、下一天计划（Day 020）

下一天继续 MoE 分支，但从“结构规模”转入“训练落地”：

```text
1. 定位 train_pretrain.py / train_full_sft.py 中的 --use_moe 分支
2. 追踪 use_moe 如何进入 MiniMindConfig、权重文件名和 checkpoint
3. 观察训练循环如何把 res.loss 与 res.aux_loss 相加
4. 用极小配置和极少数据完成一次 MoE forward/backward/optimizer.step
5. 检查 router 参数、expert 参数是否获得梯度
6. 观察一个训练 step 中 logits loss、aux_loss 和总 loss 的关系
```

Day 020 的恢复点是训练入口和真实参数更新；LoRA、蒸馏等其他训练分支暂不提前进入。
