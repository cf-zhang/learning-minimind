# Day 026：PPO 的直观目标与 critic 角色

- 日期：2026-09-09
- 学习阶段：`minimind` PPO
- 当前状态：暂停（PPO 概念暂未掌握，不计入已完成阶段）
- 计划入口：`minimind/trainer/train_ppo.py`

## 一、当前学习边界

Day 25 的 GRPO 已补充完成并归档。Day 26 先不直接进入代码，先理解 PPO 在整体上
要解决什么问题，以及它相较 GRPO 新增的角色：critic/value model。

```text
PPO 的直观目标：
让 policy 更愿意采取能带来高回报的行为，
但每次更新不要把原来的策略改变得过猛。
```

今天的内容只保留为 `[讲解]`，没有把 PPO 的 critic/value、return、TD error 或 GAE
写成已掌握。PPO 暂时跳过，后续需要时再单独补课。

## 二、Day 25 补充归档

本日开始前，已将 Day 25 后续学习补入
[Day 025](day-025-2026-09-04.md)，包括固定输入模拟执行、
`compute_per_token_logps()` 的形状与 `gather()`、EOS mask、组内 advantage，
以及 ratio 在默认 Torch 路径下通常接近 1 的原因和真正发挥作用的场景。

## 三、暂停原因与后续路线

PPO 同时引入了 value 估计、未来回报、TD error 和 GAE，当前学习成本明显高于
GRPO。继续强行推进会混淆已经掌握的 rollout、reward、advantage 和 ratio，因此本日
在直观目标层暂停。

下一阶段建议转入 Agent RL：

```text
AgentRLDataset
-> tool_call 格式
-> 多轮 rollout
-> 工具执行和结果回填
-> reward 计算
-> 复用 GRPO 的 group advantage 与 policy update
```

PPO 保留为后续可选专题，不影响继续学习 Agent RL、视觉仓库或 Omni 仓库。

## 四、原计划下一小步

下一步先解释以下四个概念的直观分工：

```text
critic/value：当前状态未来大概值多少
return：这次实际得到的未来回报
TD error：实际后果与 critic 预测之间的差
GAE：把多个时间位置的 TD error 平滑组合成 advantage
```

确认这些概念后，再回到：

```text
minimind/trainer/train_ppo.py
-> CriticModel
-> ppo_train_epoch()
```
