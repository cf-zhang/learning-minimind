# Day 009：RoPE 与 token 位置信息

- 日期：2026-08-24 至 2026-08-25
- 学习阶段：Transformer Attention 实现
- 今日主题：Attention 为什么需要位置，以及 RoPE 如何把位置加入 Q/K
- 当前状态：已完成（概念阶段完成，源码主线预览完成）
- 配套实验：学习过程中逐步创建，当前尚无 Notebook

## 一、承接 Day 008

Day 008 已经完成 MiniMind-3 的 GQA 主数据流：

```text
h
  -> q_proj / k_proj / v_proj
  -> Q [B,8,S,96]、K/V [B,4,S,96]
  -> K/V 对齐到 8 个 Q Head
  -> Q @ K.T
  -> Causal Mask
  -> Softmax
  -> weights @ V
```

已经确认：Embedding 根据 token ID 查出初始内容向量；Q/K/V 投影会
加工每个 token 的内容，但仅靠这些相同的 Linear 规则，并没有直接
给向量写入“这是第几个 token”。

MiniMind 源码中，RoPE 位于 Q/K 投影和点积之间：

```text
q_proj / k_proj
  -> 拆 Head、Q/K Norm
  -> RoPE 修改 Q 和 K
  -> Q @ K.T
```

对应代码位于 `minimind/model/model_minimind.py`：

```text
precompute_freqs_cis       第 62-78 行
apply_rotary_pos_emb       第 80-84 行
Attention 中调用 RoPE     第 111-124 行
按位置截取 cos/sin        第 205-219 行
```

这些代码位置目前只是路线坐标，不表示已经理解。

## 二、今天的问题

今天需要回答：

> token 的 Embedding 表示“它是什么”，Attention 又怎样知道它在第几个
> 位置，以及两个 token 相隔多远？为什么 MiniMind 只旋转 Q/K，不旋转 V？

## 三、今日路线

每次只推进一个小关卡：

1. 用“相同内容、不同顺序”理解为什么必须有位置信息。
2. 区分 Causal Mask 与位置编码各自负责什么。
3. 先用二维箭头理解“旋转一个向量”，暂不使用 RoPE 公式。
4. 观察同一个向量在不同位置旋转不同角度。
5. 理解为什么旋转 Q/K 后，点积可以感知相对位置。
6. 解释为什么 V 不参与旋转。
7. 用最小 PyTorch 张量亲自完成一次 RoPE。
8. 回到 MiniMind 的 `precompute_freqs_cis` 和
   `apply_rotary_pos_emb` 对照真实 shape 与调用顺序。

只有当前关卡经过讲解、提问或确认以及必要实践后，才进入下一关。

## 四、今天暂不引入

```text
YaRN 长文本外推
复数形式的 RoPE 推导
频率底数 rope_theta 的完整设计理由
KV Cache 的完整逐 token 推理代码
Flash Attention / SDPA 内部实现
```

如果这些名称在真实代码中出现，只标明作用和欠账，不展开计算。

## 五、验收标准

- [x] 能解释为什么内容向量本身不足以表示 token 顺序。
- [x] 能区分 Causal Mask 和 RoPE 的职责。
- [x] 能手工完成一个二维向量的简单旋转。
- [x] 能解释 RoPE 为什么应用到 Q/K 而不是 V。
- [x] 能说明 Q/K 点积怎样获得相对位置信息。
- [x] 能亲自运行并解释一个最小 RoPE 实验。
- [x] 能指出 MiniMind 源码中 RoPE 的输入、输出和调用位置。

## 六、逐轮互动档案

### 第 1 轮：为什么内容相同还需要位置

学习者开始 Day 009。教学从位置需求开始，不直接给出旋转公式。

Embedding 查表由 token ID 决定。同一个 token ID 每次会查到同一行
初始权重；随后 Q/K/V Linear 也对每个位置使用同一套投影规则。因此，
如果暂时不加入任何位置处理，仅靠内容投影不会直接标明：

```text
这个 token 在位置 0
这个 token 在位置 1
它和当前 token 相隔 1 个位置还是 3 个位置
```

用两段顺序不同、内容集合相同的序列建立问题：

```text
甲 乙 苹果
乙 甲 苹果
```

当最后的“苹果”读取前文时，两段序列都包含同一组内容 `甲、乙、苹果`。
如果 Attention 只根据内容算分数，交换甲和乙时，分数、读取比例和 V
会一起交换位置，加权求和结果不会因此改变。模型需要额外的位置处理，
才能让“甲在前、乙在后”和“乙在前、甲在后”影响匹配关系。

状态：`[讲解]`。等待学习者先确认“内容信息不等于位置信息”，再区分
Causal Mask 已经提供了什么、仍缺少什么。

学习者回答：

> Q 相同，因为它们初始值一样，做的运算也一样。

回答正确。这里特指第一层、尚未加入位置处理并暂不考虑其他上下文
差异时：相同 token ID 查到相同 Embedding，又经过参数完全相同的
`q_proj`，所以产生相同 Q。

学习者随后指出：

> 但是这里突然就出现了 RoPE，是先抛出一个悬念后续再展开？

是。此处的 `RoPE` 只是“MiniMind 用来解决位置问题的机制名称”，
用于标明 Day 009 的目标，不要求学习者此时理解。教学顺序保持为：

```text
先确认内容向量缺少什么
-> 再确认 Causal Mask 是否已经补足
-> 再建立二维旋转直觉
-> 最后才给 RoPE 名称填入具体含义和公式
```

状态更新：`[验收]` 已理解相同内容经过相同 Embedding 和投影会得到
相同 Q；`[纠偏]` 明确 RoPE 当前只是后续答案的名称，不提前展开。

### 第 2 轮：Causal Mask 提供了什么位置关系

比较两个序列：

```text
甲 乙 苹果
乙 甲 苹果
```

最后的“苹果”计算 Attention 时，两段序列中的甲、乙和苹果自己都
位于允许读取的范围。Causal Mask 负责把未来位置遮住，却不会修改
已经允许位置的分数来表达“距离当前 token 是 1 还是 2”。

学习者判断：

> 不能，读取的信息里面没有位置相关的信息。

结论正确。需要保留一个精确边界：Causal Mask 确实根据位置产生，
因此提供“未来禁止、过去和当前允许”的粗粒度顺序约束；但对于两个
都已允许读取的过去 token，它没有把相对距离写入 Q/K 匹配分数。

状态：`[验收]` 已能区分 Causal Mask 的可见性约束和仍然缺失的
相对位置/距离信息。下一步用数值验证交换 K/V 顺序不会改变内容型
Attention 的汇总结果。

### 第 3 轮：交换 K/V 顺序的数值验证

固定当前读取者为“苹果”，并明确每个 token 自己拥有成对的 K/V：

```text
q_苹果 = [1,0]
K_甲 = [1,0]，V_甲 = [10,0]
K_乙 = [0,1]，V_乙 = [0,20]
```

实验比较“甲、乙、苹果”和“乙、甲、苹果”。两个被读取 token 都在
苹果之前，因此都通过 Causal Mask；交换 token 时，其 K 和 V 必须
成对一起移动。

学习者实际运行输出：

```text
甲乙顺序的分数： tensor([0.7071, 0.0000])
甲乙顺序的比例： tensor([0.6698, 0.3302])
甲乙顺序的结果： tensor([6.6976, 6.6048])
乙甲顺序的分数： tensor([0.0000, 0.7071])
乙甲顺序的比例： tensor([0.3302, 0.6698])
乙甲顺序的结果： tensor([6.6976, 6.6048])
交换顺序后结果是否相同： True
```

交换后，甲的 K、由它得到的分数和比例、以及甲的 V 一起移动；乙
同理。因此加权项只是换了排列顺序：

```text
甲乙：0.6698 * V_甲 + 0.3302 * V_乙
乙甲：0.3302 * V_乙 + 0.6698 * V_甲
```

加法顺序不影响总和，所以结果相同。这验证了：在不加入位置处理时，
Attention 对“允许读取范围内 token 的成对重排”本身不敏感。

状态：`[实践]` 实验已完成；等待学习者解释分数/比例为何交换，而
最终汇总结果保持不变。

学习者解释：

> 因为分数和值都同时发生了交换，最终的值又进行了加法的交换率，
> 所以不变。

理解正确。这里的“加法的交换率”规范名称是“加法交换律”。更精确
地说，K/V 随 token 成对移动，分数及 Softmax 比例也随对应 K 移动，
于是“比例乘 V”的两个贡献项只交换次序，向量加法结果不变。

状态更新：`[验收]` 已通过数值实验解释内容型 Attention 为什么无法
仅凭 token 的排列变化产生不同汇总结果。第一项验收标准完成。

### 第 4 轮：先建立二维旋转直觉

在确认内容向量缺少位置后，正式给出 RoPE 名称的第一层含义：

```text
RoPE = Rotary Position Embedding
     = 通过旋转向量加入位置信息
```

暂时使用教学用的夸张角度：

```text
位置 0：旋转 0°
位置 1：旋转 90°
```

同一个内容向量 `[1,0]` 的结果为：

```text
位置 0：[1,0]
位置 1：[0,1]
```

学习者计算并回答：

```text
[1,0] 旋转 0°  -> [1,0]
[1,0] 旋转 90° -> [0,1]
```

结果正确。这说明位置可以通过改变向量方向写入原有维度；旋转不会
新增维度，向量长度在理想二维旋转中保持不变。真实 MiniMind 的
`head_dim=96` 时，会在 Head 内按二维子空间成组处理；具体分组和
公式留到后面再展开。

状态：`[实践]`、`[验收]` 已理解“相同内容因位置旋转而得到不同向量”。
下一步观察 Q/K 都带位置时，点积怎样反映两个位置之间的关系。

### 第 5 轮：旋转后的 Q/K 点积开始包含位置关系

教学例子固定当前读取者在位置 1，原始 Q 为 `[1,0]`；两个候选 K
也都从 `[1,0]` 开始，分别位于位置 0 和位置 1。使用同样的教学角度：

```text
位置 0：旋转 0°
位置 1：旋转 90°
```

得到：

```text
Q_位置1 = [0,1]
K_位置0 = [1,0]
K_位置1 = [0,1]
```

学习者计算：

```text
Q_位置1 · K_位置0 = 0
Q_位置1 · K_位置1 = 1
```

两项均正确。点积衡量两个方向的匹配程度：本例中位置 1 的 Q 与
位置 0 的 K 相差 90°，与位置 1 的 K 方向相同。因此加入旋转后，
匹配分数不再只由内容向量决定，也受到 Q/K 两个位置的角度差影响。

这是“相对位置信息”直觉的第一步。真实 RoPE 使用多个较小的角度和
多个二维子空间，不能把这个教学用的 90° 例子当作 MiniMind 的实际
角度或“同位置一定最高分”的一般结论。

状态：`[实践]` 已通过点积计算观察位置角度差如何进入 Attention 分数；
下一步解释为什么这套位置处理应用于 Q/K，而不是 V。

### 第 6 轮：为什么 RoPE 先作用于 Q/K，不作用于 V

Day 006 已经建立了 Attention 的职责分工：

```text
Q @ K.T -> 匹配分数
Softmax -> 读取比例
读取比例 @ V -> 汇总读到的内容
```

位置处理要解决的第一问题是：当前 token 应该怎样根据位置关系
判断“读谁、读多少”。因此 RoPE 作用在参与匹配的 Q 和 K 上：

```text
Q/K 加入位置方向
-> Q/K 点积带有位置关系
-> 匹配分数和读取比例受到位置影响
```

V 的职责是携带真正被读取的内容。如果把 V 也旋转，首先改变的会是
“读到什么数值”，而不是“应该读取哪个 token”；而且 Q/K 的分数在
没有改变时，读取比例不会改变。MiniMind 的真实调用也体现了这个
边界：`apply_rotary_pos_emb(q, k, cos, sin)` 只接收并返回 Q、K，
没有传入 V。

状态：`[讲解]` 已说明 Q/K 与 V 的职责边界；等待学习者判断“只旋转
V 是否能改变读取比例”。

学习者判断：

```text
只旋转 V：匹配分数不会改变，读取比例不会改变，最终读取结果会改变。
```

三项均正确。原因是 V 只出现在最后的 `weights @ V`；Q/K 不变时，
`Q @ K.T` 和 Softmax 都不变，但被加权汇总的内容向量变了。

状态：`[验收]` 已能根据 Attention 数据流判断只旋转 V 的影响；下一步
通过实际运行观察相同比例如何汇总出不同结果。

学习者实际运行输出：

```text
分数： tensor([1., 0.])
读取比例： tensor([0.7311, 0.2689])
原始 V： tensor([[10.,  0.], [ 0., 20.]])
只旋转第二个 V 后： tensor([[ 10.,   0.], [-20.,   0.]])
原始读取结果： tensor([7.3106, 5.3788])
旋转 V 后的读取结果： tensor([1.9318, 0.0000])
分数是否相同： True
比例是否相同： True
最终结果是否相同： False
```

结果完全符合预测：Q/K 不变，所以分数和读取比例不变；只旋转 V
改变了最后被加权汇总的内容，因此最终结果改变。

状态：`[实践]`、`[验收]` 已通过运行实验确认 RoPE 应影响 Q/K 的
匹配，而不是直接改写 V 的内容。

### 第 7 轮：二维旋转公式

对二维向量 `[x,y]` 旋转角度 `θ`：

```text
x' = x*cos(θ) - y*sin(θ)
y' = x*sin(θ) + y*cos(θ)
```

教学例子中：

```text
旋转 0°  -> [x,y]
旋转 90° -> [-y,x]
```

学习者手工计算：

```text
[3,4] 旋转 0°  -> [3,4]
[3,4] 旋转 90° -> [-4,3]
```

结果正确。旋转改变向量方向，但不改变长度：原长度和旋转后长度都
是 `5`。后续需要回答的是：真实 RoPE 怎样根据 token 的位置决定
每个二维子空间的角度。

状态：`[实践]`、`[验收]` 已能使用二维旋转公式和特殊角度计算结果。

### 第 8 轮：位置如何决定旋转角度

教学例子设定每前进一个位置旋转 90°，原向量为 `[1,0]`。学习者
计算：

```text
位置 0：旋转 0°   -> [1,0]
位置 1：旋转 90°  -> [0,1]
位置 2：旋转 180° -> [-1,0]
```

结果正确。位置编号乘以旋转步长，就得到该位置的旋转角度；因此同一
内容向量在不同位置会有不同方向。

这是教学用的夸张设置。真实 RoPE 不会让整个 Head 只使用一个 90°
角，而会把 Head 的维度分成多个二维子空间，并为不同子空间安排不同
的旋转速度。下一步先解释为什么需要“不同速度”，再进入真实频率公式。

状态：`[实践]`、`[验收]` 已理解位置编号如何转成旋转角度；下一步
建立多种旋转速度带来的长短距离感知。

### 第 9 轮：多组旋转速度能否区分重复方向

教学例子有两组二维坐标：

```text
第 1 组：每前进一个位置旋转 90°
第 2 组：每前进一个位置旋转 45°
```

比较位置 0 和位置 4。第 1 组在位置 4 旋转 360°，方向与位置 0
重复；但第 2 组在位置 4 旋转 180°，方向变为相反。因此，如果两处
的 token 内容原本相同，位置处理后的组合向量仍然不同：

```text
位置 0：第 1 组 [1,0]，第 2 组 [1,0]
位置 4：第 1 组 [1,0]，第 2 组 [-1,0]
```

学习者回答“位置 1 和位置 4 的 token 一样的话就无法区分了”。这里
发生了两个混淆：题目比较的是位置 0 和位置 4；并且“token 内容一样”
不等于“加入位置处理后的向量一样”。只要至少一组二维坐标的方向
不同，组合后的向量就能区分这两个位置。

还需保留一个边界：这是人为设置的离散角度，经过足够长的距离后多组
方向仍可能同时重复；真实 RoPE 通过多种频率把这种重复推远，而不是
声称有限维向量可以无条件编码无限位置。

状态：`[纠偏]` 已指出“内容相同”和“位置处理后向量相同”的区别；
当前等待学习者确认位置 0/4 的两组组合向量确实不同。

学习者确认：

> 因为第二组和第一组的旋转角度的速度不一样。

回答正确。不同二维组使用不同旋转速度，使某一组方向重复时，其他
组通常仍保留位置差异；组合后的完整向量因此可以携带更丰富的位置
信息。这里的“通常”很重要：人为设置的有限周期最终可能重复，真实
RoPE 通过一组不同频率把重复推得更远。

状态更新：`[验收]` 已理解多种旋转速度为什么比单一速度更能区分位置。
下一步将这个概念映射到 MiniMind-3 的 `head_dim=96`。

### 第 10 轮：`head_dim=96` 对应 48 组二维坐标

MiniMind-3 每个 Q/K Head 的向量维度为 96。RoPE 在二维子空间中
旋转，因此：

```text
96 / 2 = 48 组二维坐标
```

学习者回答：一个 K Head 的 96 维向量也需要分成 48 组二维坐标。
回答正确。Q 和 K 的每个 Head 都使用相同的维度拆分方式；V 不参与
这一步。

每组二维坐标拥有一个自己的旋转频率。对位置 `p`，该组的旋转角度
可以先直观写成：

```text
角度 = p * 该组频率
```

所以同一组随着位置增加而持续旋转，不同组因为频率不同而有不同转速。

状态：`[验收]` 已能把 `head_dim=96` 映射为 48 组二维 RoPE 坐标；
下一步用一个简单频率计算位置对应的角度。

### 第 11 轮：位置乘以频率得到角度

为避免一开始引入真实公式，先用教学频率：

```text
第 1 组：每个位置增加 30°
第 2 组：每个位置增加 10°
```

学习者计算位置 3 的角度：

```text
第 1 组：3*30° = 90°
第 2 组：3*10° = 30°
```

结果正确。这就是当前阶段的核心关系：

```text
某个位置的旋转角度 = 位置编号 * 该二维组的旋转频率
```

真实 MiniMind 不会手工写 `30°`、`10°`，而是由
`precompute_freqs_cis` 根据 Head 维度、`rope_theta` 和维度编号生成
一组频率。今天跨越了自然日期，但仍属于同一个 Day 009；Day 编号
按学习主题而不是日历日期递增。

状态：`[实践]`、`[验收]` 已理解位置编号与不同组频率如何共同决定角度；
下一步观察真实代码中“维度编号”怎样产生不同频率。

### 第 12 轮：真实频率计算的第一步

对应 `precompute_freqs_cis` 中的：

```python
torch.arange(0, dim, 2) / dim
```

学习者运行 `dim=4` 的简化观察：

```text
二维组的起始维度： tensor([0., 2.])
除以 dim 后：      tensor([0.0000, 0.5000])
```

这里的 `0` 和 `2` 是二维组的起始维度编号，不是 token 的位置；
除以 `dim=4` 后得到每组进入幂运算的比例 `[0, 0.5]`。两组比例
不同，后续就会得到不同旋转频率。

状态：`[实践]` 已识别真实代码中二维组编号与 token 位置编号的区别；
下一步把 `rope_base` 加入频率计算。

学习者运行真实公式的 `dim=4、rope_base=100` 简化版本，得到：

```text
频率： tensor([1.0000, 0.1000])
```

这表示第一组每前进一个位置增加 `1.0` 个角度单位，第二组增加
`0.1` 个角度单位。真实代码随后将位置与频率相乘，再交给
`torch.sin/cos`；其角度单位严格来说是弧度，但当前先把它理解为
程序使用的角度数值。

状态：`[实践]` 已从真实代码算出不同二维组的旋转频率；下一步计算
某个位置的实际角度。

学习者确认位置 `p=3` 时：

```text
第 1 组：3*1.0 = 3.0
第 2 组：3*0.1 = 0.3
```

即位置 3 的两组角度为 `[3.0, 0.3]`。单个位置的关系已经清楚，
下一步对应源码的 `torch.outer`，一次生成所有位置与所有二维组的
角度表。

状态：`[验收]` 已能从“位置 * 频率”得到某位置在各二维组中的角度。

### 第 13 轮：从单个位置跳到 `torch.outer` 出现断层

助手在学习者确认位置 3 的角度 `[3.0,0.3]` 后，直接说“MiniMind
源码接着做 `torch.outer`”。学习者指出：

> 断层了吧，MiniMind 源码接着做：这里接着做什么，前后对不上了。

这个反馈正确。缺少的桥梁是：此前的组编号、频率和位置角度，本来
就在拆解同一个 `precompute_freqs_cis` 函数；算位置 3 只是先手工验证
函数中的一项，而模型需要预先为所有可能位置都准备角度。因此才从：

```text
一个位置 3 * 两组频率 [1.0,0.1] -> [3.0,0.3]
```

转向：

```text
所有位置 [0,1,2,3] 分别乘两组频率 [1.0,0.1]
-> 一张 [位置数, 二维组数] 的角度表
```

源码还把角度表继续命名为 `freqs`，从“每组频率”覆盖成“每个位置、
每组的角度”，这也容易加重混淆。教学中应分别称为 `frequencies` 和
`angles`。

状态：`[纠偏]` 暂停 `torch.outer` 实验，先补全 `precompute_freqs_cis`
内部从维度分组、生成频率到为所有位置生成角度表的连续关系。

学习者进一步澄清：

> 我的意思是我们本来还在讲例子，怎么就突然到了 MiniMind？

这指出了更根本的教学顺序错误：问题不只是 `torch.outer` 前少了一句
衔接，而是从概念层的教学角度例子未经提示跳到了仓库实现层。Day 009
原定路线本应是：

```text
位置需求
-> 二维旋转直觉
-> 位置乘旋转速度
-> Q/K 点积为何反映相对位置
-> 最小完整 PyTorch RoPE
-> 最后回到 MiniMind 源码
```

实际却在“不同二维组使用不同速度”的概念例子后，提前进入了
`torch.arange`、`rope_base` 和 `torch.outer`。这些已经运行的输出
是真实有效的源码预览，但不能视为概念主线已经自然推进到源码。

纠正办法：暂时搁置第 12-13 轮的源码细节，不继续展开 `rope_base`、
`torch.outer`、cos/sin 表。恢复到最后已连续验收的概念节点：

```text
每个二维组有自己的旋转速度
某组角度 = token 位置 * 该组旋转速度
```

接下来先用纯教学例子解释“相同位置间隔为何产生相同角度差”，闭合
RoPE 的相对位置直觉；完成最小实验后，再明确宣布进入 MiniMind 源码。

状态更新：`[纠偏]` 教学层级跳转已识别；源码预览暂停，返回概念例子。

### 第 14 轮：相对位置例子中的原始 Q/K 前提

返回教学例子后，比较了两组相隔 1 个位置的 Q/K：

```text
Q 在位置 1、K 在位置 0 -> 旋转后点积 0
Q 在位置 2、K 在位置 1 -> 旋转后点积 0
```

学习者首先追问：

> 这里 Q/K 的值都是原向量吗？

需要补全前提：该例中为了隔离位置因素，旋转前的原始 Q 和原始 K
都被人为设为 `[1,0]`。显示的 `[0,1]`、`[-1,0]` 等是经过位置旋转
后的向量。真实 Attention 中 Q/K 来自不同 Linear 投影，通常不会
相同；教学例子的相同原向量不是 RoPE 的必要条件。

学习者继续解释：

> 绝对位置不同，都是相对位置的旋转都是 90 度，同样夹角和大小的
> 两个向量点积相同。

解释正确。点积由两个向量的长度和夹角共同决定。将 Q/K 的位置同时
向后平移 1，二者都额外旋转相同角度，彼此夹角仍不变；所以在内容
向量保持为同一对的前提下，相同相对位置会产生相同的位置影响。

状态：`[验收]` 已理解示例中的原始向量假设，以及相同位置差为何
对应相同旋转夹角；下一步用原始 Q/K 不同的例子排除误解。

使用不同的原始内容向量：

```text
原始 Q = [1,0]
原始 K = [1,1]
每前进一个位置旋转 90°
```

两种位置组合为：

```text
情况 A：Q 在位置 1 -> [0,1]，K 在位置 0 -> [1,1]
点积 = 1

情况 B：Q 在位置 2 -> [-1,0]，K 在位置 1 -> [-1,1]
点积 = 1
```

学习者确认结果。这排除了“只有原始 Q/K 相同时，相同位置差的点积
才相同”的误解。这里比较的是同一对原始内容 Q/K；将二者位置同时
后移，使它们共同增加相同旋转角度，彼此夹角和各自长度仍然不变，
所以点积保持不变。

状态更新：`[验收]` 已用不同原始 Q/K 理解相同相对位置带来相同的
位置影响；下一步用 PyTorch 完整执行二维旋转和点积验证。

### 第 15 轮：最小二维 RoPE 实验

学习者实现 `rotate_2d`，使用弧度执行二维旋转。原始内容向量为：

```text
Q = [1,0]
K = [1,1]
每个位置的旋转步长 = π/2（90°）
```

实际输出：

```text
原始 Q： tensor([1., 0.])
原始 K： tensor([1., 1.])
情况 A 的旋转后 Q： tensor([-0.0000, 1.0000])
情况 A 的旋转后 K： tensor([1., 1.])
情况 A 的点积： tensor(1.0000)
情况 B 的旋转后 Q： tensor([-1.0000, -0.0000])
情况 B 的旋转后 K： tensor([-1.0000, 1.0000])
情况 B 的点积： tensor(1.0000)
两个点积是否相同： True
Q 旋转前后长度是否相同： True
```

情况 A 的绝对位置为 Q=1、K=0；情况 B 为 Q=2、K=1。两者相对
位置都为 1，因此 Q/K 之间的旋转角度差相同，点积相同。旋转前后
Q 的长度保持不变。

输出中的 `-0.0000` 来自浮点近似：程序计算的 `cos(π/2)` 或
`sin(π)` 可能是非常接近 0 的微小负数，显示到四位小数成为负零；
它在当前实验中可以视为 0。

状态：`[实践]`、`[验收]` 已亲自运行最小二维 RoPE，并验证相同相对
位置产生相同位置影响、旋转保持向量长度。

### 第 16 轮：相对位置改变时点积改变

固定原始 Q/K 和 Q 的绝对位置：

```text
原始 Q = [1,0]
原始 K = [1,1]
Q 在位置 2，旋转后 Q = [-1,0]
```

只改变 K 的位置：

```text
K 在位置 1：旋转后 [-1,1]，相隔 1，点积 = 1
K 在位置 0：旋转后 [1,1]，相隔 2，点积 = -1
```

学习者计算第二个点积为 `-1`，结果正确。这与上一轮共同验证：

```text
相对位置相同 -> 旋转带来的位置影响相同
相对位置改变 -> Q/K 点积可以随之改变
```

边界：这不意味着“越近的 token 分数必然越大”。真实分数同时取决于
原始内容 Q/K、不同二维组的频率以及它们各自的数值；RoPE 的作用是
让相对位置能够进入分数计算，而不是预先规定固定的远近偏好。

状态：`[实践]` 已从正反两方面验证 Q/K 点积能够感知相对位置；等待
学习者整体复述后关闭概念例子阶段，再明确切换到 MiniMind 实现。

学习者确认概念链条，并明确要求开始源码。至此教学例子阶段关闭。

状态更新：`[验收]` 已确认：位置决定 Q/K 的旋转角度，Q/K 的位置差
决定角度差，角度差进入点积，因此匹配分数能够感知相对位置。

### 第 17 轮：明确切换到 MiniMind 源码

源码阶段从调用者和函数输入输出开始，不直接从内部公式开始。当前读
仓库原生实现：

```text
minimind/model/model_minimind.py
```

需要区分：此前 `AutoModelForCausalLM.from_pretrained("./minimind-3")`
加载的是导出为 Transformers 格式的 `Qwen3ForCausalLM`；现在阅读的是
MiniMind 仓库用于训练和原生推理的实现。类名和代码组织不同，但模型
配置和 RoPE 核心原理一致。

源码入口在 `MiniMindModel.__init__`：

```python
freqs_cos, freqs_sin = precompute_freqs_cis(
    dim=config.head_dim,
    end=config.max_position_embeddings,
    rope_base=config.rope_theta,
    rope_scaling=config.rope_scaling,
)
```

MiniMind-3 的基础配置对应：

```text
dim = 96
end = 32768
rope_base = 1000000
rope_scaling = None
```

`precompute_freqs_cis` 此时还不接收 Q/K，也不执行 Attention；它提前
生成两张供所有层共用的位置旋转表 `freqs_cos` 和 `freqs_sin`。

状态：`[讲解]` 源码阶段已从调用点开始；下一步先根据输入确定两张
表的行列含义和 shape，再运行函数检查。

学习者根据调用参数回答，`cos/sin` 展开前的角度表 shape 为：

```text
[32768, 48]
```

回答正确：`32768` 行对应位置 0 到 32767，`48` 列对应 96 维 Head
中的 48 个二维旋转组。

学习者提出：

> 这个函数我展开看有点费劲，你后面需要帮我逐行解读。

后续采用实际执行路径逐行解读。MiniMind-3 当前
`rope_scaling=None`，因此第 64-73 行的 YaRN 分支不会执行；基础阶段
先折叠该分支，只读：生成每组频率、生成位置角度表、生成 cos/sin
表和返回结果。函数中压缩在一行的多项计算和重复使用的变量名会拆成
有语义的中间变量，不要求学习者直接解析压缩写法。

状态：`[实践]` 已能从调用参数推导角度表 shape；下一步从函数签名和
第 63 行开始逐项拆解。

### 第 18 轮：逐行拆解第 63 行的 48 个频率

将源码中压缩的一行：

```python
freqs, attn_factor = 1.0 / (
    rope_base ** (torch.arange(0, dim, 2)[: (dim // 2)].float() / dim)
), 1.0
```

拆成：

```python
frequency_slots = torch.arange(0, dim, 2)[: dim // 2].float()
exponents = frequency_slots / dim
frequencies = 1.0 / (rope_base ** exponents)
attn_factor = 1.0
```

`dim=96` 时，`frequency_slots` 是 `[0,2,...,94]`，共有 48 项；
每项通过不同指数产生一个旋转速度，因此 `frequencies` shape 为 `[48]`。

学习者确认原因：

> 因为每组是 2 个值。

回答正确。RoPE 的基本旋转单位是二维坐标，96 个值组成 48 组，所以
只需要 48 个频率。`attn_factor=1.0` 在未启用 YaRN 时保持不变。

状态：`[验收]` 已读懂第 63 行的输入、拆分计算、输出含义和 shape；
下一步确认 YaRN 分支被跳过，再读位置编号第 74 行。

### 第 19 轮：区分频率与 token 位置

因为 `rope_scaling=None`，第 64-73 行 YaRN 分支全部跳过。第 74 行：

```python
t = torch.arange(end, device=freqs.device)
```

在 `end=32768` 时得到位置编号 `[0,1,...,32767]`，shape 为
`[32768]`。`device=freqs.device` 只保证位置张量与频率张量在相同
CPU/GPU 设备上，不改变数学含义。

学习者判断：`frequencies` 不是 token 位置，`t` 是 token 位置；判断
正确。但将 `frequencies` 描述成“用来缓存的旋转角度”需要修正：

```text
frequencies：[48]，每个二维组每前进一个位置增加多少角度
t：[32768]，token 的绝对位置编号
```

`frequencies` 还不是每个位置的实际角度。下一行执行
`t * frequencies` 后才得到角度表；再对角度计算 cos/sin，最终两张
表才会作为模型 buffer 常驻内存。这里的 buffer 与生成时的 KV Cache
无关，不能混为同一种缓存。

状态：`[纠偏]` 已区分旋转频率、位置编号、实际角度和最终 cos/sin 表；
下一步逐行解读第 75 行的外积。

### 第 20 轮：第 75 行生成所有位置的角度表

源码：

```python
freqs = torch.outer(t, freqs).float()
```

为避免同名覆盖，教学中改写为：

```python
angles = torch.outer(positions, frequencies).float()
```

输入和输出：

```text
positions：[32768]
frequencies：[48]
angles：[32768,48]
```

其中：

```text
angles[position, group]
= positions[position] * frequencies[group]
```

第 0 行代表位置 0。学习者回答：

> 0

回答正确。位置 0 乘以任意频率都为 0，因此第 0 行的 48 个角度
全部是 0。

状态：`[验收]` 已理解第 75 行如何从位置 `[32768]` 和频率 `[48]`
生成角度表 `[32768,48]`；下一步读取同一角度表上的 cos/sin。

### 第 21 轮：理解 `torch.cat` 沿最后一维复制系数

学习者在看到：

```python
freqs_cos = torch.cat([torch.cos(freqs), torch.cos(freqs)], dim=-1)
freqs_sin = torch.cat([torch.sin(freqs), torch.sin(freqs)], dim=-1)
```

后指出并确认：如果 `cos(freqs)` 的 shape 是 `[32768,48]`，沿
`dim=-1` 拼接同一张表后 shape 会变成 `[32768,96]`；对任意一行，
例如抽象值：

```text
[1,2,3,...,48]
```

会变成：

```text
[1,2,3,...,48, 1,2,3,...,48]
```

理解正确。`dim=-1` 指最后一维，也就是每行的二维组/特征列；
32768 行不会互相拼接，每一行都独立复制一次。复制后的 96 个系数
才能与 96 维 Q/K 的最后一维逐元素相乘。

状态：`[实践]` 已理解 `torch.cat(..., dim=-1)` 的 shape 变化和逐行
数值排列；下一步再解释为什么这种“复制一半”与 `rotate_half` 的
维度配对方式相匹配。

### 第 22 轮：`rotate_half` 的作用

学习者确认了 `torch.cat` 的复制结果，但指出：

> 这里要讲的 `rotate_half` 突然就出来了，它是干什么的？

这个问题正确地指出了下一处教学衔接。`rotate_half` 不是新的模型层、
不是新的参数，也不是又一种位置编码；它是实现二维旋转公式的辅助
函数。

二维旋转公式：

```text
[x*cos(theta) - y*sin(theta),
 x*sin(theta) + y*cos(theta)]
```

可以拆成两部分：

```text
[x,y] * cos(theta)
+ [-y,x] * sin(theta)
```

因此对二维向量 `[x,y]`，辅助操作：

```text
rotate_half([x,y]) = [-y,x]
```

MiniMind 的实现使用“前半维、后半维”配对。对 4 维向量：

```text
x = [x0,x1,x2,x3]
rotate_half(x) = [-x2,-x3,x0,x1]
```

也就是配对：`(x0,x2)`、`(x1,x3)`。这与前一轮把 48 个 cos/sin
系数复制成 96 个、排列为 `[a组,b组,...,a组,b组,...]` 相匹配。

状态：`[讲解]` 已说明 `rotate_half` 的来源和配对方式；还未把它
亲自运行或与完整旋转公式相连，下一步先观察一个 4 维输入的输出。

学习者实际运行：

```text
原向量： tensor([[1., 2., 3., 4.]])
rotate_half 后： tensor([[-3., -4.,  1.,  2.]])
```

结果正确。前半 `[1,2]` 与后半 `[3,4]` 分开处理：后半取负得到
`[-3,-4]`，再拼回前半，形成 `[-3,-4,1,2]`。这不是相邻维度
`(0,1)`、`(2,3)` 的交换，而是前后半对应维度 `(0,2)`、`(1,3)`
组成二维旋转对子。

状态：`[实践]` 已观察 `rotate_half` 的实际维度重排和符号变化；下一步
把它与复制后的 cos/sin 系数相加，完成 4 维的完整旋转。

学习者确认已经理解完整连接：`rotate_half` 提供二维旋转公式中的
`[-y,x]` 项，复制后的 `cos/sin` 为每个二维组提供对应系数，三者
通过 `x*cos + rotate_half(x)*sin` 完成不同角度的二维旋转。

状态更新：`[验收]` 已理解 `rotate_half` 的作用及其与 cos/sin 的
组合；下一步回到真实 `apply_rotary_pos_emb`，只观察输入输出 shape
和 `unsqueeze` 带来的广播对齐。

### 第 23 轮：闭合 `precompute_freqs_cis` 的返回结果

第 76-78 行对角度表 `[32768,48]` 分别计算 cos/sin，并沿最后一维
各复制一次：

```text
angles：    [32768,48]
freqs_cos： [32768,96]
freqs_sin： [32768,96]
```

当前 `attn_factor=1.0`，乘法不改变数值。函数返回两张表后，调用者
使用 `register_buffer(..., persistent=False)` 将其挂在模型上：它们
会随模型移动到 CPU/GPU，但不是需要梯度更新的参数，也无需保存进
权重文件，因为可以根据配置重新计算。

学习者判断：

> 固定工具表。

判断正确。`freqs_cos/freqs_sin` 是根据位置与配置提前计算的固定工具
表，不是像 `q_proj.weight` 一样由训练学习的参数。

状态：`[验收]` 已逐行读完 `precompute_freqs_cis` 的基础 RoPE 路径，
并能说明其输出 shape 和参数/工具表边界；下一步读取 forward 中如何
按本次 token 位置切片。

### 第 24 轮：forward 按本次序列切片位置表

`MiniMindModel.forward` 根据：

```python
batch_size, seq_length = input_ids.shape
```

再从 `freqs_cos/freqs_sin` 中取：

```python
self.freqs_cos[start_pos:start_pos + seq_length]
self.freqs_sin[start_pos:start_pos + seq_length]
```

没有旧 KV Cache 时 `start_pos=0`。学习者对 `seq_length=20` 计算：

```text
start_pos：0
本次 freqs_cos：[20,96]
本次 freqs_sin：[20,96]
```

三项均正确。切片只按 token 位置取行，不查看 token ID；同一个 token
ID 位于不同位置时，会取到不同的 cos/sin 行。

状态：`[实践]`、`[验收]` 已能根据输入长度推导位置表切片 shape；下一步
观察有旧 KV Cache 时 `start_pos` 如何跳过已经生成的位置。

### 第 25 轮：有 KV Cache 时从正确的绝对位置取 RoPE

生成场景假设：

```text
已有 KV Cache 长度：5
本次新输入长度：1
完整 freqs_cos：[32768,96]
```

缓存中的旧 K shape 第二维记录已处理 token 数，因此：

```text
start_pos = past_key_values[0][0].shape[1] = 5
seq_length = 1
```

位置表切片为：

```python
self.freqs_cos[5:6]
self.freqs_sin[5:6]
```

学习者回答：

```text
start_pos：5
本次 freqs_cos：[1,96]
新 token 使用第 5 行
```

三项均正确。第 5 行对应新 token 的绝对位置；它不会因为本次输入
只有 1 个 token 就重新从第 0 行开始。

需要始终区分两种“缓存/表”：

```text
freqs_cos / freqs_sin：固定位置工具表，模型初始化时预计算
past_key_values：      生成过程中保存旧 token 的 K/V
```

状态：`[验收]` 已能解释 KV Cache 如何决定 RoPE 的 `start_pos`，并
区分固定位置表与运行时 K/V Cache；下一步进入 Attention 内部，把
position_embeddings 传给 `apply_rotary_pos_emb`。

### 第 26 轮：从 Model forward 传入 Attention

`MiniMindModel.forward` 将同一份本次位置表传给每一层：

```python
hidden_states, present = layer(
    hidden_states,
    position_embeddings,
    past_key_value=past_key_value,
    use_cache=use_cache,
    attention_mask=attention_mask
)
```

`MiniMindBlock.forward` 不计算 RoPE，只把 `position_embeddings` 继续
传给 `self.self_attn`：

```python
hidden_states, present_key_value = self.self_attn(
    self.input_layernorm(hidden_states),
    position_embeddings,
    past_key_value,
    use_cache,
    attention_mask
)
```

真正的顺序在 `Attention.forward`：

```text
x [B,S,768]
  -> q_proj/k_proj/v_proj
  -> xq [B,S,8,96]
     xk/xv [B,S,4,96]
  -> q_norm/k_norm
  -> apply_rotary_pos_emb(xq, xk, cos, sin)
  -> （若有）拼接旧 K/V Cache
  -> transpose + repeat_kv
  -> Q @ K.T
```

这里有一个重要边界：RoPE 在 K/V Cache 拼接之前作用于当前新算出的
Q/K；旧 K 已经在它们各自生成的位置完成过 RoPE，缓存后直接复用。
V 从始至终不进入 `apply_rotary_pos_emb`。

状态：`[欠账]` 已建立 Model -> Block -> Attention -> RoPE 的调用顺序，
但 `apply_rotary_pos_emb` 的输入 shape 与广播维度尚未逐行完成；该内容
不在 Day 009 结束时标记为掌握，下一学习日从程序入口重新建立完整链路。

### 第 27 轮：结束 Day 009，保存源码恢复点

学习者决定今天先结束学习，并要求下一学习日从代码入口重新开始，
按启动顺序和有输入时的执行顺序阅读源码。

本日已完成的概念主线：

```text
内容向量不自动包含位置
-> Causal Mask 只约束能否看未来，不提供允许位置的距离
-> 位置决定 Q/K 的旋转角度
-> 不同二维组使用不同旋转速度
-> Q/K 的相对角度差进入点积
-> 相同相对位置产生相同的位置影响
-> V 不参与位置匹配旋转
```

学习者亲自完成并解释了：

```text
二维向量旋转
交换 K/V 顺序的内容型 Attention 实验
不同相对位置的 Q/K 点积实验
最小 PyTorch RoPE 旋转实验
rotate_half 维度重排实验
```

源码预览已经完成这些范围，但不宣称完整吃透：

```text
model_minimind.py 的 MiniMindConfig 相关 RoPE 配置
MiniMindModel.__init__ 中 precompute_freqs_cis 的基础路径
频率、位置编号、角度表、cos/sin 固定工具表的 shape 和关系
MiniMindModel.forward 按 start_pos/seq_length 切位置表
Model -> Block -> Attention -> apply_rotary_pos_emb 的调用顺序
```

本日源码阅读中出现了一次教学层级跳转：概念例子尚未闭合时提前跳到
MiniMind 的 `torch.arange`、`rope_base` 和 `torch.outer`。学习者指出
后，已回到概念例子完成相对位置实验，再明确宣布进入源码。该纠偏
作为过程的一部分保留，下一日不从 `unsqueeze` 中间继续，而是重新
建立完整程序调用链。

必须保留的源码欠账：

```text
apply_rotary_pos_emb 内部广播和完整逐行执行
eval_llm.py 启动入口与模型加载分支
model.generate 的逐 token 执行
MiniMind 原生实现与导出的 Qwen3ForCausalLM 路径的对应关系
```

下一学习日（Day 010）准确入口：

```text
minimind/eval_llm.py
  -> if __name__ == "__main__":
  -> main()
  -> argparse 读取参数
  -> init_model()
  -> 解释 ./minimind-3 走 AutoModelForCausalLM 分支
```

之后再分别追踪：

```text
启动/加载顺序
输入 prompt -> chat template -> tokenizer
-> model.generate -> forward -> Attention -> RoPE
```

状态：`[验收]` Day 009 学习结束。RoPE 概念已完成当前深度验收；
MiniMind 源码仅完成预览和恢复点定位，未将未读部分标成掌握。

## 七、问题与知识欠账

- YaRN：基础 RoPE 验收后再决定学习日。
- KV Cache：已理解 GQA 对缓存大小的影响，内部生成流程仍待学习。
- RMSNorm、残差连接、MLP：Attention 子层完成后返回 MiniMindBlock。

## 八、今日总结

Day 009 先从 Attention 的缺口出发：Embedding 和 Q/K/V 投影能够表达
token 内容，却不会自动表达 token 位置；Causal Mask 只能禁止读取未来，
不能告诉模型两个允许读取的 token 相隔多远。

通过交换 K/V 顺序的实验，学习者观察到没有位置处理时，分数和比例会
随 token 成对交换，最终 `weights @ V` 结果因为向量加法交换律保持不变。
随后建立 RoPE 的二维旋转直觉和公式：位置决定角度，不同二维组使用
不同旋转速度；将 Q/K 旋转后，点积受到相对角度差影响。V 仍是被读取的
内容，因此不参与这一步位置匹配旋转。

源码预览阶段逐行读通了基础 `precompute_freqs_cis`：MiniMind-3 的
`head_dim=96` 形成 48 组二维坐标，提前为 32768 个位置生成
`freqs_cos/freqs_sin` 两张 `[32768,96]` 固定工具表；forward 根据
`start_pos` 和本次序列长度切出 `[seq_length,96]`。还确认了无旧缓存
从位置 0 开始、有旧 KV Cache 时新 token 使用缓存长度对应的绝对位置。

今天没有完整读完 `apply_rotary_pos_emb`，也没有系统阅读程序入口和
生成调用链；这些内容明确留给 Day 010。今天的实际学习跨越了两个自然
日期，但按同一主题计为一个 Day。

## 九、下一学习日入口

Day 010 从 `minimind/eval_llm.py` 的 `if __name__ == "__main__"`
开始，先沿启动顺序阅读，不直接跳入 `model_minimind.py` 中间函数。
